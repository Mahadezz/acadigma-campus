# Handoff — 2026-09-25 (lead session; refreshed every 2 hours on branch `handoff/live`) — refreshed 21:05

Read this plus memory first. Lanes, brief, build log and the port rule are on main: `docs/plan/LANES.md`, `docs/plan/BUILDER-BRIEF.md`, `docs/plan/BUILD-LOG.md`.

## Main — c988d6d, CI green, every migration deployed with the smoke test passing
Merged today (21): #34 · #33 · #32 (000900) · #36 · #35 (300100) · #39 · #38 · #37 (300101) · #44 · #42 (300201) · #43 (300301) · #41 · #50 · #46 (300302) · #40 · #49 · #45 (300303) · #47 (300304) · #48 exams (300305) · #54 students (300306) · #51 Bengali app shell.
BUILD-LOG entries owed: #46, #49, #45, #47, #48, #54, #51 (one docs PR).

## Open PRs
- **#53** ops F-OP-03 P1-2 PDF engine. Reviews done (security clean; lead: Bengali renders correctly by eye). A fresh Sonnet builder is fixing: fonts not shipped to Vercel (import.meta.url baked build path; add outputFileTracingIncludes + runtime path, prove with grep/nft), logo monogram mojibake (use ScriptText), migration rename 300306 → 20260925300308, shell strings en/bn, report_run_items composite FK. Then merge + download a real PDF from production to confirm.
- **attendance** identity F-AC-03 P1-3 (D-104+, migration 20260925300309), branch feat/F-AC-03-attendance — building (the demo's 60-second moment + owner "by 9:30" screen).
- **#52** changesets release — merge once a day.

## Today's migration names (assigned by the lead)
300303 #45 · 300304 #47 · 300305 #48 · 300306 #54 students · 300308 #53 PDF · 300309 attendance. From 2026-09-26 everyone uses the real UTC time.

## Ports (PR #50)
lead 3100, identity 3101, ops 3102, billing 3103, design 3104 (+10 second builder: Bengali shell 3114).

## Builders
Retired to save usage (long contexts): old identity, design (Opus), billing, ops, Bengali-shell. Running: identity (fresh Opus, attendance), ops (fresh Sonnet, #53 fixes). Next when capacity allows: billing → F-AC-06 P3 marks entry (fresh builder; needs students ✓ and exams ✓; must add the Publish completeness gate in P4); ops → report-card template on the PDF engine after #53; design → pricing page (needs owner prices) or polish.

## Decisions made in chat, recorded or queued
D-302 grading: no rounding, .99 edges (owner to confirm the 32.5% question). Digits stay Western in Bengali UI (DESIGN-SYSTEM §1.6). Pricing page waits for owner price approval. Demo cut approved.

## Owner to-dos
Approve seeding the 40 demo students into the live demo school (procedure: create Class 6-ক + class teacher in /app/classes; confirm the demo workspace id; run supabase/seed/demo-class-6-ka.sql as the owner through admit_student via psql; verify 40 enrolments STU-2026-00001..40; SMS off). Native-speaker check of new Bengali strings (list in docs/test-reports/2026-09-25-design-bn-shell.md §8). Branch protection on main (block force pushes + deletions). 32.5% pass/fail rounding. Madrasah preset vs custom class names. Price grid + OQ-22. Company formation now. LOI template, deposit amount. Optional: Vercel Pro, `Bash(gh run view *)` permission. Weekly report 2026-10-02.

## Next steps for a fresh session
1. `gh pr list`; check each open PR's CI + review state above. 2. Merge #48 after its batch; then #51, #53, #54 as they clear. 3. BUILD-LOG docs PR for #46/#49/#45/#47 (+ later merges). 4. Merge #52 daily. 5. Next Parts: identity → F-AC-03 attendance after students (demo core); billing → marks entry; ops → report-card template on the PDF engine; design → pricing (after owner prices) or polish on a fresh Sonnet builder. 6. Usage limit hits ~every 3.5 h with 4-5 builders: keep builders on Sonnet except DB/security-heavy Parts; restart long-context builders fresh.
