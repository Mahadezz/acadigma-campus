# Handoff — 2026-09-25 end of day (owner restarting the PC after 4 days of uptime)

## FIRST THING NEXT SESSION
1. Invoke the `lead-playbook` skill (global CLAUDE.md says so; backup at docs/plan/LEAD-PLAYBOOK.md).
2. **Ask the owner:** "You wanted to explain the whole basic mode (easy mode for older / less-confident teachers) so I can write a separate spec for it — can you walk me through it now?" Write the spec as its own feature doc before building anything for it.
3. Recreate the 2-hour handover timer. The weekly report is due 2026-10-02 (the timer died with this session).
4. Check #53 (below), then continue the demo cut.

## Main — d704c7e, CI green; every migration deployed with the smoke test passing
Merged today (22): #34 wizard 1-2 · #33 release · #32 staff · #36 design D-68 + logos · #35 read-only lock · #39 settings P1 · #38 lanes + CI cuts · #37 wizard 3-4 + create_school_workspace · #44 build log · #42 billing D-301 · #43 calendar · #41 owner dashboard · #50 lane ports · #46 grade scales · #40 release · #49 readable audit · #45 auth fixes · #47 classes/sections/subjects · #48 exams · #54 students · #51 Bengali app shell · #55 attendance · (+ #56 build log, #57 lead playbook docs).
Live migrations today: 000900, 300100, 300101, 300201, 300301, 300302, 300303, 300304, 300305, 300306, 300309.

## Open
- **#53** ops F-OP-03 P1-2 PDF engine. All reviews done (lead + security), fixes applied (fonts embedded as base64 so they ship to Vercel — verified no build-machine paths in the output; Bengali logo initials fixed; composite FK; en/bn strings). Became CONFLICTING when #55 merged; its builder was asked to merge main and rename the migration to 20260925300310. If it isn't merged: merge main, check-migrations-order, green CI, merge, then download a real PDF from production to confirm the fonts.
- **#52** changesets release — merge (daily).
- BUILD-LOG entries owed: #55 (and #53 when merged).

## Next Parts (demo cut, owner-approved)
- identity: attendance follow-ups PR first (enrolment filter by dates not status; bulkMarked reset; workspace filter in the enrolled subquery; BnEnText names; parity-test note; CTE instead of temp tables), then continue.
- billing: F-AC-06 P3 marks entry (fresh builder; must also add Part 4's marks-completeness gate before Publish).
- ops: report-card template on the PDF engine (after #53).
- design: pricing page (needs owner prices) or basic mode once specced.
- Then: parent results view (F-AC-10), demo seed (needs owner yes), dry runs.
Builders: all finished/retired tonight. Start FRESH builders (Sonnet unless DB/security-heavy). Migration names from 2026-09-26: real UTC time.

## Owner to-dos / questions
- Basic mode explanation (above).
- Approve seeding the 40 demo students into the live demo school (procedure in memory).
- Branch protection on main (block force pushes + deletions).
- 32.5% pass/fail rounding (D-302 built as fail). Madrasah class names (custom for now). Any teacher can view any section's roll call (for substitutes) — confirm.
- Price grid + OQ-22; company formation; LOI template; deposit amount.
- Native-speaker check of new Bengali strings (docs/test-reports/2026-09-25-design-bn-shell.md §8).
- Optional: Vercel Pro; `Bash(gh run view *)` permission.

## Machine notes
C: full → TMP on F:. Seven empty leftover worktree folders under .worktrees/ (billing-require-writable, design-d68-logos, feat-op06-p1, identity-id05-p4, lead-lanes, ops-op07-settings, prep-audit) were locked by running Node servers — the restart frees them; delete with PowerShell Remove-Item -LiteralPath '\?\F:\...' -Recurse -Force. prep/audit-on-tenancy is backed up on origin.
