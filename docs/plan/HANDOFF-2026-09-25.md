# Handoff — 2026-09-25 (lead session, refreshed every 2 hours; branch `handoff/live`) — refreshed 07:45 Dhaka after the usage-limit reset

Read this plus the memory notes first. Nothing here is on main until its PR merges.

## Main (live at campus.acadigma.com, Supabase kekfmibwjejdhxjkmezo)
- **#33** changesets release merged (CHANGELOGs current, main 953aaf3). **#32 F-OP-06 Part 1 staff schema (D-63, migration 20260925000900) squash-merged at ~07:43 Dhaka after every required check passed** — VERIFY the Database workflow applied it and the smoke test passed before merging anything else.
- Previous merge: **#34** F-ID-05 Part 3 (create-school wizard steps 1-2, D-66, D-67). Migrations `20260925000700` and `20260925000800` applied to production by db.yml; post-deploy smoke test passed.
- Everything green on main (CI, Database, Release workflows).
- Known gaps: read-only mode is banner-only (billing lane is fixing it, due before mid-October); dashboard shows a dev placeholder; e2e live journeys skipped (OQ-27); demo account demo@acadigma.com must be deleted before launch.

## Open PRs
- **#38** lanes PR (`chore/lead-lanes-ci-cuts`, head 2093993, CI green): reviews (Fable lead + security) said FIX FIRST — the job-skipping logic fails open (e2e skipped when db skipped; `changes` failure skips everything; path allowlists miss gate configs; renames; grep SIGPIPE). Fix batch sent to its builder (make it fail closed: skip only pure-docs changes, `--no-renames`, `!cancelled()` on e2e, `changes` required, docs fixes, BUILD-LOG #33 entry). Builder was cut off by the usage limit mid-batch (1 dirty file in .worktrees/lead-lanes) and resumed.
- **#35** billing: read-only enforcement (requireWritable in actions + `app.tg_require_writable()` trigger on 7 tenant tables + `scripts/check-require-writable.mjs` + pgTAP 50_; D-300). Head fa88c2e, CI green. Security review: no bypass; fix needed so owners can still REMOVE members / revoke or decline invitations while read-only, and don't reveal access_mode_reason to non-members on insert. DB review: create triggers idempotently with the repo's `attach_*` pattern + comment the freeze_workspace coupling. Fable lead review was cut off and resumed; then one fix batch to the builder, then merge. Owner defaults: leaving a locked school allowed; accepting an invite to one refused; delete-to-get-under-limit refused.
- **#36** design D-68 + logos (head 383c2f9, CI green, ready). Fable lead + react reviews were cut off and resumed. Builder notes: dashboard not screenshotted (needs P4), no product switcher yet, wizard journey failed locally (Sunday toggle not kept after reload — check on main), two test accounts to delete in live Supabase: design-shots-d68@acadigma.test, design-shots-d68-wizard@acadigma.test.
- **#37** identity F-ID-05 P4 (draft, head b297e58) and **#39** ops F-OP-07 settings (draft, head 3772526): builders cut off mid-task (P4 was writing its decision entry; settings was starting the full gate) and resumed.

## Lanes (all builders on Opus this round; each in its own worktree; draft PRs + WIP pushes every 30 min). Usage limit hit ~03:40 and reset 07:40 Dhaka; all agents resumed 07:45.
| Lane | Part | Branch | Numbers |
|---|---|---|---|
| Identity | F-ID-05 P4 wizard steps 3-5 + `app.create_school_workspace` | `feat/F-ID-05-p4-school-wizard-3-5` | D-100+, mig `20260925100100`+, pgTAP 30_ |
| Operations | F-OP-07 P1 remainder + P2 school settings | `feat/F-OP-07-p1-p2-settings` | D-200+, mig `20260925200100`+, pgTAP 40_ |
| Billing | requireWritable on every write path + DB guard + CI check | `feat/F-CM-06-require-writable` | D-300+, mig `20260925300100`+, pgTAP 50_ |
| Design | D-68 visual refinement (acadigma.com base + Vercel-style details) + product logos | `feat/design-d68-logos` | D-68, then D-400+ |

Next per lane after these merge: the **demo-ready cut** (approved by the owner today; plan in scratchpad `demo-cut-plan.md`, to be committed as docs/plan/DEMO-CUT.md): identity F-AC-01 → F-AC-02; ops F-AC-11 P1, F-OP-03 P1-2, then F-AC-03; billing F-AC-06 P1 → P2/P3/P5/P7 → demo seed; design owner dashboard + pricing page → F-AC-10 → Bengali slice. Goal: 15-minute phone demo for schools to sign LOIs (early November).

## Lead routine
Only the lead merges, one PR at a time, after Opus/Fable lead review + ECC specialists (security, database, react, typescript by diff) and green CI; after each migration merge confirm the Database workflow + smoke test; append BUILD-LOG; merge #33 daily; handoff every 2 h; run the lane-overlap check (scratchpad `lane-overlap.sh`: files touched by more than one open PR must be zero outside the append-only docs). Live bug hunt of campus.acadigma.com was cut off by the usage limit before reporting; re-run it after the merges (lower priority than merging).

## Owner to-dos
Branch protection on main (block force pushes + deletions; do NOT require status checks yet — the release PR gets no CI): https://github.com/Mahadezz/acadigma-campus/settings/branches. Optional permission rule so the lead can read CI logs: `Bash(gh run view *)`.

From the demo plan:
Start company formation now (RJSC, trade licence, TIN, VAT BIN — 6-10 weeks; blocks bKash, SMS sender ID, DPA). LOI template bn/en; deposit amount (suggested ৳2,200); price grid approval (Starter ৳2,200 / Pro ৳4,900 / Enterprise from ৳18,000) and OQ-22 (Free plan); rotate the demo password before demos. Weekly report due 2026-10-02.

## Machine rules
C: is full — TMP/TEMP/TMPDIR on F:\tmp for everything; remove worktrees with PowerShell `Remove-Item -LiteralPath '\?\F:\...' -Recurse -Force`; never delete on C: without asking. gh download won't overwrite: `rm packages/db/src/types.generated.ts` first. GitHub won't run CI on CONFLICTING PRs. Vercel check is not required.
