# Handoff — 2026-09-25 (lead session; refreshed every 2 hours on branch `handoff/live`) — refreshed 12:50 Dhaka

Read this plus the memory notes first. Lanes, builder rules and the build log are ON MAIN now: `docs/plan/LANES.md`, `docs/plan/BUILDER-BRIEF.md`, `docs/plan/BUILD-LOG.md`.

## Main (live at campus.acadigma.com, Supabase kekfmibwjejdhxjkmezo) — main fe128c6, CI green, Vercel production current
Merged today in order: #34 wizard 1-2 · #33 release · #32 staff schema (D-63, 20260925000900) · #36 design D-68 + logos · #35 read-only lock (D-300, 20260925300100) · #39 settings P1 (D-200, D-201) · #38 lanes + CI cuts (D-69, D-70) · #37 wizard 3-4 + create_school_workspace (D-100, 20260925300101) · #44 build-log entries. Every migration deployed by db.yml with the anon smoke test passing.

## Open PRs (only the lead merges, one at a time, after reviews + green CI)
- **#42** billing D-301 read-only follow-ups (join functions check access_mode; trigger's non-member branch dropped; seed passwords via crypt()). Migration 20260925300201. Security review: CLEAN. Head 0d557e8 MERGEABLE (after merging main) — merge when CI green, then verify the db deploy + smoke.
- **#45** identity D-101 auth fixes (login rate-limit copy: banner only, disabled button, minutes; throttle keys derived server-side so nobody can lock another user out). Ready at 3e4d3ce. NEEDS REVIEW (security + react) before merge.
- **#43** ops F-AC-11 P1 school-day calendar (holidays, overrides, `app.is_school_day`). Draft 7f739ef; builder finishing. Then review (Fable lead + security + DB).
- **#41** design D-400 owner "today" dashboard from real data + nav route registry (hide links to unbuilt routes — live bug hunt HIGH). Draft 049dd37, CONFLICTING (builder merging main). Then review (Fable lead + react).
- **#46** billing F-AC-06 P1 exams: grade scales. Draft 8648f1f (lead pushed 2 commits after the limit; 15 files still uncommitted in .worktrees/billing-ac06-p1 — builder resumed).
- identity F-AC-01 classes/sections/subjects: branch feat/F-AC-01-classes-sections-subjects, worktree .worktrees/identity-classes, NOT yet pushed (2 dirty files) — builder resumed with instructions to push + open a draft first.
- **#40** changesets release PR — merge once a day (after the next feature merge).

## Rules learned today
- Migration names: today use 202609253LNNNN-style prefixes after 20260925300101 (billing 3002NN, ops 3003NN, design 3004NN, identity 3001NN); from 2026-09-26 the real UTC time sorts after them. CI's `check-migrations-order` enforces.
- Usage limit hit twice (03:40 and ~11:00, resets 5 h later). Builders on Opus burn it fast — next Parts go to Sonnet builders unless DB/security-heavy; reviews stay Fable/Opus.
- Vercel Hobby cap delayed production once; D-70's ignoreCommand now keeps previews off.
- Leftover worktree folders (billing-require-writable, design-d68-logos, feat-op06-p1, identity-id05-p4, lead-lanes, ops-op07-settings, prep-audit) can't be deleted while a `next start` holds pnpm hard-linked binaries; delete after that server stops. prep/audit-on-tenancy (19 unmerged commits from 2026-09-24) is backed up on origin — decide later whether it's still needed.

## Owner to-dos
Branch protection on main (block force pushes + deletions; don't require checks yet): https://github.com/Mahadezz/acadigma-campus/settings/branches. Madrasah class names: preset vs custom (OQ-2; custom for now). Demo cut: company formation now; LOI template; deposit amount; price grid + OQ-22. Optional: Vercel Pro (removes deploy cap); `Bash(gh run view *)` permission. Weekly report due 2026-10-02.

## Next steps for a fresh session
1. `gh pr list`; for each open PR check CI and the review state above. 2. Merge #42 (reviewed) when green → verify deploy. 3. Review #45 then merge. 4. Review #43, #41 as they go ready. 5. Merge #40 daily. 6. After each merge: BUILD-LOG entry (batch into one docs PR), memory note, handoff refresh. 7. Next Parts per lane: docs/plan/LANES.md + the demo cut (identity F-AC-01 → F-AC-02; ops F-AC-11 → F-OP-03 P1-2 → F-AC-03; billing F-AC-06 P1 → P2/P3/P5/P7 → demo seed; design dashboard → pricing page → F-AC-10 → Bengali slice).
