# Handoff — 2026-09-25 (lead session; refreshed every 2 hours on branch `handoff/live`) — refreshed 17:50 after the THIRD usage-limit reset

Read this plus memory first. Lanes, brief, build log and the port rule are on main: `docs/plan/LANES.md`, `docs/plan/BUILDER-BRIEF.md`, `docs/plan/BUILD-LOG.md`.

## Main — d1f6cca, CI green, every migration deployed with the smoke test passing
Merged today (18): #34 wizard 1-2 · #33 release · #32 staff (000900) · #36 design D-68 + logos · #35 read-only lock (300100) · #39 settings P1 · #38 lanes + CI cuts · #37 wizard 3-4 + create_school_workspace (300101) · #44 build log · #42 billing D-301 (300201) · #43 calendar (300301) · #41 owner dashboard · #50 lane ports + build log · #46 grade scales (300302) · #40 release/changelogs · #49 readable audit · #45 auth fixes (300303) · #47 classes/sections/subjects (300304).
BUILD-LOG entries still owed: #46, #49, #45, #47 (batch into one docs PR).

## Open PRs (only the lead merges, one at a time, after reviews + green CI)
- **#48** billing F-AC-06 P2 exams (D-303, migration 20260925300305). Ready, green at fa29172. Reviews (lead Opus, security + DB Sonnet) were cut off by the limit and RESUMED — send one fix batch to the billing builder when all three report, then merge + verify deploy.
- **#51** design Bengali app shell (D-401). Draft 0870fee + 33 uncommitted files; builder resumed (commit/push, merge main, mark ready). Then review (react + lead).
- **#53** ops F-OP-03 P1-2 PDF engine. Draft afbb9ad; builder was MID-MERGE (131 files); resumed to finish the merge and push. Then review (lead + security + react).
- **#54** identity F-AC-02 students (D-103+, migration 20260925300306/307). Draft c33cfe6; fresh Opus builder resumed ("Now the pgTAP file"). Children's PII — full three reviews.
- **#52** changesets release — merge once a day (last merged #40 at ~15:45).

## Today's migration names (assigned by the lead)
300303 #45 · 300304 #47 · 300305 #48 · 300306/300307 #54 students · 300308 #53 if needed. From 2026-09-26 everyone uses the real UTC time.

## Ports (PR #50)
lead 3100, identity 3101, ops 3102, billing 3103, design 3104 (+10 second builder: Bengali shell 3114).

## Idle builders
Old identity builder (756k context) and design builder (513k) retired to save usage; billing builder (a9f3477…) idle until #48 review batch. Next billing Part after #48: F-AC-06 P3 marks entry (needs #54 students).

## Decisions made in chat, recorded or queued
D-302 grading: no rounding, .99 edges (owner to confirm the 32.5% question). Digits stay Western in Bengali UI (DESIGN-SYSTEM §1.6). Pricing page waits for owner price approval. Demo cut approved.

## Owner to-dos
Branch protection on main (block force pushes + deletions). 32.5% pass/fail rounding. Madrasah preset vs custom class names. Price grid + OQ-22. Company formation now. LOI template, deposit amount. Optional: Vercel Pro, `Bash(gh run view *)` permission. Weekly report 2026-10-02.

## Next steps for a fresh session
1. `gh pr list`; check each open PR's CI + review state above. 2. Merge #48 after its batch; then #51, #53, #54 as they clear. 3. BUILD-LOG docs PR for #46/#49/#45/#47 (+ later merges). 4. Merge #52 daily. 5. Next Parts: identity → F-AC-03 attendance after students (demo core); billing → marks entry; ops → report-card template on the PDF engine; design → pricing (after owner prices) or polish on a fresh Sonnet builder. 6. Usage limit hits ~every 3.5 h with 4-5 builders: keep builders on Sonnet except DB/security-heavy Parts; restart long-context builders fresh.
