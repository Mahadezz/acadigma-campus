# Handoff — 2026-09-27 (Dhaka), refreshed ~00:40 (18:37 UTC 26 Sep)

Session resumed after the owner's Windows 11 restart. Recovery was clean: nothing unpushed, no uncommitted work.

## Main — f438896

- Merged this session: **#86** release (version packages).
- No new migrations on main since `20260926072044`. Last deploy + smoke: see HANDOFF-2026-09-26 (2885f6a, passed); #86 is release-only.
- BUILD-LOG owed: #78 #80 #76 #86 (one lead docs PR). The repo copy `docs/plan/LEAD-PLAYBOOK.md` also owes the new "testing and security first" rule (added to the skill 2026-09-27).

## Open PRs

| PR | Lane | Head | CI | Review state | What's left |
| --- | --- | --- | --- | --- | --- |
| **#83** offline P1 (D-308) | billing (Opus) | 150ab97 | green | security: **HIGH** (null snapshot + filled cache leaks user A's pages to B) + MEDIUM (in-flight cache write after wipe) + 3 LOW; lead: 5 should-fix + nits | builder applying the whole batch → security re-check → merge **first** |
| **#85** staff-parents + CT invites (D-109) | identity (Opus) | 5cd6bb4 | green | security: **MEDIUM** (class teacher accepts own guardian invite) + 2 LOW; DB: approve + revoked-probe index; lead: 3 should-fix + nits | builder applying batch (same migration file `20260926180341_guardian_followups.sql`), rerun teacher-parent journey locally (needs Docker) → security re-check → merge after #83 |
| **#82** basic class hub (D-406) | design (Sonnet) | df5fc82 | green | not reviewed yet | lead decision: swap hand-rolled tablist for shadcn Tabs (Radix measured 251 kB vs 250 budget); win back 1 kB or raise the route budget ≤ +5 kB with a D-406 addendum → react + Opus lead review |
| **#87** security & test audit (D-75) | lead/security lane (Opus) | — | — | — | NEW lane on owner direction: definer functions, RLS/grants, #76 LOW pgTAP, max_rows 1,000 audit, server-action checks; pgTAP 23_-29_, port 3110 |

Overlap: #82/#83/#85 share `(school)/app/layout.tsx`, `workspace-switcher.tsx`, `gated-shell.tsx`, messages. Reviewers ran `git merge-tree`: no overlapping hunks; only the DECISION-LOG tail conflicts. Merge order: #83 → #85 → #82; each later one merges main once.

## Running builders

- billing: #83 fix batch · identity: #85 fix batch · design: #82 Tabs swap · security lane: #87 audit.
- Operations lane idle by choice (owner: testing/security first). Its next feature is F-AC-05 timetable P1 (bell schedule), after #87 Part 1 merges.

## Decisions made in chat (to log)

- Owner 2026-09-27: "go faster without hurting quality" + "prioritise testing and places where bugs are, also security" → standing security & testing lane → **D-75** (in #87).
- Lead: #82 uses shadcn Tabs even at +1 kB (owner shadcn rule; the hand-rolled one had no keyboard support) → D-406 addendum in #82.
- Lead: #83's MEDIUM (post-wipe cache write) is fixed in Part 1, not deferred.

## Follow-ups queued

- #83 backlog: purge a child's `/family` cache on guardian revoke / 403.
- #85 follow-ups: `guardian_users (workspace_id, student_id)` index; one extra count query per `/app` render.
- OQ-27: run e2e journeys in CI against the db-integration stack.
- ~30 stale non-worktree folders under `.worktrees/` (leftovers, safe to clean).

## Owner to-dos

- Open Docker Desktop (down after restart; builders need it for local pgTAP/e2e); check the disk image location is on F:.
- Did the Windows 11 upgrade go fine? `python` is off PATH now (`py` works).
- Sentry free account · prices / LOI / deposit / company · branch protection on main · clean "Previous Windows installation" (~25 GB).

## Timers (session-only)

Handover `23 */2 * * *` · save-work sweep `13,43 * * * *` · weekly report one-shot 2026-10-02 10:07.
