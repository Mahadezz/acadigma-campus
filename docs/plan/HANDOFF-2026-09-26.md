# Handoff — 2026-09-26 (overnight, owner asleep, lead running autonomously)

## FIRST THING NEXT SESSION

1. Invoke the `lead-playbook` skill. Read memory `acadigma-campus.md` (its last lines are the freshest state).
2. Recreate the timers: handover every 2 h (`23 */2 * * *`), save-work/review/merge sweep every 30 min (`13,43 * * * *`), weekly report one-shot **2026-10-02**.
3. The owner gave **full authorization** (2026-09-26): decide, act, log every decision in DECISION-LOG. The demo seed is **cancelled** — don't raise it again.

## Main — 4a954d2, every migration deployed with the smoke test passing

Merged 2026-09-26 (8): #52 release · #59 specs F-ID-10 basic mode + F-ID-11 offline (D-403, D-71) · #58 attendance follow-ups, any teacher marks any section (D-105, mig 300311) · #61 hotfix /app/classes (ambiguous profiles embed, 4 FKs) · #62 hotfix PDF pdfkit standard fonts (serverExternalPackages + tracing) · #60 marks entry + publish completeness gate (D-304, mig 300312) · #65 release · #67 danda (।) font fix + PDF stream-integrity CI test · #63 report card template (D-206, mig 300313).
**Vercel production is still rate-limited (Hobby cap):** the live site is behind main — the #61/#62 hotfixes and every UI change since #53 go live on the next successful deploy. DB migrations deploy via GitHub (already live).

## Open PRs

- **#64** basic mode P1 display settings (design, D-404, mig **300315**) — review fix batch (shared-device cookie leak on sign-out, early return, label link) being applied by a fresh Sonnet builder; then merge.
- **#66** student Excel/CSV import (identity, D-106, mig **300316**) — review fix batch (dedupe against the roster + within the file, fflate zip-bomb pre-scan, phone rule back to strict 01[3-9], class-name/numeric-phone/header tolerance, O(n²) jsonb) being applied; security to re-verify the zip-bomb fix before merge.
- **#68** F-AC-06 P5 results/GPA/rank (billing, D-305, mig **300314**, draft) — must implement the seam `getReportCardData(supabase, ctx, studentId, examId)` with teachers limited to their class-teacher sections (RLS + pgTAP).
- **#69** changesets release — merge daily.
- Ops: F-OP-03 P5 bulk report cards (print only), fresh Sonnet builder, branch feat/operations-bulk-report-cards (D-207, mig **300317** if needed, plus the report_runs staff-select tightening).

## Migration names (lead-assigned; UTC is still 2026-09-25 so real time sorts too early)

300311 #58 · 300312 #60 · 300313 #63 · 300314 #68 · 300315 #64 · 300316 #66 · 300317 ops bulk · **next free 300318**. From UTC 2026-09-26 on, the real UTC time sorts after these.

## Decisions made in chat (logged or queued)

- Owner: the roll call is open to any teacher (D-105 ✓); D-302 no rounding confirmed (in D-304 ✓); basic mode + offline answers (D-403/D-71 ✓); full authorization; seed cancelled.
- Lead, still to log as **D-72** in the next lead docs PR: (a) student import joins the demo cut (FDE approach) + a "first week at a school" onboarding checklist; (b) production error alerting is on the lead queue (observability) — tonight's prod bugs were found only by QA; (c) a CI job with a real PostgREST (supabase start) so embed/RLS-through-API bugs get caught (#61's gap); (d) CSV exports escape = + - @; import reports purged after 90 days; the SMS sender never messages a demo workspace.

## Owed docs

BUILD-LOG entries: #55 #53 #52 #59 #58 #61 #62 #60 #65 #67 #63 — one docs-only lead PR together with D-72.

## Next demo-cut Parts after the current ones

identity: section_subjects (a prerequisite for the basic-mode home) · billing: F-AC-06 P7 publish + parent results (F-AC-10) · design: basic mode P2 home + P3 class hub · ops: attendance register / mark sheet (F-OP-03 P6) · lead: error alerting, the PostgREST CI job.

## Owner to-dos

- Vercel Pro (removes the deploy cap) — optional but the live site lags main.
- Price grid + OQ-22; LOI template; deposit amount; native-speaker check of the new Bengali strings.
- Fill the demo school before the first school visit (seed or live Excel import).

## Update — after the usage-limit reset (07:20 Dhaka)

- Merged since the first handover: **#64** basic mode P1 (D-404, mig 300315, deployed ✓) · **#70** lead docs (BUILD-LOG #52-#67, D-72, `docs/plan/ONBOARDING-CHECKLIST.md`). Main **509f64a**. BUILD-LOG owed: #64.
- The usage limit hit ~03:40-07:20 Dhaka; every cut-off agent was resumed with SendMessage (context kept). No work was lost (nothing unpushed).
- **#66** import: the zip-bomb guard was bypassable (read-excel-file re-parses the original bytes via local headers); the fix is pushed (74a4e94), finishing the merge + gate; then **security re-check**, then merge (mig 300316).
- **#68** results (mig **300318**): security clean (2 LOW: archived-section read, audit on the results wipe). Opus lead: **BLOCKER**, compute must not refuse the whole exam for one missing mark (mark that student `incomplete`, keep the publish gate); SHOULD: a null roll number still gets a card ("—"); keep AND (rewrite §5.6/§5.8); a visible fail marker; a golden .xx5 GPA case. DB review resumed; then one fix batch to the billing builder.
- **#71** ops bulk report cards (draft, resumed) · **#72** design basic-mode home (draft, resumed) · **#69** release (merge daily).
- Migration names: 300316 #66 · 300317 #71 · 300318 #68 · 300319 #72 · **next free 300320**. Merge order matters: if a higher number merges first, rename the lower open one.

## Update — ~09:40 Dhaka

- Merged since the last update (7): **#66** student import (D-106, mig 300316) · **#69** release · **#68** results/GPA/rank + the real report-card seam (D-305, mig 300318) · **#73** section_subjects + a read-only release-trigger fix incl. the live D-102 trigger (D-107, mig 20260926021923) · **#71** bulk report cards (D-207, mig 20260926022716). All deployed, smoke ✓. Main **5996a98**. 16 merges this session.
- **Migration naming is back to real UTC time** (UTC rolled to 2026-09-26): each builder renames its migration to the current UTC time right before `gh pr ready`; the lead renames at merge if something newer landed.
- Vercel production is **still rate-limited** on main — the live site lags (every UI change since #53).
- Open: **#75** publish results to parents (D-306) — fix batch (withheld = no frozen marks + a parent message; plain-words publish sheet; LOWs), then security re-check of the withheld change · **#72** basic-mode home (D-405) — fixing lint, then review · **#76** ops attendance register + mark sheet (D-208, draft) · lead CI builder: real-PostgREST integration job + markdown format check (D-73) · identity: **F-AC-02 P4 guardian linking** (D-108) — without it no parent can be linked (guardian_users has no client write) · **#74** release.
- Owner to-dos (new): **error alerting needs an account** (Sentry free tier or similar) — only the owner can sign up. Vercel Pro still recommended.
- BUILD-LOG owed: #64 #66 #68 #69 #73 #71.

## Update — ~13:30 Dhaka (owner awake)

- Merged since ~09:40 (7): **#77** CI db-integration job + docs prettier (D-73) · **#75** publish results to parents (D-306) · **#74** release · **#72** basic-mode home (D-405) · **#81** Vercel main-only deployments (D-74) · **#79** marks submit/lock/entry window (D-307). Main **8d87ebf**. 23 merges this session.
- **Vercel correction (D-74):** production was NOT stuck. It deployed main 9c49976; branch pushes were creating cancelled deployments that ate the Hobby quota. #81 stops that. **No Pro needed.** #81 was merged with the changeset check failing: owe a `@acadigma/web` changeset (next lead docs PR). Rule: gate `gh pr merge` on zero failing checks.
- Open: **#78** guardian linking (D-108): a HIGH found on re-check (the members-guard exception let a removed parent self-reactivate) → fix in progress, then a security re-check · **#76** register + mark sheet: 1,000-row cap fix → `public.attendance_register` RPC (fresh Sonnet) · **#82** basic-mode class hub (D-406, draft) · **offline P1** (F-ID-11: shell + read cache + purge, D-308, fresh Opus) · **#80** release.
- Lesson: PostgREST max_rows = 1000 silently truncates. Use jsonb RPCs for big reads.
- Owner: error-alerting signup (Sentry free); prices / LOI / deposit. Obsidian "brain" vault exported at `F:\Acadigma Suite\acadigma-brain` (graph from 17 Sep); a refresh + 3D brain page were offered for this evening.
- BUILD-LOG owed: #64 #66 #68 #69 #73 #71 #77 #75 #74 #72 #81 #79, plus the #81 changeset.

## ⚠️ RESTART HANDOVER — ~15:30 Dhaka, before the owner's Windows 11 upgrade

**The owner is restarting the PC to install Windows 11** (staged in `C:\$WINDOWS.~BT`). This session, its timers and every builder stop. Nothing is lost: every worktree was pushed and none has uncommitted changes. A backup of `.claude`, Desktop, Documents and Downloads is at `E:\backup-before-win11` (2.49 GB).

### FIRST THING NEXT SESSION (start Claude in `F:\Acadigma Suite\acadigma-campus`, so the DB env loads)

1. Invoke the `lead-playbook` skill; read memory `acadigma-campus.md` (the last lines are the freshest) and this file.
2. Recreate the timers: handover `23 */2 * * *`; save-work sweep `13,43 * * * *`; the weekly report one-shot **2026-10-02**.
3. `git fetch`; run `gh pr list`. Start fresh builders (the old ones are gone) for the 4 paused PRs below, one per lane, each told to read its PR and this section.
4. Refresh the owner's brain: `cd "F:/Acadigma Suite/_lead-tools/brain-src" && python build_brain.py` (update `Keep in mind.md` / `Questions for you.md` first if needed).
5. Ask the owner: did the Windows 11 upgrade go fine? Did Docker move to F:? (Docker Desktop → Settings → Resources → Advanced → Disk image location.)

### Main — 2885f6a, all deployed with the smoke test passing. 26 merges this session.

Today after 13:30: #84 BUILD-LOG + the #81 changeset · #78 guardian linking (D-108, HIGH fixed and re-verified) · #80 release · #76 attendance register + mark sheet (D-208, attendance_register RPC fixes the 1,000-row cap). BUILD-LOG owed: #78 #80 #76.

### Paused PRs (all pushed; resume each on a FRESH builder)

| PR                                                            | Lane     | Head    | State                                                        | What's left                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------- | -------- | ------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **#82** basic-mode class hub (D-406)                          | design   | b8e48b5 | draft; tests pass locally; bundle 244 kB                     | wait for CI on b8e48b5 → `gh pr ready` → REVIEW: note it **hand-rolled a tablist instead of shadcn/Radix Tabs** to save bundle size; check a11y (roving tabindex, aria) or use a lighter shadcn pattern. No manual 360/XL/bn screenshots yet.                                                                             |
| **#83** offline P1: shell + read cache + purge (D-308)        | billing  | 5b7e7cc | ready but CONFLICTING with main; the last CI failed (unread) | merge origin/main (DECISION-LOG / README / messages conflicts: keep all), wrap any new generate/send buttons in `OnlineOnly`, gate, push, read the CI failure, then reviews (security for the purge!). SW 14.3 kB gz.                                                                                                     |
| **#85** staff-who-are-parents + class-teacher invites (D-109) | identity | 56aec63 | draft; pgTAP 39 + integration + journey 6/6 local            | docs (D-109, §11 F-AC-02/F-AC-10, DATA-MODEL, test report, README, changeset), full gate, rename the migration to current UTC, ready. **Security review must look hard at the new members-guard exception** (a class teacher revoking a pure parent's last link removes their membership, with a same-transaction proof). |
| **#86** release                                               | lead     | —       | —                                                            | merge (daily)                                                                                                                                                                                                                                                                                                             |

### Follow-ups queued (not started)

- #76 security LOW: add a pgTAP that a parent/guardian login gets `{"section": null}` from attendance_register.
- OQ-27: run the skip-gated e2e journeys in CI against the db-integration stack (D-73 item 6).
- The Obsidian 3D brain page + graph refresh (offered to the owner, not started).

### Owner to-dos

Error-alerting account (Sentry free) · prices / LOI / deposit / company · branch protection on main · Docker disk → F: · Windows 11: after the upgrade, clean "Previous Windows installation" to free about 25 GB.
