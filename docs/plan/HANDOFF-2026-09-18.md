# Handoff — paused 2026-09-18, updated 2026-09-23

Read this first when resuming. Everything below is committed and pushed; nothing is running.

## Where we are

**Release 0 / M0 (secure foundation) is 5 of 6 chunks merged to `main`.** The last chunk is two PRs, both rebased onto `main` on 2026-09-23 and marked ready.

| Chunk                                                                | PR     | State                                                                                                                                                                                                                                                                                                                         |
| -------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Foundation (toolchain, migrations 0001–0004, packages, shell, CI)    | #1     | merged `ecb6888`                                                                                                                                                                                                                                                                                                              |
| F-OP-07 P1 settings `resolve()` + F-ID-07 P1 notification catalogue  | #4     | merged `f1d28cb`                                                                                                                                                                                                                                                                                                              |
| F-CM-06 P1–3 plans/limits engine (+ `app.is_privileged_context` fix) | #2     | merged `d6e1696`                                                                                                                                                                                                                                                                                                              |
| M0 0.9 UI primitives, OTel, `redactForAI()`                          | #8     | merged `df07612`                                                                                                                                                                                                                                                                                                              |
| F-ID-01 P1–4 authentication                                          | #3     | merged `411cefe`                                                                                                                                                                                                                                                                                                              |
| **F-ID-03 P1–3 tenancy**                                             | **#7** | **rebased onto `main` 2026-09-23** (8 commits + rebase-fallout commit `14beecc`), typecheck · lint · prettier · 566 unit tests green locally, marked ready. **Owner: approve → squash-merge.**                                                                                                                                |
| **F-ID-09 P1–3 audit substrate + viewer**                            | **#6** | **rebased onto `main` 2026-09-23** (2 commits + rebase-fallout commit `615bf66`), typecheck · lint · prettier · 596 unit tests green locally, marked ready. **Needs one more small rebase after #7 merges** (both touch `packages/domain/src/index.ts`, the `package.json` export maps, `docs/README.md`, `DECISION-LOG.md`). |
| Dependabot actions bump                                              | #5     | green; merge needs `gh auth refresh -h github.com -s workflow` (token lacks `workflow` scope).                                                                                                                                                                                                                                |
| This handoff                                                         | #9     | lint was red on `prettier --check` only; fixed in `e722a18`.                                                                                                                                                                                                                                                                  |

### What the 2026-09-23 rebases resolved (so the next reviewer does not re-derive it)

- **#7** — `packages/domain` exports and barrel: union (`nav`, `workspace` added beside `settings`, `notifications`, `plans`). `permissions.test.ts`: kept `main`'s `policies.manage` test and the branch's rewritten staff test. `packages/contracts` exports: union (`identity/workspace`). The branch's reformat of the F-ID-01 test report was dropped in favour of `main`'s newer version (25 `safeReturnTo` cases, known issue 9). Fallout fixed in `14beecc`: the F-CM-06 repository test fixtures did not carry the new `workspaceType` field, and the transcribed §2 matrix test lacked `policies.manage` (added on `main` by F-OP-07 P1).
- **#6** — `ci.yml`: both catalogue-parity steps kept (notification, then audit). `DECISION-LOG.md`: D-50 (public RPC wrappers, on `main`) then D-51 (audit substrate) — no renumbering. Export maps and barrels: union. `docs/README.md`: `main` carries the test-reports table **six times over** from earlier union merges; #6 rebuilds it as one table listing every report (#7 does not touch that section, so the fix lands when #6 merges). Fallout fixed in `615bf66`: both the notification and the audit catalogue export `catalogEntry()`, so the root barrel now re-exports the notification one by name and the audit one as `auditCatalogEntry` (the `@acadigma/domain/audit` subpath is unchanged); two docs files reformatted for prettier.

**Laptop worktrees** `.worktrees/feat-tenancy` and `.worktrees/feat-audit` still hold the pre-rebase history. Refresh each with `git fetch origin` then `git switch -C <branch> origin/<branch>` inside the worktree (every local commit is already on the remote).

## Running the app locally

`.env.local` lives in **`apps/web/`**, not the repo root — Next.js reads env files from the app directory, and the root `README.md` said otherwise (fixed in this PR; `HANDBOOK.md` §7 was already right). Then, from `apps/web`, `pnpm exec next dev --port 3000`. What renders today: the marketing page, `/design`, the auth screens (`/login`, `/register`, `/verify`, `/forgot`, `/reset`), `/account/security`, `/platform`, and `/app/dashboard` (redirects to `/login?next=…` when signed out). Verified 2026-09-23 at 360×800 and 1280×800.

## Owner actions pending (see `docs/product/OWNER-QUESTIONS.md`)

- **Approve and squash-merge #7**, then tell the next session to rebase #6 (small) and mark it ready again.
- **OQ-26** GitHub secrets `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`, `SUPABASE_PROJECT_ID=bvqzhrvcrxebawjusrxk` → lets CI `db push` migrations to the cloud project and `pnpm db:types` replace the placeholder `types.generated.ts` (root cause of the PR #7 `plan`/`plan_id` bug class).
- **OQ-27** after the first `db push`: set `E2E_LIVE_SUPABASE: "1"` in the CI `e2e` job so the 12 live-Supabase auth journeys run instead of skipping.
- `gh auth refresh -h github.com -s workflow` (merge workflow-touching PRs).
- **Reboot** — Docker Desktop + WSL2 are installed and waiting for a restart; afterwards `supabase start` / `pnpm db:test` run pgTAP locally.
- OQ-1…25 (VAT/AIT, SMS provider, Free-plan kill, payout minimum, price list sign-off, trademark search, recovery-email template + HIBP toggle, PDPA lawyer questions).

## Next build steps (in order)

1. Merge #7 (owner), rebase + merge #6. M0 complete.
2. Small docs PR: add to `CLAUDE.md`/`HANDBOOK.md` the rules learned — never `git stash` in shared worktrees (stack is repo-wide; commit WIP instead); no blanket `GRANT EXECUTE ... IN SCHEMA app` (allowlist + pgTAP invariant); `Object.hasOwn` on any untrusted-key lookup; gate pushes on the frozen install + typecheck + lint actually passing; rebase must be verified with `git merge-base --is-ancestor`; a union merge of a Markdown table duplicates it — resolve tables by hand; `.env.local` lives in `apps/web/`.
3. M0 wrap-up: wire `filterNav`/`BottomNavFromConfig` into the school shell (flagged in PRs #7/#8); global pgTAP grants-invariant test; `supabase/tests/coverage.sql`.
4. **M1 — People in the school** per `docs/plan/ROADMAP.md` §3 (F-ID-05 onboarding, F-ID-03 P4–8, F-ID-04 invitations, F-ID-07 P2–4, F-OP-07 P2–3, F-OP-06 P1–2, F-OP-05 P1–3, `(account)` settings, F-CM-06 P4, PDPA consent capture). Same pattern: Sonnet builder per chunk in a worktree, Opus review on RLS/money/auth code, draft PR, test report, squash-merge.

## Where everything lives

`CLAUDE.md` → `docs/README.md` (index) → `docs/plan/ROADMAP.md` → `docs/decisions/DECISION-LOG.md` (D-01…D-51) → `docs/features/*` (46 specs, 275 parts) → `docs/test-reports/*` (one per merged chunk). Session narrative: `F:\Acadigma Campus\Claude outputs\SESSION-LOG-2026-09-17.md`.

## Known open review comments (non-blocking, tracked in each PR)

PR #3: token in reset URL, `getOrigin()` host trust, `x-forwarded-for` trust, `remember` unused, unverified sign-in message, localStorage Playwright assertion, `inactivity_timeout` vs 30-day session, CSP still report-only. PR #6: `or=()` string interpolation in `audit.ts`, profiles contact masking, spec cursor wording. PR #7: `filterNav` unwired, audit-log write primitive unthrottled, forged-header e2e asserts copy not status. PR #8: `useFilteredNav` memo footgun, dual virtualisers.
