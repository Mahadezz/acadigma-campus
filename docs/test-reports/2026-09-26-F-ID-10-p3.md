# Test Report — F-ID-10 Basic mode, Part 3

|         |                                                                 |
| ------- | --------------------------------------------------------------- |
| Feature | F-ID-10 — Basic mode                                            |
| Part    | 3 — Class hub with the built tabs, plain confirmations and undo |
| Spec    | `docs/features/01-identity/F-ID-10-basic-mode.md` §8 Part 3     |
| PR      | #82                                                             |
| Status  | **PASS WITH KNOWN ISSUES**                                      |
| Date    | 2026-09-26                                                      |
| Run by  | design lane builder (Claude Sonnet 5)                           |

---

## 1. Scope

**What this Part is.** The class hub — `/app/classes/[sectionId]` — the screen the owner's own walkthrough described: "they can just press a class, enter the class; there will be all the handouts, marking, printing, attendance, lesson planner, routine all in one block." This Part ships the four tabs that already have a real page to reuse: **Attendance** (the existing roll call, at basic sizes, with a plain-language `ConfirmSheet` before every save and a 30-second post-save Undo), **Marks** (this section's papers the caller teaches, n/N entered, tap through to the existing marks entry screen — the first time marks are reachable from basic mode), **Students** (this section's roster, Bangla names via `BnEnText`; a row opens the student's profile, read-only), and **Print** (spec §4.5's literal row: "Print attendance register", "Print mark sheet" for this class, plus the report cards D-406 originally shipped — see the review-batch note below). A caller not assigned to a section (and not owner/admin) sees "This class is not on your list," with no student data sent to the browser. Class blocks on the home screen and the "All classes" list now open the hub instead of the roll call directly.

**Acceptance criteria covered** (spec §9):

| #     | Criterion                                                                   | Covered by                                                                                                                       |
| ----- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| AC-6  | The hub shows only tabs whose pages exist, no "coming soon" tab             | `CLASS_HUB_TABS` unit test (`packages/domain/src/class-hub/index.test.ts`); `class-hub-tabs.test.ts`; `basic-class-hub.spec.ts`  |
| AC-7  | Attendance Save opens a plain-language confirm sheet; nothing saves first   | `roll-call.test.tsx` ("confirms the plain sentence..."); `basic-class-hub.spec.ts`                                               |
| AC-8  | Undo within 30s re-saves the previous values as one audited edit            | `roll-call.test.tsx` ("offers Undo after editing an already-saved session...")                                                   |
| AC-9  | Every interactive element on a basic screen is >= 56x56px                   | `basic-class-hub.spec.ts` ("every tab renders without error, and every interactive element is >= 56x56px")                       |
| AC-11 | A caller not assigned to a section sees "not on your list," no student data | `class-hub.test.ts` ("NOT_ASSIGNED for a teacher whose listMySections does not include this section"); `basic-class-hub.spec.ts` |

**Out of scope for this Part** (per the builder brief for this Part and the spec's own dependency notes):

- **"Earlier days" (the past-session list)** in the Attendance tab — no repository anywhere in the codebase (F-AC-03 included) lists a section's past sessions yet. The brief's own wording for this Part was "today's roll call inline or one tap to it," which ships.
- **The Lighthouse hub URL** (spec §8 Part 3's own "a hub URL added to the existing Lighthouse job") — the same reason `/app/home` was not added in Part 2 (D-405 item, "the existing CI `lighthouse` job only tests unauthenticated pages"): the class hub needs an authenticated session too, which that job has no fixture for. Recorded as a deviation here and in DECISION-LOG D-406, not built quietly.
- **Routine, Handouts, Lesson plan, Homework tabs** — each is its own future feature Part (F-AC-05, F-TE-05, F-TE-01, F-AC-07), added to `CLASS_HUB_TABS` when it ships, per spec §8's closing note.
- **Part 4 (voice hints)** — untouched by this Part.

**Review batch (2026-09-27): the stale "F-OP-03 Part 6 not merged" claim.** D-406's original text said register/mark-sheet printing was omitted because Part 6 wasn't merged — Part 6 merged the same day (#76, D-208). `print-tab.tsx` now also renders `GenerateAttendanceRegisterButton` (this month's register, always available, no exam needed) and `GenerateMarkSheetButton` (tied to the latest exam, alongside the report-card actions), both already `OnlineOnly`-wrapped inside their own components (F-ID-11 §4.9, D-308) — no new wrapping needed here. The report-card actions D-406 originally shipped stay; spec §4.5's Print row doesn't name them, but nothing asked to remove already-working, already-reviewed functionality, so the smaller diff was to add what was missing rather than replace what shipped.

**Risk areas:** (1) the hub's own assignment gate (`NOT_ASSIGNED`) had to be strictly separate from, and checked before, every tab's own permission. **Security wording fixed in review (2026-09-27):** the original text here said a bug "would leak another teacher's class data" — overstated. Within one school, RLS is role-scoped, not section-scoped (`students_select`, `attendance_sessions_select`/`attendance_records_select` all read `app.has_role(workspace_id, ...)`, no section check at all): any teacher can already read any section's roster and attendance directly through the database, the same access the full (non-basic) app already gives every teacher. `NOT_ASSIGNED` is a UX narrowing consistent with that existing app-wide convention, not a data-security boundary — a bug here would make the _hub_ behave inconsistently with what its caller can already reach elsewhere, not expose anything RLS doesn't already allow. Still worth the unit test and e2e journey it got, for correctness, not because it's a security boundary. (2) `exam_subjects.teacher_id`/`sections.class_teacher_id` are `workspace_members.id`, not `ctx.userId` — the same trap `getMarkSheet` already had to resolve; `listSectionPapers` follows the identical two-query resolve-then-compare shape. (3) the route's own bundle size: reusing `RollCall` (which already pulls in Radix `Sheet`/`Checkbox`) plus a tabbed interface pushed the page over the repo's 250 kB gzipped budget on the first pass — see §6.

---

## 2. Environment

|                |                                                                                                                                                                                                                                    |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Commit         | see PR #82's head at the time this report's numbers were taken (below)                                                                                                                                                             |
| Branch         | `feat/design-basic-class-hub`                                                                                                                                                                                                      |
| CI run         | see PR #82 checks; this Part's own local numbers are what follow                                                                                                                                                                   |
| Preview URL    | n/a (Vercel builds `main` only, D-70/D-74)                                                                                                                                                                                         |
| Supabase       | project `kekfmibwjejdhxjkmezo` for CI; **`supabase start` (local Docker) for this review's e2e run** — no migration this Part either way                                                                                           |
| Migration head | unchanged — this Part adds no migration                                                                                                                                                                                            |
| Node / pnpm    | v24.x / 10.34.5                                                                                                                                                                                                                    |
| Browsers       | **Review batch (2026-09-27): run for real** — `supabase start`, Chromium via `apps/web/.env.local` (gitignored) pointed at `http://127.0.0.1:54321`, `PLAYWRIGHT_PORT=3104`, `E2E_LIVE_SUPABASE=1`. Detail and real numbers in §5. |

---

## 3. Unit and integration (Vitest)

Ran locally: `pnpm test` (`vitest run --coverage`), full monorepo. Re-run 2026-09-27 after merging `origin/main` (head c319a00, which brought in F-AC-02/F-AC-06/F-OP-03 test files from other lanes).

| Suite                        | Files | Tests | Passed | Failed | Skipped |
| ---------------------------- | ----- | ----- | ------ | ------ | ------- |
| Whole repo                   | 154   | 1553  | 1553   | 0      | 7       |
| — of which new for this Part | 3     | 22    | 22     | 0      | 0       |

One flake seen on the first run of this re-check, unrelated to this Part: `packages/pdf/src/register-marksheet-perf.test.ts` (F-OP-03 Part 6, merged from `main`) failed its 4000ms budget at 4715ms under this session's machine load; re-run in isolation (`npx vitest run --project pdf src/register-marksheet-perf.test.ts`) passed at ~1.6s, and the next full `pnpm test` run was 1553/1553 green. Not touched — out of the design lane's scope.

New/changed test files: `packages/domain/src/class-hub/index.test.ts` (2 cases — `CLASS_HUB_TABS`'s exact list, no duplicates), `packages/db/src/repositories/class-hub.test.ts` (6 cases — `SECTION_NOT_FOUND`, owner/admin bypass, `NOT_ASSIGNED`, a teacher who IS assigned, `dependency_unavailable`, `isNotAssignedError`), `roll-call.test.tsx` (4 new cases in the existing file — confirm-sheet counts and gating, Go back saves nothing, Undo re-saves the previous values, no Undo on a first save).

### Coverage

Whole-repo coverage after this Part and the `origin/main` merge (same run): **Statements 86.76% · Branches 76.97% · Functions 86.13% · Lines 89.53%** (the merge brought in more code from other lanes; this Part's own files are unaffected — see the per-file table in the raw `vitest` output).

**Delta vs `main`:** not measured — no baseline coverage run against `main` was taken in this sandbox before starting (same limitation Part 1's report already recorded).

### Notable cases proven

- `getClassHub`: owner/admin never call `listMySections` at all (a direct assertion, not just an outcome check) — the bypass is real, not an accident of mock data.
- `getClassHub`: a teacher whose `listMySections` result does not include the requested section gets the `NOT_ASSIGNED` sentinel (`fieldErrors._root: ["NOT_ASSIGNED"]`), checked via the exported `isNotAssignedError` helper rather than string-matching the message.
- `RollCall` basic mode: the Save button opens a `ConfirmSheet` and calls no action until "Yes, save" is pressed; "Go back" closes the sheet with zero calls; a first save (no prior session) never offers Undo; an edit to an already-saved session does, and clicking Undo re-saves the ORIGINAL (pre-edit) statuses, not the values the edit just wrote.

---

## 4. Database (pgTAP)

**Not applicable.** This Part adds no migration and no new table — every read goes through `listMySections`, `attendance_day`, `getRollCall`, `listRoster`, and the `exam_subjects`/`exams` tables, all already RLS-protected and covered by their own Parts' pgTAP suites. `check-coverage-test-files.mjs` passes unchanged (no new `workspace_id` table to name).

---

## 5. End to end (Playwright)

**Review batch (2026-09-27): run for real against a local Supabase stack, not skipped.** The BLOCKER in this round's review was exactly right — every prior claim in this section was untested. What was actually done:

1. `supabase start` (Docker Desktop), `apps/web/.env.local` (gitignored, never committed) pointed at the local stack's URL/keys, `E2E_LIVE_SUPABASE=1`.
2. Seeding: `supabase/seed/seed.sql` runs on `supabase start`/`db reset` (owner/teacher/parent + the school workspace, no academic year or grade levels — deliberately, per that file's own header: "academics... arrive with their own migrations/scripts"). This school has never been through `create_school_workspace` (the seed inserts the workspace row directly), so it had neither yet. Added one academic year and one Class 6 grade level with a direct `insert` matching `create_school_workspace`'s own shape exactly (same columns, same values it would have written), then ran `supabase/seed/demo-class-6-ka.sql` unmodified — the existing runbook script, which admits 40 students through the real `admit_student()` function, not a fixture. Added one more section ("খ", no class teacher) the same way, so `teacher@acadigma.test` has a real section they do **not** teach, for AC-11.
3. Found and fixed a real, pre-existing local-dev bug this uncovered: the local GoTrue image 500s on any password sign-in for a user whose `confirmation_token`/`recovery_token`/etc. are `NULL` rather than `''` (`sql: Scan error on column index 3, name "confirmation_token": converting NULL to string is unsupported`) — `seed.sql`'s direct `insert into auth.users` leaves them at the column default (`NULL`), never having gone through GoTrue's own signup path. Fixed by `update`-ing those columns to `''` for the seeded users. Not a code change in this PR (a one-off local-DB fix); flagged here since another builder hitting the exact same local-stack error will otherwise burn real time on it.
4. Ran `basic-class-hub.spec.ts` and `basic-home-tap-targets.spec.ts`, phone (360×800) and desktop (1280×800), serially (`--workers=1`, since both files sign in as the same seeded `owner@acadigma.test` and toggle that one account's server-side `ui_mode` — running them concurrently races two tests toggling the same account's preference under each other).

**What this found, and what got fixed as a direct result (not from reading the code, from watching it run):**

- **A real a11y regression in this review's own new code.** `expectNoA11yViolations` failed on the class hub: the new `ConfirmSheet` `loading` fallback (§6, item 6 below) was an icon-only `<button>` with no accessible name. Fixed: an sr-only label (`roll-call.tsx`'s `LOADING_LABEL`).
- **A real layout bug, also in this review's own new code.** The `56×56` target-size check failed: `class-hub-view.tsx`'s outer wrapper was `max-w-2xl` (672px) — narrower than `RollCall`'s own `max-w-5xl`, so the outer div won (a child's `max-width` can only narrow an ancestor's, never widen past it), squeezing the Attendance tab's 5-segment `AttendanceToggle` on desktop and clipping its "Half day" segment below the fold of `overflow-hidden`. This is exactly the class of bug real rendered evidence catches and a read of the code does not (the same class the width itself measured fine in isolation, in `RollCall`'s own file). Fixed: the outer wrapper now matches `RollCall`'s own `max-w-5xl`.
- **After both fixes, real passing evidence exists for**: sign-in as owner, turning basic mode on, opening the hub from "All classes", all four tabs present with no "coming-soon" tab, roving-tabindex keyboard navigation (Left/Right/Home/End) across the four tabs, "Mark all present" → Save → `ConfirmSheet` naming the plain-language count → "Yes, save" → "Saved: N present, M absent", and the corrected 5-segment `AttendanceToggle` rendering in full (not clipped) at 1280×800 — captured as real screenshots, not just passing assertions (below).
- **`NOT_ASSIGNED` (AC-11) was reached for real**, with the two-section seed above giving a genuine "not mine" section instead of the test's own `test.skip` fallback.

**Numbers, honestly.** No single run of the full 12-test/2-project matrix came back 12/12 green, and I am not claiming one did. Every run in this session shared one Docker Desktop / local Supabase stack that is not mine alone — the owner said as much going in ("If the local stack gets reset by another lane mid-run, just re-seed"), and I have direct evidence of it: `docker ps` showed the `db`/`auth`/`storage`/`realtime` containers restarted (`Up 2 minutes` against 2+ hours for the others) in the middle of a run I was actively watching, and the seeded owner account's `user_preferences` (`ui_mode`, `text_size`, `language`) and `last_active_workspace_id` changed under me between commands I did not run (`text_size` flipped to `xlarge`, section/student counts drifted from 40 to 77, `ui_mode` flipped basic→full→basic) — consistent with another lane's own basic-mode/Extra-large/বাংলা journeys running against the same seeded accounts concurrently. Best single serial run this session: **5 passed, 1 skipped (the built-in `test.skip` guard, before the second section existed), 6 failed** — of the 6, the two bugs above were real (now fixed) and the rest were `turnOnBasicMode` navigation timeouts I could not reproduce in isolation once the shared account's state was reset back to a known-good baseline, matching the container-restart evidence above rather than a defect in this diff. A later run, right after a `next start` process from an earlier iteration turned out to still be listening on the port serving a stale build (`taskkill`'d once found — `EADDRINUSE` in its own log the moment I tried to replace it), also produced a misleading `Something went wrong` error-boundary hit that had nothing to do with the app; excluded from these numbers once identified.

**Screenshots — real, from this run**, `docs/test-reports/assets/2026-09-26-F-ID-10-p3/`:

- [`class-hub-en-360.png`](assets/2026-09-26-F-ID-10-p3/class-hub-en-360.png) — 360×800, English, Attendance tab, mid-roll-call.
- [`class-hub-en-1280.png`](assets/2026-09-26-F-ID-10-p3/class-hub-en-1280.png) — 1280×800, English, same state; the 5-segment toggle's "Half day" (½) segment is fully visible here, the fixed layout bug above.
- [`class-hub-bn-360.png`](assets/2026-09-26-F-ID-10-p3/class-hub-bn-360.png) — 360×800, বাংলা (owner's own `LanguageSwitchButton`, not a fixture): tabs (হাজিরা/নম্বর/শিক্ষার্থী/প্রিন্ট), student names in Bangla script, Western digits throughout (D-401 — "37" not "৩৭").

| Journey                                                  | 360 × 800                                 | 1280 × 800                                | Notes                                                                                                                                                                                                                      |
| -------------------------------------------------------- | ----------------------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `basic-class-hub.spec.ts` (4 tests)                      | passed in isolation, real run (see above) | passed in isolation, real run (see above) | Only built tabs shown; roving `tabindex` + Left/Right/Home/End keyboard nav; confirm-then-save; every tab renders + 56px targets (after the layout fix); `NOT_ASSIGNED` reached for real against the second seeded section |
| `basic-home-tap-targets.spec.ts`'s second test (updated) | passed in isolation, real run (see above) | passed in isolation, real run (see above) | Asserts the All-classes row now opens `/app/classes/[id]` (the hub), not `/app/attendance/[id]`                                                                                                                            |

**Accessibility (axe):** `expectNoA11yViolations`, exercised for real. Found and fixed one real violation (above); clean after the fix, in every run that reached that assertion.

---

## 6. Performance

Measured locally from `pnpm --filter @acadigma/web build` + `node scripts/check-bundle-budget.mjs` (gzip-based, 250 kB budget); re-run 2026-09-27, final numbers below.

| Route                                                                       | First Load JS (gzipped) | Budget | Result |
| --------------------------------------------------------------------------- | ----------------------- | ------ | ------ |
| `/app/classes/[sectionId]` (shipped: Radix `Tabs` + dynamic `ConfirmSheet`) | 241 kB                  | 250 kB | PASS   |
| `/app/attendance/[sectionId]` (dynamic `ConfirmSheet`, down from 243 kB)    | 234 kB                  | 250 kB | PASS   |
| `/app/classes/all` (unchanged, for comparison)                              | 194 kB                  | 250 kB | PASS   |

**Found and fixed during this Part:** the first version measured **257 kB**, over budget. Root cause: Radix `Tabs` plus all three non-Attendance tab components bundled eagerly into the page. `next/dynamic(() => import(...), { ssr: false })` on Marks/Students/Print brought it to 244 kB.

**Review round 1 (2026-09-27, per the owner's shadcn-first rule):** swapped the hand-rolled tablist back for real `@acadigma/ui/components/tabs` (Radix) and rebuilt — 251 kB gzipped, 1 kB over the gate (245 kB for the hand-rolled version, both shifted slightly after the `origin/main` merge). Shipped the hand-rolled tablist for that reason, adding roving `tabindex` + Left/Right/Home/End by hand (WAI-ARIA APG "Tabs") since it had shipped with no keyboard handling at all.

**Lead decision, same review (2026-09-27):** reversed round 1. The owner's standing rule is no hand-rolled replacements for a component shadcn already has, and round 1 itself proved the reason for that rule — the hand-rolled tablist shipped with a real a11y gap. A 1 kB overshoot is the smaller risk. Radix `Tabs` ships; the hand-rolled tablist and its roving-tabindex code are deleted. The 1 kB (and then some) was won back a different way, without new hand-rolled UI: `RollCall`'s `ConfirmSheet` — a `Sheet` that only opens after Save is tapped, not needed for the tab's initial paint — moved behind `next/dynamic(..., { ssr: false })`, the same treatment already applied to Marks/Students/Print. Final: **241 kB** on this route, and **234 kB** on the plain `/app/attendance/[sectionId]` as a side effect (that route pays the same `ConfirmSheet` import cost even though `basic=false` never opens it). `basic-class-hub.spec.ts`'s keyboard assertions (added in round 1) are unchanged and pass against Radix `Tabs` unmodified — it implements the same WAI-ARIA APG contract natively. `roll-call.test.tsx`'s four basic-mode tests were updated to `await screen.findByRole(...)` where they interact with `ConfirmSheet`, since it now mounts one tick after Save instead of synchronously; all 12 tests in that file pass. Full detail in DECISION-LOG D-406's addenda.

Lighthouse: not run — the existing CI `lighthouse` job only tests unauthenticated pages (`/`, `/login`), same limitation Part 2's report already recorded for `/app/home`.

---

## 7. Security checks

| Check                                                                                      | Result                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| gitleaks                                                                                   | not run (no tool in this sandbox) — CI runs it                                                                                                                                                                                                                                                                                                                                                            |
| Semgrep (ERROR severity)                                                                   | passed in CI on this branch's earlier push (PR #82 checks, "Semgrep OSS": pass)                                                                                                                                                                                                                                                                                                                           |
| `pnpm audit --audit-level high`                                                            | not run in this session                                                                                                                                                                                                                                                                                                                                                                                   |
| Supabase advisors                                                                          | not applicable — no migration this Part                                                                                                                                                                                                                                                                                                                                                                   |
| `scripts/check-require-writable.mjs`                                                       | PASS — no new tenant write path in this Part (every write this Part's tabs make already goes through `saveAttendanceSession`/`createReportRun`, both already gated)                                                                                                                                                                                                                                       |
| `scripts/check-permission-parity.mjs`                                                      | PASS — no new permission key added; every tab reuses an existing one                                                                                                                                                                                                                                                                                                                                      |
| Manual: does `getClassHub` let a caller open a hub for a section they are not assigned to? | No, by construction — the gate runs before any tab data is fetched (`page.tsx` calls `getClassHub` first and returns early on `NOT_ASSIGNED`/`not_found`); `listSectionPapers` additionally filters to the caller's own member id, not just the section. (This is a UX-consistency gate, not a data-security boundary — see §1's risk-area note: RLS already lets any teacher read any section directly.) |
| Manual: does the Marks tab show a teacher papers they don't teach?                         | No — `listSectionPapers` resolves the caller's own `workspace_members.id` and filters `teacher_id`/`class_teacher_id` against it before returning any row, narrower than RLS alone would give (any workspace member can read every paper's row through the database directly; this function's own job is the "papers I teach" narrowing, not a security fix)                                              |

---

## 8. Known issues

| #   | Issue                                                                                                                                                                                                                                                                                                                                              | Severity      | Ship anyway?                                                                                                                                                                   | Tracked |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| 1   | CI's own `e2e` job does not set `E2E_LIVE_SUPABASE`, so these journeys still do not run in CI itself — repo-wide, pre-existing, OQ-27. This review batch ran them for real locally instead (§5) and no longer relies only on the built-in `test.skip` guard.                                                                                       | medium        | yes — same CI state as every prior Part; the local run is the new, real evidence                                                                                               | OQ-27   |
| 2   | Not every run of the full local matrix came back green — traced in §5 to a shared, concurrently-used local Supabase stack (direct evidence: container restarts and seeded-account preference changes mid-run that this session did not cause).                                                                                                     | low           | yes — the two bugs the real run did find (an axe violation, a layout clip) were this review's own new code and are fixed; screenshots and isolated passing runs cover the rest | —       |
| 3   | The Students tab (and Print tab's student list) shows only the first 50 students of a section (`ROSTER_PAGE_SIZE`) — no pagination inside the hub.                                                                                                                                                                                                 | low           | yes — a section this large is rare in the seed data this Part was built against; `students/page.tsx` itself accepts the same limit per-page                                    | —       |
| 4   | The Print tab's "latest published or computed exam" logic (`getLatestSectionExam`) is not covered by a dedicated unit test — only exercised indirectly through the e2e journey.                                                                                                                                                                    | low           | yes — a small, pure reduce over already-typed rows (simplified from a Map+sort in this review batch); the risk is low relative to the assignment gate this Part did test       | —       |
| 5   | `RollCall`'s new `basic`/`basicCopy` props add ~40 lines to an already-large component rather than a separate wrapper — a deliberate reuse choice (D-406 item 2), but the file is now bigger than its Part 1/2 authors sized it for.                                                                                                               | informational | yes                                                                                                                                                                            | —       |
| 6   | The `ConfirmSheet` dynamic-import `loading` fallback's sr-only label ("Loading…") is English-only regardless of the reader's language — `next/dynamic`'s `loading` callback runs at module scope, with no access to the locale-aware `t` prop (roll-call.tsx's own comment explains why). The state is normally on screen for well under a second. | low           | yes — see NIT fix in the same file; add a locale-reading mechanism only if this needs more than one sr-only word                                                               | —       |

**Deliberately not tested, and why:**

- "Earlier days" (past sessions) — not built this Part (§11 deviation).
- The Lighthouse hub URL — no authenticated-session fixture in the existing CI `lighthouse` job (§1 deviation note).
- Voice hints — Part 4, gated behind its own go/no-go.

---

## 9. Sign-off

| Definition of Done                           | Met                                                                                                                                                                                                                                                   |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Spec written and matches the build           | ☑ (§8 Part 3, §11 updated, D-406)                                                                                                                                                                                                                     |
| Migration + pgTAP isolation and escalation   | n/a — no migration this Part                                                                                                                                                                                                                          |
| Unit tests + coverage thresholds             | ☑ (1552/1552 real tests passing locally, post-merge; 1 pre-existing unrelated flake noted in §3)                                                                                                                                                      |
| UI built and verified at both viewports      | ☑ built and visually verified for real — 3 screenshots, §5                                                                                                                                                                                            |
| Playwright journey at both viewports         | ☑ written and run for real against a local `supabase start` stack (§5); not 12/12 green in one matrix run — honest numbers and cause (shared-stack contention, with evidence) in §5; CI itself still doesn't set `E2E_LIVE_SUPABASE` (known issue #1) |
| a11y — zero serious/critical + manual checks | ☑ `expectNoA11yViolations` run for real; found and fixed one real violation (§5)                                                                                                                                                                      |
| This test report, with real numbers          | ☑ §5 rewritten this batch with real local e2e numbers, screenshots and the concurrent-stack evidence; CI's own numbers are in PR #82's checks                                                                                                         |
| Docs updated in the same PR                  | ☑ (spec §11, DECISION-LOG D-406, this report, changeset, `docs/README`)                                                                                                                                                                               |

**Signed off by:** design lane builder (Claude Sonnet 5)
**Date:** 2026-09-26, updated 2026-09-27 three times (review batch 1: shadcn Tabs re-measured then hand-rolled per budget; lead decision: reversed to real shadcn Tabs, budget won back via a dynamic `ConfirmSheet` import instead; review batch 2: registry wiring, Print register/mark-sheet actions, a real local e2e run with real bugs found and fixed, and this section's security wording corrected)

> I ran `pnpm format:check`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, every `node scripts/check-*.mjs`, and `pnpm --filter @acadigma/web build` myself in this session; every number under §3 and §6 is copied from those real local runs. §5 was run for real this batch — `supabase start`, real seed data via `admit_student()`, real sign-ins, real screenshots — see it for the honest pass/fail numbers and why the full matrix wasn't 12/12 green in one run. §7's gitleaks/audit rows are still not run in this sandbox, as stated.
