# Test Report — F-AC-06 Exams and marks, Part 2 (demo cut)

|         |                                                                                         |
| ------- | --------------------------------------------------------------------------------------- |
| Feature | F-AC-06 — Exams, marks, grading, GPA and results                                        |
| Part    | 2 — Exams and papers, demo cut (D-303)                                                  |
| Spec    | `docs/features/02-academics/F-AC-06-exams-and-marks.md` §3, §4.1, §5.12, §8 Part 2, §11 |
| PR      | #48                                                                                     |
| Status  | **PASS WITH KNOWN ISSUES**                                                              |
| Date    | 2026-09-25                                                                              |
| Run by  | Claude (billing lane builder)                                                           |

## 1. Scope

**What this Part is.** An owner or admin opens Exams and creates "Half-Yearly 2026" for the year's sections and the subjects they pick. That creates one paper per section × subject, with 100 full marks and 33 pass marks. They then set each paper's date and marks and move the exam along its status chain. The exam keeps a copy of the grading policy it was created with.

| Criterion                                                                  | Covered by                                             |
| -------------------------------------------------------------------------- | ------------------------------------------------------ |
| Papers are created per section × subject in one transaction                | `53_exams.sql` A; `exams.spec.ts`                      |
| Pass marks default from the pass mark (33 of 100)                          | `53_exams.sql` A; `exams.test.ts` (`defaultPassMarks`) |
| The grading policy is snapshotted and never follows a later scale edit     | `53_exams.sql` B                                       |
| §5.12: no cycling, no skipping, reversals need a reason                    | `53_exams.sql` C; `exams.test.ts`; `actions.test.ts`   |
| Owner/admin write; teacher/staff read; parents none; other schools nothing | `53_exams.sql` E; `actions.test.ts`                    |
| Read-only schools cannot create exams                                      | `53_exams.sql` F; `actions.test.ts`                    |

**Out of scope (D-303):** terms, `section_subjects`, components, aggregate exams, calendar events, question-paper upload, idempotency keys and rate limits.

## 2. Environment

Migration head `20260925300305_exams.sql`, after #47's `20260925300304_sections_and_subjects.sql` on main. pgTAP runs in CI only. `types.generated.ts` comes from CI (D-55).

## 3. Unit (Vitest) — local

| Suite                                   | Result                                                                                                                                                                                              |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Whole repo (CI `unit`, run 36112416139) | 102 files, 1110 tests passed (before the review batch; the numbers after it are in the PR report)                                                                                                   |
| `domain/academic/exams.test.ts`         | every from → to pair of the chain, reasons, `defaultPassMarks`                                                                                                                                      |
| `app/exams/actions.test.ts`             | 6 passed: default full marks, date order before context, teacher forbidden, read-only → `payment_required`, a skipped step is refused before the DB, a reversal needs a reason, pass > full refused |

## 4. Database (pgTAP) — CI

`53_exams.sql`: 30 assertions passed at run 36112416139 (29 files, 768 tests in total); the review batch adds 8, for 38.

- Creation, 5: 4 papers, 100/33, draft, snapshot contents, created_by.
- Snapshot, 3: cannot be rewritten; a later scale edit leaves the exam's 7 bands.
- Status chain, 10.
- Guards and review additions: wrong-year section, pass > full, no grade scale, a year that cannot change, and a stranger who gets only RLS's refusal. From `marks_entry` on, papers are locked: marks cannot change, the date can, no new paper, no delete after draft. A paper's status moves one step at a time.
- RLS and composite FKs, 7.
- Read-only refusal and a postgres row count, 2.

## 5. End to end (Playwright)

CI run 36112416139: 20 passed, 58 skipped.

`e2e/journeys/exams.spec.ts` (phone + desktop, axe): the owner creates and schedules an exam, and a teacher sees no "New exam" button. It is skip-gated on `E2E_LIVE_SUPABASE` (OQ-27) and needs a live project with a current year, a section and a subject, so CI skips it. Screenshots were not taken, for the same reason.

## 6. Performance

Not measured. The list is one query with a paper count; the detail page is one query with embedded papers.

## 7. Security checks

`create_exam` is SECURITY INVOKER, so RLS, the snapshot trigger and `require_writable` decide. It is allowlisted in `12_function_grants_invariant.sql`. Every cross-tenant reference is a composite FK. The CI `security` job covers Semgrep, gitleaks and the dependency audit.

## 8. Known issues

| #   | Issue                                                                                                             | Severity | Ship anyway?   |
| --- | ----------------------------------------------------------------------------------------------------------------- | -------- | -------------- |
| 1   | Papers use the subjects the admin picks for every section; per-section subject lists wait for `section_subjects`. | medium   | yes — D-303    |
| 2   | No idempotency key or rate limit on `createExam` yet (§7).                                                        | low      | yes            |
| 3   | The Playwright journey is skip-gated (OQ-27).                                                                     | medium   | yes            |
| 4   | Publish has no marks-completeness gate yet; Part 4 must add one (D-303).                                          | medium   | yes — demo cut |
