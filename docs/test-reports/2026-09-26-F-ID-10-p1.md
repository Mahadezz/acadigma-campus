# Test Report — F-ID-10 Basic mode, Part 1

|         |                                                                  |
| ------- | ---------------------------------------------------------------- |
| Feature | F-ID-10 — Basic mode                                             |
| Part    | 1 — Display preferences: text size everywhere, basic-mode switch |
| Spec    | `docs/features/01-identity/F-ID-10-basic-mode.md` §8 Part 1      |
| PR      | #64                                                              |
| Status  | **PASS WITH KNOWN ISSUES**                                       |
| Date    | 2026-09-26                                                       |
| Run by  | design lane builder (Claude Sonnet 5)                            |

---

## 1. Scope

**What this Part is.** Every signed-in user gets a `text_size` preference (Normal/Large/Extra large) that scales the whole app's root font size, server-rendered with no flash. Owner/admin/teacher additionally get a `ui_mode` (full/basic) switch, hidden for `staff`; turning it on saves the preference, mirrors it to a cookie, and lands on `/app/home`. Both settings are reachable from `/app/settings/display`, and "Switch to basic mode" is now in the full app's user menu. `/app/home` in this Part is a minimal placeholder — Parts 2-3 build the real class-by-class home and hub.

**Acceptance criteria covered** (spec §9):

| #     | Criterion                                                          | Covered by                                                                                                                                                                     |
| ----- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AC-1  | Turn on basic mode → `/app/home`; a second device also opens there | `e2e/journeys/basic-mode-toggle-sync.spec.ts` (second `browser.newContext()`)                                                                                                  |
| AC-2  | Switch to full app, one tap, no confirmation                       | `basic-mode-toggle-sync.spec.ts`                                                                                                                                               |
| AC-3  | Extra large + বাংলা at 360×800, no horizontal scroll               | `expectNoHorizontalScroll` in the same journey (English only — see Known issues #2)                                                                                            |
| AC-14 | Text size from another device: first paint is already at that size | same journey (`data-text-size` checked right after `page.goto`, before hydration could have run)                                                                               |
| AC-15 | `staff` never sees the switch                                      | `display-settings-form.tsx`'s `showBasicModeSwitch` prop, gated in `page.tsx` by `ctx.role`; not directly e2e-tested this Part (no seeded staff account — see Known issues #3) |

**Out of scope for this Part** (per the design-lane brief, explicit descope beyond spec §8 Part 1's own wording): the real class-by-class `/app/home` (`TodayStrip`, `ClassBlock`, `getBasicHome`) and the class hub — F-ID-10 Parts 2-3. AC-4 through AC-13, AC-16, AC-17 are not this Part's to prove.

**Risk areas:** (1) `user_preferences` already existed on `main` with a different shape than the spec described — getting the migration and RLS expectations right against the _real_ table, not the spec's stale description, was the main risk (D-404). (2) The text-size root-scaling mechanism (`<html data-text-size>` + CSS) needed to apply with zero flash — proven by asserting the attribute immediately after `page.goto`, not after any client-side settle time. (3) An inline per-page Server Action closure on `/app/home` was found to add ~70 kB gzipped to that one route's First Load JS in a local production build; replaced with the codebase's existing client-component-calls-a-shared-action pattern, which brought it back in line with every other `/app/*` route.

---

## 2. Environment

|                |                                                                                                                                                                                                       |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Commit         | `e11d83e3aae93660392a3903d1c36553aed93b5b`                                                                                                                                                            |
| Branch         | `feat/design-basic-mode-display`                                                                                                                                                                      |
| CI run         | pending — PR #64 not yet marked ready at the time of this report; update this row with the run link once CI has executed                                                                              |
| Preview URL    | n/a (Vercel previews are off on non-`main` branches, D-70)                                                                                                                                            |
| Supabase       | project `kekfmibwjejdhxjkmezo`; migration not applied to `dev` from this sandbox (no `supabase` CLI network access here) — pgTAP is CI-only for this report, as CLAUDE.md's Definition of Done allows |
| Migration head | `20260925300315_user_preferences_ui.sql`                                                                                                                                                              |
| Node / pnpm    | v24.x / 10.34.5                                                                                                                                                                                       |
| Browsers       | not run locally (Playwright needs `E2E_LIVE_SUPABASE=1` against a migrated project; CI provides both)                                                                                                 |

---

## 3. Unit and integration (Vitest)

Ran locally: `pnpm test` (`vitest run --coverage`), full monorepo.

| Suite                        | Tests | Passed | Failed | Skipped | Duration                                   |
| ---------------------------- | ----- | ------ | ------ | ------- | ------------------------------------------ |
| Whole repo                   | 1275  | 1275   | 0      | 0       | 32.29 s (transform 27.75 s, tests 36.82 s) |
| — of which new for this Part | 26    | 26     | 0      | 0       | included above                             |

New test files this Part added: `packages/contracts/src/ui-preferences.test.ts` (11 cases: enum parity, patch schema), `packages/domain/src/ui-preferences/index.test.ts` (7 cases: `resolveUiPrefs` precedence, `isUiMode`/`isTextSize`), `packages/db/src/repositories/ui-preferences.test.ts` (6 cases: fetch/upsert against a fake Supabase client), plus 2 new cases in the existing `apps/web/app/(shared)/workspace/user-menu.test.tsx`.

### Coverage

Whole-repo coverage after this Part (from the same run): **Statements 90.31 % · Branches 82.39 % · Functions 88.26 % · Lines 92.37 %**. `packages/db/src/repositories/ui-preferences.ts` itself: 100 % statements/functions/lines, 91.66 % branches (line 76, `upsertError` `false`-path fallthrough not separately hit by a dedicated assertion — covered indirectly by the "returns the freshly written pair" case).

**Delta vs `main`:** not measured — no baseline coverage run against `main` was taken in this sandbox before starting. `pnpm test` was green before any of this Part's files existed (implied by every pre-existing test still passing after this Part's changes).

### Notable cases proven

- `resolveUiPrefs`: a valid cookie wins over the stored row (no-flash first paint); a garbage cookie value falls back to the row; each field (`uiMode`/`textSize`) resolves independently; no cookie and no row falls back to the documented defaults.
- `updateUiPreferencesInputSchema`: rejects an empty patch (must change at least one field), rejects an unknown key (`.strict()`), rejects an out-of-range enum value.
- `upsertUiPreferences`: only the keys present in the caller's patch are sent to the database (`{ user_id, ui_mode }`, no `text_size` key at all, when only `uiMode` was patched) — proves the partial-patch contract at the repository boundary, not just in the Zod schema.
- `UserMenu`: the "Switch to basic mode" item is absent entirely when `showBasicModeSwitch` is not passed (the personal/family shells); when present and clicked, it calls `updateUiPreferences({ uiMode: "basic" })` and navigates to `/app/home`.

---

## 4. Database (pgTAP)

**Not run locally** — no live Postgres/pgTAP toolchain in this sandbox (CLAUDE.md Definition of Done: "pgTAP ... are CI-only locally; paste real CI numbers"). Written and ready for the `db` CI job:

| Table                                           | Isolation assertions                                                                                     | Escalation assertions                                                                                                                          | Result               | File                                                                    |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ----------------------------------------------------------------------- |
| `user_preferences` (`ui_mode`/`text_size` only) | 4 (own-row read, stranger read = 0 rows, stranger write = 0 rows affected, anon has no SELECT privilege) | 4 (platform admin gets 0 rows and 0 rows affected on another user's row; both enum types reject an out-of-range value, pinned `22P02` message) | not run — pending CI | `supabase/tests/60_user_preferences_ui.sql` (13 assertions, `plan(13)`) |

**`coverage.sql` / `scripts/check-coverage-test-files.mjs`:** ran locally (the static half only — no `workspace_id` column on `user_preferences`, so this table isn't in that gate's scope at all): **PASS**, 38/38 `workspace_id` tables named, 0 `KNOWN_GAPS`.

### Specifically proven (by the pgTAP file's assertions, once CI runs it)

- A brand-new user's row defaults to `ui_mode='full'`, `text_size='normal'` with no extra insert (the existing `handle_new_user()` trigger already creates the row).
- A direct `UPDATE` aimed at another user's `user_id` affects zero rows (RLS's `USING` clause, not an application-level check).
- A platform admin gets zero rows and zero rows affected against another user's preferences — this table has no platform-admin bypass, unlike `onboarding_progress`'s documented deviation.
- The pre-existing `attach_updated_at` trigger is still attached to `user_preferences` (catalog check against `pg_trigger`, not a `now()` comparison — pgTAP runs the whole file in one transaction, so `created_at`/`updated_at` are not distinguishable by timestamp there, the same reasoning `17_onboarding_progress.sql` documents).
- An out-of-range `ui_mode`/`text_size` value is rejected by Postgres itself (`22P02`), with the exact message pinned per the 4-arg `throws_ok` rule.

---

## 5. End to end (Playwright)

**Not run locally** — `test.skip(!process.env.E2E_LIVE_SUPABASE, ...)` guards the whole file, and this sandbox has no live, migrated Supabase project to point `E2E_LIVE_SUPABASE=1` at. Written and ready for CI:

| Journey                                                                                                                | 360 × 800 | 1280 × 800 | Notes                                                              |
| ---------------------------------------------------------------------------------------------------------------------- | --------- | ---------- | ------------------------------------------------------------------ |
| `basic-mode-toggle-sync` (3 tests: text-size no-flash/no-overflow, basic-mode-on + second context, switch-to-full-app) | not run   | not run    | Update this row with real CI results before the PR is marked ready |

**Accessibility (axe):** `expectNoA11yViolations` is called on `/app/settings/display` (both text-size states) and `/app/home` inside the same journey — not run locally for the same reason.

**Manual checks:** not performed (no running app in this sandbox to click through against a real account). `pnpm --filter @acadigma/web build` succeeded and the production bundle was inspected (§6), which is the closest local substitute available.

---

## 6. Performance

Measured locally from `pnpm --filter @acadigma/web build`'s output + `node scripts/check-bundle-budget.mjs` (the repo's existing gzip-based per-route budget, 250 kB):

| Route                                        | First Load JS (gzipped) | Budget | Result |
| -------------------------------------------- | ----------------------- | ------ | ------ |
| `/app/home`                                  | 194 kB                  | 250 kB | PASS   |
| `/app/settings/display`                      | 209 kB                  | 250 kB | PASS   |
| `/app/settings/overview` (touched)           | 184 kB                  | 250 kB | PASS   |
| `/app/dashboard` (unchanged, for comparison) | 196 kB                  | 250 kB | PASS   |

**Found and fixed during this Part:** the first version of `/app/home` used an inline `"use server"` closure as a `<form action>` handler; that alone measured **264 kB gzipped**, over budget and ~70 kB heavier than every other route sharing the same shell layout. Replacing it with a small client component (`switch-to-full-app-button.tsx`) calling the already-exported `updateUiPreferences` action — the same `useTransition` + `router.push` shape `DisplaySettingsForm`/`UserMenu` already use — brought it to 194 kB. Root cause not fully diagnosed (suspected: Next's per-page action-reference chunking not de-duplicating against the shared bundle); recorded as a known issue for whoever next reaches for an inline page-level server action in this codebase.

Lighthouse (LCP, INP, the §5.5 150 KB budget specifically for `/app/home`): **not run** — the existing Lighthouse CI job needs a deployed/served build; `/app/home` and one class hub are meant to be added to its URL list per spec §5.5/Part 2, and since this Part's `/app/home` is a placeholder that Part 2 replaces wholesale, that addition is deferred to Part 2 rather than pointed at a page about to be rewritten.

---

## 7. Security checks

| Check                                                       | Result                                                                                                                                                                                                                                 |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| gitleaks                                                    | not run (no tool in this sandbox) — CI runs it                                                                                                                                                                                         |
| Semgrep (ERROR severity)                                    | not run — CI runs it                                                                                                                                                                                                                   |
| `pnpm audit --audit-level high`                             | not run in this session                                                                                                                                                                                                                |
| Supabase advisors                                           | not applicable — no migration applied to a live project this session                                                                                                                                                                   |
| `scripts/check-require-writable.mjs`                        | PASS — `user_preferences` correctly has no `workspace_id`, so it is out of this gate's scope; no write action in this Part is a tenant write, so none needed `requireWritable` (D-404 §2, mirrors `updateLocale`'s existing precedent) |
| Manual: does `updateUiPreferences` leak another user's row? | By construction — `upsertUiPreferences` always writes `user_id: <the caller's own auth.uid()>`, never a client-supplied id; RLS additionally refuses any row not matching `auth.uid()` regardless                                      |

---

## 8. Known issues

| #   | Issue                                                                                                                                                                                                                                                                           | Severity      | Ship anyway?                                                                                                                                                                                                                         | Tracked |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| 1   | pgTAP and Playwright were written but not executed in this sandbox (no live Supabase/browser toolchain available here) — real results depend on the CI run for PR #64.                                                                                                          | medium        | yes — CI will gate the merge; this report will be updated with real numbers before `gh pr ready`                                                                                                                                     | PR #64  |
| 2   | AC-3 (Extra large + বাংলা, no horizontal scroll) is only exercised in English in `basic-mode-toggle-sync.spec.ts`; a dedicated বাংলা pass across the two new screens was not added this Part.                                                                                   | low           | yes — the underlying mechanism (root `font-size` scaling of `rem` tokens) is language-agnostic, and the existing `bn-locale-shell.spec.ts` already proves বাংলা renders correctly on these same shell chrome elements at Normal size | —       |
| 3   | AC-15 (`staff` never sees the switch) has no e2e coverage — the seed data has no `staff`-role account signed into a school workspace convenient to this journey. Covered only by the `showBasicModeSwitch` prop's own logic (role check) and by inspection.                     | low           | yes                                                                                                                                                                                                                                  | —       |
| 4   | The inline-server-action bundle-size issue (§6) was found and fixed here, but its root cause in Next's chunking was not fully diagnosed — a future page reaching for the same pattern could reintroduce it silently (`check-bundle-budget.mjs` will catch it in CI either way). | low           | yes                                                                                                                                                                                                                                  | —       |
| 5   | `logo.tsx`'s `text-[17px]` wordmark was deliberately left un-audited (§5.2 rem audit) — it is a documented brand-logotype pin, not our type scale, per its own existing comment.                                                                                                | informational | yes                                                                                                                                                                                                                                  | —       |
| 6   | The pre-existing `user_preferences` delete grant (D-404) was left unchanged, though F-ID-02 §3's own proposed shape says "no delete grant" for this table. Out of this Part's scope — F-ID-02's call to make against its own spec, not a side effect of two new columns.        | low           | yes                                                                                                                                                                                                                                  | —       |

**Deliberately not tested, and why:**

- The class-by-class `/app/home` and class hub — they do not exist in this Part (Parts 2-3).
- Voice hints — Part 4, gated behind its own go/no-go.
- Every "shipped school page" at Extra large + বাংলা with no overflow (spec §8 Part 1's literal wording) — only the two screens this Part actually adds/touches were checked; a repo-wide sweep of every existing page is a larger audit than two new settings screens warrant and was not attempted.

---

## 9. Sign-off

| Definition of Done                           | Met                                                                                                     |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| Spec written and matches the build           | ☑ (§11 deviations recorded, D-404)                                                                      |
| Migration + pgTAP isolation and escalation   | ☑ written; ☐ run (CI-only, pending)                                                                     |
| Unit tests + coverage thresholds             | ☑ (1275/1275 passing locally)                                                                           |
| UI built and verified at both viewports      | ☑ built (`pnpm --filter @acadigma/web build`); ☐ visually verified (no running browser in this sandbox) |
| Playwright journey at both viewports         | ☑ written; ☐ run (CI-only, pending)                                                                     |
| a11y — zero serious/critical + manual checks | ☑ `expectNoA11yViolations` written; ☐ run                                                               |
| This test report, with real numbers          | ☑ (local numbers real; CI numbers pending)                                                              |
| Docs updated in the same PR                  | ☑ (DATA-MODEL.md, DECISION-LOG D-404, spec §11, changeset, this report)                                 |

**Signed off by:** design lane builder (Claude Sonnet 5)
**Date:** 2026-09-26
**Commit:** `e11d83e3aae93660392a3903d1c36553aed93b5b`

> I ran `pnpm format:check`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, every `node scripts/check-*.mjs`, and `pnpm --filter @acadigma/web build` myself in this session; every number under §3 and §6 above is copied from those real runs. §4 and §5's numbers are not yet real — this sandbox has no live Supabase project or browser to run them against — and the PR will not be marked ready until CI has produced them and this report is updated to match.
