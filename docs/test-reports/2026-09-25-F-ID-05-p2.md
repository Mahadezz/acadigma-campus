# Test Report — F-ID-05 Onboarding, Part 2

|         |                                                             |
| ------- | ----------------------------------------------------------- |
| Feature | F-ID-05 — Onboarding                                        |
| Part    | 2 — Onboarding shell, chooser and state                     |
| Spec    | `docs/features/01-identity/F-ID-05-onboarding.md` §8 Part 2 |
| PR      | #24                                                         |
| Status  | **PASS WITH KNOWN ISSUES**                                  |
| Date    | 2026-09-25                                                  |
| Run by  | Claude (Builder D-37)                                       |

---

## 1. Scope

**What this Part is.** A verified, workspace-less user who signs in for the first time now reaches `/onboarding` and sees exactly two choices — "Create a school" and "Join a school" — plus a quiet text link for tutoring solo, instead of the prototype's three contradictory landing paths. Choosing the tutoring link marks onboarding complete and lands on `/personal`, permanently (no forced re-entry). `onboarding_progress` exists so a mid-wizard draft (added by Parts 3-4) is resumable, and `getOnboardingState`/`saveOnboardingDraft`/`completeOnboarding` are the three server contracts everything downstream (the wizard, the join flow) builds on.

**Acceptance criteria covered** (from spec §9 — this Part covers a slice of AC3, AC4 and AC11; AC1-2, 5-10, 12-16 belong to Parts 1 and 3-5):

| #    | Criterion                                                                                                                                  | Covered by                                                                                                                                                                  |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC3  | A verified user with no school membership signing in for the first time sees exactly two cards, no "Personal Workspace"                    | `packages/ui/src/primitives/choice-card.test.tsx`, `apps/web/e2e/onboarding.spec.ts` (live-gated)                                                                           |
| AC4  | The chooser at 360×800 uses design-system tokens only, respects light/dark, both cards in the bottom two-thirds                            | `packages/ui/src/primitives/{choice-card,onboarding-shell}.test.tsx`; manual token review (no hardcoded hex anywhere in this Part's new files)                              |
| AC11 | Choosing "I'm tutoring on my own" sets `onboarding_completed_at`, lands on `/personal`, and signing in later never forces onboarding again | `packages/db/src/repositories/onboarding.test.ts` (`markOnboardingComplete`), `packages/domain/src/onboarding/exit.test.ts`, `apps/web/e2e/onboarding.spec.ts` (live-gated) |

**Out of scope for this Part** — and where it is handled instead:

- The create-school wizard's five steps and `app.create_school_workspace()` — Parts 3-4.
- The join-with-code screens and the first-run checklist — Part 5.
- The nightly `onboarding_progress` draft-cleanup cron — spec §7, not in this Part's §8 build list.
- The account menu named in §6's chooser wireframe row — no such primitive exists anywhere in this codebase yet (see spec §11's Part 2 status note).

**Risk areas** — where the testing effort went:

- RLS on a brand-new user-scoped table with a support-read carve-out (platform staff SELECT) and an intentionally absent DELETE grant — the highest-consequence part of this Part, since a mistake here either leaks another user's in-progress form data or silently allows deleting the resumability guarantee §4.7 promises.
- The `now()`-is-constant-per-transaction trap in pgTAP: an earlier draft of `16_onboarding_progress.sql` asserted `updated_at > started_at` after an UPDATE, which is **always false** inside a single `begin;`/`rollback;` transaction (Postgres's `now()` is the transaction timestamp, not the statement timestamp) — caught before push, replaced with a `pg_trigger` catalog check.
- The `draft` column's nullability: the spec's own prose ("nulling draft") contradicts every other jsonb column in this feature area (`not null default '{}'`) — resolved in favour of the spec's literal words, documented explicitly so it doesn't read as an oversight.
- A route-group path bug: `apps/web/app/(onboarding)/page.tsx` resolves to `/`, not `/onboarding` (Next.js route groups add no path segment) — collided with the marketing `/` page at build time and was caught by `next build`, not by `tsc` or `vitest`. Fixed by moving the screen to `apps/web/app/(onboarding)/onboarding/page.tsx`.
- `/onboarding` was missing from `middleware.ts`'s `PROTECTED_PREFIXES`, so a signed-out visit fell through to the page's own redirect (a bare `/login`, no `?next=`) instead of middleware's `?next=` redirect — caught by CI's `e2e` job (this Part's own `onboarding.spec.ts` signed-out-redirect case), not locally, since this repo's Playwright suite only runs in CI (no Docker on this laptop). Fixed in a follow-up commit on this PR; `docs/plan/HANDOFF-2026-09-24.md`'s "verify rebases" caution does not apply here (no force-push, no rebase — an ordinary additional commit).

---

## 2. Environment

|                |                                                                                            |
| -------------- | ------------------------------------------------------------------------------------------ |
| Commit         | `a9aabd5`                                                                                  |
| Branch         | `feat/F-ID-05-p2-onboarding-shell`                                                         |
| CI run         | https://github.com/Mahadezz/acadigma-campus/pull/24/checks                                 |
| Preview URL    | linked from the PR                                                                         |
| Supabase       | project `kekfmibwjejdhxjkmezo`; pgTAP run in CI's ephemeral Postgres, not the `dev` branch |
| Migration head | `20260925000300_onboarding_progress.sql`                                                   |
| Seed           | none used — this Part's tests create their own fixtures                                    |
| Node / pnpm    | v24.19.0 / 10.x                                                                            |
| Browsers       | Chromium (Playwright, version pinned in `apps/web/package.json`)                           |
| Feature flags  | none                                                                                       |

**Docker unavailable on this laptop** (per the branch's task brief) — `supabase test db` (pgTAP) could not be run locally. `16_onboarding_progress.sql` was reviewed line-by-line against the RLS actually written in the migration and against the one real bug that review found (the `now()` timestamp trap, above) and fixed before push. **CI's `db` job is the gate that has actually executed this file** — see the linked run before trusting the pgTAP numbers below as more than "reviewed, not executed here."

---

## 3. Unit and integration (Vitest)

Real numbers from `pnpm test` (root `vitest run --coverage`, all five projects), run on this branch:

| Suite                          | Tests   | Passed  | Failed | Skipped | Duration (this project alone) |
| ------------------------------ | ------- | ------- | ------ | ------- | ----------------------------- |
| `packages/domain` (25 files)   | 414     | 414     | 0      | 0       | included in full run          |
| `packages/contracts` (9 files) | 108     | 108     | 0      | 0       | included in full run          |
| `packages/db` (9 files)        | 88      | 88      | 0      | 0       | included in full run          |
| `packages/ui` (13 files)       | 104     | 104     | 0      | 0       | included in full run          |
| `apps/web` (4 files)           | 29      | 29      | 0      | 0       | included in full run          |
| **Total (60 files)**           | **743** | **743** | **0**  | **0**   | 10.57 s                       |

Of the 743, **42 are new in this Part**, across 7 new test files:

| File                                                   | Tests |
| ------------------------------------------------------ | ----- |
| `packages/contracts/src/identity/onboarding.test.ts`   | 10    |
| `packages/domain/src/onboarding/access.test.ts`        | 5     |
| `packages/domain/src/onboarding/chooser.test.ts`       | 6     |
| `packages/domain/src/onboarding/exit.test.ts`          | 3     |
| `packages/db/src/repositories/onboarding.test.ts`      | 9     |
| `packages/ui/src/primitives/choice-card.test.tsx`      | 4     |
| `packages/ui/src/primitives/onboarding-shell.test.tsx` | 5     |

### Coverage

Repo-wide gate (`vitest.config.ts` thresholds: 70% repo-wide, 80% for `packages/domain`) — **PASS**:

| Package                        | Lines  | Branches | Functions | Threshold | Result |
| ------------------------------ | ------ | -------- | --------- | --------- | ------ |
| `packages/domain` (overall)    | 98.92% | 95.85%   | 99.29%    | 80%       | PASS   |
| `packages/contracts` (overall) | 100%   | 100%     | 100%      | 70%       | PASS   |
| `packages/db` (overall)        | 95.13% | 78.77%   | 90.91%    | 70%       | PASS   |
| `packages/ui` (overall)        | 86.16% | 75.18%   | 84.73%    | 70%       | PASS   |
| Repo overall                   | 94.3%  | 83.71%   | 92.33%    | 70%       | PASS   |

**This Part's own new files** (`packages/*/src/**/onboarding*.ts{,x}`) are individually **100%** lines/branches/functions/statements — confirmed against `coverage/coverage-summary.json`, not estimated.

**Delta vs `main`:** not measured (no baseline coverage report committed on `main` to diff against); the repo-overall figures above are this branch's own run.

### Notable cases proven

- `resolveOnboardingAccess`: the full redirect matrix — signed-out → `/login`, signed-in-unverified → `/verify`, both membership states → allowed through (§2's table, tested as the exact matrix §8 Part 2 names).
- `resolveOnboardingChooserView`: never offers to resume a completed onboarding even with leftover draft content; distinguishes "path picked, no content yet" from a real resumable draft.
- `resolveOnboardingExitRoute`: all three named exits (`personal`/`created`/`joined`) map to §4.5's table exactly.
- `onboarding_progress` repository: `saveOnboardingDraft` upserts without ever sending `started_at`, so a resumed draft's original start time cannot move; `markOnboardingComplete` fails closed — if the `profiles` update fails, `onboarding_progress` is never touched.
- `saveOnboardingDraftInputSchema`: round-trips a real partial draft (name + board, step 3) byte-for-byte — the literal "draft round-trip" test this Part's §8 line names.
- `ChoiceCard`: renders as `<a>` when given `href`, `<button>` when given `onClick`; asserted ≥120px tall per §6's wireframe.

---

## 4. Database (pgTAP)

| Table                 | Isolation assertions                                                                                     | Escalation assertions                                                                                                 | Result                                                                                                                    | File                                        |
| --------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| `onboarding_progress` | 2 (cross-user SELECT returns 0 rows; cross-user INSERT rejected 42501; cross-user UPDATE touches 0 rows) | 2 (no DELETE grant for the row's own owner, 42501; platform-staff SELECT succeeds where an ordinary user's would not) | **not run locally (no Docker) — CI's `db` job is the gate; reviewed line-by-line, one real bug found and fixed (see §1)** | `supabase/tests/16_onboarding_progress.sql` |

**`coverage.sql`:** not applicable to this table — it checks only tables carrying a `workspace_id` column, and `onboarding_progress` deliberately has none (user-scoped, per §1's design). `scripts/check-coverage-test-files.mjs` confirms this: it still reports "16 workspace_id tables; 16 named" — unchanged by this PR, because `onboarding_progress` was never expected to be in that count.

### Specifically proven (by review; to be confirmed green by CI)

- A second user querying by the first user's `user_id` sees zero rows, and cannot insert or update on their behalf (RLS `USING`/`WITH CHECK` both key on `user_id = app.current_user_id()`).
- No role — not even the row's own owner — can `DELETE`: the table has no DELETE grant to `authenticated` at all, so the failure is a grant-level `42501` raised before RLS is ever evaluated (not a policy that could later be loosened).
- Platform staff (`profiles.is_platform_admin = true`) can read another user's row — the one SELECT branch beyond "your own row," for support.
- `app.attach_updated_at`'s trigger is actually attached to the table (checked against `pg_trigger`/`pg_proc`, not by comparing `now()` values — see §1's note on why the latter cannot work inside pgTAP's single transaction).

---

## 5. End to end (Playwright)

| Journey                                                                           | 360 × 800                         | 1280 × 800   | Duration | Notes                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------- | --------------------------------- | ------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `onboarding chooser — signed-out redirect` (`onboarding.spec.ts`)                 | not run here (needs a dev server) | not run here | —        | DB-free; will run in CI same as the rest of the suite                                                                                                                                                 |
| `onboarding chooser — two cards, tutoring exit, axe clean` (`onboarding.spec.ts`) | **SKIPPED**                       | **SKIPPED**  | —        | `test.skip(!process.env.E2E_LIVE_SUPABASE, ...)` — no seeded account exists anywhere in this repo yet (OQ-27 open, per `docs/plan/HANDOFF-2026-09-24.md`); precedent set by F-ID-09's Part 1-3 report |

**Flaky (passed on retry):** none observed (suite not executed locally; no retry data to report).
**Report artifact:** none generated locally; CI's Playwright HTML report is the artifact of record once the run linked above completes.

### Accessibility (axe, WCAG 2.1 AA)

| Screen        | Critical     | Serious      | Moderate     | Minor        | Notes                                                                                                                                                                                                                                                                                                                                                          |
| ------------- | ------------ | ------------ | ------------ | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/onboarding` | not measured | not measured | not measured | not measured | The live-gated e2e test (`onboarding.spec.ts`) calls `expectNoA11yViolations` on this screen; it did not run this session (OQ-27). `ChoiceCard`/`OnboardingShell`'s Vitest suites assert semantic roles (`link`/`button`, `heading level 1`, `progressbar` with `aria-valuenow`) but do not run a full axe pass — that only happens in the Playwright journey. |

**Manual checks** (axe cannot see these) — reviewed against the built markup, not measured with a real device:

| Check                                           | 360 × 800                                                                                                                       | 1280 × 800 |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| Touch targets ≥ 44 px                           | `ChoiceCard` is `min-h-[120px]`; well over 44px                                                                                 | same       |
| Visible focus ring on every interactive element | Uses the shared `focus-visible:ring-*` token classes already proven elsewhere (no bespoke focus styling added)                  | same       |
| Primary action completable by keyboard alone    | `ChoiceCard`/`TutoringExitLink`/`StartOverLink` are native `<a>`/`<button>` elements — no custom keyboard handling to get wrong | same       |
| `aria-live` announces save and error            | The chooser's load-error path uses `InlineAlert`, which already carries `aria-live="polite"`                                    | same       |
| No horizontal scroll                            | Not measured on a real device this session                                                                                      | n/a        |
| Contrast ≥ 4.5:1 on new surfaces                | `scripts/check-contrast-tokens.mjs` — PASS (all existing token pairs; no new colour introduced)                                 | same       |

### Screenshots

Not captured this session (no dev server run against a live Supabase project). To be attached from the CI Playwright run or a manual pass once OQ-27 is resolved.

<!-- Synthetic seed data only. Never a real name, phone number, ID number or medical detail. -->

---

## 6. Performance

| Budget                          | Target                         | Measured                                                                                                                                                          | Result                |
| ------------------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| First-load JS for `/onboarding` | < 25 kB (own JS, per spec §10) | **1.52 kB** route-specific JS (`next build` output; 188 kB total First Load JS, of which ~186 kB is the shared framework chunk every route pays)                  | PASS                  |
| Lighthouse Performance (mobile) | ≥ 85                           | not run (no deployed preview this session)                                                                                                                        | not run               |
| Lighthouse PWA                  | ≥ 90                           | not run                                                                                                                                                           | not run               |
| Lighthouse Accessibility        | ≥ 95                           | not run                                                                                                                                                           | not run               |
| LCP, phone                      | ≤ 2.5 s                        | not run                                                                                                                                                           | not run               |
| INP on the primary action       | ≤ 200 ms                       | not run                                                                                                                                                           | not run               |
| Server action p95               | ≤ 400 ms                       | not run (no live DB round-trip measured this session)                                                                                                             | not run               |
| Queries per list render         | ≤ 4                            | `getOnboardingState` issues exactly 2 (one for `onboarding_progress`, one `list_my_workspaces` RPC via `listMyWorkspaces()`), run concurrently with `Promise.all` | PASS (by code review) |

---

## 7. Security checks

| Check                                                             | Result                                                                                                                                      |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| gitleaks                                                          | not run locally — CI's `security` job is the gate                                                                                           |
| Semgrep (ERROR severity)                                          | not run locally — CI's `security` job is the gate                                                                                           |
| `pnpm audit --audit-level high`                                   | not run this session (no dependency changes in this PR)                                                                                     |
| Supabase advisors — new security advisories                       | not applicable pre-merge — this migration has not been applied to any live Supabase project yet                                             |
| Authorized DAST against preview (if auth / money / files changed) | not applicable — this Part touches neither authentication, money nor files; it is a new user-scoped, RLS-protected table and a routing gate |

**If DAST was run** — not applicable this Part.

---

## 8. Known issues

| #   | Issue                                                                                                                                                                                                                                  | Severity | Ship anyway?                                                                                                                                            | Tracked                                            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| 1   | The chooser's two `ChoiceCard`s link to `/onboarding/create-school` and `/onboarding/join`, neither of which exists yet — both 404 until Parts 3-5 ship                                                                                | low      | yes — the expected shape of building one Part at a time (CLAUDE.md: "one feature at a time"); no route in this PR promises otherwise                    | Parts 3-5 close this                               |
| 2   | pgTAP (`16_onboarding_progress.sql`) was not executed locally (no Docker) — reviewed by hand, one real bug found and fixed pre-push (the `now()` timestamp trap, §1), but "reviewed" is not "proven green" until CI's `db` job runs it | medium   | yes, pending — CI is the actual gate per CLAUDE.md ("pgTAP/e2e run in CI"); this PR is not mergeable until that job is green                            | this PR's CI run                                   |
| 3   | The chooser + axe Playwright journey is skip-gated on `E2E_LIVE_SUPABASE` and has never executed against a real account, in this PR or any prior one — OQ-27 (seed the live project) is still open                                     | medium   | yes — matches the precedent every prior Part in this repo has set (F-ID-01, F-ID-09, F-ID-03); not specific to this Part                                | OQ-27                                              |
| 4   | No account menu on the chooser, though §6's wireframe row names one — no such primitive/pattern exists anywhere in this codebase yet (not even `/app`'s `TopBar`)                                                                      | low      | yes — inventing shared chrome inside a single Part is worse than building it once, deliberately, when something actually needs it                       | not tracked as a separate issue; noted in spec §11 |
| 5   | Lighthouse/INP/LCP/server-action-p95 performance budgets were not measured this session — no deployed preview or live database round trip available locally                                                                            | low      | yes — CI's `lighthouse` job and a live preview are the actual gate; the one budget checkable from a local build (first-load JS) passes with wide margin | this PR's CI run                                   |

**Deliberately not tested, and why:**

- The wizard steps, the join flow and the first-run checklist — none exist yet; Parts 3-5.
- The nightly draft-cleanup cron — out of this Part's §8 build list.
- Concurrent-write races on `onboarding_progress` — both writers in every code path here are always "the same user, sequentially" (no server-side concurrent writer exists for this table); not a risk this Part's design introduces.

---

## 9. Sign-off

| Definition of Done                           | Met                                                                                                                                    |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Spec written and matches the build           | ☑ (spec §11 updated with this Part's status/deviations in this PR)                                                                     |
| Migration + pgTAP isolation and escalation   | ☑ written; ☐ **not yet proven green by CI at the time this report was drafted** — see §4, §8 #2                                        |
| Unit tests + coverage thresholds             | ☑ 743/743 passing, all thresholds met, new files at 100%                                                                               |
| UI built and verified at both viewports      | ☑ built (`next build` succeeds); ☐ not visually verified on a real device this session                                                 |
| Playwright journey at both viewports         | ☐ the DB-free case will run in CI; the live-account case is skip-gated pending OQ-27                                                   |
| a11y — zero serious/critical + manual checks | ☐ axe itself did not run this session (folded into the skip-gated e2e journey); manual token/semantics review only                     |
| This test report, with real numbers          | ☑ every number above is copied from a real local run; the ☐ boxes above are exactly the items this report identifies as not yet proven |
| Docs updated in the same PR                  | ☑ spec §11, DATA-MODEL.md §1.7a + §0.3 + the ERD, `docs/README.md`, this report, a changeset                                           |

**Signed off by:** Claude (Builder D-37)
**Date:** 2026-09-25
**Commit:** `a9aabd5`

> I ran every Vitest, lint, typecheck, `next build` and `node scripts/check-*.mjs` command in this report myself, on this branch, this session, and the numbers above are copied from those real runs. The pgTAP suite and the live-account Playwright journey are the two things I could **not** run myself (no Docker, no seeded Supabase account) — I reviewed the SQL by hand, found and fixed one real bug that hand-review caught (a pgTAP assertion that would always have failed for an unrelated reason — `now()` being constant inside one transaction, not the RLS logic it meant to test), and CI is the actual gate for both. This PR should not be treated as fully proven until CI's `db`, `e2e` and `lighthouse` jobs are green.
