# Acadigma Campus — Decision Log

Every non-obvious decision, with the reasoning at the time it was made.
Format: **D-nn — title** · status (PROPOSED / ACCEPTED / REJECTED / SUPERSEDED) · date.

---

## D-01 — Rebuild, do not patch the Base44 export · ACCEPTED (carried over from 2026-09-15) · 2026-09-17

**Why:** The security review shows the tenancy model is anchored to a client-writable field (`active_workspace_id`), 42/64 entities have no RLS, the marketplace has no payment processing, and the audit log is written from the browser. These are design-level, not bug-level. Patching RLS blocks one by one inside Base44 would leave the product dependent on a platform whose server behaviour we cannot see or test. The export stays untouched as the **product reference** (it tells us what was intended); the new app re-implements it.

## D-02 — Runtime stack · PROPOSED · 2026-09-17

**Context:** The 2026-09-15 spec chose a pnpm/Turbo monorepo with a Fastify API, Next.js web, Expo mobile, Tauri desktop, self-hosted Postgres + Redis + S3-compatible storage + SMTP, all via Docker. That skeleton was never run locally (pnpm was never installed, CI never placed, worktree never pushed).

**Recommendation:** **Next.js 15 (App Router) on Vercel + Supabase** (Postgres with RLS, Auth, Storage with signed URLs, Edge Functions for webhooks/jobs, Realtime for messaging/notifications, pg_cron for scheduled jobs). Server-side business rules live in Next.js route handlers / server actions using the Supabase **service role only on the server**, with RLS as the second wall.

**Why this over the Fastify design:**

1. The three non-negotiables of the spec (RLS + server policy + private files) are exactly what Supabase gives natively; the Fastify design would have us re-implement auth, storage signing, migrations tooling and job queues by hand.
2. Zero servers to run. The owner is one person shipping several products (Velora, Bengal Wireless, Nusrat Desire) on the same Vercel + Supabase pattern — operational consistency matters more than architectural purity.
3. Postgres is still Postgres: every RLS policy, trigger and audit function is plain SQL in versioned migrations; nothing locks us in.
4. Redis/queues: replaced by Postgres-backed job tables + pg_cron + Edge Functions, which is enough for print queues, credit renewal and notification fan-out at school scale (thousands, not millions, of rows per tenant).

**What we keep from the previous design:** the monorepo shape (`apps/web`, `packages/domain`, `packages/contracts`), Zod contracts, the role vocabulary, the three-layer authorization rule, the release ordering, tenant-isolation tests as a hard requirement.

**Cost of being wrong:** if the product later needs a long-running service (e.g. a real-time print agent broker), it can be added as a separate service talking to the same Postgres. Nothing in this choice prevents that.

## D-03 — Client scope for v1 · PROPOSED · 2026-09-17

**Recommendation:** Web (installable PWA, mobile-first layouts) only for v1. Expo mobile and the Tauri desktop print agent are deferred to after Release 3.

**Why:** Every feature in the Base44 app is a web feature. Mobile and desktop in the old spec are "boundary only" READMEs. Shipping three clients before one works is how projects die. The PWA covers attendance-on-phone for teachers and the parent portal; browser printing covers the print center until a local agent is justified.

## D-04 — Tenant context comes from the session, never from a client-writable field · ACCEPTED · 2026-09-17

**Why:** Root cause of the whole security review. Implementation: `workspace_members` is the only source of membership; the active workspace is selected per request (header/cookie) and **verified against membership** in a `SECURITY DEFINER` function that every RLS policy calls (`auth_workspace_id()` + `auth_role_in(workspace)`). Role is never read from the `users` row.

## D-05 — Audit events written by the database, not the client · ACCEPTED · 2026-09-17

**Why:** A client can skip a client-side call; it cannot skip a trigger. Every tenant table gets an `audit_events` trigger (actor from `auth.uid()`, before/after diff, correlation id from a request-scoped setting). Table is append-only (no UPDATE/DELETE grants).

## D-06 — Money is never computed in the browser · ACCEPTED · 2026-09-17

**Why:** The Base44 app lets the buyer write `MarketplaceTransaction` and `SellerEarnings` rows with self-chosen amounts and has two disagreeing commission rates. Prices, commission, earnings and entitlements are computed in one server function from a single `platform_settings` row; purchases only become entitlements after a verified payment webhook.

## D-07 — Existing repo, `main` as default branch · ACCEPTED · 2026-09-17

**Why:** `Mahadezz/acadigma-campus` already exists with `master` (docs only) and `main` pushed. Keeping one repo keeps history. `master` will be deleted after `main` becomes default. The unpushed `feat/production-foundation` worktree is superseded by D-02; its useful parts (domain policy tests, contracts) are ported, the rest is dropped.

## D-08 — AI provider · ACCEPTED · 2026-09-17

Claude API (`claude-sonnet-5` for planner generation, `claude-haiku-4-5` for cheap classification/extraction). Called only from server code with a per-workspace credit ledger checked **before** the call and debited **after**, in one transaction. Reason: the Base44 app never checks quota before `InvokeLLM` (`AIPlanner.jsx:88`); the credit model in the entities (AIBillingModel / CreditAllocation / CreditRequest / DailyAILimit) is the clear intent and is implemented server-side.

## Owner acceptance of D-02 and D-03 · 2026-09-17

Next.js on Vercel + Supabase; web PWA only for v1.

## D-09 — Payments: SSLCommerz (BDT) first, Stripe later · ACCEPTED by owner · 2026-09-17

**Why:** Bangladesh-first product; SSLCommerz covers cards + bKash + Nagad + Rocket through one gateway. Seller payouts are recorded as manual bank/bKash transfers via an admin payout queue (SSLCommerz has no Connect-style split payouts). The gateway sits behind a `PaymentProvider` interface so Stripe can be added without touching purchase/entitlement logic. All amounts stored as integer paisa (`bigint`), currency column present from day one.

## D-10 — Brand and suite structure · ACCEPTED by owner · 2026-09-17

**Owner's words:** "There are 4 major parts to this suite: one for campus, one for students, one for parents and one for school admin (a finance app pulling data from the campus app). Separate repos. 100% focus on the campus app — it's the flagship. There is no Career or Market product; marketplace and hiring are parts inside Campus so sellers and teachers benefit from the platform."

**Resulting structure:**

- Parent brand **Acadigma**. Flagship app **Acadigma Campus** (this build). Later: Acadigma Students, Acadigma Parents, Acadigma Admin (finance).
- One repo per app. Campus = `Mahadezz/acadigma-campus`. The other three get repos when they start.
- One Supabase project shared by the suite, owned by Campus; the other apps consume it through the same RLS + contracts. (Decision: shared DB rather than per-app DBs, because Students/Parents/Admin are views over Campus data, not separate domains.)
- Marketplace, Seller Portal and Recruitment/Hiring remain **modules inside Campus** with their own nav areas.
- Local folder: `F:\Acadigma Suite\` with `acadigma-campus\` (clone of the repo, branch `main`), and placeholder folders for the other three.
- In Campus v1, the parent role keeps the read-only Parent Portal that exists in the Base44 app (it's already built and is the data source the future Parents app will replace). Not building more parent features in Campus.

## D-11 — The `F:\Acadigma Campus\acadigma-campus-production` folder is superseded · ACCEPTED · 2026-09-17

**Why:** Its worktree was built for the Fastify design (D-02 rejected). Not deleted — the owner can remove it. Its domain policy tests and Zod contract shape are ported into the new repo.

## D-12 — Node 24 + pnpm 10 · ACCEPTED · 2026-09-17

**Why:** Node 24.19 is what's installed. The old plan pinned pnpm 11.19.0 which does not exist on this machine's corepack; pnpm 10 (current stable) is used and pinned via `packageManager`.

---

## Research-debate synthesis, adopted · 2026-09-17

Source: `docs/product/research/debate/SYNTHESIS.md` and `docs/product/research/DECISION-CHANGES.md`. The lead adopts the synthesis verdicts as defaults: Free school plan abolished pending owner sign-off, the MARKET-STRATEGY §c price list as given, attendance default unmarked, fees offline P1–6 inside the R1 gate, hiring M4 apply-only, marketplace gate immediately after M3, the listed PLG loops killed, compliance MUSTs accepted. Amendments to previously-numbered decisions are appended below rather than rewritten in place, to keep the original reasoning intact; brand-new rules are numbered D-40 onward.

## Amendment to D-22 — Attendance no longer pre-fills present · AMENDED · 2026-09-17

**Amends:** D-22 point 1 (`defaulted_present = true`, pre-fill present + confirm).
**New rule:** Default `unmarked`. "সবাই উপস্থিত / Mark all present" ships as **one explicit header tap writing an audited bulk action** carrying the actor's user id, surfaced in the monthly register. `defaulted_present` is dropped; `bulk_marked_by` / `bulk_marked_at` are recorded instead. The undo toast is retained.
**Why:** A default that fabricates attendance records poisons every downstream number (%, risk score, health score, GPA denominators) and cannot be fixed retroactively once schools rely on the history. Read-back audits catch a bad _action_ fast; a bad _default_ is invisible until it is load-bearing (SYNTHESIS X-01). Priority: R1, before M2 2.4.

## Amendment to D-27 (finalized) · 2026-09-17

**Amends:** D-27 points (1), (2), (3), (8).

1. **Fees:** offline recording (F-CM-08 P1–6) moves inside the **R1 launch gate** (M3, not M4); online payment (P7–14) stays R1.5. The CFO's objection was to the online rail's unbudgeted cost, not to recording fees at all — separating them satisfies both critics.
2. **SMS:** priced per UCS-2 segment (৳0.75) with margin, and recognised as a **legal-entity dependency** (BTRC enlistment needs a company name, trade licence, TIN, NID), not two engineering glue parts.
3. **Pricing:** band + overage confirmed as the shape; **teacher caps are deleted** on every plan.
4. **Market sizing:** BANBEIS 2022 (137 schools / 71,456 students) is the number of record; BEMA's "300+/300,000+" is a labelled trade-body claim; Eduman, ClassTune and IEIMS/EMIS are added to the competitive map as the real incumbents.
   **Why:** Fees are the market's anchor module — a school will not switch to a system that cannot collect money — and the offline half has no gateway dependency, so there is no reason to hold it for M4. SMS lead time is regulatory, not engineering, and treating it as "2 glue parts" hid a company-formation dependency from the schedule. Per-seat pricing was suppressing our own north-star metric (login frequency) by making schools ration accounts.

## Resolution of D-28 — RESOLVED from PROVISIONAL · 2026-09-17

1. Plan shape and numbers are final (MARKET-STRATEGY §c; migration `20260917010300_plans_and_notifications.sql`).
2. AI approval is universal **except that parent-facing text may never be bulk-approved** — a per-student edit or typed sentence before `parent_visible`, teacher's name on the comment.
3. `open_to_work` is a private preference; **candidate browse is deleted; hiring is apply-only.**
4. Pilot = 8–10 weeks paid, spanning one terminal exam; self-serve trial = **30 days**. **Hiring timing resolved: M4, minimal 4 parts (P1–4), apply-only.**
   **Why:** The debate round closed every item D-28 left provisional. "Approve all" for forty children at 9pm is one tap and the first wrong comment costs the school; a browsable teacher directory is safe only for the unemployed in a city where forty principals share a WhatsApp group (SYNTHESIS A-05, H-03).

## Completion of D-39 · 2026-09-17

**Completes** the D-39 entry above (allowances restated as per-MONTH). Final numbers: **trial 100 AI actions lifetime · Starter 200/month · Pro 600/month, pooled · Enterprise contractual cap.** Top-up **৳1,200 / 500 actions**, hard ceiling **3× plan allowance / month**. COGS planning at **৳1.45/action**, budgeted at **cost + 30%**.
**Why:** Pooling matches how teachers actually work — Thursday-night bursts, not daily quotas — and a hard ceiling with a priced top-up keeps an uncapped top-up from becoming an unsecured credit line against a monthly invoice-and-pay subscription (MARKET-STRATEGY §h).

## Supersession of D-26(4) · 2026-09-17

**Supersedes:** `ai_actions.min_plan_tier` gating (Free limited to haiku-backed actions).
**New rule:** Model routing is **by language risk, not plan tier.** Any Bangla, parent-facing output uses the stronger model on every plan; cost is controlled by _which actions_ a tier may call, never by degrading Bangla.
**Why:** `min_plan_tier` gates by cost, cost tracks model size, and model size determines Bangla quality — so the old rule guaranteed the worst Bengali went to the schools least able to notice the errors, directly contradicting "never ship unreviewed Bangla to parents" (SYNTHESIS A-04).

## Amendment to D-34 — Minimisation strengthened · AMENDED · 2026-09-17

**Amends:** D-34's minimisation clause.
**New rule:** **Student/guardian NID and birth-certificate _numbers_, and `monthly_income_bdt`, are dropped entirely** — replaced by an attestation record `{type, last4, verified_at, verified_by}`. Scans purge at **admission decision + 14 days** (not 90). KYC images retained **180 days** (F-CM-02 wins over COMPLIANCE §4.4's "2 years"). `redactForAI()` moves to M0/M1 and **fails closed**. Localisation gains a roadmap item, a named owner and a decision gate at M6.
**Why:** A number we never store cannot leak, be subpoenaed wrongly, or need a retention clock at all; "we don't archive" is only a true claim once the numbers themselves are gone, not just the scans. Priority: MUST.

## Update to D-17 — OPEN to the owner, recommendation attached · 2026-09-17

Current rule: payout minimum ৳1,000, monthly on the 1st. **Recommendation: ৳300, weekly auto-payout, instant-on-request with the fee stated**, bKash cash-out cost shown honestly in the earnings screen. ৳1,000 **stands until the owner rules** (OQ-15).
**Why:** Median new marketplace sellers earn ~$32 in month 2; at ৳1,000 most first-cohort sellers never cash out and never make a second product — the 29-day wait to a first payout is the retention event we are currently failing. Priority: later (M6).

## D-13 — Native Android + Windows via wrappers around one mobile-first web app · ACCEPTED (owner requirement) · 2026-09-17

**Owner's words:** "this will be a native android and windows app since it's for teachers, so the versions should also support the phone and make the app optimized for the mobile phones so it feels like it was made for a phone."

**Decision:** One codebase. The Next.js app is designed phone-first (bottom tab bar, thumb-reach actions, 44px targets, sheets instead of modals, pull-to-refresh, offline-tolerant attendance). It ships as: (1) installable PWA, (2) **Android via Capacitor** (native shell loading the app, with native plugins for push, camera/QR, file download, share), (3) **Windows via Tauri 2** (native shell + local printer access for the print center). Same screens, same API, same Supabase session.

**Why wrappers, not React Native:** every feature already exists as web UI; RN would mean a second implementation of ~70 screens. Capacitor/Tauri give store-distributable binaries, push notifications and device APIs with one UI codebase. The cost is that some very native gestures won't be present; for a teacher-productivity app that trade is right.

**Sequencing:** the wrappers are added after the web app's Release 1 is green, but every screen is built and tested at 360×800 first from day one.

## D-14 — Git workflow, CI, releases · ACCEPTED (owner requirement) · 2026-09-17

**Owner's words:** "whenever you are working on a major feature push the files and create branches so it doesn't break the app; I need test reports; packages, releases, everything tracked properly."

**Decision:**

- `main` is protected and always deployable. Every feature = branch `feat/<area>-<feature>` → PR → CI green → squash-merge.
- CI on every PR: typecheck, lint, unit tests (Vitest), DB tests against a disposable Postgres (RLS + policy tests), Playwright e2e on a preview build, security scans (dependency audit + secret scan + Semgrep). Test reports are uploaded as PR artifacts and summarized in the PR via a job summary; `docs/test-reports/` keeps a markdown report per feature.
- Releases: Conventional Commits → Changesets → tagged releases `v0.x.y` with generated CHANGELOG; Vercel deploys `main` to production, PRs to previews. Supabase migrations are applied by CI on merge to `main` (never by hand).
- Definition of done per feature (no moving on without all): spec in `docs/features/<feature>.md` ✔ · migrations + RLS tests ✔ · server logic unit-tested ✔ · UI at 360×800 and 1280 ✔ · Playwright journey ✔ · a11y check ✔ · test report ✔ · PR merged ✔.

## D-15 — Commission 30 % platform / 70 % seller · ACCEPTED by owner · 2026-09-17

Single value in `platform_settings`, editable only by platform staff, applied server-side at purchase time, snapshotted onto each order line.

## D-16 — Moderation by Acadigma platform staff via an internal console · ACCEPTED by owner · 2026-09-17

`/platform` area inside Campus for accounts with `is_platform_admin`. Queues: listing review, KYC, payouts, refunds, plan changes, audit viewer. Listings publish only after approval.

## D-17 — Payouts monthly, min ৳1,000, 7-day hold · ACCEPTED by owner · 2026-09-17

Earnings state machine: `pending` (sale) → `available` (after 7 days, unless refunded) → `paid` (staff records transfer ref in payout queue on the 1st). Seller sees statement + payout method (bank / bKash / Nagad) stored on `seller_payout_methods`.

## D-18 — Plan matrix proposed by Claude with placeholder BDT prices · ACCEPTED by owner · 2026-09-17

Plans live in a `plans` table (limits + prices), editable from the platform console without deploy. Placeholder matrix is in `docs/product/PRODUCT-DECISIONS.md` §Billing.

## D-19 — Supabase project · SUPERSEDED by D-53 (2026-09-24) · 2026-09-17

`acadigma-suite`, ref `bvqzhrvcrxebawjusrxk`, region ap-south-1, URL `https://bvqzhrvcrxebawjusrxk.supabase.co`. Publishable key `sb_publishable_6POCbYjBAcrSiT1AlCPSVQ_SG3ohfsL` (safe for browser). Service-role key is never written to disk outside `.env.local`.

## D-20 — Single Supabase project for dev and prod until launch · ACCEPTED · 2026-09-17

**Context:** Supabase branching requires the Pro plan; Docker is not installed on the dev machine. The project holds no real data before launch.

**Decision:** Until Release 1 launches, `acadigma-suite` is both dev and prod. Migrations are applied by CI from `main` only. Feature branches run pgTAP tests in CI against a throwaway Postgres 17 **service container** with the same migrations, so RLS tests never need the cloud project. Seed data is marked demo and wiped before launch. At launch: upgrade to Pro (backups, PITR, branching) and switch previews to Supabase branches — a `db.yml` config change only.

**Why:** Free tier while there is nothing to protect; CI independent of the cloud project; no rework later.

## D-21 — `main` protection is procedural until GitHub Pro · ACCEPTED · 2026-09-17

**Context:** Branch protection / rulesets on a private repo need GitHub Pro (API returned 403). Making the repo public is rejected (security-relevant source, school data platform).

**Decision:** `main` receives only squash-merged PRs with all seven CI checks green (`format`, `typecheck`, `lint`, `unit`, `build`, `e2e`, `security`). No direct pushes. The CI workflow additionally fails any push to `main` whose commit is not a merge of a PR (guard job). When the owner upgrades to GitHub Pro, the protection rule in `docs/engineering/CI.md` is applied verbatim.

## D-22 — Design-system rulings · ACCEPTED · 2026-09-17

Four points the design lead flagged (`docs/architecture/DESIGN-SYSTEM.md`):

1. **Attendance pre-fills every student as present** — ACCEPTED. It is what makes ≤2 taps/student true. Safeguards: the session is not saved until the teacher taps Submit; the submit sheet shows "N absent, M late" for confirmation; `attendance_sessions.defaulted_present = true` is recorded so analytics can distinguish "marked" from "defaulted" if a school worries about data quality.
2. **Rules (dividers) instead of cards for record lists** — ACCEPTED. Denser on 360 px; documented as a lint rule so implementers don't revert to cards.
3. **`--warning` fill carries ink text, not white** (3.25:1) — ACCEPTED as a documented exception.
4. **Custom numeric pad for phone marks entry** — DEFERRED. v1 uses a native `<input inputmode="decimal">` with next/previous-cell controls above the keyboard; the custom pad becomes a later part only if field testing shows the OS keyboard is unusable. Reason: custom input UI is a maintenance and accessibility cost we should not pay before evidence.

Fonts: Inter + Hind Siliguri (measured subset sizes; Bengali loads on demand). Tokens live in `packages/ui/tokens/tokens.css`; no feature code may introduce colours or spacing outside them.

## D-23 — Operations-spec rulings · ACCEPTED · 2026-09-17

From `docs/features/05-operations/*` conflicts: (1) staff pay lives in period-versioned `staff_compensation` (owner/admin-only) + `app.staff_hourly_rate()` — never on `staff_records`; (2) recruiter↔candidate DMs are out of v1 (candidates are not workspace members) — templated email + application notes instead; (3) report cards are per exam; Term/Annual use an `aggregate` exam kind with weighted components; (4) academics owns `academic_years/terms/holidays/grade_scales` tables, settings screens live in F-OP-07; (5) school policy blobs are jsonb validated by Zod, resolved by one `resolve()`; (6) report files expire at 30 days, print jobs keep a `file expired` state with Regenerate; (7) staff attendance records which half for half-day absences; (8) `exam_paper` print jobs visible only to owner/admin/staff + creator; (9) no fake printer telemetry columns — live status only from the future agent's table.

## D-24 — Commerce-spec rulings · ACCEPTED · 2026-09-17

From `docs/features/04-commerce/*`: (1) seller data is **user-scoped** (documented exception to D-04/1.6; selling is a user capability); buyer data is workspace- or user-scoped by buyer type; (2) SSLCommerz facts adopted as design constraints: validation-API + exact integer amount + tran_id + currency before capture; prices ৳0 or ≥ ৳10, ≤ ৳500k; ৳0 orders bypass the gateway; refunds async via `bank_tran_id` with a poll job; subscriptions renew by invoice-and-pay + 7-day grace + dunning (no card tokens exist); reconciliation job via transaction-query API; `store_amount` never used for commission; (3) `listing_pending_changes` keeps a live listing buyable while edits await review; (4) `rating_avg_milli` (×1000) naming; (5) plans/limits engine (F-CM-06 Parts 1–3) ships in Release 0 because every area gates on `assertWithinLimit`/`hasModule`; (6) malware scanning + DOCX/PPTX previews deferred (no container in the architecture) — recorded honestly as `scan_status='skipped'` and cover-only previews; owner question OQ-6; (7) AIT withholding and VAT registration → owner questions OQ-1/OQ-2 (`docs/product/OWNER-QUESTIONS.md`).

## D-25 — Identity-spec rulings · ACCEPTED · 2026-09-17

From `docs/features/01-identity/*`: (1) shell = workspace type ∧ role (parents → `/family`); PRODUCT-DECISIONS 1.1 amended; (2) `audit_events`/`notifications.workspace_id` nullable + `app.log_audit_event()` writer; ARCHITECTURE §4 amended; (3) **SMS**: `adapters/sms` with a console driver behind a feature flag; v1 launch = email invites + copyable/WhatsApp-shareable invite links; phone OTP and SMS invites switch on when a provider is chosen (owner question added); (4) platform-admin access is narrow and enumerated, no impersonation — `support_access_grants` (owner-granted, ≤24 h, read-only, logged); (5) personal workspaces carry an implicit `personal_free` plan; (6) custom label via `workspace_members.custom_label_id`; (7) durations: 30-day account-deletion grace, invitation expiry 14 days (email) / 30 days (join-code approval).

## D-26 — Teaching-spec rulings · ACCEPTED · 2026-09-17

From `docs/features/03-teaching/*`: (1) Claude has no image generation → the "image" tool produces **sanitised SVG illustrations**; photoreal images need a new vendor (future); (2) `syllabus.extract` uses `claude-sonnet-5` (accuracy over cost — a bad topic list poisons every downstream number); D-08 amended: Haiku only for low-stakes classification/rewrites; (3) credit prices corrected to cover model cost at ৳120/USD: syllabus.extract `10 + ceil(max(0,pages−10)/2)` capped at 40; worksheet 4, quiz 4, rubric 3, differentiation 3, illustration 5 (lesson plan 5, parent message 1, pacing 8 unchanged); (4) Free plan limited to haiku-backed actions via `ai_actions.min_plan_tier`; (5) CI rule: only `packages/adapters/ai` may import `@anthropic-ai/sdk`; anti-mock grep fails CI on numeric/array literals in analytics routes; (6) eligibility ranking shared between cover-teacher and workload in `packages/domain/eligibility.ts`.

## Amendment to D-21 — required checks are the eleven in `docs/engineering/CI.md`

`CI / lint, typecheck, unit, contracts, db, build, security, e2e, lighthouse, changeset, docs-sync` (+ `guard`). Supersedes the seven names listed earlier.

## D-27 — Market-research-driven scope changes · ACCEPTED (lead; owner may override) · 2026-09-17

Source: `docs/product/research/COMPETITORS.md`. (1) **Fee collection joins Campus** as F-CM-08, Release 1.5, school as merchant of record via school-owned SSLCommerz/bKash credentials; supersedes FUTURE.md "fee management belongs to Admin app" — Admin app does accounting/analysis on top. Reason: every BD competitor leads with fees; a school will not switch to a system that cannot collect money. (2) **SMS moves to R1** as metered pass-through with per-school sender-ID paperwork in onboarding (provider still OQ-10). (3) **Pricing = feature tier × student band** + one-time onboarding fee; `plan_prices` table added now. (4) **PDPA 2026 compliance** is an R0/R1 workstream: `COMPLIANCE-PDPA.md`, DPA acceptance in school creation, consent capture in guardian invitations, data-request tooling; no biometrics ever (marketing point). (5) **Marketplace pilot gate**: a 20-listing / 200-teacher paid pilot precedes the full R3 commerce stack. (6) 4th-subject GPA rule added to F-AC-06. (7) Rename settled; "Acadigma" trademark search → OQ-12. (8) Market sizing corrected: English-medium segment is small (137–300 schools); Bangla-medium private schools, kindergartens and coaching centres enter the ICP earlier than planned.

## D-28 — Research-driven product rules (pricing + teacher-side) · PROVISIONAL until debate synthesis · 2026-09-17

Adopted now: (1) `plan_prices` expresses band + per-student overage so either pricing model fits; SMS included-count per plan + metered unit price; (2) every AI output is a **draft the teacher explicitly approves** before it persists or becomes visible to anyone — universal, not report-card-only; (3) `teacher_profiles.open_to_work` is private-by-default with selective disclosure to named schools; (4) offline attendance replay is baseline correctness (35 % of BD mobile connections are 2G); (5) performance NFR reworded: the constraint is CPU/RAM on low-end Android, not data cost (৳33/GB); (6) PLG artefacts: instant badge/certificate issuance and 1080×1080 Bangla-capable share images are roadmap items; (7) marketplace is treated as retention/network value, not a revenue line, until ~5,000 schools; (8) never bundle hardware; AI is not sold as a separate SKU; sold schools get an 8–12-week paid pilot spanning one exam cycle instead of a 14-day trial (self-serve keeps the trial).
Pending debate: exact plan prices/shape; hiring release timing (OQ-16); payout minimum (OQ-15, owner).

## D-29 — Workflow-doc rulings · ACCEPTED · 2026-09-17

From `docs/workflows/README.md §4`: (1) new route group `(account)/account/*` for user-level settings (security, devices, sessions, language, data requests, delete) — ARCHITECTURE §2 amended; (2) read-only-over-limits = `workspaces.access_mode` (normal|read_only) checked by server actions (`PLAN_READ_ONLY`), set by trial-expiry/dunning jobs, cleared on upgrade payment; (3) settings named: `attendance_policy.mode ∈ daily|period`, `cover_policy{missed_punch_grace_minutes, cover_credited, unpaid_absence}`; (4) handout distribution reuses the announcements audience resolver (no second fan-out); (5) parent portal owner = F-AC-10 (academics area), view contract per WF-02 stage D.

## D-30 — Observability is OpenTelemetry-first; backend chosen at R1 launch · ACCEPTED · 2026-09-17

**Context:** owner pointed at `tracewayapp/traceway` (MIT, OTel-native, includes AI-call tracing per tenant and on-call paging). `OBSERVABILITY.md` assumed Sentry.
**Decision:** the app emits OTLP via `@vercel/otel` (traces, logs with `correlation_id`/`workspace_id`, AI spans with `gen_ai.*` + `user.id = workspace_id`). The backend is a deploy-time choice: Sentry (managed, immediate) vs Traceway Cloud vs self-hosted Traceway on a small VPS once one exists. Decide at Release 1 launch with real pricing. `OBSERVABILITY.md` to be amended to describe the OTel contract and list both backends.
**Why:** vendor neutrality costs nothing now; Traceway's per-tenant AI cost view matches the credit ledger exactly, but we have no host for it today.

## D-31 — Fee-collection rulings · ACCEPTED · 2026-09-17

From `docs/features/04-commerce/F-CM-08-fee-collection.md` (14 parts): school is merchant of record, enforced structurally (per-workspace pgsodium-encrypted credentials readable by no human role); fee money never enters Acadigma's `orders/payments` tables (`fee_payments`/`fee_transactions` instead; 0 % commission asserted by CI); `PaymentProvider` factory becomes scope-aware (platform | workspace merchant); `inbound_events` gains nullable `workspace_id`/`merchant_account_id`; fee refunds are owner/admin with no time window; split payments for > ৳500k; per-merchant reconciliation reporting to the school; bKash callback shape supported; counters unified into `document_counters`; `workspace_member_capabilities` (first: `fees.cashier`); late fees recomputed absolutely (default off); discounts never compound; public `/pay/[schoolSlug]` requires guardian phone last-4 and is rate-limited; Bengali SMS counted as UCS-2 70-char segments with mandatory cost estimate. Build order: fees ahead of the whole selling chain (fees are a mid-market ship-blocker; marketplace is the unproven assumption). `fees` module on Starter+; Free sees it locked with an upgrade prompt, not hidden.

## D-32 — Academics-spec rulings · ACCEPTED · 2026-09-17

From `docs/features/02-academics/*` (64 parts): (1) PWA offline attendance queue (IndexedDB + idempotent replay) is **in v1** (F-AC-03 Part 7) — ARCHITECTURE §6 wins over the FUTURE.md wording; only "default-on background sync" is wrapper-phase; (2) two "optional" concepts kept distinct (elective vs BD 4th subject); (3) identifiers use American English (`behavior_*`); (4) trigger-maintained rollups for attendance-% and behavior term scores; (5) risk weights must sum to 1.00 (blocked otherwise) while exam component weights renormalise with a warning — deliberate: risk flags a child, marks are audited by humans; (6) calendar (F-AC-11) builds second in the area because `app.is_school_day` is the denominator for attendance, leave and analytics; (7) `grading.show_rank_to_parents` defaults **on** → OQ-17 for the owner.

## D-33 — GSAP is the animation library · ACCEPTED (owner request) · 2026-09-17

`gsap` + `@gsap/react` in `packages/ui`, curated plugin registration (ScrollTrigger, Flip, Observer, Draggable, SplitText, TextPlugin, ScrollToPlugin, MotionPath, DrawSVG, CustomEase/Bounce/Wiggle, EasePack; dev-only GSDevTools/MotionPathHelper; excluded: Pixi, Easel, Physics2D/Props, MorphSVG, ScrollSmoother). Lazy-loaded, reduced-motion aware, CSS transitions remain the default in the core app. No framer-motion.

## D-34 — PDPA 2026 compliance rulings · ACCEPTED · 2026-09-17

Source `docs/product/COMPLIANCE-PDPA.md` (verified: Act 2026 current; enforcement ≈ 13 May 2027; localisation of "confidential/restricted" data inside Bangladesh; fines and CDO requirements conflict across sources). Adopted P0/P1: `consent_records` with the guardian invitation as consent of record (+ paper path); `legal_acceptances` and a blocking DPA/Terms/Privacy step in school creation; `redactForAI()` enforced by Semgrep + Vercel functions pinned to `bom1`, no personal data in edge middleware; `data_requests` + `exportBundle()` + `app.erase_subject()` with legal holds and a CI-checked `personal_data_map`; minimisation (NID/birth-cert scans purged 90 days after admission, religion off by default and admin-only, health restricted to owner/admin/class teacher/first-aid staff, KYC purged 2 years after last payout); audit retention 7 years rolling; sellers 18+, under-18 candidates need a guardian step; risk score documented as a transparent formula, never "AI prediction"; personal-workspace tutor students carry a restricted field set (Acadigma is controller there). **Localisation**: unresolved — depends on counsel's classification answer (OQ-18); options costed by Q1 2027 (in-country synchronised replica on a BD VPS vs self-hosted Supabase in a BD DC). Marketing: never "PDPA compliant" or "all data stays in Bangladesh".

## D-35 — Voice-of-customer rulings · ACCEPTED · 2026-09-17

Source `docs/product/research/VOICE-OF-CUSTOMER.md` (26k reviews). (1) M0/M1 exit criteria gain measurable reliability numbers: zero forced re-authentications in a 30-day soak (sessions persist; refresh silently), ≥ 99 % measured in-app notification delivery within 60 s, ≥ 90 % self-serve credential recovery (email/phone reset with no school-office dependency), cold start < 3 s on a 2 GB-RAM Android, no "server down" false positives (distinguish offline vs API error in UI). (2) Onboarding: the join flow offers **search by school name + district** and "my school isn't listed" alongside the join code. (3) Attendance is always visible to the affected person (parent sees the day's mark and the %; teacher sees what was saved). (4) Fees inside the same app and login (already D-27/D-31). (5) No hidden post-trial pricing: price page public; trial end date shown from day one. (6) Speed and login reliability are the year-one moat, ahead of feature breadth.

## D-36 — Data-model interpretations · ACCEPTED · 2026-09-17

From `docs/architecture/DATA-MODEL.md` (129 tables; 29 in foundation migrations 0001–0004): (1) UPDATE policy = role predicate in `with check` + `app.tg_freeze_workspace()` BEFORE UPDATE trigger (ARCHITECTURE §3 corrected — the earlier template was degenerate); (2) guard triggers are `security invoker` so `current_user` distinguishes client statements from server-owned paths (`app.is_privileged_context()`), which a `set_config` flag could not; (3) append-only tables (`audit_events`, `consent_records`, `legal_acceptances`) have **no foreign keys** — history outlives what it describes; (4) `UserLabel` merged into `workspace_members.label_id`; (5) `document_counters` uniqueness via `(coalesce(workspace_id, uuid_nil), kind, year)`; (6) audit reads = owner + platform staff, not admins; (7) `subscriptions.billing_interval`; (8) default grade scale id lives in `school_profiles.academic_settings` until R1 creates `grade_scales`; (9) pgTAP cross-tenant UPDATE/DELETE assert zero rows affected, `throws_ok` only where Postgres genuinely raises. Migration-from-Base44: 21 kept / 6 split / 20 merged / 17 dropped; memberships import `pending` floored at `teacher`; transactions `unverified_legacy`; earnings recomputed and landed `reversed`; prototype `AuditLog` not imported.

## D-37 — Model mix and token discipline · ACCEPTED (owner: "make sure I don't spend tokens on unnecessary stuff") · 2026-09-17

**Model routing:** Opus only for architecture, security review, research synthesis, and reviewing money/RLS code. **Sonnet** for every well-specified implementation part (builders and testers — the specs are precise enough). **Haiku** for mechanical work (renames, fixture generation, doc index updates, lint fixes). Reviewer agents (`typescript-reviewer`, `react-reviewer`, `security-reviewer`) run once per PR, not per commit.
**Token rules for agents:** read only the spec part + DATA-MODEL section + DESIGN-SYSTEM section they need (never whole inventories); write files in ≤400-line chunks; no re-reading files they just wrote; one final report ≤ 40 lines; no exploratory browsing unless the task is research. Lead messages stay short; status updates only on completions.
**Local infra:** Docker Desktop + WSL2 installed so `supabase start` runs pgTAP locally (fewer CI round-trips = fewer wasted cycles).

## D-38 — Parent identity is provider-agnostic from M0 · ACCEPTED · 2026-09-17

**Context (CFO critique, `debate/04-cfo.md`):** every parent holding a Supabase Auth identity counts toward MAU billing (~$0.00325/MAU above the included tier ≈ ৳1.3 lakh/yr at 1,000 schools); a magic link is still an auth event, so it is not a mitigation.
**Decision:** `packages/db` exposes `ParentSessionProvider`; RLS policies key on `auth.uid()`/JWT claims only, never on `auth.users` rows, so parent sessions can be issued either by Supabase Auth (M0–launch, inside the included MAU) or by **server-minted Supabase-compatible JWTs** (phone-OTP/email-link verified by us, signed with the project JWT secret, `sub` = guardian user id) once parent MAU approaches the included tier. No parent-facing feature may assume an `auth.users` row exists. Decision point: MAU > 50 % of included tier.

## D-39 — AI allowances are per MONTH, not per day · ACCEPTED · 2026-09-17

PRODUCT-DECISIONS §3.3/§5.1 said "credits/day"; costed at ৳1.45/action that makes every tier negative-margin on AI. All plan allowances are restated as **AI actions per month** with pooled use (burst-friendly for Thursday-night planning), hard cap, and top-ups priced above cost (placeholder ৳1,200 / 500 actions, ceiling 3× allowance/month). Exact per-plan numbers set by the debate synthesis; the plans seed in migration 0004 must be updated before M0 0.5 builds on it.

## D-40 — Attendance is never fabricated by default · ADDED · 2026-09-17

The full rule is D-22 (amended) above. **Why:** a default that fabricates records poisons every downstream number and cannot be fixed later (SYNTHESIS X-01). Priority: R1.

## D-41 — One price list · ADDED · 2026-09-17

The grid in `MARKET-STRATEGY.md` §c is the **only** price list. `OUTREACH-TEMPLATES.md` and the pricing page quote it verbatim; no document may state a different number.
**Why:** Three research documents were quoting three different prices (৳2,999/৳7,999, ৳1,500/৳3,500, and the new ৳2,200/৳4,900 grid); a prospect who sees two numbers trusts neither. Priority: M0-0.5.

## D-42 — The Free school plan is abolished · ADDED · 2026-09-17

The free teacher personal workspace (zero AI) replaces it, plus a capped NGO/madrasa programme. Cash effect ≈ **+৳87,000/month at 100 free schools** (removed burn). **Pending owner confirmation (OQ-22)** — the plans seed removes the `free` row now; `personal_free` is kept.
**Why:** ৳870/school/month of burn against a ~5% free-to-paid conversion is 19 loss-making schools per convert; it also anchors the market at ৳0 and is trivially matched by an incumbent bundling free hardware (MARKET-STRATEGY §c). Priority: M0-0.5.

## D-43 — Hiring is apply-only and is not an acquisition channel · ADDED · 2026-09-17

Candidate browse is deleted; `open_to_work` is a private preference that surfaces jobs to the teacher, never the teacher to schools; the verification badge is renamed "Documents checked by Acadigma on {date}" with the method stated.
**Why:** The 3.7M-membership NTRCA cluster waits for a central government recommendation a school cannot post against, so hiring cannot be the acquisition engine; a browsable availability directory in a city where forty principals share a WhatsApp group is safe only for the unemployed (MARKET-STRATEGY §f). Priority: later.

## D-44 — No child-scoped data on an unauthenticated surface · ADDED · 2026-09-17

Covers the parent invite loop (it sends an **invitation**, never a view), OG/share images (school-mark only, never a child's name/photo/marks/attendance), and QR codes on student-facing artefacts (none, ever).
**Why:** An unauthenticated single-child URL is fetched and cached by link-preview crawlers before any consent exists; a marketing QR code on a report card or ID card is a second, uncontrolled distribution channel for a child's record (SYNTHESIS G-09, G-10, G-11). Priority: MUST.

## D-45 — Company formation is on the critical path · ADDED · 2026-09-17

RJSC + trade licence + TIN + VAT BIN gate BTRC sender ID, bKash merchant, SSLCommerz eligibility, Mushak 6.3 invoicing and DPA signature. Added as roadmap item 0.
**Why:** It was on nobody's critical path despite blocking five other workstreams and taking 6–10 weeks; starting it late is a pure schedule tax with no offsetting benefit (SYNTHESIS F-06). Priority: MUST, owner action this fortnight (OQ-20).

## D-46 — The roadmap is anchored to the BD school calendar · ADDED · 2026-09-17

Pilot-ready **1 Sep 2027**; base closes **January 2028**; proof on the **April–May 2028 terminal exam**. **Sep 2026 and January 2027 are already lost** — stated plainly so nobody plans against them.
**Why:** R1 needs 149 parts and today is 17 Sep 2026; pretending an already-passed window is still open produces a schedule nobody can hit and a funnel nobody can trust (MARKET-STRATEGY §i, G-01, G-02). Priority: R1.

## D-47 — Teacher metrics are not management surveillance · ADDED · 2026-09-17

Admins see **scheduled periods only**; workload variance is teacher-private by default (per-teacher opt-in to share); thresholds are BD-calibrated (26/30/32 periods/week, not 20/24/25); a first-run disclosure page states plainly what the school can and cannot see.
**Why:** BD private-school teachers routinely teach 28–32 periods/week, so the old thresholds flagged normal load as burnout on day one; and a workload dashboard that reads as surveillance is how `lesson_logs` fills with fiction (SYNTHESIS T-01, T-02). Priority: R1.

## D-49 — Dependency release-age cooldown is 2 days, security patches exempt · ACCEPTED · 2026-09-17

**Context:** Semgrep's supply-chain rules led to `minimumReleaseAge: 7 days` in `pnpm-workspace.yaml`. With every dependency pinned exactly, that rejected `@turbo/linux-64@2.10.13` (published 3 days earlier) and aborted every install — which silently prevented the postcss/browserslist security overrides from applying.
**Decision:** `minimumReleaseAge: 2880` (2 days) — still blocks same-day malicious publishes, which is the threat the rule targets — plus `minimumReleaseAgeExclude` for packages being patched for a published advisory, and `overrides` in `pnpm-workspace.yaml` (not `package.json`) with exact versions. Rule for the future: when pinning a dependency released < 2 days ago, add it to the exclude list in the same commit, and remove it in the next.
**Why:** a supply-chain control that breaks installs gets disabled; one that costs nothing stays on.

## D-50 — Client-callable RPCs live in `public`; `app.*` stays server-internal · ACCEPTED · 2026-09-17

**Context (F-ID-01 build):** Supabase exposes only the `public` schema (and schemas explicitly added to PostgREST) as RPC. Functions in `app.*` — designed as the SECURITY DEFINER helper layer — cannot be called from the browser or from `supabase-js`, so `app.redeem_invitation`, `app.request_join_with_code` and the auth throttle helpers were unreachable as written.
**Decision:** every function a client must call gets a thin `public.<name>()` wrapper that validates arguments and delegates to the `app.*` implementation; `public` wrappers are the only functions granted `EXECUTE` to `authenticated`/`anon`. `app.*` keeps the logic, is granted to no client role, and is the only layer tests target for behaviour. RLS helpers used inside policies remain in `app`. Each feature spec that names an RPC (F-ID-03, F-ID-04, F-CM-08 …) must list its `public` wrapper.
**Why:** keeps the security-definer surface auditable and the PostgREST surface minimal; the alternative (exposing `app` to PostgREST) would publish every helper.

## D-51 — Audit substrate: generic-trigger actions stay separate from the named catalogue, and correlation id rides a PostgREST pre-request hook · ACCEPTED · 2026-09-17

**Context:** F-ID-09 Part 1 (append-only substrate). Two things the spec left for the builder: (1) the generic trigger `app.tg_audit()` (D-05, shipped in 0003) has always written `<table>.<op>` actions (`workspace_members.update`), while F-ID-09 §5.1's catalogue names business actions (`member.role_changed`) — these do not line up, and acceptance criterion 17 requires every action actually written to exist in `app.audit_action_catalog`. (2) the trigger already read a transaction-local `app.correlation_id` setting, but nothing in the stack ever set it — `supabase-js` talks to PostgREST, which runs one transaction per HTTP request, so a `set_config(..., true)` made by one RPC call cannot be seen by a later `.from().insert()` call in the same server action.
**Decision:** (1) Seed **both** sets into `app.audit_action_catalog`: the curated business actions from §5.1 (for explicit `app.log_audit_event()` calls business logic makes going forward) and a generated `<table>.insert|update|delete` row for every table the trigger is attached to (`GENERIC_AUDIT_TABLES` in `packages/domain/src/audit/catalog.ts`, mirrored in the migration's `v_tables` array and diffed by `scripts/check-audit-catalog-parity.mjs`). `app.log_audit_event()` raises on an uncatalogued action; `app.tg_audit()` defaults a missing lookup to `info` rather than blocking a tenant write. (2) `app.pre_request()`, wired as PostgREST's `db-pre-request` function (`alter role authenticator set pgrst.db_pre_request = 'app.pre_request'`, guarded exactly like the 0001 `pg_cron` block), copies `x-correlation-id` into `app.correlation_id` inside the request's own transaction before RLS runs. `app.set_correlation_id(uuid)` is the explicit fallback for callers outside that path (jobs, webhooks).
**Why:** rejecting every non-catalogued action outright (matching the trigger's real output to the letter) would mean re-deriving ~50 business-meaning actions from raw CRUD before Part 1 could ship at all; separating "what the trigger writes today" from "what business logic will name tomorrow" ships the append-only guarantee now without inventing fake specificity. The pre-request hook is the standard, Supabase-documented mechanism for exactly this per-request-GUC problem — the alternative (thread correlation id as an explicit parameter through every future repository write) would touch every area's migrations, not just this one.
**Consequences:** a future migration that calls `app.attach_audit()` on a new table must also seed that table's three generic rows (`packages/domain/src/audit/catalog.ts` + the migration's `v_tables`) or the CI parity check fails; this is the intended forcing function. CI's disposable Postgres has no `authenticator`-role restrictions matching a locked-down hosted project, so the pre-request wiring is exercised in CI but its _hosted_ behaviour is verified only once against the Supabase dev branch — tracked as a Part 2 follow-up, not blocking Part 1.

## D-52 — F-ID-03 review follow-ups: a removed/pending member is not a forger; the tenant-freeze trigger has exactly one cascade exception · ACCEPTED · 2026-09-24

> Numbering note: D-51 exists on a not-yet-merged PR at the time this entry was written. If D-51 lands first with a different subject, this entry keeps the number D-52 (`DECISION-LOG.md`'s own rule: "numbers are never reused") — the PR that introduced it flags the collision risk for the merge reviewer to confirm before merge.

**Context:** an Opus review of the merged F-ID-03 Parts 1-3 PR (#7) found two related tenancy gaps.

1. `resolveWorkspaceContext`'s header path (`packages/db/src/workspace-context.ts`) treated "no ACTIVE membership row for the candidate workspace" as a single case: fire the `tenancy.context_rejected` forgery tripwire and fail `not_a_member`. But a **removed** member (F-ID-03 §9 AC6) or a **pending** join-by-code applicant replaying a stale `x-workspace-id`/`acadigma_workspace` cookie — most concretely, a second person signing in on a shared phone after the first person's session expired without signing out — hits the exact same code path as a genuine forged-header attack, and gets logged as one: a false audit row lands in the FIRST person's school, naming the second person as a would-be attacker.
2. `public.data_requests.workspace_id` is `on delete set null` (a DSAR must outlive the school it names), but F-ID-03 Part 1's tenant-freeze audit attached `app.tg_freeze_workspace` to that table (closing a real re-parenting gap, D-36's degenerate-role-predicate warning). `ON DELETE SET NULL` is itself an internal UPDATE, so the freeze trigger — an unconditional "`workspace_id` never changes" — fired on that cascade and aborted any hard delete of a workspace with a `data_requests` row.

**Decision:**

- `resolveWorkspaceContext` now runs a second, narrower query — "does the caller have ANY `workspace_members` row for this workspace, in any status" (RLS already lets a user see their own row in every status, F-ID-03 §3) — only when the active-membership check comes back empty on the header path. A row found (removed/pending) fails a new, distinct reason, `membership_inactive`; no row at all fails `not_a_member`. **Both fire the tripwire** (amended after the PR #12 Opus review, see below). `public.log_tenancy_context_rejected` (`20260924020000_tenancy_tripwire_membership_status.sql`) looks up the caller's own membership status server-side and writes it into the row as `membership_status` (`none`/`pending`/`removed`) plus `severity` (`forgery` when `none`, otherwise `inactive`), so alerting can rank them without the row ever being skipped.
- **Amendment (PR #12 review):** the first draft skipped the tripwire for removed/pending callers. Rejected: a `pending` row is one invite code away for anyone (`join_workspace_by_code`), so skipping it would let an outsider probe a school silently, and a removed ex-employee's probes would go unrecorded. The shared-phone false positive that motivated skipping is already fixed at its source by clearing the cookie on sign-in, so the skip bought nothing and weakened AC1. `requireWorkspace()` (`apps/web/lib/workspace.ts`) needed no change — it already renders the same `forbidden()` screen for any reason other than `unauthenticated`, so AC6 ("the removed member sees the no-longer-have-access screen") still holds exactly as before.
- `app.tg_freeze_workspace()` (`20260924010000_tenancy_freeze_cascade_exception.sql`) allows exactly one transition: `old.workspace_id is not null and new.workspace_id is null`, on `public.data_requests` only, and only when `pg_trigger_depth() > 1` — the signature of being invoked from inside another trigger (the FK action's own internal UPDATE), which a direct client statement (`pg_trigger_depth() = 1`) can never produce. Every other re-parent attempt, on every frozen table including `data_requests` itself, still raises `42501`.
- Separately (not a schema/behaviour change, but the same review): the `acadigma_workspace` cookie is now cleared on every path that mints a session for a possibly-different user (`signInWithPassword`, `resetPassword`'s `verifyOtp`, and `/api/auth/callback`'s `verifyOtp`), and `resolveLandingRoute` resolves workspace context with an empty header bag rather than the request's real headers, since middleware has already mirrored a possibly-stale cookie into it before that function runs.

**Why:** a security tripwire that fires on ordinary account lifecycle events (being removed, or replaying a shared device's leftover cookie) trains whoever reads `audit_events` to ignore it — the opposite of what a tripwire is for. A blanket immutability trigger that cannot tell "an attacker re-parenting a tenant row" from "the one FK action the schema itself declares as legitimate" is the same class of bug D-36 already named: correct in the common case, wrong at the one boundary that matters. Distinguishing both narrowly, rather than loosening the general rule, keeps the tenant-freeze invariant intact everywhere except the one place it was never meant to apply.

**Consequences:** `WorkspaceContextFailureReason` gains `membership_inactive`; any future caller that branches on `.reason` (none does today outside `workspace-context.ts` itself) must treat it the same as `not_a_member` unless it specifically wants to show different copy. The freeze-trigger exception is narrow by construction (table name + exact transition + nesting depth) and pgTAP (`supabase/tests/10_tenancy_cascade.sql`) proves both the allowed cascade and that a direct client re-parent is still blocked. `docs/features/01-identity/F-ID-03-workspaces-and-membership.md` §11 records both as deviations from the Part 1-3 build.

## D-53 — Campus moves to its own Supabase project on a separate account · ACCEPTED · 2026-09-24

> Numbering note: D-51 (PR #6, audit) and D-52 (PR #12, tenancy follow-ups) are on open PRs at the time of writing. This entry takes the next number after both.

**Context:** the owner created a separate Supabase account (organisation `Acadigma Suite`) for the Campus app. The old project `acadigma-suite` (`bvqzhrvcrxebawjusrxk`) never had the Campus migrations applied (OQ-26 secrets were never set), so it holds only the marketing site's `public.waitlist` table.

**Decision:** Campus uses project **`Acadigma Campus`**, ref **`kekfmibwjejdhxjkmezo`**, region ap-south-1 (same as before), URL `https://kekfmibwjejdhxjkmezo.supabase.co`, publishable key `sb_publishable_VbUqiEvQiuObUZMf5IGYOA_P_EiiyGy` (safe for browser). The service-role/secret key is never written to disk outside `apps/web/.env.local`, Vercel and GitHub secrets. D-20 (one project for dev and prod until launch) still applies, to this project. The marketing website **stays** on the old project for its waitlist; the two apps no longer share a database.

**Consequences:** Vercel and Claude Code keep the owner's existing accounts; only Supabase changed. Every dashboard-only Supabase setting must be (re)applied to the new project — `supabase/config.toml` auth settings via `supabase config push`, plus the dashboard items in OWNER-QUESTIONS (OQ-25 recovery email template, breached-password protection, SMTP, redirect URLs). Historical test reports keep the old ref because that is what they ran against.

## D-54 — Deny function EXECUTE by default in `public`; CI mirrors Supabase's default privileges · ACCEPTED · 2026-09-24

**Context:** Live verification against the production Supabase project `kekfmibwjejdhxjkmezo` (all main migrations applied at the time) showed `has_function_privilege('anon', 'public.switch_workspace(uuid)', 'execute')` returning `true`, despite `20260917020300_tenancy_hardening.sql` §5 granting that function to `authenticated` only. The cause: Supabase provisions every project with `ALTER DEFAULT PRIVILEGES FOR ROLE postgres, supabase_admin IN SCHEMA public GRANT EXECUTE ON FUNCTIONS TO anon, authenticated, service_role`. Every migration's `revoke all on function ... from public` revokes the PUBLIC pseudo-role's grant, not the explicit per-role grant the default already handed to anon/authenticated at `CREATE FUNCTION` time — so the "revoke, then grant back the intended roles" pattern used throughout `identity_auth.sql` and `tenancy_hardening.sql` left the unintended roles granted anyway. Affected: `public.log_auth_event_service` (intended service_role-only — anon/authenticated could forge `account.registered` audit rows with an arbitrary user id, ip and user agent), `public.switch_workspace`, `public.list_my_workspaces`, `public.log_tenancy_context_rejected` (all intended authenticated-only). `log_tenancy_context_rejected` additionally has no `auth.uid() is null` guard, so this was live-exploitable: anon could write a `tenancy.context_rejected` audit row into any workspace with an attacker-chosen `attempted_workspace_id`. CI never caught this because its Postgres 17 service container has no such default, so `has_function_privilege('anon', ...)` pgTAP assertions passed for the wrong reason.

**Opus review of this PR found the first cut was still not deny-by-default for anon:** per-schema default privileges (`... IN SCHEMA public ...`) only _add to_ the role-wide default; Postgres's built-in role-wide default already grants EXECUTE on functions to `PUBLIC`. A future `public` function that skips its own `revoke ... from public` is still anon-callable through that global default even after the schema-scoped fix below, because `PUBLIC` membership is implicit for every role including `anon`.

**Decision:** (1) `supabase/ci/bootstrap.sql` now runs the same `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public GRANT EXECUTE ON FUNCTIONS TO anon, authenticated, service_role` Supabase runs on project creation, so CI's grant assertions are false exactly when they would be false live. (2) `20260924030000_revoke_default_function_grants.sql` revokes the mis-granted EXECUTE from the four functions above and, going forward, flips the default itself at **both** levels: the schema-scoped `alter default privileges for role postgres in schema public revoke execute on functions from anon, authenticated` (service_role keeps it), **and** the role-wide `alter default privileges for role postgres revoke execute on functions from public` — closing the gap the review found, since a schema-scoped revoke alone cannot undo a grant to the implicit `PUBLIC` role that every future function still inherits. Every new `public` function is now genuinely deny-by-default and must grant EXECUTE explicitly, or it is uncallable rather than silently open. (3) `supabase/tests/12_function_grants_invariant.sql` asserts the allowlists, the default-ACL shape, **and** (new, from the review) a live probe: it creates a throwaway ungranted function inside the test transaction and asserts anon/authenticated cannot execute it while service_role can — a probe that would have failed before the role-wide revoke, so it actually tests the fix rather than just re-describing it. `log_tenancy_context_rejected`'s missing null-check is NOT fixed in this PR — its body was already being replaced by PR #12 (`fix/tenancy-review-followups`, `20260924020000_tenancy_tripwire_membership_status.sql`, merged as D-52); adding the check here would have conflicted. The grant fix alone closes the PostgREST path; the null-check follow-up belongs to a small PR after both merge.

**Knock-on to test infrastructure:** revoking EXECUTE from `PUBLIC` role-wide also affects pgTAP's own `tests.*` helper functions (`tests.login()`, `tests.logout()`, `tests.mkuser()` — created by `postgres` in each spec, called after `SET ROLE authenticated`), which previously worked only because of the same implicit `PUBLIC` grant this fix removes. `supabase/ci/bootstrap.sql` adds `create schema if not exists tests; alter default privileges for role postgres in schema tests grant execute on functions to public;` (plus `grant usage on schema tests to public`) — test infrastructure only, never shipped to the real project, and safe because per-schema default-privilege entries are additive on top of (never replaced by) the role-wide default, so scoping the grant back to `public` for the `tests` schema alone does not reopen anything in `public`/`app`.

**Two further, lower-priority review findings, same PR:** (a) `12_function_grants_invariant.sql`'s allowlist checks originally filtered to `prosecdef` (SECURITY DEFINER) functions only; dropped, so a future plain SECURITY INVOKER function in `public`/`app` is covered by the same allowlist — every function in both schemas today is already either DEFINER or an already-excluded `app` utility, so this cost nothing to extend. Merging `main` (D-51, D-52) into this PR while making that change surfaced one genuine addition the allowlist needed regardless: `app.pre_request()` (D-51, `20260924000100_audit_substrate.sql` §9), PostgREST's `db-pre-request` hook, must be anon-executable because it runs on every request — including anonymous ones — before RLS; it is now on the A1 allowlist with its rationale inline. (b) `supabase/ci/bootstrap.sql` also mirrors Supabase's default table/sequence privilege (`ALTER DEFAULT PRIVILEGES ... GRANT ALL ON TABLES/SEQUENCES TO anon, authenticated, service_role`) for the same reason as the function one — a table-grant pgTAP assertion in a plain container could otherwise be passing because nothing was ever granted, not because it was explicitly revoked. This PR's test report records whether that surfaced a real table/sequence-grant gap (in which case it is a separate finding, not papered over here) or stayed green.

**Why:** a security boundary that depends on the platform's default privileges silently agreeing with the repo's intent, with nothing testing that they do, is not actually enforced — CLAUDE.md rule 1 ("the database and the server are the security boundary") requires the database layer to be verified, not assumed. A schema-scoped revoke that leaves the role-wide `PUBLIC` grant standing is not deny-by-default, it is a default that still works by accident for exactly the schema this repo audits — the review caught that the fix needed to be provably complete, not merely plausible.
**Consequences:** every future migration that adds a `public` function MUST grant EXECUTE explicitly to whichever of anon/authenticated it intends, or the function is uncallable from the client entirely (service_role is unaffected, and so is anything created inside `supabase/ci/bootstrap.sql`'s `tests` schema). `supabase/tests/README.md` documents the new test file and its probe.

## D-55 — Generated types come from the PR's migrations in CI, not from the live project · ACCEPTED · 2026-09-24

> Numbering note: D-51 (PR #6) and D-52 (PR #12) are on open PRs; D-54 is reserved by the in-progress default-grants security PR. This entry takes D-55.

**Context:** `CI / contracts` regenerated `types.generated.ts` from the live Supabase project and byte-compared it with the committed file. The check was skipped until the Supabase secrets existed (OQ-26); once they did, it exposed that the design cannot work: the live project only receives a PR's migrations after merge (D-20), so any PR that changes the `public` schema (PR #6 adds `set_correlation_id` and audit columns) generates types without its own changes and fails, while committing types that include them also fails.

**Decision:** the freshness check moves to `CI / db`, after the PR's migrations are applied to the CI Postgres: `supabase gen types typescript --db-url <ci postgres> --schema public`, Prettier-formatted, byte-compared. The live-based check is removed from `contracts`. On a mismatch the job uploads the correct file as the `types-generated` artifact, so a developer (or agent) with no local Docker fixes it with `gh run download`.

**Consequences:** the committed file is in `--db-url` output format. Drift between the live project and `main` is a deployment concern (the `push` job applies `main`'s migrations), not a per-PR type check.

## D-56 — M0 wrap-up: one nav system, RLS/grants invariants split across `db` and `contracts`, and a tracked baseline for pre-existing coverage gaps · ACCEPTED · 2026-09-24

**Context:** three independent gaps left over from M0's parallel streams (`docs/plan/HANDOFF-2026-09-24.md`).

1. **Two nav systems.** `packages/domain/src/nav` (F-ID-03 Part 3, PR #7) grew its own `NavConfig`/`NavItem` types **and** its own transcription of DESIGN-SYSTEM §3.2's five curated trees (`config.ts`), because the nav engine needed something to test `filterNav`/`isRouteVisible` against and domain cannot depend on `packages/ui`. Separately, `packages/ui/src/primitives/nav-config.ts` (M0 0.9, PR unrelated to F-ID-03) independently transcribed the same five trees **plus** `sellerNav`/`platformNav`, with its own parallel `NavItem`/`NavConfig` types, an `ownerOnly`/`isOwner` gate domain's version does not have, and its own `filterNavConfig` duplicating `filterNav`'s logic. Neither was wired into the real shell: `apps/web/app/(school)/app/nav.tsx` was a third, ad-hoc `SCHOOL_NAV` list filtered by `can()` alone, reading neither engine.
2. **Module-key taxonomies don't match.** The nav trees gate items per SCREEN (`NAV_MODULE_KEYS`: `timetable`, `exams`, `marks`, `students`, …, 19 keys), transcribed from DESIGN-SYSTEM §3.2 in F-ID-03 Part 3. The plans engine (F-CM-06 Part 1, `20260917010300_plans_and_notifications.sql`) gates a plan per BUNDLE (`plan_modules.module`: `academics`, `attendance`, `lessons`, `messaging`, `resources`, `reports`, `print`, `hiring`, `cover`, `analytics`, `marketplace_school_funded`, `custom_labels`, `priority_support`). 16 of the 19 nav keys map onto 10 distinct `plan_modules` codes (`staff`, `billing`, `ai` have no equivalent at all); wiring the shell to `filterNav`'s `entitledModules` naively (treat "no matching plan module" as "not entitled") would have hidden most of the nav for every real workspace, which is a functional regression far worse than the drift being fixed.
3. **`coverage.sql`.** `CI / db`'s "RLS coverage" step has warned that `supabase/tests/coverage.sql` is missing since the CI workflow was written. The intended contract per its own comment was three checks (RLS off / no test file / no audit trigger); the plan for this Part narrowed it to two (RLS off, no test file) and asked for the simpler robust choice between `pg_read_file` and a Node script for the "named in a test file" half.
4. **(Opus review of this PR, PR #17) A real bug in the module lookup itself.** `getWorkspacePlan` (`packages/db`) selects `workspaces.plan_id, plans(*)` — the `plans(*)` embed is subject to `plans`' own RLS (`plans_select_public`: `status = 'active' and is_public`), so any workspace on a private, contact-sales or archived plan (`personal_free` is the clearest example, `is_public = false`; a custom enterprise contract behaves the same) makes the embed resolve to `null`. The school shell's `resolveEntitledNavModules` treated that as an ordinary "plan lookup failed" and degraded to `buildEntitledNavModules([])` — hiding Attendance/Students/Timetable/Exams/Messages, exactly the items the module map above gates, for every such workspace. This was already a documented caveat one layer up (`workspace-context.ts`: "a private or contact-sales plan embeds as null here… treat `plan === null` as unknown, never as free") that the new nav-wiring code did not itself account for.
5. **(Opus review) The static coverage-file parser had three real gaps.** `check-coverage-test-files.mjs` matched a table name anywhere in a test file's text, including inside a `--`/`/* */` comment; required an exact `public.<name>` prefix on `create table`, missing a table created relying on `search_path`; and had no detection at all for `workspace_id` added via `alter table … add column`. The header comment's stated reason for the `db`/`contracts` split was also factually wrong: `psql` is an ordinary client process on the GitHub Actions **runner**, which already has the full checkout, and could read `supabase/tests/*.sql` directly — only `pg_read_file()`, a server-side Postgres function running inside the `db` job's separate Postgres **service container**, has no path back to them.

**Decision:**

1. **Nav — domain's curated trees stay canonical; `packages/ui` becomes a thin adapter.** `packages/domain/src/nav/config.ts`'s five `NAV_CONFIGS` (already unit-tested against DESIGN-SYSTEM §3.2 in `config.test.ts` and `filterNav.test.ts`) are the one copy; `packages/ui/src/primitives/nav-config.ts` deleted its own parallel types and trees and now imports `NavItem`/`NavGroup`/`NavConfig`/`NavConfigKey`/`NavModuleKey`/`NAV_CONFIGS`/`filterNav`/`BOTTOM_NAV_PRIMARY_MAX` straight from `@acadigma/domain/nav` (already a real dependency of `packages/ui`), re-exporting `schoolAdminNav`/`schoolTeacherNav`/`schoolStaffNav`/`parentNav`/`personalNav` as the exact `NAV_CONFIGS[...]` objects for callers that named them directly (the dev-only `/design` page). What stays UI-only, because domain has zero UI dependency by design: the `IconName` union and its `lucide-react` `ICON_MAP`/`resolveNavIcon` (in `bottom-nav.tsx`, reused by the new `sidebar-from-config.tsx`), and the `sellerNav`/`platformNav` trees — `resolveNavConfigKey` deliberately returns `null` for both (they are entered from a link/the avatar menu, never resolved from `workspace type ∧ role`), so they have no home in the domain nav engine. `filterNavConfig`/`useFilteredNav` in `packages/ui` are now a thin adapter onto domain's `filterNav`, not a second implementation. **`ownerOnly`/`isOwner` were evaluated and dropped, not extended**: `owner` is already its own `WorkspaceRole`, distinct from `admin`, so `roles: ["owner"]` on the Billing & plan / Audit log items expresses "owner-only within More" (DESIGN-SYSTEM §3.2's footnote) with no extra field, exactly as domain's already-tested curated trees already did. The school shell (`apps/web/app/(school)/app/layout.tsx`) now resolves `getNavConfig(ctx.workspaceType, ctx.role)` from `requireWorkspace()`'s server-side `WorkspaceContext`, renders `BottomNavFromConfig` (phone) and the new `SidebarFromConfig` (desktop, replacing the hand-rolled `SchoolSidebar`) off the identical filtered tree, and passes both down to a small client component (`nav.tsx`) that only adds `usePathname()` and the `next/link` renderer.
2. **Module taxonomy — a documented, one-directional mapping (`packages/domain/src/nav/entitlements.ts`), never a blanket gate.** `NAV_MODULE_TO_PLAN_MODULE` names the 16 nav keys that have a real `plan_modules` equivalent today, onto 10 distinct codes (`attendance`→`attendance`; `timetable`/`students`/`exams`/`marks`/`assignments`/`curriculum`→`academics`; `lessons`→`lessons`; `resources`/`library`→`resources`; `reports`→`reports`; `print`→`print`; `messages`→`messaging`; `hiring`→`hiring`; `cover`→`cover`; `marketplace`→`marketplace_school_funded`). `buildEntitledNavModules(planModules)` (using the plans engine's own `hasModule`, not a hand-rolled `.includes()`) includes every UNMAPPED nav key unconditionally — `staff`, `billing`, `ai` have no plan gate yet, so they stay visible rather than silently disappearing because nobody has reconciled the two taxonomies. **Full reconciliation of the two taxonomies is out of scope for this Part** (a comment to this effect already existed in `NAV_MODULE_KEYS` before this PR) and is carried forward as a note in `DESIGN-SYSTEM.md` §3.2.
3. **The `getWorkspacePlan` embed bug — fixed with a narrower query, not a wider RLS policy (PR #17 Opus review).** `packages/db/src/repositories/plans.ts` gets `getWorkspacePlanId(ctx, client)`, selecting only `workspaces.plan_id` — no `plans(*)` embed, so nothing about it depends on `plans_select_public`'s `status = 'active' and is_public` restriction (`workspaces_select` is membership-only, per ARCHITECTURE §3, and was always readable). `plan_modules_select`/`plan_limits_select` are already `using (true)` — unrestricted — so once the id is known, `listEnabledModules` succeeds regardless of the plan's own visibility; no RLS policy changed, and no migration was needed. The school shell's module resolution moved into a new, directly unit-tested helper, `apps/web/lib/school-nav-entitlements.ts` (`resolveEntitledNavModules`), which calls `getWorkspacePlanId` instead of `getWorkspacePlan`. **Fail-visible decision:** when the plan id itself genuinely cannot be resolved (a dependency-down error, or a workspace mid-provisioning with no plan row at all — not the RLS-embed case above, which the fix removes entirely), the nav falls back to `FALLBACK_PLAN_MODULES_WHEN_UNKNOWN` (`packages/domain/src/nav/entitlements.ts`): every module bundle the cheapest real school plan (`starter`) ships stays visible — the daily-loop screens are never hidden by a transient error — while the two unambiguously premium, upsell-gated bundles (`hiring`, `cover`, `marketplace_school_funded`) stay hidden until a real plan is known. Nav visibility is not the security boundary (HANDBOOK/CLAUDE.md rule 1 — server actions and RLS are, independently, for every route this nav merely links to), so failing visible here is a UX choice, not a security one.
4. **`coverage.sql` — plain SQL for the DB-catalog half; a Node script for the file-presence half; scoped to `workspace_id` tables, not narrowed to RLS-off alone.** `supabase/tests/coverage.sql` does the half that needs the live catalog after this PR's migrations applied: every `public` table with a `workspace_id` column has `pg_tables.rowsecurity = true`, via a plain `do $$ … raise exception … $$` block (not pgTAP — this file is a `db`-job step of its own, run with `ON_ERROR_STOP=1`, separately from the `pg_prove` step). The "named in a `supabase/tests/*.sql` file" half is `scripts/check-coverage-test-files.mjs`, wired into `CI / contracts` (a plain checkout, no DB needed): it derives "table has a `workspace_id` column" the same way `check-audit-catalog-parity.mjs` already derives audit facts — parsing `create table [public.]<name> (...)` blocks AND `alter table … add column … workspace_id` across `supabase/migrations/*.sql`, with SQL comments stripped first, not a live query — then checks each qualifying table name (also comment-stripped) appears in `supabase/tests/*.sql` (excluding `coverage.sql` itself, which is infrastructure, not a behavioural test). **Corrected reasoning (PR #17 Opus review):** the split is a job-boundary choice, not a hard technical requirement that `pg_read_file` forces — `psql` itself is an ordinary client process on the GitHub Actions **runner**, which already has the full checkout, and could read these files directly; only `pg_read_file()`, a server-side Postgres function running inside the `db` job's separate Postgres **service container**, genuinely has no path back to them. The static-analysis half is kept in a different job anyway because it needs no database connection at all and can run before migrations even apply — a job-scheduling convenience, not a workaround for something `psql` itself could never do. `supabase/tests/13_rls_grants_invariants.sql` (next free number after `12`) separately asserts the broader "every table in `public`, tenant or not, has RLS enabled" and "anon has no USAGE on schema `app`" — it does **not** re-assert `12_function_grants_invariant.sql`'s function-allowlist checks, per this Part's brief.
5. **A tracked baseline, not a silent pass, for pre-existing coverage gaps.** Running the new script for the first time found five real tenant tables — `consent_records`, `legal_acceptances`, `email_log`, `file_access_log`, `subscription_events` — with a `workspace_id` column and zero mentions anywhere in `supabase/tests/`, predating this PR and each needing its own RLS test design (PDPA consent capture, notification delivery logs, file access logs, subscription lifecycle events — none of them this Part's scope). **Fixing the comment-stripping bug above (point 5 of Context) surfaced a sixth**: `notifications` was previously counted as "covered" only because its name appears inside a `--` comment in `09_tenancy.sql` (a historical note about the tenant-freeze trigger, not a real isolation/escalation assertion) — added to the allowlist for the same reason as the other five. Writing a new required check that is red on `main` on day one gets the check disabled, not the gap fixed, so `check-coverage-test-files.mjs` carries an explicit `KNOWN_GAPS` allowlist naming exactly these six, each with which future feature owns writing its test, and asserts the allowlist itself can only shrink (a table named in a test file while still listed in `KNOWN_GAPS` is itself a failure, so nobody forgets to delete the line; discovering a new real gap by fixing a parser bug, as happened here, is not the same as letting a tracked one go stale, and is not itself a violation of "shrink-only").
6. **(Opus review, not fixed here) The `(school)/app` shell has no workspace-type/role gate.** `SchoolLayout` calls `getNavConfig(ctx.workspaceType, ctx.role)`, which for a `parent` role or a `personal` workspace resolves to the `family:parent`/`personal:owner` trees — hrefs under `/family/*` and `/personal/*`, neither of which exists yet (M1/M2). This is pre-existing (the old hand-rolled `SCHOOL_NAV` had no such gate either, and no parent-onboarding flow exists yet to reach this state in practice) and out of this Part's scope; tracked in the test report's known issues and as a follow-up in `docs/plan/HANDOFF-2026-09-24.md`.

**Why:** two independently-tested, independently-shipped systems claiming to be "the nav" is the exact drift ARCHITECTURE and CLAUDE.md rule 1 exist to prevent — a route guard and a rendered nav item disagreeing about visibility is precisely prototype finding D6 (`module_*` toggles no navigation code read) reappearing at the shell level. Picking the already-tested, zero-UI-dependency copy as canonical (rather than picking `packages/ui`'s richer-looking one and porting its tests) means the school shell inherits `filterNav`'s existing pgTAP-adjacent unit coverage for free, and keeps the dependency direction the codebase already has (`ui` → `domain`, never the reverse). Gating nav visibility on a module key with no real plan-catalogue equivalent, silently, is worse than the duplication it would have replaced: it would have shipped a nav nobody could see modules in. Reading the plan's own row when only its `id` was ever needed is the same class of mistake CLAUDE.md rule 1 warns about at the RLS layer specifically — the fix is narrowing the query to what the caller actually needs, never widening the policy to fit an over-broad query.

**Consequences:** any new nav item that needs a plan gate must add its `plan_modules` equivalent to `NAV_MODULE_TO_PLAN_MODULE` explicitly — an unmapped `module` key on a `NavItem` is a promise nobody is keeping yet. `packages/ui`'s `nav-config.ts` module docblock and this entry are the record of the "single nav system" shape; a future change to any of the five curated trees is a change to `packages/domain/src/nav/config.ts` only. `DESIGN-SYSTEM.md` §3.2 gets a short note pointing here. The `KNOWN_GAPS` allowlist in `scripts/check-coverage-test-files.mjs` is real, tracked technical debt — closing each entry is future work, not something this Part claims to have done. Any future caller resolving a workspace's plan for anything beyond its id (billing pages, display) still has `getWorkspacePlan`'s original RLS caveat and must decide for itself whether it needs the full row (and therefore inherits the same non-public-plan `not_found`) or just the id.

## D-57 — Visual language: ink/paper monochrome chrome from acadigma.com, JetBrains Mono · ACCEPTED · 2026-09-24

> Numbering note: D-56 is taken by PR #17 (`feat/m0-wrapup`, in flight). This entry takes D-57.

**Context:** the owner's review of the shipped Campus UI was "looks bad, nothing like the website that you built" — the reference is [acadigma.com](https://acadigma.com), whose source is on disk at `acadigma-website` (read-only for this repo). Its `src/app/globals.css` defines a fully achromatic ink (`#0b0b0b`) / paper (`#f4f4f2`) / chalk (`#fbfbfa`) system with a single destructive red (`#b42318` light, `#f97066` dark) and no other hue; `src/app/layout.tsx` self-hosts Inter, JetBrains Mono and Hind Siliguri via `next/font`. Campus's existing DESIGN-SYSTEM.md (§1.2, §2.1) specified an indigo `--primary`, an amber `--accent` used as a rationed "one highlight per screen" device, and a dark-navy `--sidebar` kept from the Base44 prototype — none of which read as the same product as the reference site.

**Decision, overriding the DESIGN-SYSTEM's existing indigo/amber/navy direction:** every chrome token — `--background`, `--foreground`, `--card`, `--popover`, `--muted`, `--border`, `--input`, `--ring`, `--primary` (and its `-hover`/`-foreground`/`-soft`/`-ink` steps), `--secondary`, `--accent` (and its `-soft`/`-ink` steps), and every `--sidebar-*` token — is now literal, achromatic hex copied from acadigma-website's `globals.css`: ink `#0b0b0b` on paper `#f4f4f2`, card/chalk `#fbfbfa`, secondary/muted surface `#e9e9e6`, border `#e2e2df`, dark background `#0b0b0b` / card `#121212` / muted-foreground `#8e8e8e`. `--sidebar` is now a chalk surface with ink text (light: `#fbfbfa`; dark: `#161616` "graphite"), not the inverted navy — the previous entry ("reads as the crest bar over a school gate") is superseded here, since the owner's instruction is for Campus to read as the _same_ product as a site that has no inverted surface. `--accent` — previously amber, "the highlighter, one element per screen, ration it" (DESIGN-SYSTEM §1.2/§1.5/§1.6, `packages/ui/tokens/README.md` rule 2) — is retired to a plain neutral surface identical to `--secondary`, matching acadigma-website's own `--accent` (which equals its `--secondary`). Colour survives ONLY where DESIGN-SYSTEM §2 already makes it semantic and unrelated to brand chrome: attendance status (§2.4, token values untouched), grade bands (§2.5, token values untouched), the chart palette (§6.1, token values untouched — but see the amendment below on what this PR's own cascade-layer fix means for what actually renders), and `--danger`/`--destructive`, which is recoloured to the website's literal red (light `#b42318`, dark `#f97066`) so the one "something is wrong" colour matches across both products. `--success`, `--warning` and `--info` are explicitly left unchanged — out of scope for this Part; they still carry their previously-measured hues and are needed by UI this Part does not touch (the offline banner, toasts). Radius multipliers move to the website's (`0.6/0.8/1/1.4/1.8`, plus `2.2`/`2.6` reserved) on the unchanged `0.75rem` base. Shadows are retinted from the hue-272 neutral to plain ink (`rgb(11 11 11 / …)` light), matching the website's `shadow-input`. A new `--ease-out-expo` (`cubic-bezier(.16,1,.3,1)`) is added for entrances, alongside the existing easings — it does not relax DESIGN-SYSTEM §2.7's "motion must be motivated" rule. JetBrains Mono is added as `--font-mono` via `next/font`, self-hosted the same way as Inter/Hind Siliguri (`apps/web/app/fonts.ts`), Latin-only. `html` gets `font-feature-settings: "cv11" 1, "ss01" 1` (single-storey `a`, open forms), matching the website; `--fs-prose`/`--fs-tabular` repeat the same two features since `font-feature-settings` does not merge across redeclarations. Campus's own type scale (15px base, phone-first) and 44px touch targets are unchanged — this Part is chrome, not layout or type scale.

**One deliberate deviation from the literal website values, and why:** acadigma-website's `--input` (`#d6d6d2`) measures 1.32:1 against its own `#f4f4f2` background — acceptable for a marketing page, where a field's shape, padding and label carry its boundary. Campus's `Field` primitive has no other way to show an unfocused input's edge, and DESIGN-SYSTEM §7.1 requires non-text UI boundaries to clear WCAG 1.4.11's 3:1. `--input` is darkened to `#808080` light / `#6b6b6b` dark, clearing 3:1 against `--background`, `--card` **and** `--muted` (3.59 / 3.81 / 3.25:1 light, 3.69 / 3.52 / 3.20:1 dark), computed and asserted by `scripts/check-contrast-tokens.mjs`. `--border`/`--border-strong`/`--rule` (hairline row and table dividers, never a form control's own edge) keep the website's literal, sub-3:1 values — consistent with how this token set has always treated a divider as decorative rather than a component boundary WCAG 1.4.11 scopes.

**Amendment (Opus review, PR #20).** Four gaps in the first cut, all fixed in the same PR before merge:

1. **`--muted-foreground`/`--sidebar-muted` failed 4.5:1 against the surfaces real text actually sits on.** The website's literal `#6f6f6f` cleared 4.5:1 against `--background` (4.56:1) but only 4.13:1 against `--muted`/`--secondary`/`--sidebar-accent` (`#e9e9e6`) — and `AttendanceToggle`'s unselected 13px-semibold letters, avatar initials and `EmptyState`'s icon label all render on exactly that surface. Both tokens move to `#636363` (5.46:1 on background, 4.94:1 on muted/secondary/sidebar-accent, both themes). `--input`'s first value, `#8a8a86`, had the same shape of gap — 3.15:1 on `--background` but 2.85:1 on `--muted`, below the 3:1 UI-boundary minimum — fixed to `#808080` above.
2. **The chart palette's "token values unchanged" claim needed the render caveat above** — this PR's own §1.1 cascade-layer fix is what makes tokens.css's `--chart-1`…`--chart-6` render at all in place of the fallback's stock 5-series shadcn palette; "unchanged" was true of the hex and false of the rendered page before this PR shipped.
3. **`packages/ui/globals.css`'s `@theme inline` block mapped `--color-destructive: var(--destructive)`**, a variable that exists only in that file's own `@layer base` fallback — never in tokens.css, which instead defines `--danger` and its own alias `--color-destructive: var(--danger)`. Because both `@theme inline` blocks declare the same custom property and globals.css's copy is textually later, it silently won, and only rendered the right colour because its fallback `--destructive` hex was hand-kept in sync with tokens.css's `--danger`. Fixed to `--color-destructive: var(--danger)`, with a `--danger` added to the fallback `@layer base` block (identical value to its `--destructive`) so the alias still resolves in the momentary window before tokens.css's import lands.
4. **JetBrains Mono was preloading on every page with nothing rendering `font-mono` yet.** `apps/web/app/fonts.ts` now sets `preload: false` on it, the same choice already made for Hind Siliguri and for the same reason — the token stays available, the file downloads only once something actually paints in it.

`scripts/check-contrast-tokens.mjs` was also rewritten to parse its hex/`oklch()` values directly out of `tokens.css` (brace-counted block extraction, plus an OKLab→linear-sRGB conversion for the `oklch()` palette overrides) instead of a hand-copied table, specifically so a future token change cannot silently drift out of sync with what the script asserts — the class of bug that produced gap 1 above. It also now checks `--muted-foreground` against `--muted`/`--secondary`/`--sidebar-accent` (not just `--background`/`--card`), `--input` against `--muted`, and every `palette-*` override's `--primary` against the unchanged `--primary-foreground` it will actually be painted under.

**Out of scope for this Part, by the main session's explicit instruction:** `packages/ui/src/primitives/bottom-nav.tsx`, `nav-config.ts`, a not-yet-existing `sidebar-from-config.tsx`, and everything under `apps/web/app/(school)/` are untouched — PR #17 (`feat/m0-wrapup`) is rewriting them concurrently. The sidebar and bottom-nav restyle here is therefore tokens-only; nav-specific component styling (spacing, the active-item treatment, the workspace switcher) follows in a Part after #17 merges. `--success`/`--warning`/`--info` recolouring, if wanted, is also deferred — see the note above.

**Why:** the owner's own words are the requirement — the product should look like the company that built it. Retiring amber-as-decoration is a judgement call, not something the owner stated explicitly; it follows from "Keep ONLY semantic colour: attendance, grade bands, danger, chart palettes" in the session's brief, read together with acadigma-website having no amber at all. A future PR can reintroduce a rationed accent colour if the owner wants one back, as a new decision.

**Consequences:** every screen's default chrome is now achromatic; a user's `palette-*` preference (unaffected by this decision — see tokens.css §9) remains the only way `--primary`/`--ring`/`--chart-1` carry a hue. Component files that were already 100% token-driven (`badge.tsx`, `label.tsx`, `separator.tsx`, `top-bar.tsx`, `app-shell.tsx`, `empty-state.tsx`, `inline-alert.tsx`) needed no source changes — they inherit the new look through the tokens alone; `button.tsx`, `card.tsx`, `input.tsx` and `auth-card.tsx` additionally moved from Tailwind's stock `shadow-xs`/`shadow-sm` to the ink-tinted `shadow-flat` token, and `button.tsx` gained the website's `active:translate-y-px` press state alongside (not replacing) DESIGN-SYSTEM §3.5's `active:scale` on higher-frequency controls. DESIGN-SYSTEM.md §1 and §2 are updated in this PR with the new token tables and this reasoning; `packages/ui/tokens/README.md` documents JetBrains Mono and the new verification script.

## D-58 — Registration's personal-workspace audit stays the generic `workspaces.insert` row, not a curated `workspace.created` action · ACCEPTED · 2026-09-24

**Context:** F-ID-05 §4.1 names an explicit `workspace.created` audit action (`source='registration'`) for the personal workspace `app.handle_new_user()` creates at signup. `public.workspaces` has carried the generic `app.attach_audit()` trigger since M0 (`20260917010200_audit_and_files.sql`, re-affirmed by D-51's audit substrate), which already writes a `workspaces.insert` row — actor, workspace, timestamp — on every insert, including this one. The first draft of the F-ID-05 Part 1 PR left this as an undiscussed deviation in the spec's §11 rather than a decision; an Opus review of that PR asked for it to be a numbered decision instead, since Part 4's school-creation transaction (`app.create_school_workspace`) will face the identical choice and needs a settled answer to stay consistent with, not a second ad-hoc call.

**Decision:** no separate, semantically-named `workspace.created` row is added. The existing generic `workspaces.insert` row is treated as satisfying the spec's underlying intent (an audit trail entry exists for every workspace creation, attributable to an actor, timestamped, queryable by `workspace_id`) even though its action string differs from the one named in the spec text.

**Why:** D-51 already drew this line deliberately — the generic `<table>.<op>` action space (written automatically by `app.tg_audit()` for every attached table) is kept separate from curated business-action names introduced by explicit `app.log_audit_event()` calls, specifically so the two spaces do not have to be reconciled action-by-action as new tables and new business events are added. Adding a second, explicit `log_audit_event('workspace.created', …)` call inside `app.handle_new_user()` for the same INSERT `app.tg_audit()` already covers would write two audit rows for one event — the append-only log is more useful when redundant near-duplicate rows are the exception, not the norm for the platform's single most common write. `workspace.created` was never actually wired as a curated action anywhere (it is not in `app.audit_action_catalog`'s curated list, `20260924000100_audit_substrate.sql` §4.1); adding it now would also mean seeding it into both the SQL catalogue and `packages/domain/src/audit/catalog.ts` (parity enforced by `scripts/check-audit-catalog-parity.mjs`) for a row that would sit unused.

**Consequences:** F-ID-05's audit column in §4.1 is documented, not implemented literally — a reader who greps `audit_events` for `workspace.created` will find nothing; the real trail is `action = 'workspaces.insert'` filtered to rows where `after->>'type' = 'personal'`. Part 4 (`app.create_school_workspace`) must make the same call for its own `workspaces` insert and every other row that transaction writes (`school_profiles`, `academic_years`, `grade_levels`, `workspace_join_codes`, `subscriptions`) — consistent with this entry, not a fresh decision. If a future feature genuinely needs to distinguish "workspace created at registration" from "workspace created via the school wizard" in the audit UI without relying on `after->>'type'`, that is grounds for a new, superseding decision — not a silent reversal of this one.

## D-59 — `app.tg_workspace_billing_bootstrap()` stops doing a second `UPDATE`, instead of teaching the guard to tolerate one · ACCEPTED · 2026-09-25

**Context:** Confirmed pre-existing bug, found by PR #19's reviews and recorded as Known issue #3 in `docs/test-reports/2026-09-24-F-ID-05-p1.md` and `15_personal_workspace_registration.sql` test 7's comment. `app.tg_workspace_billing_bootstrap()` (`20260917010300_plans_and_notifications.sql`, AFTER INSERT, `SECURITY DEFINER`) set `workspaces.plan_id`/`.trial_ends_at` with a nested `UPDATE public.workspaces ... WHERE id = new.id` against the row it had just inserted. That UPDATE fires `app.tg_workspaces_guard()` (BEFORE UPDATE, latest definition in `20260925000100_personal_workspace_uniqueness.sql`, unchanged from `20260917010100_identity.sql:606-644`), which since `20260917020100_plans_limits_engine.sql` §5b reads the `role` GUC via `app.is_privileged_context()` rather than `current_user` — a deliberate, correct fix for a DIFFERENT false positive (a `SECURITY DEFINER` function's `current_user` becomes its owner, not the real caller, at every nesting depth, which previously disarmed every caller check built on `is_privileged_context()`). PostgREST issues `SET ROLE authenticated` once per request; entering a `SECURITY DEFINER` function does not re-issue that `SET`, so `role` correctly reports `'authenticated'` for the nested UPDATE too. `is_privileged_context()` therefore cannot distinguish "the platform's own bootstrap trigger, running inside the same INSERT statement that just created this row" from "an ordinary authenticated client UPDATE" — both report `role = 'authenticated'` — and the guard's plan/trial check (added for the latter) raised `42501` for both. Consequence: any workspace INSERT made as `authenticated` failed in the bootstrap chain; a school created from the app would fail outright, and F-ID-05 Part 4's planned `app.create_school_workspace()` would hit this on day one.

**Decision:** split `app.tg_workspace_billing_bootstrap()` into two functions rather than teach the guard a `pg_trigger_depth()` exception (D-52's `data_requests` cascade pattern, the alternative option): a new `app.tg_workspace_billing_defaults()`, BEFORE INSERT, sets `NEW.plan_id` (and, for schools, `NEW.trial_ends_at`) directly — no `UPDATE` statement at all. `app.tg_workspaces_guard()` is `before update` only (it reads `OLD`, which does not exist for an INSERT), so a BEFORE INSERT trigger never reaches it — there is nothing left for the guard to see or misclassify. `app.tg_workspace_billing_bootstrap()` itself is narrowed (`create or replace` on the same signature, forward-only per HANDBOOK §1 rule 5) to only what genuinely needs the row to already exist — the school's `subscriptions`/`subscription_events` rows, which reference `new.id` as a foreign key checked immediately at the end of their own INSERT statement, before the outer `workspaces` INSERT has necessarily completed if done any earlier. Migration: `20260925000200_billing_bootstrap_vs_workspace_guard.sql`. Proof: `supabase/tests/16_billing_bootstrap_guard.sql`.

**Why not the guard exception (option a):** would have worked (mirroring D-52 exactly: `pg_trigger_depth() > 1` plus the specific `plan_id`/`trial_ends_at` NULL → bootstrap-value transition a client can never manufacture, since the guard already blocks any client `UPDATE` that would put a row's `plan_id` back into `NULL`), but it is strictly wider than necessary — it teaches a security trigger a new case to reason about, forever, for a problem that has a zero-new-surface fix. Removing the second statement is narrower: nothing is added to the guard's job, and a client's direct `UPDATE` of `plan_id`/`trial_ends_at`/`access_mode` is refused exactly as before — same code path, same message, same errcode (`16_billing_bootstrap_guard.sql` test 2). **Why not a transaction-local flag (option c):** PostgREST lets `authenticated` reach `app.*` only through the `SECURITY DEFINER` surface D-50 already defines, not arbitrary SQL, so a client-forged `set_config('app.bootstrap', 'on', true)` is likely not reachable today — but "likely unreachable" is not a security boundary on its own, and it would still need something to distinguish caller from callee, which is precisely what removing the second statement makes unnecessary.

**Consequences:** the personal-workspace bootstrap now also goes through `app.tg_workspace_billing_defaults()` (previously reached only via `app.handle_new_user()`'s privileged execution context, so it never actually tripped the guard in practice — but it is fixed on the same path regardless, not left as a coincidence). **Not fixed here, same class of bug, flagged for follow-up:** `app.transfer_ownership()` (`20260917010100_identity.sql:785-819`) has the identical shape — a `SECURITY DEFINER` function, callable by a plain `owner` (checked explicitly via `app.member_role`), does a nested `UPDATE public.workspaces SET owner_id = ...`, which would trip the guard's `owner_id` immutability check for the same `role`-GUC reason the moment anything calls it as `authenticated` rather than a privileged role. No `public` wrapper exists for it yet (D-50) and no test exercises it as `authenticated`, so this is untested and latent, not confirmed-broken-in-production — recorded here so whoever wires up ownership transfer checks this decision first instead of rediscovering D-59 from scratch.

**Hardening added on Opus review (same PR, before merge):** `workspaces_insert`'s `WITH CHECK` (`20260925000100_personal_workspace_uniqueness.sql`) constrains `owner_id`/`created_by`/`type` only — it says nothing about `plan_id`/`trial_ends_at`, so a client's own INSERT statement could set either to anything (a paid plan, a decade-long trial). The BEFORE INSERT trigger already overwrote both on every success path, but the failure path — the expected `plans` row missing — did not: `if found then ... end if` around each catalogue lookup meant a missing row silently left `NEW.plan_id`/`NEW.trial_ends_at` exactly as the client supplied them, and for a school that value would then flow straight into the AFTER trigger's `subscriptions` insert as a real trialing subscription on the client's chosen plan. Fixed two ways in `app.tg_workspace_billing_defaults()`: (1) `new.plan_id := null; new.trial_ends_at := null;` unconditionally, as the first statement in the function body, before any lookup — so there is no code path where a client-supplied value can survive; (2) `if not found then raise exception ... using errcode = '22023'` in place of the old silent `if found`, for both the `personal_free` and `pro` lookups — a missing catalogue row is a bootstrap misconfiguration, not a client input problem, and per this repo's own precedent (`app.log_audit_event()`'s uncatalogued-action check, `20260924000100_audit_substrate.sql`) that must abort the transaction loudly rather than produce a row nobody asked for. Both fixes are kept together: clearing alone would still leave a signup silently under-entitled (a workspace with `plan_id is null`); raising alone would still, in principle, depend on no future edit reordering the function ahead of the clear. Proof: `16_billing_bootstrap_guard.sql` — a caller-supplied paid `plan_id` and a decade-long `trial_ends_at` are both overwritten by the bootstrap regardless; and, under a savepoint as the privileged `postgres` role with the `pro` plan row temporarily renamed, the INSERT raises `22023` rather than creating a workspace with client-controlled billing.

**Audit-trail note (Opus review):** moving `plan_id`/`trial_ends_at` from an AFTER INSERT `UPDATE` onto the BEFORE INSERT `NEW` changes what `app.tg_audit()`'s generic trigger records, not just how the guard sees it. Before this PR, the second `UPDATE` statement meant the audit trail carried two rows per workspace creation — a `workspaces.insert` row (without a plan yet) immediately followed by a `workspaces.update` row (recording the plan/trial being set, and bumping `updated_at`). After this PR there is exactly one `workspaces.insert` row, and it already carries the final `plan_id`/`trial_ends_at` in `after` — `updated_at` is never bumped at creation at all, because no UPDATE against `public.workspaces` happens during bootstrap any more. This is a strictly more accurate trail (a workspace was created once, already priced, not created-then-immediately-amended) and is consistent with D-58's existing position that `workspaces.insert` is the row of record for workspace creation; no `app.audit_action_catalog` or `packages/domain/src/audit/catalog.ts` change is needed, since both `workspaces.insert` and `workspaces.update` already exist in the generic-audit catalogue and this only changes which one fires for billing fields, not the catalogue's shape.

## D-60 — `onboarding_progress`'s repository is keyed on `userId`, not `WorkspaceContext` · ACCEPTED · 2026-09-25

**Context:** CLAUDE.md's non-negotiable rule 6 reads "every repository method takes `WorkspaceContext` first." F-ID-05 Part 2's `packages/db/src/repositories/onboarding.ts` (`getOnboardingProgress`, `saveOnboardingDraft`, `markOnboardingComplete`) instead takes `(supabase, userId: string, ...)` — no `WorkspaceContext` argument anywhere in the file. Flagged for a decision entry during PR #24's Opus review rather than left as an undiscussed deviation, the same standard D-58 set for F-ID-05 Part 1.

**Decision:** `onboarding_progress` repository methods are keyed on the caller's own `userId` (the id `auth.getUser()` returned for this request — never client-supplied), not a `WorkspaceContext`. `WorkspaceContext` is not merely omitted; it is unavailable in the paths this table is written from — `resolveWorkspaceContext` requires an existing `workspace_members` row to resolve, and onboarding's whole job (F-ID-05 §2: "Onboarding runs before a role exists") is bridging the gap between "has an account" and "has a workspace worth resolving a context for." The same shape already exists precedent-free in this codebase: `packages/db/src/repositories` has no `user_preferences` or `device_registrations` repository today, but both of those `public` tables (`20260917010100_identity.sql`) are user-scoped exactly this way, and F-ID-03's own `switchWorkspace`/`listMyWorkspaces` actions (`apps/web/app/(shared)/workspace/actions.ts`) are the documented precedent for "there is no `WorkspaceContext` yet — that is the point of these two actions," resolving `userId` from `auth.getUser()` directly instead.

**Why:** the security boundary rule 6 protects is real regardless of the parameter name — every write must be provably scoped to the right tenant/user, never trusting a client-supplied id. `onboarding_progress`'s RLS (`user_id = app.current_user_id()`, class U1 with two deviations, `20260925000300_onboarding_progress.sql`) is that boundary here, exactly as `WorkspaceContext` plus RLS is the boundary for tenant tables; `userId` flows into every repository call from the same trusted source (`supabase.auth.getUser()` in the calling server action, `apps/web/app/(onboarding)/actions.ts`) that `WorkspaceContext` itself is built from downstream of. Inventing a `WorkspaceContext`-shaped wrapper around a user id that has no `workspaceId` to carry would not add a boundary — RLS already provides it — it would only add a type that lies about having tenant scope it does not have.

**Consequences:** a future reviewer scanning `packages/db/src/repositories/*.ts` for "does every exported function's first parameter say `WorkspaceContext`" will find one legitimate exception here (and, if either is ever extracted into `packages/db`, two more precedent-consistent ones in `user_preferences`/`device_registrations`). Rule 6 in CLAUDE.md is not reworded by this entry — it still correctly describes the default and near-universal case; this decision is the recorded exception for the one class of table (§0.3's **user**-scoped group in DATA-MODEL.md) where a `WorkspaceContext` cannot exist by construction, not a precedent for skipping it anywhere a workspace genuinely is resolvable.

## D-61 — Shell layout gate reuses `resolveLandingRoute`; the switcher reuses `FormSheet` instead of a bespoke desktop dropdown · ACCEPTED · 2026-09-24

**Context.** PR #17's review (`docs/plan/HANDOFF-2026-09-24.md` item 3, `docs/test-reports/2026-09-24-m0-wrapup.md` §8 known issue #7) found that `(school)/app/layout.tsx` picks its nav tree from `getNavConfig(ctx.workspaceType, ctx.role)` with no check that the resolved context actually belongs to `/app`: a `parent` role or a `personal` workspace would render the `family:parent`/`personal:owner` curated trees, whose `/family/*`/`/personal/*` hrefs did not exist. F-ID-03 §8 Part 4 is the switcher/landing-resolution Part, and is where the roadmap (M1 1.1) assigned the fix. Separately, §6's UI table specifies the workspace switcher as a phone bottom `Sheet` and a distinct desktop anchored `Dropdown`.

**Decision.**

1. A new pure function, `resolveShellGate(shell, {workspaceType, role})` (`packages/domain/src/workspace/shellGate.ts`), decides `allow` / `redirect(to)` / `forbidden` for each of the three shells (`school`, `personal`, `family`) by calling the existing `resolveLandingRoute` and comparing its answer to the shell's own canonical route, rather than re-deriving F-ID-03 §4.4's table a second time. Every workspace-scoped layout calls it immediately after `requireWorkspace()`.
2. The gate deliberately passes only `{workspaceType, role}` — never the two forced-onboarding fields F-ID-05 Part 2 (PR #24) adds to `resolveLandingRoute`'s input. A caller already inside a shell has, by construction, an active membership (`requireWorkspace()` would have 403'd otherwise), so that override must never fire mid-shell; this matches the convention `switchWorkspace` already established for the same reason.
3. `WorkspaceSwitcher` (`apps/web/app/(shared)/workspace/workspace-switcher.tsx`) reuses `FormSheet` — bottom Sheet below 1024px, centred Dialog above it — instead of building a separate anchored `Dropdown` component for desktop, matching the one-container-per-breakpoint pattern already used everywhere else in this app (e.g. the re-auth flow in `ChangePasswordForm`).

**Why.** (1)/(2): a gate that re-derived the routing table would be a second source of truth that could silently drift from `resolveLandingRoute` — exactly the two-drifting-sources failure this codebase already fixed once for permissions (D6 in the Base44 prototype, §2). Reusing the existing, already-tested function and narrowing its input via `Pick<...>` makes the gate immune to that class of bug by construction, and immune to PR #24's in-flight addition regardless of merge order. (3): building a second sheet/dropdown pattern for one control is exactly the kind of "two ways to do the same thing" this codebase's `packages/ui` primitives exist to prevent; `FormSheet` is already accessible, tested and keyboard-operable, and the spec's intent (a dismissible list of workspaces, reachable at both breakpoints) is served identically either way.

**Consequences.** `(personal)` and `(family)` get minimal shells (guard + one placeholder page, no nav wired) rather than 404s or the wrong shell — F-ID-06/F-AC-10 replace the placeholder pages, not the gate. The switcher's desktop appearance is a centred dialog, not an anchored dropdown from the chip — a cosmetic deviation from spec §6, recorded there too (F-ID-03-workspaces-and-membership.md §11, "Part 4"). Any future shell (a seller shell, a platform-console-with-membership shell) that needs the same gate should extend `ShellName`/`SHELL_ROUTES` in `shellGate.ts` rather than hand-rolling its own check.

## D-62 — Trial expiry moves a workspace to `access_mode = read_only`, not to a Free plan · ACCEPTED · 2026-09-25

**Context:** F-CM-06 §4.2/§8 Part 4 and its acceptance criteria (§9 AC1-AC3) describe trial expiry as "the plan becomes Free" — an expired 14/30-day Pro trial falls back onto a `free` school plan, which then carries its own (lower) `plan_limits`/`plan_modules`, and the read-only-over-limit machinery (§5.6) enforces those lower limits. That target plan no longer exists: D-42/OQ-22 retired the `free` school plan row entirely (`20260917010300_plans_and_notifications.sql` §2.4 — "'free' school plan row removed... `personal_free`... is kept"), replacing it with one 30-day Pro trial and nothing self-serve underneath it (`plans` seed: `personal_free`, `starter`, `pro`, `enterprise` only). ROADMAP M1 1.10 already reflects the narrower, current framing — "trial expiry job → `access_mode=read_only` banner" — rather than the spec file's older "falls back to Free" wording, which `DATA-MODEL.md` §5.2 still carries verbatim from before D-42 landed.

**Decision:** `public.expire_pro_trials()` (`20260925000500_trial_expiry_billing_tick.sql`) does not change `workspaces.plan_id` at all. An expired trial's workspace stays on `plan_id = pro` — so its `plan_limits`/`plan_modules` are unchanged, which matters because the current Pro seed has `max_teachers`/`max_students` as NULL (unlimited) — and is instead put into `access_mode = 'read_only'` via the already-built `app.set_access_mode()` (Part 3, `20260917020100_plans_limits_engine.sql` §6). `subscriptions.status` moves `trialing → expired`, and a `trial_expired` `subscription_events` row is appended, both already-defined values (`public.subscription_status`'s `expired`; `subscriptionEventTypeSchema`'s `trial_expired`, `packages/contracts/src/plans.ts`).

**Why:** read-only-over-limit mode (§5.6) already exists and does exactly the job a plan downgrade would have done — block a CREATE that would grow a counter, allow everything else — without needing a second Free-tier plan row to exist purely as an enforcement target. Inventing a plan for that purpose (reseeding `free`, giving it real limits, reversing D-42) is a business-pricing decision this Part has no mandate to make on its own; using the mechanism that is already there is the narrower, reversible choice. It also matches what a school that has not paid actually needs blocked: with `pro`'s current teacher/student limits unlimited, a plan swap alone would not have restricted anything meaningful anyway — `access_mode = read_only` is the actual gate.

**Consequences:** §9 AC1/AC3/AC4's literal wording ("the plan becomes Free") does not hold under this schema; `docs/features/04-commerce/F-CM-06-plans-and-subscriptions.md` §11 is updated in this PR to record the deviation, and `DATA-MODEL.md` §5.2's "falls back to Free" sentence — stale from before D-42 — is corrected in this same PR. Parts 5-8 (checkout/proration, invoices, renewal/dunning, platform overrides) are unbuilt, so `runSubscriptionJobs`'s full `{remindersSent, pastDue, lapsed, downgradesApplied}` response shape (§7) is not implemented — only `{trialsExpired}` — and the T-3 nudge email/banner and the one-email-per-day dunning guard from §8 Part 4's original scope are deferred to whichever Part adds the email adapter. If the owner later reintroduces a real Free tier (reversing D-42), `expire_pro_trials()` is the function to revisit — it would gain a `plan_id` update alongside the `access_mode` change, not replace it, since read-only-over-limit mode would still be the correct enforcement for whatever that tier's limits are.

## D-63 — F-OP-06 Part 1: staff schema shape, `app.staff_hourly_rate`'s signature, and the `staff_directory` view pattern · ACCEPTED · 2026-09-25

**Context:** F-OP-06 §11 OQ-1 flags "compensation table vs column" as "the single most important call in this feature" and explicitly defers to `DATA-MODEL.md`. `DATA-MODEL.md` §6 already carried a short, pre-spec sketch of `staff_records`/`staff_compensation` (Base44-mirroring columns: `contract_signed`/`id_issued`/`orientation_done`/`system_access`, no `staff_status` lifecycle) and, in §11's illustrative policy-template block, a 3-argument `app.staff_hourly_rate(p_workspace_id, p_user_id, p_on)`. Separately, `workspace_members` (F-ID-03, already shipped) already carries `label_id`, `employee_code`, `department`, `subjects text[]` and `phone` — a rough approximation of several fields F-OP-06 §3.1 also wants on `staff_records` (`designation_label_id`, `staff_code`, `department`, `subject_ids uuid[]`, `work_phone`).

**Decisions:**

1. **`staff_compensation` is a separate table**, confirming OQ-1's own recommendation and `DATA-MODEL.md`'s independent prior lean — RLS is row-level, not column-level, so a `staff_records`-column rate would either be readable by everyone with record access or would force `staff_records` itself to `{owner,admin}`-only, breaking "a teacher sees their own joining date." Keyed by `staff_record_id` (not `(workspace_id, user_id)` as `DATA-MODEL.md`'s sketch had it) — a compensation row is meaningless before a person exists as a `staff_records` row, and keying off the record sidesteps the pre-hire-placeholder `user_id is null` case entirely. Exclusion constraint: `exclude using gist (staff_record_id with =, daterange(effective_from, effective_to, '[]') with &&)`, `effective_to` **inclusive** (spec §3.2's own example: a period "ending 30 Sep" and the next "starting 1 Oct" do not overlap only if 30 Sep is included in the first). A `before insert` trigger (`app.tg_close_prior_compensation_period`) closes the previous still-open row to `new.effective_from - 1` for the common forward-dated case; a genuinely overlapping or back-dated insert is left for the exclusion constraint itself to refuse.
2. **`app.staff_hourly_rate(p_workspace_id uuid, p_user_id uuid, p_on date)` — three arguments, matching `DATA-MODEL.md` §11's original sketch, not the 2-argument self-checking signature this entry originally described.** A first draft dropped `p_workspace_id`, resolving the staff record's workspace implicitly from `staff_records.user_id` alone and checking the CALLER'S own standing inline. That is wrong for a person staffed at more than one school (PRODUCT-DECISIONS §7 accepts this as normal): with no workspace filter, the query's `order by effective_from desc limit 1` can pick whichever school's compensation row happens to sort first, silently returning the WRONG school's rate. Restored on lead review (item 9 below) with `p_workspace_id` filtering both the join and the authorization check (`p_user_id = app.current_user_id() or app.has_role(p_workspace_id, '{owner,admin}')` — see item 7 for how that second clause was separately corrected).
3. **`staff_records`' overlapping columns with `workspace_members` are NOT reconciled in this Part.** `staff_records.designation_label_id/department/subject_ids/work_phone` are new, independent columns — this migration does not read, migrate or deprecate `workspace_members.label_id/department/subjects/phone`. Reconciling the two (or migrating one into the other) is a real, separate architectural call this Part deliberately does not make, to keep the branch self-contained per its build-ahead-of-slot instruction. Flagged in `F-OP-06-staff-directory-and-records.md` §11 for whichever Part next touches the directory (Part 2) or the record editor (Part 3) to settle.
4. **`staff_directory` is a view with default (definer) privileges, not `security_invoker`.** Every other manually-secured view in this codebase so far (`audit_events_view`) uses `security_invoker = true`, because its job is read-time column redaction UNDER THE SAME row visibility the base table's RLS already grants. `staff_directory`'s job is the opposite — WIDEN visibility beyond `staff_records_select` (owner/admin-or-self) to "every active non-parent member" — so it deliberately omits `security_invoker`, runs with the view owner's (postgres, RLS-exempt-by-ownership) privileges, and re-implements the intended row check itself in its own `WHERE app.has_role(sr.workspace_id, '{owner,admin,teacher,staff}')`, with `security_barrier = true` as insurance against a future added column creating an evaluation-order side channel. This is the first instance of this pattern in the codebase and is recorded here as the template for it, alongside the existing `security_invoker` template for redaction-only views.
5. **`check-audit-catalog-parity.mjs` now scans every file in `supabase/migrations/`, not only `20260924000100_audit_substrate.sql`.** The script hard-coded that one path; `staff_records`/`staff_compensation`/`staff_documents` need `<table>.insert|update|delete` rows in `audit_action_catalog` the same way every other generic-audit table gets them, and the already-applied audit-substrate migration can never be edited to add them (HANDBOOK: forward-only). The script now aggregates every migration's `v_tables text[] := array[...]` block and every `insert into audit_action_catalog ... values ... on conflict` block, and `packages/domain/src/audit/catalog.ts`'s `GENERIC_AUDIT_TABLES` gained the three new table names to match.
6. **The seeded `custom_labels` defaults are nine, not the spec's ten — Guardian → `parent` is omitted.** First drafted with all ten of spec §3.4's rows, including `('parent', 'Guardian', ...)`. CI's `db` job caught this before merge: `custom_labels_base_role_not_parent` (`20260917010100_identity.sql`, pre-existing — `check (base_role <> 'parent')`) rejected the insert, and because the seeding runs inside `app.tg_workspace_bootstrap()` (every workspace's own creation trigger), the failure was not confined to one test — it aborted the fixture setup of every pgTAP file that creates a school workspace, roughly a dozen files across the whole suite, all reported as collateral "Bad plan" failures with zero relation to their own logic. Fixed by dropping the Guardian row rather than touching the pre-existing constraint (out of scope for this Part, and the constraint is arguably correct — permissions come from `base_role`, and a `parent` custom label is not currently a concept this schema supports anywhere else). Caught by CI, not local testing — this laptop has no local Postgres this session (`HANDOFF-2026-09-24.md`), so this trigger body had never actually executed before its first `db` job run.
7. **`app.staff_hourly_rate`'s first draft let any active member of the row's workspace — including a teacher or staff colleague — read the rate, not just owner/admin or the row's own person. A real, confirmed leak, not a false alarm.** The `or app.has_role(sr.workspace_id, '{owner,admin,teacher,staff}')` clause (mirroring `staff_directory`'s intentionally-wide visibility, item 4) was wrong here: `staff_compensation_select`, the RLS policy this function exists specifically to provide a narrower alternative to, never grants a teacher visibility into a colleague's compensation row at all — copying the wider role list onto the function opened exactly the side door the table's own RLS refuses. Caught by CI: `20_staff_schema.sql`'s own `app.staff_hourly_rate never returns a colleague's rate to a teacher` assertion failed against a live Postgres, `have: 45000, want: NULL` (Teacher A querying Teacher A2's real rate and getting it). Fixed to `'{owner,admin}'`, matching `staff_compensation_select` exactly. This is the reason the Part 1 demo's central claim — "a teacher cannot read a colleague's rate by any query" — did not hold before this fix; it holds now, and is the whole reason this function's own pgTAP coverage exists rather than trusting the design read alone.
8. **The three new tables were missing their explicit `revoke all ... from anon, authenticated` — the table-grant equivalent of D-54.** `supabase/ci/bootstrap.sql` mirrors a real Supabase per-project default privilege — `alter default privileges for role postgres in schema public grant all on tables to anon, authenticated, service_role` — so every new table starts with a raw GRANT to `anon` unless a migration explicitly revokes it, the same gap D-54 closed for functions. Every other table in this schema does this (`onboarding_progress`, `platform_settings`, ...); this migration's first draft copied the `grant select, insert, update ... to authenticated` lines without the preceding revoke. Caught by CI: `20_staff_schema.sql`'s `anon has no privileges on staff_records/staff_compensation at all` assertions failed. **Not a data leak** — RLS was already enabled with every policy scoped `to authenticated`, so `anon` (no applicable policy, no `BYPASSRLS`) saw zero rows regardless of the table grant — but it is a real violation of this codebase's grant-hygiene invariant, worth fixing on its own terms rather than arguing RLS alone was sufficient. Fixed: `revoke all on public.staff_records/staff_compensation/staff_documents from anon, authenticated;` added before each table's `grant` line.
9. **`app.staff_hourly_rate` regained its third argument, `p_workspace_id`, on lead review — see item 2's rewrite above.** New pgTAP directly proves AC-30 (a person staffed at two schools, with two different overlapping-date rates, gets back exactly the rate for the workspace they asked about) rather than treating AC-30 as merely "implied" by the general tenant-isolation proof, which is what the Part 1 test report originally — incorrectly — claimed.
10. **`staff_compensation` and `staff_documents` gained a COMPOSITE `(workspace_id, staff_record_id)` foreign key against `staff_records (workspace_id, id)`, replacing a plain single-column FK on `staff_record_id` alone. A real, confirmed cross-tenant write, not a false alarm — BLOCKING lead review.** A plain FK only proves the referenced `staff_records` row exists somewhere, never that it belongs to THIS row's own `workspace_id`. Concretely: an owner/admin of workspace B (anyone can become one — school creation is self-serve) could `insert into staff_compensation (workspace_id, staff_record_id, ...) values (B, <a staff_record_id that actually belongs to workspace A>, ...)` — `staff_compensation_insert`'s RLS only checks `has_role(workspace_id, {owner,admin})` against the INSERTED row's own `workspace_id` (B, which they legitimately own), never that it agrees with the referenced record's real workspace. The row would then insert successfully, `app.tg_close_prior_compensation_period()` would close the REAL School A period sharing that `staff_record_id` (the closing trigger only filters by `staff_record_id`, not `workspace_id`), and `app.staff_hourly_rate` called with `p_workspace_id = B` would return the forged rate for a person who has never worked at School B. `staff_records` gained `constraint staff_records_workspace_id_key unique (workspace_id, id)` (redundant with the PK alone but required as an explicit constraint object for the two tables to reference) as the FK target. Fixed on both tables; proven by pgTAP (`23503` on the forged insert, both tables).
11. **`app.tg_staff_records_self_update_guard()` was rewritten from a deny-list to an allow-list of self-editable columns.** The first draft enumerated admin-only columns (`v_admin_only_cols`) and let anything NOT in that list through — which fails OPEN: a column this migration forgot to list, or a column a FUTURE migration adds to `staff_records` without also touching this trigger, defaults to self-editable. Rewritten to enumerate `v_self_editable_cols` (the genuinely personal fields) and reject every OTHER column (except a small `v_system_cols` set — `id`, `workspace_id`, `created_at`, `created_by`, `updated_at` — that are either immutable or managed by a different trigger; `updated_at` specifically is touched by `app.tg_set_updated_at()` on every update, including a legitimate self-update, and must never be mistaken for an admin-only column). New pgTAP proves the allow-list rejects `designation_label_id` and `joined_on` individually, not only the three columns the original test happened to name.
12. **`staff_documents_insert`'s self-insert branch was missing checks on `verified_by`/`verified_at`/`uploaded_by` — a real security gap, not a false alarm.** A self-inserting teacher could set `verified_by`/`verified_at` to anything (faking their own document as admin-verified) or set `uploaded_by` to someone else's id entirely. Fixed: the self-insert branch of the `with check` now additionally requires `verified_by is null and verified_at is null and uploaded_by = (select app.current_user_id())`; the owner/admin branch is unaffected (they ARE the verifier). Proven by two new pgTAP throws_ok cases.
13. **Bare `app.current_user_id()` calls inside RLS `USING`/`WITH CHECK` clauses wrapped as `(select app.current_user_id())`**, matching this repo's existing convention (a scalar-subquery wrapper lets Postgres cache a `STABLE` function's result once per statement as an initplan, instead of re-evaluating it per row scanned) — six call sites across `staff_records_select`, `staff_records_update_self`, `staff_compensation_select`, `staff_documents_select` and the new `staff_documents_insert` self-insert branch. Left bare inside the two plain-SQL functions (`app.staff_hourly_rate`, `app.can_open_staff_document`), which are single-row lookups, not per-row policy predicates evaluated over a scan.
14. **New indexes from lead review:** `staff_compensation (workspace_id)`, `staff_documents (workspace_id)` (a full index alongside the existing `(workspace_id, expires_on) where expires_on is not null` partial one), `staff_records (user_id, workspace_id)` (the reverse column order of `staff_records_workspace_user_key`, for "which of my records, in which school" when a person holds more than one).
15. **The allow-list guard's own `v_system_cols` skip-list still let `created_by`/`created_at` through unchecked — a real gap in item 11's fix, found on a security re-check of the fix itself.** Rewriting the guard from a deny-list to an allow-list (item 11) is only as safe as the small "never checked either way" system-column list sitting outside both lists — and that list originally named `created_by`/`created_at` alongside `id`/`workspace_id`/`updated_at`, on the reasoning that all three are "system-managed." `id` and `workspace_id` genuinely are (immutable, and separately frozen); `updated_at` genuinely is (touched by `app.tg_set_updated_at()` on every legitimate update, so it must stay skipped or every harmless self-update would false-trip the guard). `created_by`/`created_at` are NOT system-managed after insert — nothing else protects them from a client `UPDATE`, so skipping them meant a self-updating member could rewrite their own record's provenance (who created it, when) in the same statement that legitimately touched their phone number. Fixed: removed from `v_system_cols`, so both now fall through to the ordinary allow-list check like any other column (neither is in `v_self_editable_cols`, so any change by a non-admin is rejected). Proven by two new pgTAP cases using the 4-arg `throws_ok` form with the exact pinned exception message, not just the errcode.
16. **`app.staff_hourly_rate`'s third parameter is named `p_on`, not `p_on_date` — a forced rename, not a style choice.** An M0 stub of this exact function already exists — `20260917010000_extensions_and_app_schema.sql` §6, applied to `main` long before F-OP-06 Part 1, signature `app.staff_hourly_rate(p_workspace_id uuid, p_user_id uuid, p_on date)`, with a body written against a `staff_compensation` shape (`user_id`/`valid_from`/`valid_to`) that never actually shipped that way. This migration's own `create or replace function app.staff_hourly_rate(...)` is what gives the stub its real, working body — Postgres refuses a `CREATE OR REPLACE FUNCTION` that renames an EXISTING parameter (`cannot change name of input parameter "p_on"`), caught by CI (`db` job, run 36061148429) on `p_on_date`. The alternative — `DROP FUNCTION` then `CREATE FUNCTION` under the preferred name — was explicitly rejected: a drop discards the function's existing grants and would break any (even latent, not-yet-built) dependent that already resolves it by OID. Fixed by reverting the parameter name to `p_on` throughout (signature, body, comment) — everywhere else in this migration and its docs, callers already used purely positional arguments, so no test or TS call site needed a matching rename.

**Why:** all four schema/function calls in items 1-4 are the direct, narrow answers OQ-1 and DATA-MODEL.md's own sketch left open; none is speculative — each is required for the Part 1 demo ("a teacher can read their own record and their own rate, cannot read a colleague's rate by any query, and `app.staff_hourly_rate` returns the rate in force on a past date") to be true. Item 3 is a deliberate non-decision, recorded so the duplication is visible rather than silently repeated. Items 6-16 are all fixes found by CI (and, for 9-16, one or more subsequent lead/security reviews) rather than by local testing — this laptop had no local Postgres available this session (`HANDOFF-2026-09-24.md`), so this migration's trigger and function bodies ran against a real Postgres for the first time only inside CI, and each review read the actual shipped SQL rather than re-trusting the design narrative — including, in item 15's case, re-checking the PREVIOUS review's own fix rather than assuming it was complete, and in item 16's case, CI catching a collision with a pre-existing M0 stub this Part's author did not know existed until the error named it.

**Consequences:** `DATA-MODEL.md` §6 is rewritten to match the shipped schema (columns, RLS, indexes, the corrected function signature) in this PR. `workspace_members.label_id/department/subjects/phone` remain live, unused by this feature, until a future Part reconciles them — a directory screen built carelessly against `workspace_members` instead of `staff_directory`/`staff_records` would show stale or duplicate data; Part 2 must read from `staff_directory`. `check-audit-catalog-parity.mjs`'s new multi-file scan is a superset of its old behaviour (unchanged result for every migration before this one) and is itself exercised by this PR passing. Any future function or view that reads across `staff_records`/`staff_compensation`/`staff_documents` for a person who might hold more than one staff record must take a `workspace_id` and filter by it explicitly (item 9's pattern) — resolving "the" workspace implicitly is the specific mistake items 2 and 9 correct.

## D-64 — Infrastructure and repository decisions taken with the owner on 2026-09-24 · ACCEPTED · 2026-09-24

> Numbering note: D-60 to D-63 were reserved by open PRs (F-ID-05 P2, F-ID-03 P4, F-CM-06 P4, F-OP-06 P1) at the time this entry was written; all four have since merged (D-60/61/62 as their own entries above, D-63 below). This entry records decisions made in conversation that no PR's own decision entry covers.

1. **The repository is public.** GitHub Actions minutes on the private repo ran out on 2026-09-24 (every job refused with a billing message). The owner chose to make `Mahadezz/acadigma-campus` public so CI is free and unlimited. Before the switch, all 147 commits were scanned for secrets (Supabase `sbp_`/`sb_secret_` keys, service-role JWTs, GitHub/Vercel/Anthropic/Resend/AWS tokens, private keys, credentialed connection strings) and for ever-committed `.env`/key files: none found. Consequences: code and planning docs (pricing, owner questions) are world-readable; secrets live only in GitHub Actions secrets, Vercel env and gitignored local files. Reversible in Settings → General.
2. **Public-repo protections switched on:** dependency graph + Dependabot alerts, secret scanning, secret-scanning push protection. The dependency graph also re-enabled CI's `dependency-review` step (designed to activate when the repo is public).
3. **GitHub Actions may create and approve pull requests** (repository Settings → Actions → Workflow permissions), so `release.yml` can open the changesets "version packages" PR; every release run had failed on this since M0. Risk accepted: merges require no approvals (no enforced branch protection, OQ-7), `release.yml` already holds `contents: write`, fork PRs get a read-only token, third-party actions are pinned by commit SHA and CI defaults to `contents: read`. **Revisit** if required reviews are ever enforced.
4. **Vercel functions are pinned to `bom1` (Mumbai)** (`apps/web/vercel.json`, PR #25): production had been running in `iad1` (US East) while the database is in ap-south-1. Also completes COMPLIANCE-PDPA P19's function-region half.
5. **A demo owner account exists in the live project:** `demo@acadigma.com`, owner of "Acadigma Demo School" (Pro trial), created confirmed through the Auth admin API with a random password shared with the owner out of band. It is demo data under D-20 and must be deleted (or its password rotated) before launch.
6. **The Campus app will be served at `campus.acadigma.com`** (DNS at Namecheap: a `campus` CNAME to Vercel; `www.acadigma.com` stays the marketing site). Supabase Auth's redirect allow-list already includes it; `site_url` and Vercel `APP_URL` switch once the domain verifies. Custom domains bypass Vercel deployment protection, so the app becomes publicly reachable there.

## D-65 — The PostgREST pre-request hook runs through a `public` wrapper (production hotfix) · ACCEPTED · 2026-09-24

> Numbering note: D-60, D-61, D-62 were reserved by open PRs at the time this entry was written; all have since merged, alongside D-63 and D-64.

**Context:** `20260924000100_audit_substrate.sql` (D-51) set `pgrst.db_pre_request = app.pre_request`. PostgREST runs the hook as the request role, and `anon` has no USAGE on schema `app` (D-50; asserted by `13_rls_grants_invariants.sql`), so every anonymous API request failed with `42501 permission denied for schema app`. The sign-in throttle RPCs are anonymous (pre-session) and fail closed, so the live site answered every sign-in with "Too many attempts": nobody could sign in, register or reset a password. Found on 2026-09-24 by signing in to campus.acadigma.com with the demo account. CI could not catch it: its Postgres has no PostgREST, the hook-wiring DO block is a no-op there, and the live-account e2e journeys are skipped until OQ-27.

**Decision:** add `public.pre_request()`, a SECURITY DEFINER wrapper that calls `app.pre_request()`, executable by anon/authenticated/service_role, and point `pgrst.db_pre_request` at it (`20260925000250_pre_request_public_wrapper.sql`, dated before the open PRs' reserved timestamps so their deploys still sort after it). `anon` still has no USAGE on `app`. The `db.yml` push job now smoke-tests an anonymous RPC against production after every deploy and fails loudly if it does not return 200.

## D-66 — EIIN availability is a partial unique index plus a boolean-only SECURITY DEFINER probe, checked ahead of the school-creation transaction · ACCEPTED · 2026-09-25

**Context:** F-ID-05 §4.3 step 1 must tell the user, before they ever reach the final "Create school" button, whether an EIIN they typed already belongs to another school (AC6: "an inline error names the collision ... the wizard does not advance"). §5 separately states "EIIN ... unique across the platform." Part 3 (this PR) builds only steps 1-2 of the wizard — the actual `app.create_school_workspace()` transaction that would otherwise raise `EIIN_TAKEN` is Part 4's, not yet built — so step 1's own Continue button had no enforcement to check against at all. `school_profiles`' RLS (DATA-MODEL.md §1.3, class T2) only lets an active member of a school read its row; a caller filling in the wizard is a member of nothing yet, so there is no RLS path to "does another school already have this EIIN" for any school, including the one that collides.

**Decision:** `20260925000700_school_eiin_availability.sql` adds two things, both scoped narrowly to the read-only availability question this Part actually needs:

1. `school_profiles_eiin_unique`, a partial unique index (`where eiin is not null`) — the real enforcement, and the same constraint Part 4's `app.create_school_workspace()` will eventually catch (`23505`) and translate to `EIIN_TAKEN`. A client-side probe alone cannot prevent a race between two concurrent submissions; only a database constraint can.
2. `public.check_eiin_available(eiin text) returns boolean`, a `SECURITY DEFINER` function (with an `app.eiin_is_available()` inner helper, matching this codebase's `public` = PostgREST-reachable / `app` = server-internal split, D-50) that answers the one boolean question without exposing which school holds the EIIN or any other column. Granted to `authenticated` only — unlike `public.throttle_status` and its siblings (§1.8a), this surface is reachable only after sign-in and email verification (F-ID-05 §2), so there is no pre-session case for `anon` to reach it.

**Why not defer this to Part 4:** the alternative was building an EIIN-collision check nobody could see until the whole five-step wizard existed, silently violating AC6 and §4.3's "step 1 ... does not advance" for the length of an entire Part. The index and the probe are both narrowly scoped to the read-only question Part 3 owns; neither creates a workspace, seeds any row, or reaches into territory Part 4's brief reserves (`app.create_school_workspace`, grade levels, academic years, the trial). Building the index now, rather than when Part 4 lands, also means Part 4 never has to choose between "does the uniqueness constraint already exist" and "am I the one who adds it" — it exists once, here, and Part 4's transaction only has to handle the `23505` it can already produce.

**Why a SECURITY DEFINER boolean probe, not a widened RLS policy:** widening `school_profiles_select` to let any authenticated caller read `eiin` (or any other column) across every school would leak far more than this one field asks for, permanently, for every future caller of that policy — not just the wizard's one pre-submit check. A function that answers exactly one boolean question, and nothing else, is the narrower and more reversible surface; it is also the pattern this codebase already uses for the same shape of problem (`public.throttle_status`, `public.list_my_workspaces`).

**Consequences:** `packages/db/src/repositories/school.ts` (`checkEiinAvailability`) and `apps/web/app/(onboarding)/actions.ts`'s `checkEiinAvailability` action are the only callers today. `supabase/tests/12_function_grants_invariant.sql`'s `authenticated` allowlist and `22_school_eiin_availability.sql` (new) both assert the grant shape. If a future Part needs to answer a richer "why is this EIIN taken" question (e.g. "is it yours"), that is a new, separately-considered surface — this one stays boolean-only on purpose.

## D-67 — PR #34 review follow-ups: an `eiinCheck` throttle bucket, and the wizard's hand-rolled pickers become shadcn primitives · ACCEPTED · 2026-09-25

**Context:** Opus review plus two ECC passes (security, react) on PR #34 (F-ID-05 Part 3) raised, in order of severity: (1) security, medium — `public.check_eiin_available` (D-66) has no rate limit, so a single signed-in account could enumerate which of the ~10^6 possible EIINs already belong to a school; (2) React, high — `wizard.tsx` never moved focus to a step's `<h1>` on a client-side stage change, despite `OnboardingShell`'s own docblock already documenting that contract; (3) React, high — `segmented-control.tsx`'s `tabIndex` was `0` on every option until a value was chosen, not just the first; (4) several Opus/owner findings on the same file (missing `backLabel`, an unused `eiinChecking` message key, three touch targets under 44px, a `FormLabel` pointing at an id no element carried, a stale cross-field date-range error). Separately, the owner asked that the fix for (2)/(3)/the timezone `Select` (Opus 3: unusable on touch at ~400 entries) go through this repo's shadcn/ui primitives (`packages/ui/src/components/ui`) rather than staying hand-rolled, copying whatever is missing from the owner's local `shadcn-ui` registry (`new-york-v4`).

**Decision:**

1. `public.throttle_record_failure` (20260925000800_throttle_eiin_check_bucket.sql) gains an `eiinCheck` bucket — 30 attempts / 15 min / 15 min block, keyed per user id (`throttleKey("eiin-check", user.id)`, `apps/web/lib/request-context.ts`) rather than per IP, since the RPC already requires `authenticated`. `checkEiinAvailability` (`(onboarding)/actions.ts`) checks `throttleStatus` and, if not blocked, calls `throttleRecordFailure` unconditionally — before the RPC runs, every call counts, not just a "failed" one, because the thing being capped is enumeration volume, not wrong guesses. This is a deliberate departure from every other bucket in this file (`register`, `loginByEmail`, ...), which all record only on a failed attempt; the docblock on the new bucket says so explicitly, so a future reader does not "fix" it back to failure-only.
2. `packages/ui/src/components/ui/toggle.tsx` and `toggle-group.tsx` are added (copied from `/f/shadcn-ui/apps/v4/registry/new-york-v4/ui/`, adjusted only to this repo's `cn` helper and import paths). `segmented-control.tsx` (medium, single-select) and `day-picker-row.tsx` (working days, multi-select) are deleted; `wizard.tsx` now uses `ToggleGroup type="single"` and `type="multiple"` respectively. Radix's own roving-tabindex implementation is what fixes finding (3) — there is no bespoke `tabIndex` logic left to get wrong. `type="single"` renders `role="radiogroup"`/`role="radio"` (same ARIA shape `segmented-control.tsx` already had, so the e2e spec's `getByRole("radio", ...)` selectors are unchanged); `type="multiple"` renders `role="toolbar"` with `aria-pressed` per button, not `role="checkbox"` — `create-school-wizard.spec.ts` is updated for that shape.
3. The timezone `Select` becomes a combobox built from this repo's existing `command.tsx` (cmdk) and `popover.tsx` (Radix `Popover`) primitives — not the newer `@base-ui/react`-backed `combobox.tsx` the same registry also carries, which would have added a second UI primitives dependency for one field. `DEFAULT_TIMEZONE` ("Asia/Dhaka") is sorted first in the list this component already builds from `Intl.supportedValuesOf`, rather than adding a second, separately-maintained short list.
4. `date-field.tsx` is deleted (ponytail: a native `<input type="date">` inside the same `FormControl` every other field already uses needs no bespoke wrapper). Its manually-tracked cross-field range error becomes a real `react-hook-form` error (`form.setError("academic_year.ends_on", ...)` / `form.clearErrors(...)` on either date field's `onChange`) instead of separate local state — this is also what fixes the `FormLabel`-pointing-at-a-missing-id finding for `medium`/`working_days` (replaced by a `<fieldset>`/`<legend>` pair, the same pattern `day-picker-row.tsx` already used, since neither field's control is a single focusable element `FormControl`'s `Slot` can attach an `id` to).
5. `OnboardingShell` forwards a ref to its `<h1>` (`tabIndex={-1}`, focusable but not tab-stoppable); `wizard.tsx` adds one small hook (`useFocusHeadingOnMount`) that each of `Step1`/`Step2`/`DoneScreen` — now three separate function components, `DoneScreen` newly extracted — calls once per mount, which is once per stage transition.

**Why:** (1) mirrors the shape (and the "N2: a caller must not be able to name the limit" discipline) this codebase already established for every other throttle bucket, rather than inventing a different rate-limit mechanism for one RPC. (2)-(4): reusing Radix/shadcn primitives this repo already depends on (`radix-ui`, `cmdk`, `class-variance-authority` are all existing `packages/ui` dependencies — no new package was added) fixes the roving-tabindex and touch-target findings by construction instead of patching the hand-rolled versions a second time, and stops the wizard from being the one screen in this codebase using bespoke pickers where a maintained primitive already exists. (5) matches `OnboardingShell`'s own docblock, which already said the page component owns this — Part 3 built the shell's side of the contract but never actually called it.

**Consequences:** `docs/test-reports/2026-09-25-F-ID-05-p3.md` §5/§9 is corrected in the same PR (OPUS 5): CI's `lighthouse` job only audits `/` and `/login` (`.github/workflows/ci.yml`'s `lighthouse` job), never `/onboarding/create-school`, so its Accessibility budget passing was never evidence about the wizard specifically — the report now says so, and the wizard's own axe assertions (`create-school-wizard.spec.ts`) remain skip-gated on OQ-27, unproven either way until a live account exists. `packages/ui`'s component library now has a general-purpose `ToggleGroup`/`Toggle` pair any future single/multi-select picker in this app should reach for first, rather than a third hand-rolled primitive.

## D-68 — Visual refinement "Blend" (Vercel details on the acadigma.com palette) and the Acadigma product marks in Campus · ACCEPTED · 2026-09-25

**Context:** D-57 moved Campus onto acadigma.com's ink/paper palette and fonts, but kept the older shape language: 12px radius, drop shadows on cards and popovers, bold headings, a 15px body. The owner compared that against a Vercel style reference and was offered three options. Separately, Campus still showed "Acadigma Campus" as plain text and a placeholder "A" app icon, while the brand repo (`acadigma-brand/exports/`) and the website (`acadigma-website/src/components/brand/`) already carry the real grid marks for Acadigma, Campus, Ledger, Students and Parents.

**Owner decision (2026-09-25):** option 1, "Blend" — keep the acadigma.com colours and fonts (D-57: Inter, Hind Siliguri, JetBrains Mono, #f4f4f2 / #0b0b0b) and adopt the Vercel details. And: "fetch the logos of the apps".

**Decision:**

1. `--radius` is 0.375rem (6px); the D-57 multipliers stay, so the steps are ~4 / ~5 / 6 / ~8 / ~11px. Cards, popovers, dropdowns and select menus use `rounded-lg` (6px).
2. Cards and popovers are separated by a hairline 1px ring, never a drop shadow: `--shadow-flat` (the existing ink-tinted ring) on cards, `--shadow-raised` / `--shadow-overlay` become `0 0 0 1px var(--border-strong)`. The extra `border` class on Card, AuthCard, ChoiceCard and the popover-type menus is dropped so the edge is one ring, not border plus ring. Only the bottom sheet (and the shadcn dialog/sheet overlays, which sit on a scrim) keep a real shadow.
3. An `eyebrow` utility (tokens.css): JetBrains Mono, 12px, uppercase, `--tracking-eyebrow` 0.08em, muted ink. First use: the onboarding wizard's step count ("01 / 05", digits only so it needs no copy in either locale; `aria-hidden` because the progress bar already announces it).
4. Headings are lighter and tighter: weight 500 (was 600/700) and `--tracking-tight` -0.03em (was -0.02em) on AuthCard, OnboardingShell, TopBar, CardTitle and the dialog/sheet/drawer titles. Bengali is never tracked: the `:lang(bn) { letter-spacing: 0 }` rule moves out of `@layer base` so it beats a `tracking-*` utility.
5. `--text-base` is 16px (was 15px). Inputs were already 16px.
6. Unchanged: 44px touch targets, 4.5:1 text contrast (`scripts/check-contrast-tokens.mjs`), 360px-first layout, dark mode.
7. `packages/ui/src/primitives/logo.tsx` ports the website's grid-mark geometry (not redrawn; a unit test pins the Campus mark to `campus-mark-black.svg` rect by rect) as a static `GridMark` (no `motion` dependency) plus a `Logo` lockup. Marks draw in `currentColor`, the grey cell at 35%, which gives the website's #a3a3a3 on paper and #5e5e5e on ink, so dark mode needs no extra token. The Campus logo is on the sign-in pages, the onboarding chooser header and the school shell's top bar. `/design` shows all five product marks.
8. App icons come from `acadigma-brand/exports/products/campus/`: `app/icon.svg` (the Campus mark with the brand favicon's light/dark `<style>`), `app/favicon.ico` (from `favicon-32.png`), `app/apple-icon.png` and the PWA icons (resized from `campus-app-icon-light-1024.png`), `app/opengraph-image.png` (`campus-og-image-1200x630.png`, byte-identical to the website's copy). The manifest's background and theme colours become paper #f4f4f2.

**Why:** the owner picked it. Rings instead of shadows and a smaller radius make the calm working screens flatter and denser without changing a single colour, so every D-57 contrast measurement still holds. A static port keeps one source of geometry for all products without adding the website's animation library to a phone-first app.

**Consequences:** there is no product switcher in Campus yet; when one is built it uses `GridMark` with `PRODUCT_NAMES` rather than new artwork. Screens that use a `Card` lose the `border` class; one that needs a stronger edge uses `shadow-raised`. The eyebrow is available for the design passes on shipped screens (next in the design lane's queue, dashboard first).

## D-69 — Four parallel lanes with per-lane number ranges; only the lead merges · ACCEPTED · 2026-09-25

**Context:** Two builders working at once had already collided twice: both picked the same next decision number, and one PR's migration was written against a timestamp another PR's (already-merged) migration had since made stale — `supabase db push` refuses a migration dated before the newest one already applied. With more than one builder running concurrently, an unordered shared decision-number sequence and an unordered shared migration timestamp are both races with no lock.

**Decision (owner decision, 2026-09-25):** the build runs as four parallel lanes plus the lead, each with its own reserved decision-number range and pgTAP file-number range, so two lanes can never collide on those by construction. Migration timestamps are **not** lane-scoped — a lane digit fixed at write time would make a lane-1 migration always sort before a lane-3 one on the same day regardless of which PR actually merged first, forcing a re-date on almost every merge. Instead every migration is timestamped with the real UTC time it is written (`date -u +%Y%m%d%H%M%S`), which sorts correctly in the overwhelmingly common case on its own; `scripts/check-migrations-order.mjs` (D-70) is the mechanical backstop for the rare case where a lane's migration still falls behind another lane's between being written and being merged.

- **Identity and onboarding** (lane 1) — `(auth)`, `(onboarding)`, identity packages, F-ID-\* specs.
- **Operations** (lane 2) — `(school)/app/{staff,settings,...}`, F-OP-06/F-OP-07, F-ID-07 notifications.
- **Billing** (lane 3) — billing, plans/limits, access mode, F-CM-\* specs; `requireWritable` on every write path is first, due before mid-October.
- **Design** (lane 4) — tokens and theme in `packages/ui`, brand components, `DESIGN-SYSTEM.md`, icons/OG assets; D-68 visual refinement and product logos first.
- **Lead** (lane 0) — CI, releases, migration order, docs index, reviews, merges; process and infrastructure changes only.

The exact ranges, queue order and merge rules are recorded once, in `docs/plan/LANES.md`, not duplicated here — this entry is the decision to run lanes at all and why. Only the lead merges, one PR at a time, and confirms the live deploy and smoke test before merging the next PR when a merge carries a migration.

**Why:** the two collisions already happened, on real PRs, in the same week lanes were introduced — this is a fix for an observed failure mode, not a speculative one. A per-lane reserved range is the cheapest fix that removes the race by construction rather than by discipline: no lane has to check what another lane is doing before picking a number, because the ranges never overlap. `docs/plan/LANES.md` is the single place the ranges live so they are never redefined inconsistently in two documents.

**Consequences:** lanes still merge in whatever order they finish, so a lane's real-time migration timestamp can still fall behind another lane's between when it was written and when it merges — `scripts/check-migrations-order.mjs` (D-70) catches that and the lane renames the file with `git mv`. `docs/plan/BUILDER-BRIEF.md` is the brief every lane builder reads before starting a Part, and links to `LANES.md` for the ranges. A lane that needs a number outside its own range (a genuine cross-lane decision) takes it from the lead's range (D-69 to D-99) instead, recorded as a lead decision. The lead also now keeps `docs/plan/BUILD-LOG.md`, a dated running log of every merged PR (what shipped, decisions, migrations applied to production, deploy/smoke-test result, review findings), appended after each merge — with four lanes merging independently, this is the one place that stays a single, ordered account of what actually happened, rather than four lanes' worth of PR descriptions someone has to reassemble later.

## D-70 — Cut CI and Vercel usage with free options · ACCEPTED · 2026-09-25

**Context (owner request, 2026-09-25):** with four lanes running builders concurrently, CI and Vercel usage rises with them. The owner asked for CI and Vercel usage to be cut using free options only, no new paid tier.

**Decision:** three changes, all free-tier:

1. **`scripts/check-migrations-order.mjs`** (new): fails CI when any migration added in a PR (compared to the merge-base with `origin/main`) has a filename timestamp at or before the newest migration timestamp already on `main`, printing a `::error` naming the offending file and a suggested new name that sorts after it — the current UTC time, or one second past main's newest if that isn't later (D-69: migration timestamps are real UTC time, not lane-scoped, so there is no lane digit or sequence number to reason about here). Wired into `ci.yml`'s `db` job, next to `check-migrations-append-only.mjs`, same job so it shares that step's `fetch-depth: 0` checkout. This is the mechanical backstop for D-69's real-time migration timestamps — real time sorts correctly on its own in the common case, this catches the rare case where a lane's PR falls behind because another lane merged a later migration first.
2. **`ci.yml` job skipping via a new `changes` job, fail-closed by construction:** computes `db` (`true` only when `supabase/**` or `.github/**` — workflows, actions, dependabot, everything — changed: the narrow, expensive-job case) and `app` (`false` only when _every_ changed file matches a narrow docs/prose/dotfile allowlist: the broad, default-on case) from `git diff --no-renames --name-only <base>...HEAD` — no third-party action, `--no-renames` so a code file renamed into `docs/` still shows its old path and isn't misread as docs-only. On `push` to `main` and on `merge_group` both are forced true (a full run — `merge_group` is never partial). `db` runs unless its output is the literal string `'false'`; `lint`/`typecheck`/`unit`/`contracts`/`build`/`security` run unless _both_ outputs are `'false'`; `e2e`/`lighthouse` run on `pull_request` (not draft) and on `merge_group`, never on `push` to `main`, under the same not-`'false'` rule. Every one of those `if` conditions is prefixed with `!cancelled()`: GitHub silently ANDs an implicit `success()` across every job in `needs` unless the expression contains a status-check function, so without it a `db` job that is legitimately `'skipped'` would still take `e2e` down with it regardless of what the rest of the condition said (actions/runner#491) — `!cancelled()` removes that implicit gate so the written condition, not GitHub's default, decides. A skipped job reports as passing to GitHub, so required checks stay green; `CI / changes` is itself added to the required-checks list (CI.md §3) specifically so a broken `changes` job blocks the merge instead of silently costing everyone a full, unskippable run. `changeset`, `docs-sync`, `report` and `guard` are unchanged in intent; `report` (`if: always()`) still resolves correctly against a mix of skipped and run `needs`.
3. **Vercel builds only `main`:** `apps/web/vercel.json` gets `"ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]"` (exit 0 skips the build), so PR previews are dropped entirely. `CI / build` already proves the app builds on every PR; the Vercel PR check now shows as skipped/canceled and was never a required check.

**Rejected options, with reasons:**

- **A self-hosted runner on the owner's PC**, to move CI compute off GitHub's shared runners: rejected. The repo is public (D-64), so a self-hosted runner would let a fork PR's workflow run arbitrary code on the owner's own machine — GitHub's own documented warning against self-hosted runners on public repos. It would also need Docker, and Docker's default data root is on `C:`, which is already full (the same constraint every builder already works around with `TMP`/`TEMP`/`TMPDIR` on `F:`).
- **Local pgTAP**, to move the `db` job's Postgres service container off Actions minutes entirely: rejected for the same reason CI.md §6 already gives for not having per-PR Supabase branches — it needs Docker or the Supabase local stack, and the dev machine has no Docker (§6: "The dev machine also has no Docker, so there is no per-PR local database either").
- **Note, not a change:** GitHub Actions minutes are already free and unlimited on this public repo (D-64 item 1) — the real limit `changes`-job skipping addresses is the shared ~20 concurrent-job queue across four lanes' PRs running at once, not minutes. `cancel-in-progress` was already present in both `ci.yml` and `db.yml` before this PR (`true` on `pull_request`, `false` on `main`/pushed migrations) — not added by this decision, carried over unchanged.

**Why:** all three changes reduce actual usage (concurrent job count, Vercel build count) rather than working around a quota after the fact. Honestly: the skip logic does not weaken a merge gate _by design_, but that design is only as good as the `changes` job's own correctness — a bug in its diff logic that wrongly reports `app=false`/`db=false` would skip real gates and still show green. The mitigations are the fail-closed defaults themselves (§2: `app` defaults true, `db`'s skip requires a specific narrow pattern, and any unset/errored output reads as "run" downstream because every comparison is `!= 'false'`, never `== 'true'`) plus making `CI / changes` a required check (§3), so a `changes` job that errors outright is a red, blocking check rather than a silent full-permissive run. A docs-only PR skipping `e2e`/`lighthouse`/`build` is exactly what those jobs would have reported anyway (no app code changed to break), and the migration-order check is a net new gate, not a relaxed one.

**An honest gap this PR does not close:** `main` currently has **no branch protection at all** (verified via `gh api repos/Mahadezz/acadigma-campus/branches/main/protection`, 2026-09-25 → `404 Branch not protected`). CI.md §3's required-checks list, including the new `CI / changes` entry above, is therefore the _documented contract_ CI implements, not an _enforced_ one yet — nothing today stops a force-push or branch deletion, and nothing requires any of these checks to pass before a merge. Turning on branch protection (required status checks, no force-push, no deletion, the settings already itemised in CI.md §3) is an owner action, not something this PR does unilaterally to the shared repository's protection settings. Required-status-checks enforcement is deferred specifically until `release.yml`'s changesets "version packages" PR (D-64 item 3: Actions can open/approve PRs) itself has a passing `CI` run to point at — turning on enforcement before that PR-flow is proven green would risk locking the release process out of its own repository.

**Consequences:** this PR itself touches `.github/workflows/**`, so its own `changes` job reports `db=true` and every job runs — expected, not a bug. A future PR that touches only `docs/**` will show `lint`/`typecheck`/`unit`/`contracts`/`build`/`security`/`e2e`/`lighthouse` as skipped and still merge, since GitHub treats a skipped required check as passing. Vercel PR previews no longer exist; a lane builder makes screenshots locally with Playwright instead (`docs/plan/LANES.md`, `docs/plan/BUILDER-BRIEF.md`). `docs/engineering/CI.md` §2.3, §3 and its generated-types section are updated in this PR to document the skip rules, the migration-order check and the re-date procedure.

## D-300 — Read-only mode refuses every tenant write, in the server action and in the database · ACCEPTED · 2026-09-25

**Context:** D-29 put read-only mode in `workspaces.access_mode`, checked by server actions (`PLAN_READ_ONLY`), and DATA-MODEL §1.2 kept it out of RLS. D-62 made trial expiry set it. But no server action called `requireWritable`, a member's direct PostgREST write skipped the check entirely, and D-62/the shell banner described the mode as "only creates are blocked" while D-29 and DATA-MODEL say "any write". The lead's lane brief (LANES, billing queue item 1) asks for enforcement on every write path before mid-October.

**Decision:** (1) `read_only` refuses every tenant write — create, update and delete. Reads, exports, billing/upgrade, sign-out, workspace switching and the individual's own records stay available. (2) Every tenant write server action calls `requireWritable` after the policy check (parse → context → policy → **writable** → repo) and returns `err(planReadOnlyApiError(...))`: `payment_required` (402), the reason plus "Upgrade to make changes — you can still view and export everything, and nothing has been deleted." The shell banner shows the same sentence. (3) Defence in depth is one shared `security definer` BEFORE INSERT/UPDATE/DELETE row trigger, `app.tg_require_writable()`, on every tenant table — not an RLS predicate. It exempts `app.is_privileged_context()` and `app.is_platform_admin()`, so the billing tick and `app.set_access_mode(..., 'normal')` always work. (4) CI: `scripts/check-require-writable.mjs` (contracts job) fails a write server action or `POST/PUT/PATCH/DELETE` route that resolves a workspace context without calling `requireWritable`; a name starting with a read verb counts as a read. `supabase/tests/50_require_writable.sql` fails any `workspace_id` table without the trigger that is not on its exempt list.

**Why:** A trigger touches only writes, so DATA-MODEL's objections to RLS (per-row cost on every read, a billing bug hiding in a hundred policies) do not apply. It also covers SECURITY DEFINER RPCs, because `is_privileged_context()` reads the caller's `role` GUC at any nesting depth. A catalog invariant and a source scan are the two places a future table or action cannot quietly skip. Reusing `payment_required` avoids widening the shared `ApiError` enum that every lane's actions return.

**Consequences:** D-62's "allow everything else" and the banner's "edits and deletes still work" are superseded (F-CM-06 §11 updated). Every lane's new write action and every new tenant table must add the guard (CLAUDE.md rule). Exempt tables and their reasons are listed in DATA-MODEL §1.2 and `50_require_writable.sql`. **Review follow-ups (PR #35):** removing access stays possible in read_only — a membership set to `removed`, a capability deleted/revoked, an invitation revoked or declined — while their siblings (role change, grant, invitation edit) are refused. The trigger raises only for an active member (and, generically, for a non-member's membership INSERT), so a stranger never learns the workspace's `access_mode_reason`. Triggers are attached through `app.attach_require_writable()`; the three F-OP-06 staff tables are guarded. The action check is file-level and fail-closed: an action guarded through a same-file helper passes, one that skips it fails.

**Owner's answers (2026-09-25):** leaving a read-only school — **allowed**; the leave RPC does not exist yet, and when it lands the trigger gains its exception (self status → `removed`). Accepting an invitation to a read-only school — **refused**, "ask the owner to upgrade". §5.6 delete-to-get-under-a-limit — **not** for read_only; that is the `LIMIT_EXCEEDED` path only.

## D-200 — F-OP-07 Part 1 ships without logo upload or the `school_public_settings` view; Part 2 waits for the academics tables · ACCEPTED · 2026-09-25

**Context:** F-OP-07 Part 1's remainder (after M0 landed `resolve()`, the defaults and the policy-blob read/patch) lists the settings shell, the profile form, logo upload with server-side variants, a live PDF header preview "rendered by the real F-OP-03 renderer", and a `school_public_settings` view through which teachers read. Part 2 (academic years, terms, exam weighting) was assigned in the same Part. Four of those depend on things that do not exist on `main`: there is no file-upload pipeline (no storage bucket wiring, no `/api/files/[id]`, no image processing), there is no F-OP-03 renderer, and there are no `academic_years`, `terms` or exam tables (DATA-MODEL §2 names them; no migration creates them, and F-ID-05 Part 4 in the identity lane is about to create the first academic year).

**Decision:**

1. This PR (#39) is **Part 1 remainder only**: settings home (grouped rows + search), the read-only "How this school works" page, the school profile form (typed `school_profiles` columns), and branding with a live header preview. No migration.
2. **Logo upload is deferred** until a files pipeline exists (CLAUDE.md rule 10 makes it a security surface of its own). `updateBrandingInputSchema` omits `logo_file_id`; the preview shows the school's initials.
3. **No `school_public_settings` view.** DATA-MODEL §1.3 (binding over the spec) widens `school_profiles` SELECT to every active member, and no owner-only column lives on this table (module visibility is `workspaces.hidden_modules`). A view would add a second read path with nothing to hide. Revisit if a column that teachers must not see is ever added.
4. **The header preview is HTML**, built from a new pure `renderHeaderLine()` / `unknownHeaderTokens()` (`packages/domain/settings/header.ts`), which F-OP-03 should reuse so that what the preview shows is what prints. Unknown `{tokens}` are flagged live and refused by `updateBranding`.
5. **Optimistic concurrency uses `updated_at` as the version** and returns the existing `conflict` error code (the envelope has no `stale_version` code). The UI offers a reload instead of the current value inline.
6. **Part 2 is not started.** It needs `academic_years`/`terms` (academics area, and F-ID-05 Part 4's `create_school_workspace`) and an exams table for weighting.

**Why:** each deferred piece would otherwise mean building another lane's or another feature's foundation inside a settings PR. The screens that shipped are complete on their own and use only tables that exist.

**Consequences:** F-OP-07 §11 records the deviations. F-ID-03 Part 8 (`/app/settings/workspace`, identity lane) overlaps with this profile form (name, EIIN, address, contacts): the lead should decide which screen owns those fields before either is extended. The new actions live in `profile-actions.ts`, not `actions.ts`, so the billing lane's `requireWritable` edits to the M0 actions do not conflict.

## D-201 — `/app/settings/school` owns the school profile fields; F-ID-03 Part 8 is narrowed to workspace lifecycle · ACCEPTED · 2026-09-25

**Context:** F-OP-07 Part 1 (PR #39) built `/app/settings/school`, which edits `school_profiles`' identity, address, contact and BIN/VAT columns. F-ID-03 Part 8 (identity lane) specified `/app/settings/workspace` with the same fields plus logo upload, ownership transfer, module visibility and the suspended/archived states. Two screens editing the same columns would give two sources of truth for one record. The lead decided in review of PR #39.

**Decision:** `/app/settings/school` (F-OP-07) owns the school profile fields. F-ID-03 Part 8's `/app/settings/workspace` is narrowed to lifecycle: ownership transfer, module visibility, and suspended/archived. It links to `/app/settings/school` for profile edits. Logo upload lands as `branding.logo_file_id` through `updateBranding` once a files pipeline exists (D-200).

**Why:** one screen per record keeps validation, optimistic concurrency and audit history in one place.

**Consequences:** F-ID-03 §8 Part 8 carries a one-line pointer to this entry. When the identity lane builds Part 8, it must not add profile inputs.

## D-100 — The create-school transaction: one `public` SECURITY DEFINER function on top of the existing workspace triggers; four wizard steps (logo deferred); grade levels keep DATA-MODEL's columns plus `name_bn` · ACCEPTED · 2026-09-25

**Context:** F-ID-05 Part 4 builds wizard steps 3-5 and `app.create_school_workspace` (spec §4.3 "On submit", §7, AC7-AC9, AC16). Building it surfaced five places where the spec, DATA-MODEL.md and the code that already exists disagree or where the spec promises something no other part of the system can deliver yet.

**Decision:**

1. **`public.create_school_workspace(p_input jsonb)`, not `app.create_school_workspace`.** Only `public` is reachable through PostgREST (D-50); `public.switch_workspace` is the precedent for a client-callable SECURITY DEFINER function living there directly. It inserts the `workspaces` row and lets the triggers that already exist do what they already do — owner membership and the empty `school_profiles` row (`tg_workspace_bootstrap`), the join code (`tg_workspace_defaults`, `workspaces.invite_code`), the Pro plan and trial (`tg_workspace_billing_defaults`), the trialing subscription and `trial_started` event (`tg_workspace_billing_bootstrap`, D-59) — then writes the school profile fields, the current academic year, the grade levels, `profiles.onboarding_completed_at`, the finished `onboarding_progress` row and an `app.idempotency_keys` record, all in one transaction. When no `x-correlation-id` header set one, it mints a correlation id first, so every audit row shares it (AC7). Errors are raised by name in the exception message, as `switch_workspace` does.
2. **Audit stays generic (D-58).** No curated `workspace.created` / `grade_levels.seeded` rows: the generic triggers already write `workspaces.insert`, `workspace_members.insert`, `school_profiles.update`, `subscriptions.insert`, `academic_years.insert` and `grade_levels.insert`, grouped by the correlation id. The migration seeds the two new tables' generic actions into `audit_action_catalog` (the D-63 pattern: insert info, update notable, delete critical) and `GENERIC_AUDIT_TABLES` lists them.
3. **Checks live where they cannot drift, and this function is the only door.** The migration drops the `workspaces_insert` policy and revokes INSERT on `workspaces` from `authenticated` (PR #37 review, HIGH: before that, a direct PostgREST insert of a `type='school'` row skipped every limit below). Personal workspaces are unaffected: `app.handle_new_user()` is SECURITY DEFINER and no app code inserts workspaces. The function then runs in a fixed order: (a) auth, key, a per-user advisory lock and the idempotency replay (`app.idempotency_keys`, scope `create_school_workspace`, SHA-256 of the body without the key; same key + same body replays, anything else is `IDEMPOTENCY_KEY_REUSED`); (b) every input check, including the academic year and each grade level, so a malformed request never reaches the EIIN index; (c) the attempt is counted in a `createSchool` throttle bucket (30 per 15 minutes per user); (d) the 3-schools-per-day count (real `workspaces` rows, backed by `workspaces_created_by_type_created_idx`) and the 20-membership cap; (e) the writes. Failures from (c) on — `EIIN_TAKEN`, `RATE_LIMITED`, `WORKSPACE_LIMIT_REACHED` — are returned as `{"error": CODE}`, not raised, because raising would roll back the throttle count and make failed attempts free; the writes sit in a sub-block whose failure undoes them but keeps the count. That closes the EIIN existence oracle the review found (D-67 throttled the probe, but a failed create cost nothing). `EIIN_TAKEN` still comes only from the D-66 unique index, so a submission that loses a race takes the same path. For the same reason a client can no longer change `school_profiles.eiin` by UPDATE (`app.tg_school_profiles_eiin_guard`; only the transaction that created the row may set it) until Settings gets a throttled "change EIIN" function. The academic-year rule is also a table CHECK. `created_by` on both new tables is immutable (`app.tg_created_by_immutable`), and both carry `app.attach_require_writable` (D-300).
4. **Grade levels: DATA-MODEL's columns, one addition, one rename.** `grade_levels(name, name_bn, level_number, stage)` — DATA-MODEL.md §2's `name`/`level_number`/unique `(workspace_id, level_number)`, plus `name_bn` (AC9 needs Bangla names) and a unique `(workspace_id, lower(name))`. DATA-MODEL's `group` column is named `stage` (`public.grade_stage`: `early|primary|secondary|higher`) because `GROUP` is a reserved word; it is null for a custom level. The spec's `is_active` is not added (nothing reads it yet). Level numbers: Play −2 … KG 0, Class n = n, O-Level 13, A-Level 14, custom 100+ (`packages/domain/src/academic/gradeLevels.ts`).
5. **The wizard has four steps; the logo step waits.** Step 4 (logo) needs file storage (`files`, a private bucket, `/api/files/[id]`) and a Settings → Workspace screen for the "add this later" promise to be true (§10 content rule); neither exists. Showing a skip-only step would be a dead end in a sales demo, so the wizard is Identity → Where and when → Classes → Review. `logo_url` is not in the input. The review step reads the trial length from `plans.trial_days` (30 today, PRODUCT-DECISIONS §5.2) instead of the spec's "14-day".
6. **Not built here:** a `short_code` (DATA-MODEL has no such column; the join code is `workspaces.invite_code`), the first-run checklist on `/app` (Part 5), madrasah presets (OQ-2's assumed default was "Ebtedayee/Dakhil/Alim presets when the board is madrasah"; this Part replaces it with custom levels only, pending the owner's naming), DPA acceptance (ROADMAP M1 1.11, a separate lane item; the spec does not put it in the wizard).

7. **Known, not fixed here (queued for the lead):** `public.throttle_record_failure` is callable by `anon`/`authenticated` with any key, the pre-existing pattern for every bucket. Anyone who knows a user's id can therefore push `create-school:<id>` past 30 and lock that user out of school creation for 15 minutes. The fix belongs to all buckets at once: record throttle attempts server-side only (keys derived inside SECURITY DEFINER functions, no client-supplied key).
8. **Settings no longer edits the EIIN** (F-OP-07, #39): the field is read-only there, `schoolProfilePatchSchema` omits it, and `app.tg_school_profiles_eiin_guard` refuses a client UPDATE. That path was the same unthrottled EIIN oracle. A change goes through support until a throttled "change EIIN" function exists.
9. **Only the EIIN index maps to `EIIN_TAKEN`.** The write block reads the violated constraint's name; any other unique violation returns `CONFLICT` (shown as "try again").

**Why:** reusing the triggers means a school created by the wizard and a school created any other way are the same shape by construction, and the function stays short enough to review. Putting the rate limit, uniqueness and date rules in the database, and closing the direct INSERT, means no client path can skip them.

**Consequences:** `supabase/tests/30_create_school_workspace.sql` proves the happy path, replay, the named errors, all-or-nothing under a failure injected at each of nine writes, and T2 isolation/escalation for both new tables. `12_function_grants_invariant.sql` allows `authenticated` to execute the new function. Adding a logo step later means: file uploads first, then a step between Classes and Review, then `logo_url` in the input.

## D-301 — Joining a read-only school is refused by the join functions; seed passwords are hashed at seed time · ACCEPTED · 2026-09-25

**Context:** PR #35's security re-check (low): D-300's trigger refused a non-member's `workspace_members` INSERT with `PLAN_READ_ONLY` on a read-only school and RLS refused it otherwise, so anyone holding a workspace uuid could learn its access mode. Separately, Semgrep flagged the bcrypt literal of the fourth seed account (`supabase/seed/seed.sql`); the other three were suppressed with `nosemgrep`.

**Decision:** (1) `app.accept_invitation()` and `app.join_workspace_by_code()` check `access_mode` themselves, after the token/email binding or the invite code is verified, and refuse with `PLAN_READ_ONLY` / "This workspace is read-only. Ask the owner to upgrade." (the owner's D-300 ruling). `app.tg_require_writable()` drops its non-member branch, so a stranger's write only ever meets RLS. (2) The seed hashes `password123` with `extensions.crypt(..., gen_salt('bf'))` at seed time for all four accounts; no hash literal and no `nosemgrep` remain. (3) This migration is named `20260925300201_…` per the lead's rule for 2026-09-25 (billing prefix `202609253002NN`): a real-UTC-time name would sort before `20260925300100` (D-300) and `supabase db push` refuses an older-dated migration. Real UTC time applies from 2026-09-26.

**Why:** Only someone who has already proven they were invited learns the mode. Hashing at seed time is one function call and removes the alert class instead of suppressing it.

**Consequences:** `supabase/tests/51_readonly_join_and_seed.sql` applies the seed inside pgTAP (`\ir ../seed/seed.sql`), so CI now proves the seed runs on the migrated schema — it did not before. Its first run found the seed broken on main since F-OP-06 (#32): the school bootstrap now creates the default labels, and the seed's own `Principal`/`Vice-Principal`/`Senior Teacher` inserts collided on `custom_labels_workspace_name_key`. The seed now picks those labels by name instead of inserting them.

## D-401 — বাংলা covers the signed-in app shell, not only the auth screens; one locale resolver, not two; digits stay Western everywhere · ACCEPTED · 2026-09-25

**Context:** F-ID-02 §4.3/Part 4 ("Bangla localisation and the language switch") is a full feature (`user_preferences` table, theme/palette/density alongside language, a Settings → Language row) that has not been built — no migration for it exists on `main`. What does exist is the demo-cut mechanism F-ID-01 added for the auth screens only (`apps/web/lib/i18n.ts` / `lib/locale.ts`, cookie `acadigma_locale`, plain `messages/{en,bn}.json`, no `next-intl` runtime yet — a scope note already recorded there). A demo walkthrough that signs in with বাংলা selected and then lands on an all-English school shell contradicts the product's core pitch to BD schools. This Part is the demo slice of F-ID-02 Part 4: make the existing cookie mechanism reach the whole signed-in app, not build the preferences table.

**Decision:**

1. **No migration, no `user_preferences` table.** The `acadigma_locale` cookie remains the only source of truth for this Part; `getLocale()`/`getMessages()` (`lib/i18n.ts`) are unchanged. `apps/web/app/layout.tsx` now calls `getLocale()` and sets `<html lang>` from it (previously hardcoded `"en"`), which is also what makes the `:lang(bn)` CSS (D-57 Bengali font, D-68 zero letter-spacing) apply for the whole document instead of only inside components that separately set `lang="bn"`.
2. **The school shell's nav was silently English-only.** `SchoolBottomNav`/`SchoolSidebar` (`(school)/app/nav.tsx`) never received the `locale` prop `BottomNavFromConfig`/`SidebarFromConfig` (`packages/ui`) already support — a pre-existing gap, not something this Part's nav config changed. Both now take and thread `locale` from `getMessages()`.
3. **A `UserMenu` (`(shared)/workspace/user-menu.tsx`)** — a `DropdownMenu` + `RadioGroup`, both existing `packages/ui` shadcn primitives, no new component — puts the language switch in every signed-in shell's top bar (school, personal, family via `GatedShell`), not only on `/login`. It shares `setLocaleCookie` (`lib/locale.ts`) with the signed-out `LanguageToggle`, which was refactored to call the same function instead of inlining `document.cookie`.
4. **Digits stay Western (0-9) in বাংলা, including dates** (DESIGN-SYSTEM §1.6; corrects this Part's original brief, which said Bengali digits). `toIntlLocale(locale)` (`lib/locale.ts`) returns `"bn-BD-u-nu-latn"` for `bn` — the `-u-nu-latn` Unicode extension pins Latin digits independently of the locale's own default numbering system — and `"en-GB"` for `en`. One existing call site formatted dates with the bare `"bn-BD"` tag (`(school)/app/settings/settings-history.tsx`), which renders Bengali digits by default; it now goes through `toIntlLocale`. A vitest guard (`lib/bn-locale-guard.test.ts`) scans `apps/web` and `packages` for the literal `"bn-BD"` string outside `MoneyText`/`BnEnText` (the two components allowed to choose the digit script on purpose, via their own `numerals="bn"` prop for printed report cards) so this cannot regress silently.
5. **404/error boundaries translated.** `not-found.tsx`/`forbidden.tsx` (server components) call `getMessages()` directly. `error.tsx` must stay a Client Component (Next requirement), so it cannot call the server-only `getMessages()`; it reads the locale cookie itself (`getClientLocale()`, computed fresh on every render, not mirrored into state — avoids a `setState`-in-effect + hydration-mismatch tradeoff for a boundary with no "no flash" requirement) and imports both message JSON files directly.
6. **Missing-key parity is a vitest test, not a new script.** `docs/engineering/I18N.md` §3 describes a `check-i18n-keys.ts` CI script for the full `next-intl` migration; this Part adds `lib/i18n-parity.test.ts` instead (flattens both catalogues to dotted key paths, asserts equal sets) — same enforcement on every `pnpm test`, no new script to wire into CI.
7. **`/app/dashboard` is translated as the placeholder it still is.** D-400 (PR #41, in review concurrently) replaces this page's developer debug card with the real "today" dashboard; this Part only translates what is currently on `main` (a `dashboard` message namespace, `formatIsoDate` + `toIntlLocale` for the date) so it does not ship English-only in a বাংলা session in the meantime. Whichever PR merges second rebases the other's `dashboard`/nav changes.
8. **Onboarding wizard and F-OP-07 settings screens were already correct** — both already call `getMessages()`/`t` throughout; this Part only verified it (no hardcoded strings found) rather than changing them.
9. **`getLocale()` is now the one locale resolver for the whole app, not two disconnected ones.** `profiles.locale` (a `text` column with `en`/`bn` check constraint) already exists on `main` from an earlier migration and was already read directly by the audit viewer's `getReaderLanguage()` (`(school)/app/audit/actions.ts`) — a second mechanism that ignored the `acadigma_locale` cookie entirely, so switching language from this Part's new `UserMenu` left the audit page on whatever `profiles.locale` last happened to be. `getLocale()` now resolves cookie first, then `profiles.locale` (only queried when there is no cookie — a signed-out visitor never pays for it, and `getUser()`/`requireShell()` calls elsewhere in the same request are deduped by Next's per-request `fetch` memoisation), then `en`; `getReaderLanguage()` is rewritten to delegate to it, keeping its own `requireWorkspace()` gate. `UserMenu`'s switch now calls a new `updateLocale` server action (`(shared)/workspace/actions.ts`) that writes `profiles.locale` in addition to the cookie `setLocaleCookie` already wrote — best-effort, not blocking the UI — so the choice follows a signed-in user to a browser that has no cookie yet, proven manually (§ test report) by switching, clearing the local cookie, and reloading. This is still not `user_preferences` (point 1): `profiles.locale` is a narrower, pre-existing column, not the richer preference row F-ID-02's full Part 4 specs.
10. **`getLocale()` is memoised per request with React's `cache()`** (review follow-up on PR #51). Root layout, `GatedShell`, the `(personal)`/`(family)` layouts, `SchoolLayout` and `getReaderLanguage()` can all call it in one request; without memoisation, every call with no cookie was its own `auth.getUser()` plus a `profiles` query. React's real `cache()` only memoises inside an actual Server Component render — verified directly that a `cache()`-wrapped function called twice from plain Node runs twice, not once — so `lib/i18n.test.ts` mocks `"react"`'s `cache` with a small Map-based stand-in, reset every test, to prove this codebase's usage is correct without needing a full RSC render harness.
11. **A "Sign out" item, reusing the existing `signOut` server action.** The signed-in shell had no sign-out control anywhere (review follow-up on PR #51, confirmed on `main`). `UserMenu` adds a destructive-styled `DropdownMenuItem` (existing shadcn variant, separated from the language `RadioGroup` by a `DropdownMenuSeparator`) that calls `signOut()` (`(auth)/actions.ts` §4.10 — single-device scope, clears the workspace cookie, writes `account.logout` to the audit log, redirects to `/login`) — no new server action, this is simply the first UI wired to the existing one. Verified manually end to end (sign out, then re-request `/app/dashboard` — redirects to `/login?next=…`, proving the session is actually gone, not just a client-side navigation) and with a `user-menu.test.tsx` unit test; Radix's `DropdownMenuTrigger` opens on `pointerdown`, not `click`, so the test fires `pointerdown`/`pointerup`/`click` rather than a bare `.click()` (which left the menu `data-state="closed"` in jsdom on the first attempt).

**Why:** the alternative — building `user_preferences` and the full F-ID-02 Part 4 UI to fix a shell that ignores the locale it already has — is a much larger, cross-cutting change for a demo gap that the existing cookie mechanism already closes once it is threaded through the two places (`<html lang>`, the nav's `locale` prop) that never read it.

**Consequences:** F-ID-02 §11 gets a note that this demo slice shipped ahead of the full Part 4 (theme/palette/density/`user_preferences` still open). The real F-ID-02 Part 4 build later replaces the cookie-only mechanism with the DB-backed preference and must keep `UserMenu`'s language item working the same way. PR #49 (D-402, readable audit sentences) had already merged with its own report against the old `getReaderLanguage()` by the time this PR rebased onto `main`; its viewer components call the same function signature (unchanged) and get the fixed precedence automatically — no follow-up edit needed there. This PR's top-bar subtitle also picked up D-400/PR #41's `shell.signedInAs`/`shell.roles` (a translated role label, e.g. "মালিক" for owner) on rebase, in place of this PR's own first draft, which left the role name untranslated — the merge kept #41's version. `docs/test-reports/2026-09-25-design-bn-shell.md` lists the Bengali strings added in this Part for the owner to spot-check, and flags that this journey's e2e spec mutates the shared seeded account's `profiles.locale` (mitigated with an `afterEach` reset, not fully race-proof against another parallel spec — recorded as a follow-up, not fixed here: a dedicated locale-test seed account removes the race entirely). Sign-out is deliberately unit-tested only, not added to that same e2e spec, so it does not widen that race window with a second shared-state mutation.

## D-202 — F-AC-11 Part 1 ships as a demo cut: holidays + overrides + `app.is_school_day` (SECURITY INVOKER), no scopes or academic year yet · ACCEPTED · 2026-09-25

**Context:** F-AC-11 Part 1 specifies `holidays` (with `academic_year_id`), `holiday_scopes` (grade/section), `working_day_overrides`, `app.is_school_day(workspace, date, section?)` as SECURITY DEFINER, a TS mirror with a SQL↔TS parity test, and `GET /api/calendar/school-days`. When this Part started, `main` had no `academic_years` table (PR #37 merged while this PR was open), and there is still no `sections` table for scopes to reference. The lead asked for the demo cut the rest of M2 depends on: the tables, `is_school_day`, and a holidays screen under settings.

**Decision:**

1. `20260925300301_school_calendar.sql` adds `holidays` and `working_day_overrides` with RLS (staff read, owner/admin write, parents none), freeze, audit and `attach_require_writable`, plus `app.is_school_day`, `app.school_days` and `app.school_day_count`.
2. **SECURITY INVOKER, not DEFINER.** _SUPERSEDED by D-203._ The functions read `school_profiles`, `holidays` and `working_day_overrides`, all of which every staff member may already read. The caller's RLS is therefore the right filter, and a non-member learns only the defaults. A definer function would let any signed-in user probe any school's calendar by id.
3. **Deferred:** `holidays.academic_year_id` (a follow-up now that #37 has merged, together with the "inside its year" check); `holiday_scopes` and the `section_id` argument (they need `sections`; adding a defaulted argument later keeps every caller compatible); recurrence (materialised per year, Part 2); the TS mirror + parity test and the school-days route (no TS consumer yet); an override screen.
4. The holidays screen is `/app/settings/calendar`, in line with D-201's "settings owns school configuration". `/app/calendar` views remain Part 3.

**Consequences:** attendance, leave and analytics can call `app.school_day_count(workspace_id, from, to)` today. When scopes land, `is_school_day` gains `p_section_id uuid default null` in a new migration.

## D-203 — The school-day functions are SECURITY DEFINER behind a membership guard (supersedes D-202 point 2) · ACCEPTED · 2026-09-25

**Context:** D-202 made `app.is_school_day`, `app.school_days` and `app.school_day_count` SECURITY INVOKER so that the caller's RLS would filter them. The lead's review of PR #43 found this gives parents the wrong answer. Parents can read `school_profiles` but have no policy on `holidays` or `working_day_overrides`, so a declared holiday came back as a school day for them. Parent leave counts and F-AC-10's attendance % will call these functions.

**Decision:**

1. The three functions are **SECURITY DEFINER** (as the spec says), with `search_path = ''`. Each first calls `app.can_read_school_calendar(workspace_id)`, which is true for an active member of that workspace in any role (parents included), for platform staff, and for a privileged (service) context.
2. Anyone else gets `NULL` from `is_school_day`, and `42501 FORBIDDEN` from `school_days`/`school_day_count`, so a signed-in user cannot probe another school's calendar by id.
3. Grants are unchanged: `authenticated` and `service_role` only.
4. Same review: `created_by` is fixed on both tables by `app.tg_created_by_immutable()` (#37). `holidays` gains `unique (workspace_id, name, starts_on)`, so the same holiday cannot be entered twice. The spec's `HOLIDAY_OVERLAP` warning for differently named overlapping holidays stays deferred; overlapping holidays still close each date once. `school_days` accepts at most 731 dates.

**Why:** the denominator must be the same for every member. Restricting who may ask is the boundary, not what each role may read.

**Consequences:** pgTAP `41_school_calendar.sql` asserts that a parent gets postgres's answer for a holiday, an override and a month count, and that a member of another school gets NULL or FORBIDDEN.

## D-400 — The school dashboard ships now, built only from data that exists, with empty slots for what does not · ACCEPTED · 2026-09-25

**Context:** `/app/dashboard` was a developer placeholder ("Workspace resolved … Role owner, workspace <uuid>"). The owner approved a demo cut: replace it with a real owner/admin "today" dashboard before timetable, attendance and marks exist. F-TE-07's analytics views, and DESIGN-SYSTEM §8.2's teacher wireframe (the "NOW" period card), both depend on tables that are not built yet.

**Decision:**

1. Show only what the database already holds: the workspace name and F-OP-07 letterhead, plan code and `trial_ends_at` (days left counted on the school's calendar, `trialDaysLeft`), `access_mode` (a Read-only chip), active members by role, the staff-directory count, the current academic year and grade levels (both created by the F-ID-05 wizard, D-100), and — for roles with `audit.read` — the last five audit events that have a curated sentence, rendered with the existing `renderAuditSentence`. Generic `<table>.<op>` rows name raw tables ("a profiles record"), so they stay on `/app/audit` only (`isCuratedAuditAction`).
2. Counts are `head: true` count queries under the caller's RLS (`getDashboardSummary`, `packages/db`). No member or staff row reaches the page; a parent never reaches the shell. A settings read that fails for any reason other than a missing profile row is an error state, not a silent "no letterhead" default.
3. Attendance and exam results are real `EmptyState` cards with copy that says what fills them. No sample number appears anywhere on the page (PRODUCT-DECISIONS 3.9, F-TE-07's anti-mock rule).
4. A setup checklist (letterhead, academic year, teachers, staff records, students) comes from `buildSetupChecklist` (`packages/domain/dashboard`). A step links only to a page that exists (`/app/settings/branding` for the letterhead today; the others link when their pages ship). "Academic year and classes" is done when the school has a current academic year and at least one grade level — one step, because the wizard does both on one screen. Students stay "to do" until the student table exists. The card is hidden once every step is done.
5. Owners and admins get the full view; teachers and office staff get the lighter one (date, school, Today slots, Members).
6. No migration and no new permission: every read is already allowed by existing RLS.
7. The school shell shows only nav items whose page exists (`apps/web/lib/implemented-routes.ts`, unit-tested against the `page.tsx` files in both directions). The nav configs stay complete; a Part adds its route to the list when it ships its page. Before this, every signed-in page prefetched up to 14 routes that returned 404. The owner's "School settings" item now points to `/app/settings` (D-201).

**Why:** a demo that shows invented numbers would contradict PRODUCT-DECISIONS 3.9 the first time a school compares it with its register. A page built from real counts and honest empty states is useful today and gets richer as each Part ships, without rework.

**Consequences:** F-TE-07 Part 1's `analytics.overview` replaces the Members/Today cards' data source when it ships. F-AC-03 fills the attendance slot, F-AC-05 adds the "NOW" card, and the exams Part fills the results slot. When the student table lands, `page.tsx` passes a real count to `buildSetupChecklist` in place of the constant. The dashboard has its own `loading.tsx` and `error.tsx`, so a slow or failed count keeps the shell on screen.

## D-302 — Grade scales: DATA-MODEL's shape, rules stay in settings, coverage checked at commit · ACCEPTED · 2026-09-25

**Context:** F-AC-06 Part 1 builds grade scales. The spec's §3 draft and DATA-MODEL §2 disagree: `grade_scale_bands (min_pct, max_pct, colour)` plus seven rule columns on the scale (pass mark, F-zeroes-GPA, 4th-subject rule and threshold, decimals, effective date, active flag) versus DATA-MODEL's `grade_scales (code, name, is_default)` / `grade_bands (letter, min_percent, max_percent, grade_point, sort_order)`. Those rules already exist in `school_profiles.academic_settings` (`pass_mark_percent`, `fail_any_subject_zero_gpa`, `grade_scale_code = BD_GPA5`) and `academic_years.fourth_subject_bonus_threshold_gp`.

**Decision:** (1) DATA-MODEL's tables and names, plus `grade_bands.is_fail` (§5.3 needs it; a fail band is not always point 0 on a custom scale). (2) The rule columns stay where they already live; they move onto the scale only when Part 2's exams need to snapshot them. (3) Coverage (0.00-100.00, no gap, no overlap, 0.01 steps) is a DEFERRED constraint trigger, so a whole band set is replaced in one transaction by `public.save_grade_scale`, which then sets the constraint IMMEDIATE to surface `BAND_GAP`/`BAND_OVERLAP` by name. Not a gist exclusion constraint — that would need `btree_gist` and still not catch gaps. (4) Both write RPCs are SECURITY INVOKER, so RLS and `require_writable` decide, not the function. (5) One parity table: the pgTAP rows between `-- parity:` markers in `52_grade_scales.sql` are read verbatim by the domain's vitest, so SQL `app.band_for`/`app.round_half_up` and TS `bandFor`/`roundHalfUp` are asserted against the same cases.

**Why:** Two homes for the pass mark would drift. Replacing bands one by one would pass through invalid states.

**Consequences:** Part 2 decides the snapshot shape (copy rules onto the scale, or snapshot `academic_settings` on the exam). Only the default scale is editable in Part 1; `SCALE_IN_USE` versioning arrives with exams.

**Lead decisions on review of PR #46 (2026-09-25; owner confirmation pending on (a)):**

- **(a) Rounding.** F-AC-06 §5.1 wins: `subject_pct = round(100 × obtained / full, 2)`, then banded as-is with `min_percent <= pct <= max_percent` on the .99 upper edges (79.5 → A, 32.5 → F). There is no rounding before banding: `app.band_for` and `bandFor` compare the value they are given. F-OP-07 §5.2's "percent comparisons use integers" and its OQ-4 round-half-up default are superseded and now point here.
- **(b) Route and permission.** F-OP-07's names are used: `/app/settings/grade-scale` and `settings.grade_scale.write`. From F-OP-07 Part 4's extra rules, "grade points must not decrease band by band" is enforced now, in the database (`BAND_POINTS_DECREASE`) and in Zod. Scale versioning and "the pass boundary equals `pass_mark_percent`" are deferred to F-AC-06 Part 2, where exams snapshot the scale and the pass mark.
- **Review fixes:**
  - An empty band set is `BAND_GAP`.
  - Bands change only through the two RPCs: `authenticated` has only SELECT on `grade_bands`, and the RPCs are SECURITY DEFINER and re-check owner/admin.
  - The coverage check locks the scale row.
  - `seed_bd_grade_scale` uses `insert ... on conflict do nothing`.
  - `save_grade_scale` takes the workspace id and filters on it.
  - `grade_scales.code` and `is_default` are immutable to clients.
  - Band letters are unique case-insensitively.
  - Band audit rows are info-level.

## D-402 — The audit trail reads as sentences: per-table nouns, no empty placeholders, no raw codes in the list · ACCEPTED · 2026-09-25

**Context:** `/app/audit` (F-ID-09) rendered generic trigger rows as "Demo Owner updated a profiles record ()": the sentence template used the table name and a `({fields})` placeholder no caller ever filled. Unknown actions showed their raw code ("performed an unrecognised action (mystery.happened)"), and the desktop table's "Action" column printed the action code, although F-ID-09 §4.1 says the list shows sentences, not action strings. The owner saw this on the demo school; the dashboard (D-400) had to hide these rows.

**Decision:**

1. Generic sentences come from a per-table noun (`GENERIC_TABLE_NOUNS` in `packages/domain/src/audit/catalog.ts`, English and Bengali): "{actor} added a holiday", "{actor} updated a school setting", "{actor} removed a member". A table without a noun falls back to "a record", and a unit test fails until the noun is added, so a newly audited table cannot ship a raw name.
2. `renderAuditSentence` never leaves a gap: a parenthetical whose value is missing is dropped, a missing subject or workspace gets a plain stand-in ("a member", "the school"), and spaces are collapsed. An unknown action reads "{actor} made a change" / "{actor} একটি পরিবর্তন করেছেন".
3. The list, the desktop "Action" column, the detail sheet title and the correlation list all render the same sentence through `BnEnText`, so an English actor name inside a Bengali sentence gets the right font and line height. Digits stay Western.
4. The detail sheet leads with the readable noun and keeps the raw table and row id as a muted technical reference, as F-ID-09 §4.1 intends. Changed-field names show with spaces instead of underscores.
5. The SQL `audit_action_catalog` rows keep their original wording. No code reads those sentence columns, and the parity check covers action names and tables, not sentence text; changing them would need a migration for no visible effect.
6. No permission change: the trail stays owner-only (`audit.read`).

**Why:** the trail exists so an owner can answer "who changed what". A sentence with a table name and an empty "()" answers neither, and fixing it once in the renderer fixes every screen that shows audit events.

**Consequences:** the dashboard's curated-only filter (`isCuratedAuditAction`, D-400) could now show generic rows too; it is left as is and can be relaxed in a later design pass. A future audited table adds its noun in the same PR (the unit test enforces it).

## D-101 — Per-user throttle keys are derived from `auth.uid()` in the database; rate-limit waits are shown once, in minutes · ACCEPTED · 2026-09-25

**Context:** Two follow-ups. (1) D-100 item 7: `public.throttle_status`, `throttle_record_failure` and `throttle_reset` accept any key from `anon`/`authenticated` (sign-in runs before a session, so they must be callable). The IP/email buckets are safe because the app sends a salted hash nobody else can compute, but `create_school_workspace` keyed its bucket on the plain user id, so anyone who knew a user's id could lock them out of school creation for 15 minutes. (2) A production bug: after the sign-in limit tripped, `/login` showed "Too many attempts. Try again in 900s." twice — in the banner and as the submit button's label.

**Decision:** (1) `app.throttle_key(p_key)` rewrites any key starting `user:` to `user:<bucket>:<auth.uid()>` (and refuses it for anon); all three public functions go through it, and `throttle_record_failure` ignores `p_key` for the per-user buckets (`changePassword`, `eiinCheck`, `createSchool`), deriving it the same way. The app sends `user:changePassword` / `user:eiinCheck` (`USER_THROTTLE_KEYS`). Client-keyed buckets and the D-65 anon smoke test are unchanged. (2) `ApiError` gains an optional `retryAfterSeconds`; every rate-limited action sets it and words the message in whole minutes, rounded up (`apps/web/lib/throttle-copy.ts`, en + bn). The login form shows the wait only in the banner, counting down from `retryAfterSeconds` instead of parsing the message; the button stays a disabled "Sign in".

**Review follow-ups (PR #45):** `throttle_reset` refuses `user:` keys from clients (42501) — with a fixed public key, any signed-in user could otherwise have cleared their own createSchool/eiinCheck/changePassword limit; per-user rows expire with their window, and `changePassword` no longer resets on success. Minutes keep Western digits in both languages ("15 মিনিট", DESIGN-SYSTEM §1.6), and the login banner wakes only on minute boundaries. Two older gaps closed in the same function: an expired block (or window) now starts a fresh window instead of re-blocking on the next single failure, and a running block is never shortened (a `user:<bucket>` key is refused under any other bucket, and a new block keeps the later end). **Accepted trade-off:** anyone who knows an email address can fail sign-in five times and lock that account out of password sign-in for 15 minutes (`loginByEmail` is keyed on the salted email; the per-IP bucket is separate, and either one blocks). That is the intended F-ID-01 behaviour (§5 rate limits), recorded here so it is a known choice rather than a surprise.

**Why:** tying the key to the session inside the database makes "somebody else's bucket" unnameable by construction, for every per-user bucket at once, instead of relying on an app-side secret the database cannot check.

**Consequences:** `supabase/tests/31_throttle_per_user_keys.sql`; `30_create_school_workspace.sql` reads the new key name. Existing per-user rows under the old salted keys simply expire.

## D-102 — F-AC-01 demo cut: sections and subjects on the current year, tenant-bound foreign keys, rooms as text · ACCEPTED · 2026-09-25

**Context:** The owner needs the Classes screen for sales demos before the full F-AC-01 build (six Parts: years/terms, grades, sections/rooms, subjects/templates, section-subjects, setup wizard). `grade_levels` and `academic_years` already exist (D-100); schools created by the wizard have a current year and grade levels.

**Decision:**

1. **Scope:** `sections` (F-AC-01 Part 3 minus rooms) and `subjects` (Part 4 minus `grade_level_subjects`), plus `/app/classes`: a card per grade with this year's sections (add, archive), and a Subjects tab (add, "Use the NCTB starter list"). Terms, rooms, grade templates, section-subjects and the setup wizard stay in their own Parts.
2. **Sections always belong to the current academic year**, resolved server-side; the client never names a year.
3. **Tenant-bound foreign keys:** `academic_years`, `grade_levels` and `workspace_members` gain `unique (id, workspace_id)`, and `sections` references them with composite keys that include `workspace_id`. A section cannot point at another school's grade, year or member even through a SECURITY DEFINER path, not just through RLS.
4. **Class teacher rules in the database:** a trigger requires an active owner/admin/teacher (`MEMBER_NOT_ELIGIBLE`); a partial unique index keeps one live section per teacher per year (§5 rule 10, `CLASS_TEACHER_TAKEN`; `allow_multi_class_teacher` is not built).
5. **`room` is free text** until the `rooms` table lands (Part 3); **archive, not delete** (`archived_at`), for both tables.
6. **The NCTB starter list** lives in `packages/domain/src/academic/structure.ts` and is copied into the school's own rows; running it twice adds nothing.
7. **Permissions:** `academics.structure.read` (owner, admin, teacher, staff), `academics.section.write` and `academics.subject.write` (owner, admin). Parents read nothing here (T2); their view is F-AC-10.
8. **Migration timestamps:** `20260925300304` sorts after main's newest (`20260925300302`, #46) and after #45's `20260925300303`. The three `add constraint ... unique (id, workspace_id)` statements are plain, not `CONCURRENTLY`: migrations run inside a transaction, where `CONCURRENTLY` is impossible, and these tables are tiny.
9. **Review follow-ups (PR #47):** when a member stops being an active owner/admin/teacher (removed, or moved to staff/parent), `app.tg_members_release_class_teacher` clears them as class teacher of live sections (§4.5; archived sections keep their history). Sections and subjects are archive-only: no DELETE policy or grant. The NCTB starter list has Bangla and English as two papers each (marks are kept per paper), religion as one subject per faith, and "Physical Education and Health". A Bangla school is offered ক, খ … as section names.

**Why:** the smallest slice that gives a demo school a real class list, without inventing shapes the later Parts would have to undo.

**Consequences:** `supabase/tests/32_sections_and_subjects.sql`. When `rooms` lands, `sections.room` becomes `room_id` (expand, backfill, contract).

## D-303 — Exams demo cut: no terms, papers from picked subjects, grading snapshotted on the exam · ACCEPTED · 2026-09-25

**Context:** F-AC-06 Part 2 assumes `terms` and `section_subjects` (F-AC-01), and neither exists; F-AC-01's demo cut (#47, D-102) ships sections and a subject catalogue only. D-302 deferred scale versioning and the pass boundary to this Part.

**Decision:**

1. `exams` has no `term_id`; `exam_type` (midterm, term_final, annual, ...) says which part of the year it is. `term_id` is added, nullable, when `terms` lands.
2. `exam_subjects` is exam × section × subject. `createExam` takes the subjects the admin picks and creates one paper per section × subject. When `section_subjects` lands, the default subject list comes from it. `section_subject_id` is then added and the paper keeps `section_id`/`subject_id` (the spec's denormalised columns).
3. The grading policy is snapshotted on the exam at insert by a trigger: the scale's bands, the pass mark, F-zeroes-GPA and the year's 4th-subject threshold. It is never client-supplied and never changed afterwards. That snapshot is the "versioning" D-302 deferred: editing a scale cannot change an existing exam, so no draft/active/retired scale lifecycle is needed yet.
4. The pass mark reaches papers as `pass_marks = round_half_up(full × pass_mark_percent / 100, 0)`. "The pass flag flips exactly at the pass mark" is enforced when results are computed (Part 5).
5. Status moves only along §5.12's chain, in the database (`app.tg_exams_status_guard`) and in the domain (`checkExamTransition`). Every status button in the UI names its step.

**Why:** It builds only on tables that exist, and it keeps grading history fixed by construction.

**Consequences:** Components, aggregate exams, calendar events, question-paper upload, idempotency keys and rate limits on `createExam` are later Parts. Parents read exams only through Part 7's published view.

**Review of PR #48 (2026-09-25):**

- **Result computation reads `grading_snapshot.bands`, never `app.band_for` on the live scale.** The snapshot also carries `rank_by`. A snapshot-reading helper is added in Part 5, the first Part that computes a result.
- **Pass marks are not rounded to whole marks (D-302):** `full × pass_mark_percent / 100`, stored at 2 decimals (33 % of 50 = 16.50). This supersedes item 4's `round_half_up(…, 0)`.
- **Papers lock when marks entry opens.** From `marks_entry` on, no section or paper is added and no full marks, pass marks or membership change; a paper's date can still move. After `draft`, sections and papers are never deleted (`app.tg_exam_papers_lock`). A paper's own status moves one step at a time: pending → entering → submitted → locked. Part 4 adds unlock.
- **Other rules:**
  - `exams.academic_year_id` is immutable.
  - `exam_subjects` has `unique (id, workspace_id)` for marks' composite FK.
  - The snapshot trigger returns early unless the caller is owner/admin of the school or privileged, so a stranger only ever sees RLS's refusal, never `NO_GRADE_SCALE`.
  - A reversal needs its own reason, one that is not blank and not the stored one, in both the database and `checkExamTransition`.
- **Known gap:** Publish has no marks-completeness gate yet. Part 4 (submit and lock) must add one before results can be published for real.

## D-204 — F-OP-03 Part 1 PDF engine: `@react-pdf/renderer`, Hind Siliguri over the spec's proposed Noto Sans Bengali, no build-time font subsetting · ACCEPTED · 2026-09-25

**Context:** F-OP-03 §7 already names the render library (`@react-pdf/renderer`, Node runtime, not Edge, not headless Chrome) and §3.6 proposes **Noto Sans Bengali** for the Bengali face, with reasons (OFL licence, the widest Bengali coverage). DESIGN-SYSTEM §1.6 (D-57) separately fixes **Hind Siliguri** as the one Bengali face this product uses everywhere on screen, specifically because it is the same face acadigma-website ships, and rejects Noto Sans Bengali there as "the safe default, but wider and less economical... than Hind Siliguri." A report card is the one artefact a parent associates with the school as strongly as the website; shipping it in a different Bengali face than the rest of the product would be a visible inconsistency the spec's own §3.6 note did not anticipate because it was written before D-57.

**Decision:**

1. Library: `@react-pdf/renderer` 4.9, per spec §7 — not evaluated further since the spec already decided it and gave the reason (Node APIs + font buffers, incompatible with Edge; a headless-Chrome route would blow past Vercel Hobby's function size/cold-start budget for zero benefit here).
2. Font: **Hind Siliguri** (Regular/SemiBold TTF, from the same `google/fonts` OFL source acadigma-website uses) for Bengali, **Inter** (Regular/SemiBold, static TTFs from the `rsms/inter` v4.1 release — the current Google Fonts distribution ships Inter only as a variable font plus woff2, and `@react-pdf/renderer`'s font registration wants one static file per weight) for Latin and every digit. Both OFL-licensed; the licence files ship alongside the fonts in `packages/pdf/assets/fonts/`.
3. **No build-time font subsetting toolchain** (no `pyftsubset`/fonttools step). `@react-pdf/renderer` embeds fonts through `fontkit`, which — like the `pdfkit` it sits on — subsets the embedded font program to the glyphs a given rendered document actually uses, automatically, per PDF. The spec's "subset to the Unicode ranges actually used" (§3.6) is therefore already satisfied by the rendering pipeline itself; adding a separate subsetting step would subset the _source_ files to a fixed Unicode range ahead of time for no additional benefit, and one more toolchain dependency to maintain.
4. Hyphenation is disabled globally (`Font.registerHyphenationCallback(w => [w])`) — `@react-pdf/renderer`'s default Latin hyphenation otherwise breaks a Bengali conjunct mid-word, and no template in this product's records-office register wants English hyphenation either.

**Consequences:** `packages/pdf`'s golden test (`golden.test.ts`) renders the same document from two school fixtures and extracts real text back out of the produced PDF bytes via `pdf-parse`, proving both the Bengali conjunct ("ক্ষ") and the letterhead difference survive font embedding — not just that the JSX tree looks right. A school-supplied brand font (spec §11 OQ5) is unaffected by this decision and remains FUTURE.

## D-205 — F-OP-03 Part 2 run pipeline: one `'sample'` report kind, synchronous in-request rendering, no stored PDF yet · ACCEPTED · 2026-09-25

**Context:** F-OP-03 Part 2's full pipeline (§4 W1, §7) is a `report_runs`/`report_run_items` job model with a cron drainer, Realtime progress, and a stored PDF served by a signed URL from Supabase Storage. Every real report kind (`report_card`, `mark_sheet`, ...) needs exam/marks data from F-AC-0x, which does not exist on `main` yet — inventing content for them would be exactly the "fake data in the product" this build avoids. Separately, `files`/Storage exists as _config_ (the `private` bucket is provisioned, `public.files` is a shipped table) but has **no consuming code anywhere in the repo** — no `storage.objects` RLS policies, no `/api/files/[id]` route, no upload path. Building that from scratch inside this PR would mean inventing unreviewed, cross-cutting storage security policy that every future file-handling feature would then have to either reuse or diverge from, which is a bigger decision than one Part of one feature should make alone.

**Decision:**

1. `report_kind` ships with exactly one Postgres value, `'sample'` — the run pipeline's own proof (spec §8 Part 2 demo literally says "enqueue a stub report"). Later Parts add their real kinds with `alter type public.report_kind add value ...` (additive, forward-only); nothing about this Part's schema changes shape when they do.
2. Rendering is **synchronous, in the same request** that creates the run (`createReportRun` calls a render step directly, under `withServiceRole` for the `queued -> rendering -> ready|failed` status transitions only) rather than waiting on a cron drainer. Spec §4 W1 point 4 already allows this: "the job drainer... plus an immediate in-request kick for single-item runs" — `'sample'` is always exactly one item. The cron drainer and Realtime progress are deferred to whichever Part first needs to render something slow enough to need them (bulk rendering, Part 5).
3. **No `files`/Storage row is created, and no PDF is persisted.** `report_runs.file_id` stays `null` for every run this Part creates. `GET /api/pdf/[runId]` re-renders the same deterministic bytes from `report_runs.params` + a fresh branding read on every authorized request, instead of streaming a stored object or a signed URL. `page_count` is hardcoded to `1` (accurate — `SampleDocument` is always exactly one page — not a placeholder).
4. Idempotency (`report_runs.idempotency_key`, unique per `(workspace_id, idempotency_key)` while `queued|rendering|ready`) uses a 10-minute time bucket as `data_version`, which is the fallback the spec's own §11 OQ3 names ("if [a per-kind `data_version` function] is not provided, the fallback is a 10-minute time-boxed cache only") — `'sample'` reads no mutable table, so there is nothing to derive a real `data_version` from yet.
5. The plan-entitlement gate (§9 AC29, "Free-plan workspace -> `plan_required`") reuses the existing `plan_modules` row for the `reports` module code (already present on `starter`/`pro`/`enterprise`, already wired into `packages/domain/nav`'s entitlement engine by an earlier lane) via `resolveEntitledNavModules`, mapped onto the `payment_required` `ApiErrorCode` (`apiErrorCodeSchema` has no separate `plan_required` code).

**Consequences:** the demo (a Bengali sample document with the school's real letterhead, downloadable end to end, authorized, idempotent) is fully real end to end except for storage persistence. When F-OP-03 or another feature builds `storage.objects` RLS and `/api/files/[id]`, `createReportRun`'s render step gains one insert (`files` row + upload) and `GET /api/pdf/[runId]` becomes the signed-URL redirect the full spec describes — neither change touches `report_runs`' schema or RLS. Tracked as an explicit open item in this PR's test report and spec §11 addendum, not silently left out.

## D-103 — F-AC-02 demo cut: students with a private-details split table, one admission function, guardians as private contact rows · ACCEPTED · 2026-09-25

**Context:** The sales demo needs a class list for Class 6 – ক with about 40 students, search, quick admission and a student profile. F-AC-02 is nine Parts; the demo needs Parts 1 and 2 and the guardian contact half of Part 4. The spec (§3 "RLS in words") protects sensitive fields with two repository projections over one `students` table, which leaves every column readable to any teacher or staff member through PostgREST — the database would not be the boundary (CLAUDE.md rule 1). The lead asked for date of birth, guardian phone and address to be readable only by the roles the spec allows.

**Decision:**

1. **Split table, the D-63 pattern.** `students` holds roster fields only (code, English and Bangla names, gender, status, soft delete). `student_private_details` (1:1, PK `student_id`) holds date of birth now, and address and health when they arrive. `guardians` is private as a whole. Both are readable only through `app.can_read_student_private(workspace_id, student_id)`: an active owner/admin, or the active class teacher of the student's live section. DOB counts as private because COMPLIANCE-PDPA §4.1 limits the child's core record to the section's teachers and §5.14 limits staff to name, class and roll. Subject teachers and staff read the roster (`students`, `enrollments`, the `student_roster` view); parents read nothing until F-AC-10; platform staff read nothing ("never by default").
2. **One door for creation.** `public.admit_student(workspace_id, jsonb)` (SECURITY DEFINER, owner/admin, idempotent by key) writes the student, private details, primary guardian and active enrolment in one transaction under one correlation id. `authenticated` has no INSERT on `students`, `student_private_details` or `enrollments`, so a code can only come from `app.next_id` and a roll number only from the function (given, or next free under a section row lock). Named errors: `FORBIDDEN`, `VALIDATION`, `IDEMPOTENCY_KEY_REUSED`, `SECTION_NOT_FOUND`, `SECTION_ARCHIVED`, `YEAR_CLOSED`, `ROLL_TAKEN`.
3. **Rules in the database.** One active enrolment per student per year and unique roll numbers among a section's active enrolments are partial unique indexes. Every foreign key between tenant rows is composite with `workspace_id` (the D-63 item 10 lesson); the enrolment's key is `(section_id, academic_year_id, workspace_id)`, so its year can never disagree with its section's. Enrolments have no DELETE (history); students are soft-deleted and hidden from teachers and staff once deleted.
4. **Scope cut:** no `admissions`/`ADM-` number, duplicate warning, capacity warning, guardian invite/`guardian_users`, documents, transfer, import or ID cards. Search is ILIKE on both names and the code (trigram-indexed), 50 rows a page with offset paging. Permission `students.read_sensitive` (owner, admin, teacher) is added to the matrix; RLS narrows teachers to the class teacher.
5. **Demo data** is a script, not a migration: `supabase/seed/demo-class-6-ka.sql` admits 40 fictional students through `admit_student` as the school's owner; a second run replays and adds nobody.

**Why:** the split makes "a subject teacher cannot read a child's date of birth or a parent's phone" true at the database, by construction and by pgTAP, rather than in the repository. One function means the rules cannot be skipped by a direct PostgREST insert.

**Consequences:** `supabase/tests/33_students_and_guardians.sql` (62 assertions). Address, health, religion and ID attestation must go into `student_private_details` (or another private table), never `students`. Part 4 adds `guardian_users` and a parent branch to the `students`/`enrollments` policies; Part 3 adds `admissions` and calls the same insert path. When the teacher's `students.write_own_section` edits land they need their own narrow policy or function; today teachers write nothing.

**Review of PR #54 (2026-09-25):**

- **Date of birth never reaches the audit trail (HIGH).** `student_private_details` is audited with `date_of_birth` as a free-text column: the name stays in `changed_fields`, the value is nulled — platform admins can read `audit_events`, and "platform staff see nothing" must hold there too. Guardian phone was already masked by the contact pattern; guardians store no address. Names (student and guardian) stay in the trail like every other table's names: they identify the record an owner is looking at.
- **Class-teacher access is current-year only.** `app.can_read_student_private` also requires the enrolment's year to be current and the class teacher to still be an owner/admin/teacher, so last year's class teacher loses access at promotion and a stale `class_teacher_id` cannot resurface it.
- **Roster by year.** `student_roster` carries `academic_year_id`; the roster lists the current year's enrolments only, so a promoted student appears once. The profile picks the higher class mid-promotion.
- `admit_student` takes an optional `enrolled_on` (from the year's first day, never in the future; default today); the demo script enrols from the year's start so the students count in attendance history. The script now needs the school's exact name as well as its id, does nothing when Class 6 – ক already has students, and uses the unassigned `+88010…` range.
- `enrollments` gains `unique (id, workspace_id)` for the marks/attendance composite keys; `student_code` gets a trigram index; the unused `(workspace_id, status)` and guardian-phone indexes are dropped until a query needs them. Search strips `%` and `_` (LIKE wildcards); Bangla names and queries are NFC-normalised.

## D-104 — F-AC-03 demo cut: daily roll call through one save function, the policy's booleans as the weights, no offline queue yet · ACCEPTED · 2026-09-25

**Context:** The demo's 60-second moment is a class teacher opening Class 6 – ক on a phone, tapping "সবাই উপস্থিত", flipping three absentees and saving; the owner's is the "by 9:30" Today screen. F-AC-03 Parts 1-3 cover the tables, the percentage, the roll-call screen, the Today views and the bulk action; the spec also asks for a policy screen, reminders and guardian notifications, and Parts 4-7 add locking, the register, alerts and the offline queue.

**Decision:**

1. **One writer.** `attendance_sessions` and `attendance_records` have no INSERT/UPDATE/DELETE grant; `public.save_attendance` (SECURITY DEFINER) writes the session and every record in one transaction. It enforces, in the database: the section's active class teacher or an owner/admin (`NOT_ASSIGNED`); a teacher only inside the edit window (default 2 days, `OUTSIDE_EDIT_WINDOW`) while an admin may go beyond it and the session is stamped `edited_after_window` (a light version of Part 4); no future date (`FUTURE_DATE`, the school's timezone); a non-school day (F-AC-11 `app.is_school_day`) refused unless the teacher confirms (`NOT_SCHOOL_DAY` / `allow_non_school_day`, spec AC7 "warns before allowing"); exactly the students enrolled on that date (§5.3), each with an explicit status (`UNMARKED_STUDENTS`, D-22); idempotency by key; and no silent overwrite — a re-save must name the session version it loaded (`CONFLICT`, AC4) instead of the spec's merge sheet.
2. **"Mark all present" is a flag on the save**, not a separate write: the screen fills the unmarked students (with Undo), and the save stamps `bulk_marked_by`/`bulk_marked_at`. Nothing is written until Save.
3. **The percentage follows DATA-MODEL's policy, not the spec's weight map.** `school_profiles.attendance_policy` already has `late_counts_present` and `half_day_counts_present` (booleans, F-OP-07); present counts 1, late/half day 1 unless switched off, excused/absent 0, every record in the denominator. `app.attendance_pct` and `packages/domain` `attendancePercentage` share the AC5 fixture (90.91; 86.36 with half day off). A 0.5 half-day weight (AC6) needs a policy change first.
4. **Audit:** sessions get the generic trigger; records are audited on UPDATE/DELETE only, so a first save is one session row, not 40 record rows (§4.1). The session's update severity is info (a re-save happens every morning).
5. **Today:** `public.attendance_day` (SECURITY INVOKER) returns every live section of the current year with its enrolled count and session; the page shows the teacher's own class first, then every class with marked / not marked, who marked it and when, and the school's rate so far. The dashboard's attendance slot shows the same rate and "n of m classes marked".
6. **Deferred:** the offline queue and conflict sheet (Part 7), locking and correction requests (Part 4), the register and rollup (Part 5), alerts (Part 6), reminders and the absent-guardian notification (Part 3 — no notification sender exists yet), copy-yesterday, notes and the excused-note sheet, period mode, the policy settings screen, parents' view (F-AC-10). The roll call uses DESIGN-SYSTEM §4.4's segmented `AttendanceToggle` (one tap per exception) instead of the spec's tap-to-cycle pill.

**Why:** the smallest slice that makes the demo true end to end, with every rule a pgTAP case can break rather than a UI convention.

**Consequences:** `supabase/tests/34_attendance.sql` (47 assertions; 45 after D-105). **Point 1's class-teacher rule (`NOT_ASSIGNED`) is superseded by D-105:** any active teacher of the school may mark any section. `enrollments (id, workspace_id)` (D-103) and `attendance_sessions (id, workspace_id)` are the targets later tables use. When the offline queue lands it calls `save_attendance` unchanged; when period mode lands, the `(section_id, date)` key widens.

## D-403 — Basic mode: a per-user, class-by-class layout over the same actions, with bigger everything and plain confirmations · ACCEPTED · 2026-09-26

**Context (owner decision 2026-09-26):** many teachers are older and not comfortable with technology, and need only the classroom jobs, not the in-depth features. The full app is organised feature by feature (Attendance, Exams, Students, Reports), which asks them to hold the whole product in their head. The owner walked through what a simpler app should be and picked the home layout.

**Decision:** spec `docs/features/01-identity/F-ID-10-basic-mode.md`.

1. **Per-user, synced, reversible.** `user_preferences.ui_mode` (`full|basic`), chosen by each user for themselves, mirrored in a cookie for a no-flash first paint; owners and admins may use it; a labelled **Switch to full app** button is always on the home screen. `user_preferences` is not built yet, so F-ID-10 Part 1 creates it with only the two columns it needs (`ui_mode`, `text_size`) and F-ID-02 Part 1 adds the rest later. Language stays in `profiles.locale` (D-401); basic mode never forces Bangla.
2. **Class by class.** Home = a today strip (greeting, next period only once a timetable exists, to-dos such as "2 roll calls not taken"), one big block per class the teacher is assigned to (section + subject, student count, today's attendance tick or cross), then Profile · Settings · Help · Switch to full app. A block opens that class's hub (`/app/classes/[sectionId]`) with that class's data only; the hub shows **only tabs whose pages exist** (the D-400 implemented-routes rule), no "coming soon" tabs. Advanced features not tied to a classroom are hidden or moved into class tabs.
3. **A layout, not a second app.** Every tab reuses its feature's server actions and permission checks unchanged; basic mode adds no write path and no permission.
4. **Bigger and plainer.** 56 px minimum tap targets, 18 px body text, 28 px icons, icon **and** label on every button, no swipe or long-press-only actions. A **text size** setting (Normal / Large / Extra large = 100 / 112.5 / 125 % root size, rem tokens) works in both modes. Every save confirms in plain words with counts ("Save attendance for 6-ক? 38 present, 2 absent"); undo where the data allows; a Help button on every screen with **Call school office** (`tel:` to `school_profiles.phone`); voice hints via Web Speech in a later Part, shown only when the device has a voice for the language.
5. **Performance budget for old Android** (Android 8+, 2 GB RAM, Slow 4G, 4× CPU): basic home LCP < 3.5 s first visit, < 1.5 s repeat open, < 150 KB JS.

**Why:** the owner's own walkthrough; organising by class matches how a teacher thinks about the day, and reusing the actions keeps one set of rules to test. Showing only built tabs keeps the demo honest (PRODUCT-DECISIONS 3.9, D-400).

**Consequences:** four Parts (preferences + text size; home + shell + help; class hub + confirmations + undo; voice). Parts 1–3 are scheduled after marks entry and before the school demos (ROADMAP). Every later class-shaped feature (routine, printing, handouts, lesson planner, homework) adds its hub tab in its own PR. **Follow-ups settled (lead decision under owner authorization 2026-09-26):** (a) office staff do not get the basic-mode switch; (b) owners/admins with no classes get an **All classes** block; (c) subject-only teachers get blocks only once F-AC-01 Part 5 (`section_subjects`) ships, and that Part is a prerequisite of the basic-mode demo (ROADMAP); (d) the post-save Undo lasts **30 s in basic mode**; the full app keeps DESIGN-SYSTEM §5.4 (6 s for a single record, 10 s for a bulk action); (e) voice hints stay the last Part (P4) behind a go/no-go gate — ship only if a Bangla voice is available and understandable on a 2019-era Android test phone, otherwise drop it and record why here. (f) **Deliberate deviation from DESIGN-SYSTEM §5.4:** in basic mode every save confirms first in plain words (§5.4 prefers undo over confirmation); older users need the effect named before it happens, and undo is kept as well. (g) `ui_mode` is global to the user; the staff exclusion is per workspace — a member whose role is `staff` in the active workspace does not see the switch there.

## D-71 — Offline is platform-wide: everything except generating or sending works offline, through one outbox that replays the same server actions · ACCEPTED · 2026-09-26

**Context (owner decision 2026-09-26):** school work must not stop without internet. F-AC-03 §4.6 had specified an attendance-only offline queue and a revocation purge, and F-AC-06 Part 4 a marks queue; neither is built (D-104 deferred it). The owner widened the scope to the whole app and chose the conflict rules.

**Decision:** spec `docs/features/01-identity/F-ID-11-offline.md` (a lead-range decision because it binds every lane).

1. **Scope — "literally everything except generating anything".** Offline: attendance, marks entry, viewing class data (students, routine, past marks), lesson planner and handout edits (files wait in the queue), and other normal edits. **Needs internet** (a clear disabled "Needs internet" state, never queued): PDFs and report cards, AI tools, SMS and notifications, payments, invitations and join codes, creating a school. Every feature spec declares its level — `full`, `read` or `needs internet` — in a new `Offline` row of `docs/features/_TEMPLATE.md`. **`read` is interim** (lead decision under owner authorization 2026-09-26): the owner asked for "literally everything" except generating, so areas that start read-only offline (admissions and transfers, routine, settings) get offline editing in later Parts.
2. **One mechanism.** A read cache (serwist, pages opened plus a warm-up of the teacher's own classes, with "Last updated" stamps) and one generic IndexedDB outbox per user, with client idempotency keys (a new key after every successful save or enqueue; a new save for an entity with a pending item replaces that item's payload), replaying serially through the **same server actions** — no separate sync endpoint. A pending chip, a queue sheet and a **Needs attention** list; a refused item is never dropped silently. No Background Sync on any platform (iOS has none): replay on app open, on coming back online and on visibility change.
3. **Conflicts.** Attendance keeps the existing `CONFLICT` version check (D-104) and adds the conflict sheet (keep theirs / use mine / per student). **Marks sync per cell: the later edit wins, and the overwritten value is kept in the audit trail** (owner's choice) — "later" is the time the value was typed (`client_edited_at`, clock-corrected against the server) — lead decision under owner authorization 2026-09-26.
4. **Server time decides, with one bounded exception.** School days, read-only mode and permissions are checked at replay against the server clock. **Late attendance (lead decision under owner authorization 2026-09-26):** a roll call replayed outside the edit window is accepted only if no session exists yet for that section and date, its device `captured_at` is between the session date and the server's now and falls inside the window, and it arrives within 7 days; it is stamped `synced_late` (like `edited_after_window`), audited and shown to admins. Rural schools can be offline for days and losing a day's register is worse than the risk; clock tampering is visible in the audit. Anything older goes to Needs attention for an admin.
5. **Security.** Session expiry offline: the queue holds; re-sign-in as the **same** user resumes it, a **different** user means purge after a confirmation. The F-AC-03 revocation purge is generalised (membership removed or session revoked → outbox and caches purged before anything renders; role changed → caches purged) and ships with what it purges: caches in Part 1, the outbox in Part 2a; sign-out wipes the user's database and caches (asking first if items are pending); caches clear on workspace switch and on every sign-in; storage persistence and a quota banner; a 14-day maximum offline age locks cached data until the next check.

**Why:** a second, offline-only write path would be a second set of rules to secure and test; replaying the real actions with idempotency keys means the only new contract is "the key must dedupe". Generating and sending need the server's current data or an external provider, so queuing them would produce stale documents or late messages.

**Consequences:** six Parts on their own track (ROADMAP): app shell + read cache + cache purge → 2a the outbox with attendance and the outbox purge (absorbs F-AC-03 Part 7's offline half) → 2b conflict sheet, late sync, session-expiry resume → marks (absorbs F-AC-06 Part 4's offline half, adds `marks.client_edited_at`; online saves stamp server `now()`) → lesson plans and handouts as those features ship → the 14-day lock and storage. `ARCHITECTURE.md` §6 and F-AC-03 §4.6 are updated to match (whole-app offline, no Background Sync). Every `full`-level action must take an idempotency key, return named errors and, where edits collide, a base version. Stated honestly: this is about ten days of work that touches every offline-capable feature's contract, and Parts 1–2b carry most of the risk.

## D-105 — F-AC-03: any teacher may take any section's attendance; enrolment on a date by its dates · ACCEPTED · 2026-09-26

**Context:** D-104 let only the section's class teacher (or an owner/admin) save its roll call. On a normal morning the class teacher is sometimes absent, and the teacher covering the class could view the roster but not save it, so the day went unmarked or an admin had to do it. Separately, the expected class list filtered enrolments by `status = 'active'` as well as by date, so a student transferred out today vanished from last week's register.

**Decision (owner, 2026-09-26):**

1. **Any active teacher of the school may view and mark/save the roll call for any section** — substitutes cover. `public.save_attendance`'s role guard (active owner/admin/teacher of this workspace, else `FORBIDDEN`) is the whole rule; the class-teacher check (`NOT_ASSIGNED`, `app.can_mark_attendance`) is removed. Every other guard stays: edit window for teachers, no future date, school day unless confirmed, exactly the enrolled students, idempotency, the `CONFLICT` version check, the read-only guard. The audit already records who saved (`taken_by`, `marked_by`, the audit actor).
2. **"Enrolled in the section on that date" is `enrolled_on <= date <= ended_on`** (open-ended when `ended_on` is null), plus the student being active and not deleted — not the enrolment's current status. If two enrolments in the same section cover the date, the student counts once (the later enrolment).
3. **A closed enrolment has an end date:** `enrollments_closed_has_end` — `status = 'active' or ended_on is not null` — so a transfer or withdrawal can never leave a student on a register by accident.

**Why:** schools substitute every week; a register that only one person can save is a register that goes unmarked. History must not change when a student moves.

**Consequences:** migration `20260925300311_attendance_any_teacher.sql` (CREATE OR REPLACE of `save_attendance` and `attendance_day`, drops `app.can_mark_attendance`); `supabase/tests/35_attendance_any_teacher.sql` (16 assertions); the two `NOT_ASSIGNED` cases leave `34_attendance.sql`. The UI offers "Take attendance" on every class to anyone with `attendance.write`. `33_students_and_guardians.sql` now closes last year's enrolments with an `ended_on`.

## D-206 — F-OP-03 Part 3 report card: fixture-backed until marks entry lands, one seam function, grading reused from #46/D-302 · ACCEPTED · 2026-09-26

**Context:** F-OP-03 Part 3 (§8) is "the report card template consuming `app.compute_exam_result` exclusively" — but that function, and the `marks`/`results` tables it reads, belong to F-AC-06 Part 3 (marks entry), which is being built in the billing lane in parallel and is not on `main`. D-205 already established the precedent for this exact situation (Parts 1-2's `'sample'` kind): ship the real pipeline plumbing now, on realistic-but-fixture data, with an explicit, narrow seam for the one thing that isn't real yet, rather than inventing fake marks tables or waiting on another lane.

**Decision:**

1. **`report_kind` gains `'report_card'`** (`alter type ... add value`, additive — D-205's own stated forward-compatible pattern), migration `20260925300313_report_card_kind.sql`. No shape change to `report_runs`/`report_run_items`; idempotency and service-role-only status transitions already cover the new value, and the same migration recreates the RLS policies only to admit staff (item 5). pgTAP `42_report_card_kind.sql` proves both.
2. **`ReportCardDto` (`packages/contracts`) carries only computed academic data — no branding, no grade-band table** (spec §5.1: "`packages/pdf` contains no grade band table, no percentage-to-letter map and no averaging code"). Branding stays a separate set of props on `ReportCardDocumentProps`, read live from `school_profiles` exactly as `SampleDocument`'s caller already does — never baked into the DTO, so a stale branding read can never survive a re-render.
3. **Grading reuses `@acadigma/domain/grading`'s `bandFor`/`BD_GRADE_BANDS`/`roundHalfUp`** — the exact functions and BD default bands #46/D-302 shipped for `/app/settings/grade-scale` — rather than re-deriving a band table for this Part. This is "the grade scale from #46" the task names, used by the fixture builder (`report-card-fixture.ts`), never by `packages/pdf` itself (rule 2 above).
4. **One seam function, real params already.** `ReportCardParams` (`{ kind: 'report_card', studentId, examId }`) takes real uuids now, not fixture indices — `getReportCardData(supabase, ctx, studentId, examId)` (`apps/web/.../report-card-data.ts`) is the only place any caller (`createReportRun`'s render step, `GET /api/pdf/[runId]`) resolves a `ReportCardDto`. Today it looks the student up in a 40-student Class 6-ক fixture (`report-card-fixture.ts`, in the style of `supabase/seed/demo-class-6-ka.sql`'s D-103 fictional roster) and does not consult a marks table at all, honouring the constraint that F-AC-06 Part 3's tables are not on `main`. The real body reads the `results` / `result_subject_lines` rows F-AC-06's `app.compute_results(exam_id)` writes, through the caller's own RLS client scoped to the workspace — never service role. The billing lane implements it in F-AC-06 Part 5 (`feat/academics-results`); the DTO shape, the params shape and every caller stay as they are. The fixture never serves in production: the seam returns `not_found` there and `/app/reports` hides the button, so a fixture student can never print under a real school's letterhead.
5. **Permission (lead decision, 2026-09-26):** `report.render.report_card` = owner, admin, staff and teachers; teachers only for sections they are class teacher of. Staff is a deliberate change from the spec's "—" (the office prints report cards): staff also gets `report.view`, and RLS lets staff insert only a `report_card` run and read only their own runs. **Interim:** the teacher's class-teacher limit is not enforced yet — the render source is a fixture — and is enforced in the seam and by RLS on `results` when real data lands. The download route checks the run kind's action as well as `report.view`.
6. **One A4 portrait layout, no template designer** (owner instruction), titled "প্রগতিপত্র" / "Progress Report" — header/footer/watermark/signatures from the existing `ReportShell` (Part 1), a subject table, totals, an incomplete-marks banner (§4 W1), and an attendance line that warns but never blocks below the minimum (§5.2).
7. **The DTO follows F-AC-06's `results` shape.** `result` is `pass|fail|incomplete|withheld`; `gpa`, `overallLetter` and `rank` are nullable and must be null for an incomplete or withheld result (a contract `refine`). Such a card prints "অসম্পূর্ণ — ফলাফল স্থগিত" / "Incomplete — result withheld" (or "ফলাফল স্থগিত" / "Result withheld") and hides totals, percentage, GPA, grade, pass/fail and rank. Each subject line carries `status` (`entered|absent|exempt`: absent prints "অনুপস্থিত", an entered line with no mark prints "—") and `subjectKind` (`compulsory|optional_fourth`, marked "(৪র্থ বিষয়)"); the card also carries `gpaWithoutOptional` and a `rankTied` flag ("২ (সমান)"). Bengali numerals render only on the card itself (`formatNumber`/locale, already opt-in per DESIGN-SYSTEM §1.6) — never a screen-default.

**Why:** the alternative — inventing a `marks` table shape inside this PR to unblock the "real" render — would be exactly the premature, unreviewed cross-cutting decision D-205 already declined to make for storage; a fixture behind one narrow, already-real-shaped seam ships the actual template, the actual pipeline wiring and the actual grade-scale integration today, and costs one function body when the real data exists.

**Consequences:** `packages/pdf/src/templates/report-card.test.ts` (golden PDF-text-extraction tests, same technique as `golden.test.ts`); `apps/web/.../report-card-fixture.test.ts` (the BD GPA-zero rule, the missing-mark null-not-zero rule, competition ranking with ties, every card valid against the contract); `apps/web/.../report-card-data.test.ts` (the seam's contract cases, including no fixture in production); `actions.test.ts`/`route.test.ts` extended for the `report_card` branch. `report-card-fixture.ts` and the fixture half of `report-card-data.ts` are deleted, not refactored, once F-AC-06 Part 5 ships.

## D-304 — Marks entry demo cut: the paper names its teacher, one save function with per-row checks, Publish waits for complete marks · ACCEPTED · 2026-09-26

**Context:** F-AC-06 Part 3 (marks entry) builds on grade scales (D-302) and exams (D-303). The spec gives marks to "the primary or assistant teacher of that section_subject", but `section_subjects` does not exist yet (D-303 item 2). D-303 also left a known gap: Publish had no marks-completeness gate, which Part 4 was to add. The owner confirmed D-302's rounding on 2026-09-26 and decided how offline marks will sync when that queue is built.

**Decision:**

1. **The paper names its teacher.** `exam_subjects.teacher_id` (composite FK to `workspace_members`) is set by an owner/admin on the exam page. Who may enter and read a paper's marks is `app.can_enter_marks`: an active owner/admin, the paper's teacher or the section's class teacher (each still an active owner/admin/teacher). Staff read; parents never read `marks` (they will read published results, Part 7); a teacher of another subject reads nothing (AC14, pgTAP). When `section_subjects` lands, it seeds `teacher_id`.
2. **One writer.** `marks` has no INSERT/UPDATE/DELETE grant. `public.save_marks` (SECURITY DEFINER) upserts every valid row in one statement: only while the exam is in `marks_entry` (`ENTRY_CLOSED`) and the paper is not `locked` (`SUBJECT_LOCKED`), only students actively enrolled in the paper's section (`STUDENT_NOT_ENROLLED`), idempotent by key (`app.idempotency_keys`, scope `save_marks`).
3. **Per-row checks, so valid rows still save (§4.2, AC12).** A value below 0, above full marks or with more than 2 decimals is `MARK_OUT_OF_RANGE` for that row. **The version check is per row:** each row carries the `updated_at` it was loaded with (null for a new row); a row changed by someone else since is `CONFLICT` and is not overwritten. The response lists `rejected` rows and every saved row's new `updated_at`. The first save moves the paper `pending → entering`.
4. **Audit.** Every save that writes marks logs one paper-level `marks.entered` event with the count (§4.2), not one row per student; a mark is audited when it changes (before/after) or is removed, so an overwritten value is never lost.
5. **Publish waits for complete marks (Part 4's gate, D-303's known gap).** `app.tg_exams_publish_gate` refuses `→ published` with `MARKS_INCOMPLETE` while any active enrolment in any of the exam's papers has no mark row. Absent and exempt count as marked.
6. **Rounding (owner-confirmed 2026-09-26):** "D-302 stands: percentages to 2 decimals, no rounding" before banding — 79.5 → A, 32.5 → F. Marks are stored as entered, to 2 decimals.
7. **Offline sync, when it is built (owner decision 2026-09-26, recorded in F-AC-06 §11; not built now):** per cell, the later edit wins, and the overwritten value is kept in the audit trail. D-71 item 3 records the same rule for the platform-wide outbox ("later" = `client_edited_at`).
8. **Screen.** `/app/marks/[examSubjectId]` follows F-AC-06 §4.2/§6 (72 px rows, a large `inputmode="decimal"` input, Enter to the next student so the keypad stays up, Absent/Exempt chips acting on the selected student, a sticky "24/40 · Save"). The desktop is the same list with ↑/↓, Shift+Enter, Esc, A, E and Ctrl+S. It uses `ui/input` and `ui/toggle-group`, not `MarkCell`: `MarkCell` clamps an out-of-range value to full marks on blur, which AC12 forbids (60 of 50 must be refused, not saved as 50), and DESIGN-SYSTEM §5.2's custom one-student keypad is not what the spec asks for. Both are left for the design lane to reconcile. Bengali digits typed on a Bangla keypad are read as Western; the UI shows Western digits.
9. **Fix found on the way:** `exam_subjects` had no FK to `sections`, so `getExam`'s `sections(...)` embed had no PostgREST relationship. A composite `(section_id, workspace_id) → sections` FK is added.

**Why:** the rules a teacher can break by mistake — a wrong subject, a closed exam, 60 of 50, a colleague's newer mark — are refused by the database row by row, and a correct row is never held back by a wrong one.

**Review of PR #60 (2026-09-26):**

- Ctrl/Cmd/Alt+A and +E keep their browser meaning; only a bare A or E marks Absent or Exempt.
- A dropped connection during Save keeps every typed mark and the idempotency key, so the retry replays the same save.
- After a `CONFLICT` the row adopts the server's value and version and keeps what was typed: Save again keeps mine, Esc takes theirs.
- The Absent/Exempt badge describes the input; the chip group is labelled with the selected student.
- One paper-level `marks.entered` audit event per save that writes marks (security LOW-1); a `TODO(F-AC-06 Part 4)` marks where the entry-date window goes (LOW-2).
- The exam page's "n/m marked" comes from `public.exam_marks_progress` (SQL counts, currently enrolled students only — the gate's definition), not from `marks(count)` and a full enrolment list.
- `updateExamSubject` leaves the teacher alone when `teacherId` is omitted; null clears it.
- The journey fills one of two subjects' papers, so the `MARKS_INCOMPLETE` refusal is asserted every time.
- **Declined:** splitting the two new `exam_subjects` FKs into `NOT VALID` + `VALIDATE`. The migration runs in one transaction, so it gains nothing, and the table is tiny.

**Consequences:** `supabase/tests/54_marks.sql` (40 assertions). Deferred: the entry date window (`entry_opens_on/closes_on`), submit/lock/unlock with reasons and the progress screen (Part 4), the offline queue and autosave draft (Part 4, item 7), components (Written/MCQ), paste-a-column and the distribution histogram on desktop, `withheld`, remarks, a teacher's "my papers" list and dashboard card. Teachers reach the screen from the exam page.

## D-404 — F-ID-10 Part 1: `user_preferences` already existed; ADD `ui_mode`/`text_size` rather than create the table, and follow its real (full-CRUD) RLS over §3's "no delete grant" line · ACCEPTED · 2026-09-26

**Context:** F-ID-10 §3 and D-403 point 1 both say "`user_preferences` is not built yet, so F-ID-10 Part 1 creates it". That was true when D-403 was written; it is not true on `main` at Part 1's start. `20260917010100_identity.sql` (F-ID-01/F-ID-02's earlier demo-cut work) already created `public.user_preferences` with eight columns (`theme_mode`, `palette`, `density`, `language`, `timezone`, `email_digest`, `push_enabled`, `channels`), `class U1` RLS (DATA-MODEL.md §1.7: select/insert/update **and delete**, own row only, no platform-admin read), and a row inserted eagerly for every account by `app.handle_new_user()`. F-ID-02 §3's own proposed shape for this table (which D-403 defers to) separately says "no delete grant" — narrower than what is actually granted.

**Decision:**

1. **ADD, don't create.** `20260925300315_user_preferences_ui.sql` adds two enum types (`public.ui_mode` `full|basic`, `public.text_size` `normal|large|xlarge`) and `alter table ... add column if not exists` for both, defaulting to `full`/`normal`. No new table, no new RLS policy, no new grant, no new trigger.
2. **DATA-MODEL.md wins (the spec's own rule, F-ID-10 §3).** The two new columns ride the table's existing `class U1` RLS unchanged — full CRUD on the caller's own row, no platform-admin bypass. `60_user_preferences_ui.sql`'s pgTAP proves isolation and escalation against the REAL policy, not the narrower one F-ID-10 §3 described; it does not assert "no delete", because that would be false against `main`.
3. **No lazy-create path needed.** `app.handle_new_user()` already inserts the row for every account, so "no row needed to read, first write upserts" (F-ID-10 §3) is satisfied by the existing trigger — `fetchUiPreferences` still resolves the documented defaults if a row is ever absent (defensive, not load-bearing), and `upsertUiPreferences` always upserts rather than plain-updating, so a future change to that trigger cannot silently break a write.
4. **`/app/home` in this Part is a placeholder, not F-ID-10 Part 2's home.** Per the design-lane builder brief: "the basic HOME and the CLASS HUB are Parts 2-3, not this PR: a 'basic' choice may only store the preference in this Part." Turning basic mode on still saves the preference and still navigates to `/app/home` (AC1), and "Switch to full app" still works from there (AC2) and from the full app's Settings → Display and user menu — but `/app/home` itself is a minimal screen that says outright, in its own copy (en/bn), that the class-by-class home is coming in a later update, rather than a partially-built version of Part 2's `BasicShell`/`TodayStrip`/`ClassBlock`.
5. **`logo.tsx`'s `text-[17px]` is left as px, not rem-audited.** Its own comment already says the 17px/-0.02em pairing "deliberately match[es] acadigma-website's wordmark, not our type scale" — a brand logotype pin, not a missed instance of §5.2's rem audit. The one ordinary UI instance found (`audit-viewer.tsx`'s avatar-fallback initial, `text-[10px]`) was converted to `text-[0.625rem]`.

**Why:** the alternative — writing a migration that recreates a table already on `main`, or narrowing an already-shipped, already-depended-on RLS grant to match a sentence written before the table existed — is a much larger, riskier change for a demo-scope Part, and DATA-MODEL.md is the document both F-ID-10 §3 and D-403 already name as authoritative over their own proposed shapes.

**Consequences:** F-ID-10 §3's "creates the table if it does not exist" line and §11 now note this deviation. F-ID-02 Part 1 (whoever picks it up) adds its remaining columns to the same table with more `alter table` statements, same as before — nothing here blocks that. The pre-existing delete grant on `user_preferences` is unchanged and out of this Part's scope; if it should be narrowed, that is F-ID-02's call to make against its own spec, not a side effect of this Part's two columns.

## D-72 — Student import joins the demo cut, alerting and a real-PostgREST CI job go on the lead queue, four standing rules · ACCEPTED · 2026-09-26

**Context:** Lead decision under owner authorization, 2026-09-26, made after a QA pass (not an alert) caught two production bugs the same night — `/app/classes` throwing a generic error for every real user (#61, `PGRST201` from an un-hinted `profiles` embed) and every PDF download 500ing (#62/#67, pdfkit's standard fonts not resolving in the Vercel bundle). Both were reviewing the demo-cut sprint (attendance, classes, exams, students, marks, report cards) that has been landing since 2026-09-25.

**Decision:**

1. **Student Excel/CSV import joins the demo cut.** A school with an 800-student register will not retype it by hand — the forward-deployed approach (visit the school, import their existing file) only works if import exists. This is a new Part on the identity lane's queue (student roster area, alongside `F-AC-02`), ahead of anything not needed for a first school visit. It ships with a preview step (show what will be created/changed before committing) and an import report (what succeeded, what was skipped, why). Scope, column mapping and error handling are specified when that Part is briefed; this entry only puts it in the queue and names its constraints (items 4 and the onboarding checklist below).
2. **A "first week at a school" onboarding checklist** for the owner's own visits, `docs/plan/ONBOARDING-CHECKLIST.md`, published alongside this decision: what to ask for before the visit, what to do on day 1 (create the school, import the register), what to do days 2-5 (one real roll call, one test's marks, one report card), and what to log afterwards. It exists because the owner is the first support line for every school and the messy realities (a register with merged cells, a teacher who can't read small text) surface fastest in person.
3. **Production error alerting goes on the lead queue.** Tonight's two bugs were found only because someone happened to click around, not because anything paged anyone. A workspace can be down for hours with nobody the wiser. Scope (which errors, which channel, what counts as noise) is decided when that Part is briefed; this entry only commits to building it soon, ahead of most feature work.
4. **A CI job runs a real PostgREST** (`supabase start`, matching `db.yml`'s local-stack pattern) and exercises repository functions against it. This is the exact gap #61 fell through: the `unit` job's vitest suite mocks the Supabase client, so it never resolves a real PostgREST relationship; the `db` job's pgTAP runs against bare `postgres:17` with no PostgREST in front of it at all; the one e2e journey that opens `/app/classes` as a real owner is skip-gated on `E2E_LIVE_SUPABASE` + OQ-27, which CI never sets. #61's own opt-in integration test (`DB_LOCAL_SUPABASE=1`) is the seed for this job — wiring it to run by default in CI, not opt-in, closes the gap for every embed and every RLS-through-API path, not just this one.
5. **Standing rule — CSV export escaping.** Any cell whose first character is `=`, `+`, `-`, `@`, tab or carriage return is prefixed with a leading `'` (or otherwise neutralised) before being written to a CSV a human will open in Excel/Sheets, to close the formula-injection class of bug. Applies to every CSV export in the app, present and future.
6. **Standing rule — import reports are purged after 90 days.** An import report can contain a full uploaded register (names, IDs, guardian contacts) beyond what the resulting student records need to retain; keeping it indefinitely widens the data-protection surface for no ongoing benefit. The purge job itself is a later Part (tracked against the student-import Part, item 1); this entry fixes the retention period now so the import feature is designed against it from the start.
7. **Standing rule — the SMS sender never messages a workspace flagged demo.** A demo workspace (used for sales visits, screenshots, the owner's own testing) must never be able to text a real phone number by accident; the sender checks the workspace's demo flag before every send, not just at the call site that happens to be reviewed.
8. **System-design principle, recorded:** no queues, read replicas or sharding until a measured bottleneck forces them. The first candidate is bulk report-card generation (F-OP-03) — if rendering 40 cards in one request measurably exceeds roughly 20 seconds, that is the trigger to move it to a background job, not a guess made now. Nothing today crosses that line, so nothing is built for it today.

**Why:** an 800-student register is the actual blocker to onboarding a real school, so it outranks features that assume the data is already in the system. Both bugs tonight were invisible without a human looking — alerting and a CI job that talks to real PostgREST are the two cheapest changes that would have caught them automatically, one in production and one before merge. The four standing rules close small, specific, previously-unstated gaps (a spreadsheet formula, an unbounded retention period, a demo/production leak, premature infrastructure) before they turn into incidents or wasted work.

**Consequences:** `docs/plan/ROADMAP.md` gains the student-import Part (identity lane) and the alerting Part (lead queue); `docs/plan/ONBOARDING-CHECKLIST.md` is published and linked from `docs/README.md`; the real-PostgREST CI job is scoped as a `db.yml`/`ci.yml` change when picked up (builds on #61's `DB_LOCAL_SUPABASE=1` test); the CSV-escaping and 90-day-purge rules bind every future CSV export and the import-report feature once built; the demo-workspace SMS rule binds the messaging Part (F-OP-05, billing lane queue) when it ships its sender.

## D-106 — F-AC-02 §4.7 bulk student import, demo cut: fixed bilingual template, server-side parse, every row through `admit_student` · ACCEPTED · 2026-09-26

**Context (lead decision under owner authorization, 2026-09-26):** schools will not retype an 800-student register to try the product, and the register lives in Excel. Importing it on day one is how the first schools sign. F-AC-02 §4.7/Part 7 specifies the import; D-103 made `public.admit_student` the only way to create a student.

**Decision:**

1. **One door, again.** The app parses and validates the file and stores the report in `student_import_batches` (status `preview`; nothing is admitted). `public.import_student_batch` admits the valid rows, 100 per call, each through `public.admit_student` — codes, roll numbers, guardian, enrolment, audit and every rule are the quick-admit ones, with no second writer. Idempotent twice over: the batch row is locked and each row's outcome is written back in the same transaction, and each row's `admit_student` key is `md5(batch_id || ':' || line)::uuid`, so a double tap, a retry after a timeout or a reload never admits a row twice. A row already on this year's roster (same section, `lower(first || ' ' || last)` and date of birth) is refused twice over: the preview marks it `already_admitted` with the existing `STU-` code (from `public.student_import_existing`, SECURITY INVOKER, one jsonb value so no row cap), and `import_student_batch` re-checks it before admitting, so re-uploading a register — or importing two previews of it — never creates twins; a repeat of the same student inside one file is `duplicate_in_file`. A row the database refuses (`ROLL_TAKEN`, `SECTION_ARCHIVED`, …) is marked `failed` with that code and the rest continue; a read-only school or a lost role stops the call. The batch id is every audit row's correlation id. The table has no UPDATE/DELETE grant; `report` is redacted from the audit trail (dates of birth, phones).
2. **Fixed template, no column mapping.** §5.17 fixes the column order, so there is no mapping step. Headers are "English / বাংলা"; a column is recognised by the whole header, either half or its key, in any order; unknown columns are ignored and listed. **Demo-cut columns are the ones `admit_student` stores:** first name, last name, Bangla name (optional), date of birth, gender, class, section, roll number (optional), guardian relation, name and mobile. Religion, blood group, address, second guardian, previous school, notes and `student_code` wait for the fields they belong to (§3 deviations). **Class and section are two columns** (not §5.17's one "Class 6 – A" cell): matched case- and space-insensitively within the current year's live sections — class by English name, Bangla name (with or without "শ্রেণি"), or number however written ("6", "Class 6", "6th", "Six", "Class Six", "VI", "৬"), section by name; an unknown class or section is a row error, never created. The header is the first of the top 10 rows naming at least three template columns (registers often start with a title), and errors keep the spreadsheet's own line numbers. A mobile number Excel stored as a number (lost leading 0) gets its 0 back. Values accept Bangla (ছেলে/মেয়ে, বাবা/মা, Bangla digits) and dates as `YYYY-MM-DD` or `DD/MM/YYYY`. Every row finally passes `quickAdmitInputSchema`, so an import can never accept what the admit sheet refuses. A roll number used twice for one section in the file is a row error.
3. **Parsing server-side, two small libraries.** CSV through **`papaparse` 5.7.0** (named by §4.7; RFC 4180, no dependencies). `.xlsx` through **`read-excel-file` 9.3.10** (MIT, maintained, 4 small dependencies, reads the first sheet, date cells to dates). Rejected: SheetJS `xlsx` from npm (frozen at 0.18.5 with published advisories), `exceljs` (much larger, writes as well as reads). Nothing installed parsed spreadsheets. `.xls` (old binary Excel) is not accepted; schools save as .xlsx. **Zip-bomb guard (corrected after the security re-check):** the 1 MB upload does **not** bound decompression — a zip can expand a thousandfold, and read-excel-file's unzipper walks the local headers and inflates every `.xml` it finds, whatever the sizes claim. So read-excel-file never sees the uploaded bytes: `fflate` 0.8.3 (read-excel-file's own dependency, now direct) reads the central directory, refuses any entry declaring more than 10 MB unpacked or 20 MB in total ("too large"), unpacks only `[Content_Types].xml`, the rels, workbook, styles, shared strings and sheet entries — each into a buffer of its declared size, so a lying size truncates (the workbook then fails to parse) instead of expanding — and re-zips just those parts for read-excel-file. An entry hidden from the central directory is never unpacked at all.
4. **Limits:** 2,000 rows (the spec states none) and **1 MB** instead of §7's 5 MB — a server action's body limit is 1 MB and a 2,000-row register is about 300 KB as CSV and less as .xlsx. A preview older than 24 h must be uploaded again (`BATCH_EXPIRED`, §7).
5. **BD phone rule unchanged:** mobiles are `01[3-9]` plus 8 digits, as everywhere else (`common.ts`). An earlier draft of this PR accepted the unassigned 010 range for test data; the lead reversed that — fixtures use valid-format numbers (`+88017000000xx`) instead. The database itself does not check the phone format (only E.164), and the SQL demo seed and pgTAP keep using the unassigned 010 range; the security review accepted this as LOW. **Rule for F-OP-05:** the SMS sender must never message a workspace flagged demo, because demo and test data use valid-format numbers that could belong to real people.
6. **Permission** `students.import` (owner, admin) joins the matrix; both actions call `requireWritable`.
7. **Deferred:** "download errors as CSV", the per-cell highlight in a desktop table (errors are a list: line, column, what is wrong, what was typed), duplicate-student warnings (§5.7), "update existing by `student_code`" (OQ 6 stays insert-only), the queued "Invite N guardians" job (no invite flow exists yet; nothing is ever sent by an import), the §7 rate limits (owner/admin only; every row still passes `admit_student`). The `import_status` enum is `preview`, `importing`, `completed` only (validation is synchronous; a refused row is reported, the batch never "fails").
8. **Rules for later Parts:** any future CSV export (including "download errors as CSV") prefixes a cell starting with `=`, `+`, `-`, `@`, tab or carriage return with `'` (formula injection). Import reports hold dates of birth and phones: a later Part adds a retention purge of `student_import_batches.report` after 90 days.

**Why:** the fastest way to get a real school's register in, with no new way to create a student and nothing a pgTAP case cannot break.

**Consequences:** migration `20260925300316_student_import_batches.sql`; `supabase/tests/36_student_import.sql` (45 assertions); `12_function_grants_invariant.sql` allow-lists `import_student_batch` and `student_import_existing`. The report array is built once per call (`jsonb[]` then `to_jsonb`), and the import page allows 60 s (`maxDuration`). A later admission-wizard or promotion import must still go through `admit_student` (or its successor). When guardian invites land, the import's success screen offers them as a separate queued action (AC 11).

## D-207 — F-OP-03 Part 5 bulk report cards, demo cut: new `report_card_bulk` kind, `pdf-lib` merge with duplex padding, per-student failure isolation via `report_run_items` · ACCEPTED · 2026-09-26

**Context:** F-OP-03 Part 5 (§8) is "chunked rendering with per-student items, merged + split output, ordering options, duplex padding, the publish/unpublish flow and the parent-side list." The task narrowed this to a demo cut: one merged PDF (no split files, no publish-to-parents — that is F-AC-06 Part 7), synchronous like D-205, on the same D-206 fixture until F-AC-06 Part 5 (marks/results, building in the billing lane, PR #68) lands.

**Decision:**

1. **New kind, not a `report_card` param.** `report_kind` gains `report_card_bulk` (`alter type ... add value`, migration `20260926022716_report_card_bulk_kind.sql`) rather than widening `ReportCardParams`. The params shape genuinely differs (`sectionId` + `order` + `duplex` vs. one `studentId`) and the spec already names the action `report.render.report_card_bulk` distinctly from `report.render.report_card` (§2) — a new discriminated-union member is the smaller, more honest change than an optional-everything params object.
2. **One merge utility, `pdf-lib`.** `packages/pdf`'s `mergeReportCardBulkPdf` (new dependency, `pdf-lib` 1.17.1 — no already-installed package merges PDF byte streams, and hand-rolling page-tree merging is reinventing PDF internals) copies each already-rendered single-student PDF's pages into one document, in the caller's chosen order. Duplex padding: after each student's pages, if the running page count is odd, one blank A4 page is appended — this keeps every student's block starting on an odd page unconditionally, including the last, so 40 one-page cards duplex-pad to exactly 80 pages (verified: `packages/pdf/src/merge.test.ts`).
3. **Failure isolation via `report_run_items`, no pre-creation.** Unlike the full spec's "34 items exist before rendering starts" (progress UI, not built in this demo cut), this Part fetches every roster id through `getReportCardData` — the seam, called once per student — and writes ALL `report_run_items` rows (success and failure) in one batch after rendering, via the new `createReportRunItems` repository function (`packages/db/repositories/reports.ts`). A student whose seam call or render throws is recorded `failed` with the seam's own message and excluded from the merge; the run still succeeds for every other student. `report_runs.item_count` is now set for bulk runs (`markReportRunReady`'s new optional 5th argument).
4. **Roster resolution is a second, narrow seam.** `getReportCardBulkStudentIds` (`report-card-data.ts`) resolves a section's student ids — today the D-206 fixture's one Class 6-ক section (`FIXTURE_SECTION_ID`), refused in production exactly like `getReportCardData`. It is NOT the DTO seam: the bulk render step still calls `getReportCardData` once per id this returns, so a student it cannot build a card for (missing data today; once F-AC-06 Part 5 lands, also a real result with no roll number — `getReportCard` refuses to print one, D-305) surfaces as a per-item failure, never a silent drop. The real body is `getSectionResults(ctx, client, examId, sectionId)` (`@acadigma/db/repositories/results`, F-AC-06 Part 5) — it does not filter out a student with no roll number either, so the refusal stays visible at the `getReportCardData` layer where D-305 already puts it.
5. **Synchronous, like D-205.** No chunking, no drainer, no Realtime progress — `renderReportCardBulkPdf` (`apps/web/.../report-card-bulk.ts`) runs the whole section in-request, shared by `createReportRun`'s render step and `GET /api/pdf/[runId]` (which re-renders on every download, same "no Storage yet" precedent as the single card, D-205 item 11). Measured: 40 one-page fixture cards render + merge in ~3.4 s with no duplex, ~2.9 s with duplex (`packages/pdf/src/bulk-perf.test.ts`, real numbers in the test report) — well under the spec §10 budget of 25 s, so no background job is justified yet.
6. **Permission `report.render.report_card_bulk`:** owner, admin, teacher (interim: the class-teacher-of-that-section limit is not enforced at this layer yet, same interim D-206 already established for the single card — enforced by RLS on `results` once real). Staff is deliberately excluded (spec §2: bulk is not an office-counter action, unlike the single card D-206 gave staff).
7. **Security LOW from the #63 review (defence in depth), same migration.** `report_runs_select` and `report_run_items_select` now additionally require `kind = 'report_card' OR has_role(...,{owner,admin,teacher})` — staff can already only ever see their own runs (`requested_by = self`) and can already only ever insert a `report_card` run (D-206), so this was already true in practice; the new clause makes the SELECT policy itself enforce it, not only the insert-time restriction. pgTAP `43_report_card_bulk_kind.sql` proves a report_card_bulk row directly attributed to a staff member (a state the insert policy already refuses to create, simulated as the table owner) stays invisible to that staff member.
8. **UI is a demo button on `/app/reports`, not yet on the results preview.** The lead's review of this PR named the real home: the exam results preview's "Print all report cards" action (`/app/exams/[id]/results`, F-AC-06 Part 5, PR #68 — not yet on `main`). That page and `getSectionResults` do not exist on `main` today, so this PR cannot wire into it without inventing scaffolding in the academics/billing lane's own folder. `GenerateReportCardBulkButton` on `/app/reports` is the same fixture-guarded, `NODE_ENV !== 'production'` demo pattern `GenerateReportCardButton` (D-206) already uses, and is expected to move (or be duplicated as a real entry point) once #68 merges and this PR's own `origin/main` merge lands.

**Why:** the alternative — waiting for #68 to merge before writing any bulk-rendering code — would idle this lane for no reason: the seam's signature (`studentId, examId`) does not change, the merge/duplex logic has nothing to do with where the data comes from, and D-206 already established that a narrow, explicitly-temporary fixture seam is cheaper than inventing fake data or blocking on another lane.

**Review update (2026-09-26) — #68 merged, real seam, lead review fixes.** #68 (D-305) landed: `getReportCardData` is real, `report-card-fixture.ts` is deleted, and `ReportCardDto.rollNumber` is now **nullable** — D-305 item 9: a student with no roll number still gets a card (roll prints "—"); the seam refuses only a student with **no paper counted at all**. This PR's own text above (items 4, 8) is corrected accordingly, not rewritten in place:

- **The bulk button now lives on the results preview for real** — `/app/exams/[id]/results`, next to each row's `GenerateReportCardButton`, same `report.render.report_card_bulk` permission gate, real `sectionId`/`examId` (no more fixture ids). `/app/reports` no longer has any report-card button, single or bulk (matching #68's own move of the single-card button off that page).
- **Sort-by-roll now treats `rollNumber` as nullable**, matching the real DTO: `(a.rollNumber ?? Number.MAX_SAFE_INTEGER) - (b.rollNumber ?? Number.MAX_SAFE_INTEGER)`, tie-broken by the locale-appropriate name (`localeCompare(..., locale)`), so a roll-less student sorts after every rolled student instead of corrupting the sort with `NaN`.
- **An all-failed run now writes its `report_run_items` before the run is marked failed**, not only on success — a teacher who cannot read the section at all (e.g. not its class teacher) gets a run that says _why_ (every item's `errorDetail`), not just a bare `no_data`.
- **`createReportRunItems`'s own result is checked.** A failed items write now marks the run `failed` (`items_write_failed`) instead of `ready` — a run must never claim success while its own bookkeeping silently dropped.
- **pgTAP gains a positive regression case**: staff still read their own `report_card` run after the tightened SELECT policy (not only the negative "staff cannot see a bulk run" case already proven).

**Consequences:** `packages/pdf/src/merge.ts`/`merge.test.ts` (the merge/duplex algorithm, including mixed page-count cases); `packages/pdf/src/bulk-perf.test.ts` (the §10 performance budget, fixture-independent by design — it builds its own DTOs rather than importing `report-card-fixture.ts`, so it keeps proving the render/merge budget after that fixture is deleted); `apps/web/.../report-card-bulk.ts`/`report-card-bulk.test.ts` (failure isolation, ordering, the roster-resolver error path); `apps/web/.../report-card-data.test.ts` (the new roster resolver's own fixture contract); `actions.test.ts`/`route.test.ts` extended for the `report_card_bulk` branch. `getReportCardBulkStudentIds` and the fixture half of `report-card-data.ts`/`report-card-fixture.ts` are deleted, not refactored, once F-AC-06 Part 5 ships and this branch merges `origin/main`.

## D-305 — Results demo cut: computed in SQL from the exam's snapshot on locked marks, a missing mark makes one student incomplete, recomputed whole · ACCEPTED · 2026-09-26

**Context:** F-AC-06 Part 5 computes results and rank. It builds on the grading snapshot (D-303), marks (D-304) and D-302's rounding. `section_subjects`, the 4th-subject rule (Part 6), force-absent (Part 4) and publishing (Part 7) do not exist yet. F-OP-03's report card (D-206, PR #63) needs real results behind its seam, and the lead revised its DTO on 2026-09-26: result `pass | fail | incomplete | withheld`, nullable GPA/letter/rank, a per-subject status and subject kind, `gpaWithoutOptional`, and a rank tie flag.

**Decision:**

1. **Tables.** `results` (one per student per exam: totals, percentage, GPA, `gpa_without_optional`, overall letter, `result_status`, failed papers, section rank, who and when) and `result_subject_lines` (one per paper: subject name snapshotted, full and pass marks, status, `subject_kind`, mark, percentage, letter, grade point, passed). Every FK is composite with `workspace_id`; a result's `(exam_id, section_id)` must be a section that sits the exam. `result_status` has all four values and `subject_kind` both, so the report card's shape is fixed now, but `withheld` and `optional_fourth` are not produced until Parts 7 and 6. A line's `status` is null while the paper has no mark yet.
2. **One writer.** `app.compute_results` (called by `public.compute_results`, owner/admin) has no client write grant beside it. It reads `exams.grading_snapshot` (D-303 review), never the live scale, through `app.snapshot_band` — a snapshot-reading twin of `app.band_for`, not an overload, because an overload on `(jsonb, numeric)` makes existing untyped calls to `band_for` ambiguous.
3. **The rules** (§5.1–5.6, §5.8), with D-302's rounding: paper % = `round_half_up(100 × obtained / full, 2)`, banded as is (79.5 → A, 32.5 → F). Absent = 0 % and a failed paper. Exempt is left out of totals, percentage and GPA. **A paper is passed when `obtained ≥ pass_marks` AND its band is not a fail band** (lead decision on review, 2026-09-26; §5.6 is rewritten to match): AND respects the paper's own pass mark and never passes a printed F, and the flag flips exactly at the pass mark (D-303 item 4). The preview shows a visible "(Fail)" next to a failed paper's letter, since under AND a 35/100 with pass mark 40 prints "C". GPA = `round_half_up(mean grade point of non-exempt papers, 2)` (4, 4, 3.5, 3 → 3.625 → 3.63), 0.00 when any paper failed and `fail_any_subject_zero_gpa` is on. Overall letter = the first band, top down, whose grade point is ≤ the GPA.
4. **Rank.** `rank() over (partition by section order by gpa desc nulls last, total_obtained desc, percentage desc)`. **`student_code` is not a rank key** (§5.8 is rewritten to match): every student's code is unique, so adding it to `rank()`'s ORDER BY would break every tie. It only orders the list. A student with no GPA (incomplete, or every paper exempt) gets no rank and, sorting last, takes no rank from anyone. The snapshot's `rank_by` is checked: only `gpa_then_total` exists (`RANK_BY_UNSUPPORTED` otherwise).
5. **When, and missing marks (lead decision on review, 2026-09-26: one missing mark must not block a school).** Only on a `marks_locked` exam (`MARKS_NOT_LOCKED`). Marks are left-joined: a student with a paper not yet marked is `incomplete` — no GPA, no letter, no rank (§5.10); totals and percentage cover the papers marked so far. D-304's publish gate is unchanged, so nothing incomplete is ever published.
6. **Recompute.** Each run locks the exam row, deletes the exam's results (lines cascade) and inserts them again in one statement, then logs one `results.computed` event with the counts. Going back to `marks_entry` deletes the results (`app.tg_exams_clear_results`) and logs one `results.cleared` event with the exam id and the count, so a report card never prints a result the marks no longer support.
7. **Who reads.** Owner/admin/staff and the active class teacher of a live (not archived) section (`app.can_read_results`); lines follow their result. Parents read nothing until Part 7. New permissions `results.read` (owner, admin, teacher, staff; RLS narrows teachers) and `results.compute` (owner, admin).
8. **Screens.** "Compute results" and "View results" on the exam page; `/app/exams/[id]/results` lists one section in rank order (rank, "tied", name, roll, GPA, grade, pass/fail), and a row opens to its per-subject marks, %, letter and grade point. Western digits in both languages. The picker and list are one client component: referenced one by one from the server page, the `radix-ui` barrel came in whole (273 kB first-load JS, over the 250 kB budget); as one client boundary the page is 199 kB.
9. **The report card seam is real.** #63 merged first, so this PR implements `getReportCardData(supabase, ctx, studentId, examId)`: it calls `getReportCard` (`packages/db/src/repositories/results.ts`) through the caller's RLS client and checks the DTO against `reportCardDtoSchema` before the template sees it. A teacher gets a card only for a section they are the active class teacher of; anyone else's student is `not_found` (pgTAP: a 6 B teacher and another school's teacher read nothing of 6 A). `rankOf` is the number of ranked students in the section, `rankTied` whether another shares the rank; attendance is the student's records from the year's first day to the exam's last day (or today), weighted by the school's policy (F-AC-03 §5.4), as a whole percentage, flagged below `min_attendance_bp` (default 75 %). A student with no roll number still gets a card (`rollNumber` is nullable in `ReportCardDto`; the card prints "—"); with no attendance taken, `percent` is null and not flagged. Only a student with no paper counting yet has no card. Each button's accessible name includes the student's name. **The fixture is deleted** (`report-card-fixture.ts` and its test, and the production guard with it), and the Class 6 sample button leaves `/app/reports`: the same button now sits in each student's row on the results preview, so it only offers a card whose result exists.
10. **Parity.** `computeResults` in `packages/domain/src/grading/results.ts` is the TypeScript reference. The golden fixture (12 students × 6 papers, hand-computed, with an incomplete student and a GPA half-up case) lives once, between `-- parity:golden_*` markers in `supabase/tests/55_results.sql`; pgTAP asserts `app.compute_results` against it row by row and `results.test.ts` asserts the TS against the same rows.

**Review of PR #68 (2026-09-26):** incomplete instead of refusing the exam (item 5); AND kept for "passed" (item 3); nullable roll number and attendance percent on the card (item 9); `results.cleared` audit (item 6); archived sections excluded from the class-teacher read (item 7); `results.percentage` checked 0–100; the preview's fail marker is visible, rank has a screen-reader label, GPA is announced once, paper rows are keyed by paper id; the exam page's status region is always mounted.

**Why:** the rules a report card depends on are computed once, in the database, from a grading policy that cannot change under them, and a second implementation is held to the same numbers so they cannot drift (§10's "single most important test").

**Consequences:** migration `20260925300318_results.sql` (written as 300314; renamed on the lead's assignment after #64 took 300315); `supabase/tests/55_results.sql` (40 assertions); `12_function_grants_invariant.sql` allow-lists `compute_results`. Deferred: `grade_rank`, `published`/`frozen_payload` and parent read (Part 7), force-absent (Part 4), the 4th subject and `gpa_without_optional` values (Part 6), idempotency keys and rate limits on `computeResults`, the GPA distribution chart and search/filter on the preview.

## D-107 — F-AC-01 Part 5 demo cut: `section_subjects` with one teacher, one replace-the-list function, `listMySections` for the basic home, and exam papers take their teacher from it · ACCEPTED · 2026-09-26

**Context (lead assignment, 2026-09-26):** the basic-mode home (F-ID-10 Part 2, D-403) shows one block per class a teacher teaches. Until now that came only from `sections.class_teacher_id` and, as an interim, `exam_subjects.teacher_id` (D-304). In a Bangladeshi school most teachers teach subjects and are class teacher of at most one section, so they would see an empty home. D-403 (c) makes F-AC-01 Part 5 a prerequisite of the basic-mode demo. D-303 item 2 and D-304 item 1 said `section_subjects` would seed the paper's teacher once it existed.

**Decision:**

1. **Table, demo cut.** `section_subjects(section_id, subject_id, teacher_id)`. `unique (section_id, subject_id)`. The foreign keys to `sections`, `subjects` and `workspace_members` are composite with `workspace_id` (D-102's pattern), so a row cannot mix schools. `teacher_id` is DATA-MODEL's name (§3 of the spec calls it `primary_teacher_id`; DATA-MODEL wins). Not built yet: assistants (`section_subject_teachers`), `periods_per_week`, full/pass marks, `is_optional_fourth`, room, `academic_year_id` (the section carries the year), `is_active`, and copying from `grade_level_subjects` (that table does not exist).
2. **Rules in the database.** A teacher must be an active owner, admin or teacher (`MEMBER_NOT_ELIGIBLE`, same trigger as class teachers). A member who is removed or moved to staff or parent stops teaching their subjects (`app.tg_members_release_subject_teacher`, the §4.5 half D-102 left for this Part). RLS is class T2: owner, admin, teacher and staff read; owner and admin write. There is also the standard trigger set: tenant freeze, `updated_at`, generic audit with catalogue rows, `require_writable`, immutable `created_by`.
3. **Delete, not archive.** Nothing references a section_subject yet, so taking a subject off a section deletes the row (DELETE policy for owner/admin). When timetable, lesson or homework rows reference it with `on delete restrict`, a used row can no longer be deleted, and that Part adds archive.
4. **One write, the whole list.** `public.set_section_subjects(workspace_id, section_id, jsonb)` is SECURITY INVOKER, so RLS and the triggers decide. It replaces a live section's subject list: subjects missing from the list are deleted, and the rest are inserted or get their new teacher (an unchanged row is not touched, so it is not audited). It raises `SECTION_NOT_FOUND` for an archived section or one outside the workspace. The Classes screen saves one sheet per section with one button, so a partial save is impossible. `setSectionSubjectsInputSchema` refuses a subject listed twice and caps the list at 40.
5. **UI.** On `/app/classes` each section row gets a **Subjects** button (owner/admin) that opens a sheet. The sheet has a checkbox per catalogue subject and, when ticked, a teacher picker. Pickers are native selects (the page's existing pattern and phone-friendly), with 44 px targets. Every reader sees "n subjects" on the row. English and Bangla, Western digits.
6. **`listMySections(supabase, ctx): Promise<Result<MySection[], ApiError>>`** (`@acadigma/db`, type in `@acadigma/contracts`). It returns the current year's live sections where the signed-in member is the class teacher, a `section_subjects` teacher, or both, with `isClassTeacher` and the subjects they teach there (name and Bangla name). Order is by grade `level_number`, then section name. An owner or admin gets only their own classes. The "All classes" block is the caller's (D-403 (b)). No class teacher or subject assignment means an empty list. The design lane's basic home calls it.
7. **Exam papers default to it.** `public.create_exam` now fills each paper's `exam_subjects.teacher_id` from the matching `section_subjects.teacher_id`, a left join, so a section or subject without a row keeps a null teacher as before. The signature, the grant and every other behaviour are unchanged. Existing papers are not rewritten, and the exam page still lets an owner/admin change a paper's teacher (D-304).

**Why:** this is the smallest shape that gives every subject teacher their classes, uses only tables that exist, and gives the admin a single save that cannot half-apply.

**Consequences:** migration `20260926021923_section_subjects.sql`; `supabase/tests/37_section_subjects.sql` (31 assertions); `12_function_grants_invariant.sql` allow-lists `set_section_subjects`. `getClassesOverview`'s `Section` now carries `subjects: {subjectId, teacherId}[]`. `app.can_enter_marks` still reads `exam_subjects.teacher_id`, not `section_subjects`, so a teacher changed on the Classes page after an exam is created does not move that exam's papers. The full Part 5 adds assistants, bulk assign by subject, the unassigned banner, notifications and grade templates.

**Review of PR #73 (2026-09-26):**

- **Security MEDIUM (D-300, removing access always works):** in a read-only school, removing a member who teaches failed. The release triggers (this Part's `app.tg_members_release_subject_teacher` and D-102's `app.tg_members_release_class_teacher`) update rows that `app.tg_require_writable` guards, and a SECURITY DEFINER trigger is not a privileged context, so the guard raised `PLAN_READ_ONLY` and the removal rolled back. Fix: the guard also lets through an UPDATE that **only** clears `sections.class_teacher_id` or `section_subjects.teacher_id` (`updated_at` aside), in the same way it already lets through a member removal or a capability revoke. Clearing a teacher is itself a removal of access, so doing it directly is allowed too. One function is replaced in this migration, which fixes both triggers; no flag, and neither trigger changes. pgTAP: removing a teacher who is a class teacher and a subject teacher in a read-only school succeeds and clears both; clearing the teacher together with any other column is still `PLAN_READ_ONLY`.
- **Security LOW:** the teacher-eligibility trigger also requires `m.workspace_id = new.workspace_id`. Another school's member id is now refused as `MEMBER_NOT_ELIGIBLE` before the FK is checked, so the error reveals nothing.
- **Security LOW:** `set_section_subjects` raises `SUBJECT_ARCHIVED` (22023) when a **newly added** subject is archived. A subject assigned before it was archived may stay, so the admin is not forced to drop it to change anything else. The sheet offers only live subjects, keeps an already-assigned archived one in the list it saves, and does not count it.
- **Shared primitive (lead exception for this lane):** the mobile `FormSheet` footer is `sticky bottom-0 bg-background`, so Save stays reachable in a long sheet at 360 px. It applies to every sheet; the add-section and add-subject sheets were checked at 360 px.
- The sheet lists the section's current subjects first (sorted once when it opens, so rows do not jump), asks before a swipe or Escape discards changes (`isDirty`), and archived subjects are not counted in "n subjects".
- Audit: `section_subjects.delete` is `notable`, not `critical`, because taking a subject off is routine (`GENERIC_SEVERITY_OVERRIDES`).
- `MySection` is `mySectionSchema` + `z.infer`; `types.generated.ts` comes from CI's artifact.

## D-208 — F-OP-03 Part 6, demo cut: monthly attendance register + exam mark sheet, `attendance_register_signoffs` deferred (not tiny) · ACCEPTED · 2026-09-26

**Context:** F-OP-03 Part 6 (§8) is "both templates with landscape switching, legends that print the school's own policy, column statistics, incomplete-data banners; `attendance_register_signoffs` schema + RLS, the sign/countersign/reopen actions, the `PERIOD_LOCKED` guard on `attendance_records` writes for a countersigned month, and the re-render with both signature lines" (§3.1a, §4 W7, §5.2a). The task narrowed this to the two PDF templates only, instructing the sign-off schema to be built "unless it's tiny" — it is not: §3.1a's table, the three-state `register_signoff_status` state machine (§5.2a: `open -> class_teacher_signed -> countersigned`, no direct path), three new server actions (sign/countersign/reopen) each with their own role and precondition checks (`NOT_YET_SIGNED`, `FORBIDDEN`), a new cross-feature write-path guard on `attendance_records` (`PERIOD_LOCKED`, owned by F-AC-03's save path, not this one), a re-render with real signature names, and five acceptance criteria (26a-26e) of its own. That is a Part on its own, not a follow-up.

**Decision:**

1. **Two new `report_kind` values, migration is `alter type ... add value` only** (`20260926072044_report_register_marksheet_kinds.sql`): `attendance_register`, `mark_sheet`. No shape change to `report_runs`/`report_run_items`, no RLS change — both kinds are single-item, synchronous renders (D-205's precedent), the same as `sample`/`report_card`; neither is staff-visible so the existing role-gated insert/select policies need no new per-kind clause.
2. **`attendance_register_signoffs` is deferred whole**, not built even partially. The register renders and prints correctly without it — the spec's own §5.7 rules (draft watermark, page numbers, empty-cell rules) already apply; only the legal "who attested this" record and the `attendance_records` write-lock are missing. Tracked for a future Part under F-OP-03's own queue.
3. **No new SQL function for calendar data.** `app.is_school_day`/`app.school_days` live in the `app` schema and are not exposed via PostgREST (`supabase/config.toml`'s `schemas = ["public", "graphql_public"]`) — calling them from a range would need a new `public.*` wrapper. Instead, `getAttendanceRegister` (`@acadigma/db`) reads `school_profiles.working_days`, `holidays` and `working_day_overrides` directly (all three are already SELECT-able by an active owner/admin/teacher/staff member per `20260925300301_school_calendar.sql`'s own RLS — the wrapper functions are SECURITY DEFINER only for the _parent_ case, D-203) and recomputes the same override > weekly-pattern > holiday > true precedence in TypeScript (`@acadigma/domain/calendar`'s `isSchoolDay`, unit-tested against the same four cases `app.is_school_day`'s own comment names). This keeps the migration to the two `alter type` statements the task asked for.
4. **The register's cell rule.** A cell is a status only when the day is a school day, the student was enrolled in that section that day (the register's own roster query, same `enrolled_on <= date <= ended_on` window F-AC-03 §5.3/D-105 already established), and a session was taken that day — `public.save_attendance` guarantees a record for every enrolled student once a session exists (§5.3), so "session taken, no record" cannot happen for an enrolled student. Every other case prints "-", distinguishable from the day's own `isSchoolDay`/`sessionTaken` flags and the roster window, never guessed by the template. Status codes on the grid are the plain Latin letters P/A/L/E/H in both locales — several Bengali status words share a first letter close enough to collide at one glyph (উপস্থিত/অনুপস্থিত), so the legend, not the cell, carries the Bengali meaning.
5. **The mark sheet reuses `getSectionResults` (F-AC-06 Part 5, D-305) entirely** — no second grading/averaging implementation (spec §5.1's "one grading truth" rule extends to this template exactly as it does to the report card). `getMarkSheetData` (`packages/db/src/repositories/results.ts`) reshapes "one row per student, subjects as a list" into "one column per paper," computing only simple arithmetic (highest/lowest/average/pass-rate) over already-computed per-line numbers. Column order is alphabetical (the same order `toRow` already sorts each student's lines in), not the exam's configured paper order the full spec names — inherited from `getSectionResults`, not a new cut this Part introduces.
6. **"Results not computed" is a run failure, not an inline banner.** `getSectionResults` returns `rows: []` both when a section genuinely has no students and when `compute_results` has never run for the exam; the mark sheet cannot tell those apart from here, so both surface as `no_data` ("Compute results for this exam before printing a mark sheet"). An individual student whose own result is `incomplete`/`withheld` still gets a row (D-305 item 5: `compute_results` writes one per student in the section), which is how the template prints "অসম্পূর্ণ"/"Incomplete" for that student alone without failing the whole sheet — the two are genuinely different states and only the second needed inline handling.
7. **Permissions.** `report.render.attendance_register` and `report.render.mark_sheet`, owner/admin/teacher, not staff (spec §2's matrix: "—" for staff on both). Unlike the report card's teacher grant (D-206, an acknowledged interim while the render source was a fixture), this is asserted for real: attendance reads are not narrowed to "own sections" at the RLS layer yet (any teacher may read any section, D-105's own precedent for substitutes), and the mark sheet's data already narrows to a teacher's own class-teacher sections through `results`' RLS (`app.can_read_results`, D-305) — so a plain role grant matches each table's actual current row-scoping rather than papering over a gap.
8. **Buttons live where the data is used, not on `/app/reports`** (same placement rule D-207 item 8 established): the register's button is on the roll-call page (`/app/attendance/[sectionId]`), defaulting to the currently-viewed date's month; the mark sheet's button is on the results preview (`/app/exams/[id]/results`), next to the report-card buttons.

**Why:** the two templates are genuinely independent of the sign-off state machine — a register and a mark sheet are useful documents on their own, and building them now rather than waiting for a Part that also has to design and test a legal attestation flow ships real value without underbuilding that flow to fit a demo-cut timebox.

**Consequences:** `packages/pdf/src/templates/attendance-register.tsx`, `packages/pdf/src/templates/mark-sheet.tsx`; `packages/domain/src/calendar/school-day.ts` (+ tests) as a new `@acadigma/domain/calendar` subpath; `packages/db/src/repositories/attendance-register.ts`; `getMarkSheetData` added to `packages/db/src/repositories/results.ts`; `apps/web/app/(school)/app/reports/{generate-attendance-register-button,generate-mark-sheet-button}.tsx`. Deferred: `attendance_register_signoffs` and the whole W7 sign/countersign/reopen flow (§3.1a, §5.2a, AC26a-e) — a future Part; exam-subject-configured column order on the mark sheet (inherited gap, item 5); a register that spans more than one landscape page for an unusually large section has no repeated table header (the measured case, a 40-student section over a 30-31 day month, fits on one page — see the test report).

**Review of PR #76 (2026-09-26) — supersedes item 3 above:**

- **BLOCKER (correctness): `getAttendanceRegister` selected `attendance_records` directly through PostgREST, which caps a single response at `max_rows` (`supabase/config.toml`, 1000).** A 40-student x 26-day month is already 1,040 records — the register silently dropped roughly 40 of them, with wrong "-" cells and wrong % and no error anywhere. Fixed by superseding item 3's approach entirely: a new `public.attendance_register(p_workspace_id, p_section_id, p_month)` (SQL, `stable`, SECURITY INVOKER, `search_path ''`, same migration) returns the month's calendar days (`app.is_school_day` computed per day, same D-203 SECURITY DEFINER carve-out item 3 already named), the roster and every attendance record as ONE `jsonb` value. PostgREST's row cap applies to the number of rows in a resultset, never to a function's single scalar return, so this is immune to `max_rows` by construction rather than by raising the limit. The TypeScript reimplementation of `app.is_school_day`'s precedence (`isSchoolDay`/`isoDayOfWeek`, `@acadigma/domain/calendar`) is deleted along with its tests — the SQL function is now the one place that logic lives; `getAttendanceRegister` only shapes what the RPC returns. pgTAP (`45_attendance_register.sql`): a 1,120-record fixture (40 students x a full month) proves no truncation; cross-school RLS isolation (a School F member gets a null `section` for School E's real ids, and for a mismatched workspace/section pair).
- **Nit → fixed as part of the same BLOCKER migration: the roster filtered `students.status = 'active'`, dropping a student who withdrew mid-month from that month's own register.** `public.attendance_register`'s roster is now every enrolment whose `enrolled_on`/`ended_on` window overlaps the month, regardless of the enrolment's or student's current status — a student who left last week still appears on last month's register. Covered by the same pgTAP file (a student who withdrew mid-month, one who joined mid-month, one who left before the month started, one not yet joined).
- **SHOULD: `createReportRun` queued a `mark_sheet` run even when the caller could never read that section's results,** leaving a misleading `results_not_computed` failure behind instead of a clear refusal — `app.can_read_results` (F-AC-06 §3/D-305) already decided this at the RLS layer, but `can()` only checks the role, not the section. Fixed with a thin `public.can_read_results(p_workspace_id, p_section_id)` SQL passthrough (`app` is not PostgREST-exposed, so the existing DEFINER function needed a public wrapper to be reachable from a server action's `.rpc()` call) — `createReportRun` now calls it before creating a `mark_sheet` run and returns `forbidden` synchronously instead. The register stays open to any teacher (unchanged, D-105's own precedent).
- **SHOULD: the register's "মোট/Total" column printed `recordedDays` (days with any recorded status) instead of days PRESENT** (the policy-weighted count `app.attendance_pct`/the % column already use) — `packages/pdf/src/templates/attendance-register.tsx` now prints `presentEquivalent`; the % column was already consistent and is unchanged. This also resolves the parity-drift this item and item 3 above created between the "Total" column and the "%" column.
- **Nits (noted, not fixed):** `examNameBn` duplicates `examNameEn` in `getMarkSheetData`/`getReportCard` (`packages/db/src/repositories/results.ts`) — no per-exam Bangla name exists in the schema yet, same pre-existing gap the report card already has. A Bengali-locale value ending in exactly ".00" loses its text-layer mapping for the period in pymupdf/pdf-parse extraction (glyph renders correctly; extraction-only artefact of `document-shell.tsx`'s run-splitting, already documented in the original test report).

## D-405 — F-ID-10 Part 2: the basic-mode home ships on `listMySections`, a class tap opens the existing roll call, and the whole `/app` shell — not only `/app/home` — swaps to the reduced-chrome BasicShell in basic mode · ACCEPTED · 2026-09-26

**Context:** F-ID-10 §8 Part 2 builds the real class-by-class home (`TodayStrip`, `ClassBlock`, `getBasicHome`, `HelpSheet`) behind Part 1's placeholder `/app/home`. Two of its own dependencies were not built yet when this Part started: `section_subjects` (F-AC-01, needed for "every (section, subject) a teacher teaches", §2 footnote ²) was in review as PR #73, not merged; the class hub (`/app/classes/[sectionId]`) is Part 3 and still does not exist. Neither blocks a usable home — the spec's own §2 footnote ² named an interim (class-teacher sections plus `exam_subjects.teacher_id`) for exactly this gap, and this PR built and shipped that interim first. #73 then merged (as F-AC-01 Part 5, D-107) while this PR was still open, with `listMySections(supabase, ctx)` — built and named specifically for this loader (D-107 item 6: "The design lane's basic home calls it") — so the interim was swapped out for it before this PR was marked ready, per the lead's standing instruction to do so if #73 landed first.

**Decision:**

1. **`getBasicHome` calls `listMySections` (D-107), not its own interim query.** The first version of this Part built the class list itself, in `packages/db/src/repositories/basic-home.ts`, from `attendance_day`'s `is_mine` plus a direct `exam_subjects.teacher_id` query — kept behind one function specifically so the eventual swap would be small. Once `listMySections` merged, `getBasicHome` calls it directly instead; the interim function and its `exam_subjects`/`workspace_members` queries are gone. `packages/domain/src/basic-home`'s `buildClassBlocks` takes `MySection[]` (from `@acadigma/contracts`) directly rather than a bespoke `Assignment[]` shape.
2. **One block per section, not per (section, subject).** `listMySections` already groups "I am the class teacher" and "every subject I teach here" into a single entry per section (`isClassTeacher: boolean`, `subjects: {subjectId, name, nameBn}[]`) — the design this Part's first version guessed at (two separate blocks for a teacher who is both) is superseded by this actually-shipped shape. `ClassBlock`'s title joins the labels instead: "Class 6 – ক · Bangla" (subject only), "Class 6 – ক · Class teacher" (class teacher, no subject), or "Class 6 – ক · Class teacher, Bangla, English" (both) — built by `classBlockTitle` in `apps/web/app/(school)/app/home/format.ts`, not by `packages/ui` (which never owns translated copy).
3. **"Taken 38/40" reads as marked-then vs. enrolled-now.** The spec's `taken?`/`expected?` fields are ambiguous between "present count" and "marked count"; this Part reads them as `taken` = how many were actually marked when the session was saved (`attendance_day`'s own `session.expected`, a confusing name for that count) and `expected` = the section's live enrolled count, so the two can differ exactly when a student enrols after the day was saved — the literal field names, and the only reading consistent with §5.3's "exactly the enrolled students, each with an explicit status" rule at save time.
4. **A class block opens the existing roll call, not a hub — deliberately, this Part.** `/app/classes/[sectionId]` (Part 3) does not exist yet. Every `ClassBlock` on the home, every row in the "All classes" list, and the "roll calls not taken" to-do link to `/app/attendance/[sectionId]` (or `/app/attendance` for the to-do) — the same, already-shipped, not-yet-basic-sized roll call F-AC-03 built. Part 3 replaces both the destination and its styling; this Part only makes the tap go somewhere real instead of nowhere.
5. **The reduced-chrome shell applies to every `/app` page in basic mode, not only home.** `(school)/app/layout.tsx` now branches on `ui_mode`: basic (and not `staff`, §2 note 4) renders `BasicShellWrapper` (no sidebar, no bottom nav, a Home+brand+Help top bar) around `{children}` for whatever page is being requested; full mode is unchanged. Consequence, intended: a class block's link into the existing (full-styled) roll-call page still opens inside the reduced-chrome shell, not suddenly the full sidebar/bottom-nav one, which is closer to what a basic-mode teacher expects, and it gives §9 AC17 ("a deep link opens inside the basic shell with a Home button") for free — though a non-home route's Help text falls back to a generic line until a Part adds that route's own catalogue entry. The alternative (wrap only `/app/home` in `BasicShell` and leave every other route inside the full `AppShell`) would show the full sidebar/bottom-nav chrome the instant a basic-mode user tapped into anything, which is the opposite of "the home is the nav" (§4.10).
6. **The essentials row is Profile · Settings · Switch to full app — Help is not repeated.** Help (§4.7) already sits in `BasicShell`'s top bar on every basic screen, including home; a second button opening the identical sheet is a redundant control. "Profile" has no dedicated screen anywhere in this app yet (basic or full) — it links to `/account/security`, the closest existing personal-account page, until a real one is built. "Settings" reuses `/app/settings` unchanged (it already routes a teacher/staff to the read-only overview and an owner/admin to the grouped list) — no basic-specific settings screen was in this Part's file list.
7. **"All classes" is a new, minimal searchable list, not a new hub.** `/app/classes/all` (owner/admin only, always shown per §4.4 footnote ¹, not only when the caller has zero classes) reuses `getClassesOverview` (F-AC-01, already gated on `academics.structure.read`) and a client-side text filter — a school's whole class list is small enough that a per-keystroke server round trip is not worth its own loader. Each row opens the existing roll call, same as item 4.
8. **A real Radix-barrel bundle-size bug, found and fixed.** `pnpm --filter @acadigma/web build` + `check-bundle-budget.mjs` measured `/app/home` at 276 kB gzipped first-load JS, over the 250 kB budget (Part 1's own baseline was 194 kB) — the third time this codebase has hit the same root cause (`(school)/app/reports/runs/[id]/page.tsx`'s `DOWNLOAD_LINK_CLASSNAME`, then D-305 item 8's results page): a Server Component importing `@acadigma/ui/components/button` at all (any export, `asChild` or not) pulls the whole `radix-ui` package — needed only for `Slot` — into that route's client bundle, because Next's RSC client-reference boundary handling does not tree-shake past a barrel package's own "use client" file. Fixed the same way the precedent did: inlined the outline/primary `Button` classes as literal strings in `essentials-row.tsx` and `home/page.tsx` instead of importing `Button`/`buttonVariants` from a server file. `/app/home` now measures 195 kB; `/app/classes/all` 194 kB. `HelpSheet` (behind `Sheet`, the same `Slot` dependency) is additionally loaded via `React.lazy` + `Suspense` in `BasicShell`, fetched only once Help is actually tapped — a real improvement on its own even though it turned out not to be this route's actual cost driver.

**Why:** the owner's 2026-09-26 walkthrough asked for the real home now, not a second placeholder; the class hub this Part is still missing already has a documented "not yet" behaviour (item 4), and its other missing dependency (`section_subjects`) landed and was picked up before this PR shipped, so the final result depends on nothing left interim.

**Consequences:** the Help catalogue (`t.basicMode.help.routes`) has only `home` and `default` entries; each feature Part that ships a class-shaped hub tab (Part 3 onward, per F-ID-10 §8's own closing note) adds its own route's catalogue entry in its own PR. `/app/classes/all`'s reuse of `getClassesOverview` means it has no attendance-status column — the same list, once the hub exists, could gain one, but that is Part 3's call, not this Part's. No migration was needed or written; every read in this Part goes through `listMySections`, `attendance_day`, `getSchoolProfile` or `getClassesOverview`, all already RLS-protected and already covered by existing pgTAP.

**Review batch (PR #72, 2026-09-26) — a11y/i18n fixes for the §1 persona (an older teacher on a 360px phone, Extra large text, বাংলা):**

1. **Sign out and the language switch move into the essentials row.** Basic mode replaces `AppShell`'s `TopBar` — and with it `UserMenu`, the only place either control lived — with `BasicShellWrapper`'s reduced top bar (Home, brand, Help only). Item 6 above already named that gap for "Profile"/"Settings"; it missed that `UserMenu` itself disappears too, taking Sign out and Language with it (spec §6's "Basic settings: Text size, Language, Switch to full app, Sign out" row list is the evidence this was always meant to be reachable). Fixed in `essentials-row.tsx`: a `<form action={signOut}>` (the same single-device sign-out `UserMenu` already used, no new action) and a new `LanguageSwitchButton` (`home/language-switch-button.tsx`, same `setLocaleCookie` + `updateLocale` + `router.refresh()` mechanism as `UserMenu`'s radio group, one tap to the other language) — both icon + label, `min-h-14`, matching the row's existing items. Text size and Switch to full app were already reachable (Settings → "How this school works" already links to `/app/settings/display` for every role; the essentials row already had Switch to full app), so neither needed a new control.
2. **"Profile" is dropped, not given a shell.** Item 6's `/account/security` link had no layout of its own — no Home button, no basic-mode chrome — so tapping it from a 360px basic screen was a dead end with no way back except the browser's own back button. The alternative this batch considered, wrapping `(account)` in a shell with a Home link, is strictly more code for a screen this Part never needed in the first place (no profile screen exists anywhere in the app, basic or full, per item 6's own text); dropping the link is the smaller fix and the one taken. A real profile screen, when one is built, can add itself back.
3. **`HelpSheet`'s close control is now a full-width 56px "ফিরে যান / Go back" button** in the sheet's footer (`SheetClose asChild`), not `SheetContent`'s default 16px "✕" with an English-only sr-only "Close" (`showCloseButton={false}`). The label is the same literal bilingual string in both `en.json` and `bn.json` — deliberately not locale-conditional — so an exit control this important reads regardless of which script the reader is confident in.
4. **`(school)/app/layout.tsx`'s basic-mode branch no longer waterfalls a second `getSchoolProfile` query after its own `Promise.all`.** The call moved into the same `Promise.all` as `requireWritable`/`getUiPreferences` (it depends on neither) and now goes through `getCachedSchoolProfile` (`apps/web/lib/school-profile.ts`), a `React.cache()` wrapper keyed on `ctx.workspaceId` — a plain string, not the `ctx`/`client` objects each of `requireShell()`/`createClient()` freshly builds per caller, which `React.cache()`'s reference-identity dedup would otherwise never match. `home/page.tsx`'s own `getSchoolProfile` call (empty-state phone number) now goes through the same cached wrapper, so a basic-mode `/app/home` request pays for the query once, not twice. Traded off deliberately: full-mode requests now also pay for this query (previously basic-mode-only) — accepted because it is a single indexed-row read and full-mode pages that already call `getSchoolProfile` elsewhere in the same request (settings, branding) now get it free from the cache too.
5. **The `home.error` state is now `t.classes.errors.generic` (localised) plus a Retry link to `/app/home`**, not `home.error.message` (an un-translated `ApiError` string leaking English into a বাংলা screen with no way to recover except reloading by hand).
6. **Marks entry gets a Help line.** `t.basicMode.help.routes.home` (en/bn) gains "To enter marks: Switch to full app → Exams." — there is no marks entry surface in basic mode yet (F-AC-06's marks entry lives under Exams, full-mode only), so the Help sheet is the only place this Part can tell a teacher where to find it. Noted in spec §8's Part 3 line: the class hub Part 3 ships adds its own Marks tab, which is expected to make this Help line unnecessary once it lands.
7. **`buttonVariants` moves to a radix-free `packages/ui/src/components/button-variants.ts`.** Item 8 above already found that a Server Component importing anything from `ui/button.tsx` pulls in the whole `radix-ui` package for `Slot`, and worked around it by inlining literal button classes as strings in `essentials-row.tsx`/`home/page.tsx` — copies that immediately drifted from the real `buttonVariants` (missing D-57's `active:translate-y-px` press feedback and the `disabled`/`aria-invalid` states, so they looked like a plain button, not this app's real one). `ui/button.tsx` now imports `buttonVariants` from this new file and re-exports it unchanged, so every other `import { buttonVariants } from ".../components/button"` call site is untouched. `essentials-row.tsx`, `home/page.tsx` and `help-sheet.tsx`'s outline "Add school phone number" button now compute their classes from the real variants (with `text-base` added, §5.1's basic-mode body size, over the variants' default `text-sm`) instead of a literal string. Bundle budget after the swap: see the test report (`docs/test-reports/2026-09-26-F-ID-10-p2.md`) for the measured number — this decision only records that it stayed under `check-bundle-budget.mjs`'s 250 kB gate.
8. **`basic-mode-toggle-sync.spec.ts`'s বাংলা test and `basic-home-tap-targets.spec.ts`'s essentials-row assertion were already stale for reasons items 1-2 above make permanent, not test flakiness**: the former clicked a "Account menu" button that basic mode has never rendered (`UserMenu` does not exist there, per item 5 above and this Part's original D-405 item 5) — fixed to use the essentials row's own language switch button instead, labelled with the language it switches to. The latter asserted a "Profile" link that item 2 above removes — fixed to assert Sign out and the language switch instead. Neither spec runs in this sandbox or in CI yet (OQ-27), so this is a correctness fix for whenever OQ-27 is resolved, not a currently-passing-then-broken test.

**Why:** the review named a real, user-facing gap (an older teacher who turns on basic mode loses Sign out and the language switch entirely, with no way back to `UserMenu` since the whole point of basic mode is that the full shell is gone) — not a style nit; every other item is a smaller, adjacent fix touched while already in these files for item 1.

**Consequences:** no new decision range item beyond this addendum — kept as an addition to D-405 rather than a new D-4xx entry, since it is the same Part's own review round, matching the precedent `D-107`'s "Review of PR #73" section already set for this file. `t.basicMode.home.profile` (en/bn) is deleted, now unused. `apps/web/lib/school-profile.ts` is new; `getSchoolProfile` itself (`packages/db/src/repositories/settings.ts`) is unchanged — only these two call sites route through the cached wrapper.

## D-73 — A real-PostgREST CI job (`db-integration`) closes the gap #61 fell through; docs-only PRs get prettier back · ACCEPTED · 2026-09-26

**Context:** D-72 item 4 put "a CI job runs a real PostgREST" on the lead queue after `/app/classes` (#61, `PGRST201` from an un-hinted `profiles` embed) reached production despite a green PR: `unit`'s vitest suite mocks the Supabase client and never resolves a real PostgREST relationship, and `db`'s pgTAP runs against bare `postgres:17` with no PostgREST in front of it at all. The one e2e journey that opens `/app/classes` as a real owner is skip-gated on `E2E_LIVE_SUPABASE` + a seeded account (OQ-27, still open), which CI never sets. #61's own fix added an opt-in integration test (`packages/db/src/repositories/academics.integration.test.ts`, `DB_LOCAL_SUPABASE=1`) that reproduced the bug against a real local PostgREST, but nothing wired it into CI. Separately, #57 and #70 both landed unformatted `.md` on `main`: D-70's `changes` job skips the entire `lint` job (prettier included) for a docs-only PR, and nothing else checks markdown formatting.

**Decision:**

1. **New job `db-integration`, gated by a new `changes` output.** `changes` gains a third boolean, `dbIntegration` — true when a changed file matches `supabase/`, `packages/db/`, `packages/contracts/`, or `.github/` (the last so a CI workflow change tests itself instead of skipping the very job it adds, same reasoning `db`'s own output already applies to `.github/`). `db-integration` needs only `changes` (siblings with `db`/`unit`/`build`, not sequenced after them) and runs on drafts — this is a correctness gate like `db`/`unit`, not a cost-driven skip like `e2e`/`lighthouse`.
2. **The job itself:** `supabase/setup-cli` pinned to 2.117.0 (the version `db.yml`'s `push` job uses) → `supabase start` (a GitHub-hosted runner is always a fresh Postgres volume, so this also applies every migration and the seed before PostgREST/GoTrue come up) → `supabase status -o env` supplies `DB_LOCAL_SUPABASE_URL`/`_ANON_KEY`/`_SERVICE_KEY` (no secrets needed — fixed local-CLI keys for a stack torn down with the runner) → every `*.integration.test.ts` found anywhere in the workspace (`find`, not a hardcoded list) is passed as an explicit file argument to `vitest run` from the repo root. Explicit file arguments, not `cd <package> && pnpm test`: the latter would still resolve `vitest.config.ts`'s root `projects` array from that cwd and run every package's whole suite, not just the one file that changed — this repo has one root vitest config with named projects, not one config per package. Each integration test already carries its own `describe.skipIf(!RUN)` guard keyed on `DB_LOCAL_SUPABASE`, so the job doesn't need to know which suites exist; a lane's next `*.integration.test.ts` is picked up automatically.
3. **Fixed a real seeding gap this PR's own CI run surfaced: `supabase/config.toml` had no `[db.seed]` block.** `supabase/seed/seed.sql`'s own header says "Applied by `supabase db reset` after every migration," but the seed lives at the nonstandard `supabase/seed/seed.sql` path, not the CLI's default `supabase/seed.sql` — the first `db-integration` run on this PR logged `WARN: no files matched pattern: supabase/seed.sql` and silently seeded nothing. `[db.seed] sql_paths = ["./seed/seed.sql"]` fixes it, naming only `seed.sql`: `supabase/seed/demo-class-6-ka.sql` is a manual runbook script gated on `set_config('demo.workspace_id', ...)` for one specific real school and must never auto-apply to a fresh database. Pre-existing bug, not introduced by this PR — nobody had run `supabase start`/`db reset` in CI before to notice it.
4. **Not required (yet).** `db-integration` is a real gate today (a red run fails the PR), but is not added to `docs/engineering/CI.md` §3's branch-protection list in this PR — that needs a few real runs to confirm the job stays reliably fast first, per the brief's own instruction. This PR's own CI run is the first data point; see its checks for the observed runtime.
5. **OQ-27 (live Playwright journeys) is not resolved by this PR.** The brief allowed doing the integration tests alone if running the skip-gated journeys in the same job was too large, and it is: those journeys need (a) a second `next build`/`next start` pointed at the local stack's URL/anon key instead of the production `NEXT_PUBLIC_SUPABASE_*` baked into the `build` job's artifact, (b) a seeded owner account plus the fixtures each journey expects (a school, a section, a class-teacher assignment, …), and (c) `pnpm test:e2e` scoped to those specs with `E2E_LIVE_SUPABASE=1` and `E2E_OWNER_EMAIL`/`E2E_OWNER_PASSWORD` set. None of that is built here. Next step for whoever picks up OQ-27: build (a)-(c) as a second job (or a later step in this one) that depends on `db-integration`'s local stack.
6. **Docs-only PRs get prettier back, in `changes` itself.** A tiny step, gated on `app == 'false'` (so it only ever fires for the exact docs-only case that skips `lint`, never redundantly alongside it), runs `pnpm exec prettier --check` on the PR's changed `.md` files. Kept inside the existing `changes` job rather than a new one: a new job's runner-spinup overhead would cost more than the check itself, and `changes` already computes the file list this step filters.

**Why:** the two cheapest changes that would have caught #61 and #57/#70 automatically, scoped to what actually closes each gap — a real PostgREST in front of the repository layer, and a prettier check that survives the docs-only skip path — without changing the cost or behavior of any PR that isn't docs-only or db/contracts-touching.

**Consequences:** `.github/workflows/ci.yml` — `changes` job gains the `dbIntegration` output and the conditional markdown-prettier step; new `db-integration` job; `report`'s `needs` list gains `db-integration`. `supabase/config.toml` gains `[db.seed]`, so `supabase start`/`db reset` now actually seed a fresh local database anywhere they're run, not only in this CI job — a fix for every future local-stack use, not just this one. `docs/engineering/CI.md` §1 and §3 updated, plus a new `db-integration` subsection after §2.3. OQ-27 stays open in `docs/product/OWNER-QUESTIONS.md`, now with the concrete next step recorded here instead of only "needs `E2E_LIVE_SUPABASE: "1"`". Whoever adds `db-integration` to branch protection also updates CI.md §3's fenced list in the same PR (job ids are contract there).

## D-306 — Publishing results demo cut: published follows the exam's status, the report card is frozen in SQL, parents read through `guardian_users`, which this Part creates · ACCEPTED · 2026-09-26

**Context:** F-AC-06 Part 7 publishes results to families. It builds on results (D-305), marks' completeness gate (D-304) and the status chain with its reasoned reversals (D-303). Parents can sign in and land on `/family` (F-ID-03 Part 4, seed `parent@acadigma.test`), but nothing links a parent's account to a child: `guardians` are contact rows (D-103), `app.is_guardian_of` (2026-09-17) queried a `guardian_users` table that did not exist, and the invite/accept flow that creates links is F-AC-02 Part 4 (not built). F-ID-07 has `app.notify` but no notification centre or delivery, so a `result.published` notification would reach no one.

**Decision:**

1. **`guardian_users` is created here** in F-AC-02 §3's shape (`guardian_id`, `student_id` denormalised, `user_id`, `status invited|active|revoked`, `invited_at`, `accepted_at`, `revoked_at`), with a composite FK `(guardian_id, student_id, workspace_id)` → `guardians` so a link's student is always its guardian's own student in the same school. RLS: the user reads their own links, an owner/admin reads the school's; **no client write grant** — F-AC-02 Part 4's invite flow adds the writer. `app.is_guardian_of` is redefined to the documented probe (`user_id`, `student_id`, `status = 'active'`, partial index). Until Part 4, a link exists only if inserted with service role; **so parents cannot yet see a result end to end in the product.**
2. **Published follows the exam's status.** `results` gain `published`, `published_at`, `published_by`, `withheld_reason` and `frozen_payload` (a check: published ⇒ frozen). `app.tg_exams_publish_results` (AFTER UPDATE OF status) runs on `→ published` from either `public.publish_results` or `setExamStatus`: after D-304's `MARKS_INCOMPLETE` gate it refuses `NOT_COMPUTED` (no results) and `INCOMPLETE_PRESENT` (any incomplete result — so nothing incomplete is ever published, even when results went stale), then stamps every result and logs one `results.published` event with the published and withheld counts and the withheld student ids. On the existing `published → marks_locked` reversal (reason required, D-303) it clears `published`, keeps the frozen payload and withheld reasons, and logs `results.unpublished` with the reason. Results cannot change while published: `compute_results` runs only on `marks_locked`.
3. **`public.publish_results(ws, exam, withhold)`** — owner/admin (`FORBIDDEN`), exam `marks_locked` (`MARKS_NOT_LOCKED`), each withheld entry a student with a result in the exam and a 1–500 character reason (`VALIDATION`); it sets every result's `withheld_reason` (null for the rest) and moves the exam to `published`. `PLAN_READ_ONLY` comes from the table guard. The server action `publishResults` checks `results.publish` (new: owner, admin) and `requireWritable` first. Withholding is chosen at publish; releasing one student later is unpublish + republish.
4. **The freeze is built in SQL** (`app.result_card_payload`): the report card's `ReportCardDto` exactly — names, class and section, exam, subject lines, totals, GPA, letter, rank, `rankTied`, `rankOf`, and attendance up to the exam's end weighted by the school's policy — the same values `getReportCard` builds live (D-305 item 9). A withheld student's card is frozen as `withheld` with no GPA, grade or rank. It is built by the database, never sent by the client, so an admin cannot publish a card the marks do not support. `getReportCard` returns the frozen payload whenever one exists, so reprints and parents both read what was published; renaming a subject or section afterwards changes nothing (AC16, pgTAP). School branding is not frozen (the DTO carries none; the header reads `school_profiles` at render, as for every report).
5. **Parents read** a result only when it is published, not withheld, the caller is an active `parent` member of that school and `app.is_guardian_of(student_id)`; lines follow their result. They still read no `students`, `exams` or `marks` rows: the family screen renders entirely from the frozen payload. New permission `family.results.read` (parent).
6. **Screens.** "Publish" on a `marks_locked` exam opens a sheet explaining the freeze, listing every student with a "Withhold" checkbox and a required reason; the confirm button counts "Publish n · withhold m". `/family` lists the parent's children's published results newest first (name, exam, class, pass/fail, GPA, grade, "rank of n", "tied") with a native disclosure for marks by subject and "Download report card" — `GET /api/family/report-card?examId&studentId`, which renders the card through the parent's own RLS client (only their child's published card; anything else is `not_found`) and writes nothing, so a read-only school's parents can still download. Western digits on screen in both languages; the downloaded card uses Bangla numerals in Bangla (D-206).
7. **No notification fan-out.** `results.published` is the recorded event; `TODO(D-306)` in the trigger marks where `result.published` goes to linked guardians once F-ID-07 delivers (Parts 2–5).

**Review of PR #75 (2026-09-26):** (1) `getReportCard` uses the frozen payload only while the result is published and not withheld — after an unpublish, school users print live data, not the kept snapshot; a withheld result prints live for the school, marked withheld with no GPA, grade or rank. (2) The download filename keeps only `[A-Za-z0-9_-]` of the student code. (3) **A withheld result freezes no marks**: `app.result_card_payload` freezes only names, class and exam, with empty subjects and null totals, percentage, GPA, grade, rank and attendance. Parents now see a withheld result — "Result withheld: please contact the school." / "ফলাফল স্থগিত — বিদ্যালয়ে যোগাযোগ করুন" — through `public.family_results(ws)` (definer, parent + active link, published only), which returns `(exam, student, published_at, withheld, card)` and never the reason. The RLS policy still never returns a withheld row: RLS cannot hide columns per role, so the raw GPA and the lines would reach the parent (pgTAP: the parent of a withheld child reads no raw row or line, and `family_results` gives them the row with no marks and no reason). (4) The publish sheet names its effect: "Publish {exam} results for {class} ({n} students)?", button "Publish · parents will see {n}". (5) "Publish" is disabled with "Compute results first." when there are no results, or "{n} results incomplete." when any is. (6) `/family` tells "Your account isn't linked to a child yet. Ask the school office." (no active `guardian_users` row) apart from "nothing published yet". Nits: dates in Bangla use Bangla month names with Western digits (`bn-BD-u-nu-latn`); the screen uses the exam's one name. **Recompute after an unpublish replaces the kept `frozen_payload`** (results are deleted and re-inserted), rather than §5.14's "new version"; the audit trail keeps the publish and unpublish events. Declined: a demo-seed guardian link (the owner cancelled the seed; F-AC-02 Part 4 is building linking as D-108).

**Why:** publishing is the moment a school's result becomes a family's record, so it must be impossible to publish an incomplete result or to change a published one, whichever button moves the exam; and a parent's view is a security boundary, so it rests on one predicate and one table, tested per case, even before the invite flow exists.

**Consequences:** migration `20260926023537_publish_results.sql` (lead-assigned as 300321, renamed to the real UTC time on the lead's instruction after the date rolled over); `supabase/tests/56_publish_results.sql` (48 assertions); `12_function_grants_invariant.sql` allow-lists `publish_results` and `family_results`; `guardian_users` joins the generic audit tables. Deferred: the `result.published` notification and republish notice (F-ID-07), the invite/accept/revoke flow that creates links (F-AC-02 Part 4), releasing one withheld student without unpublishing, `grade_rank`, `show_rank_to_parents`, idempotency keys and rate limits on `publishResults`, F-AC-10's child switcher and tab strip (Part 1) and its security-definer views (Part 2).

## D-108 — F-AC-02 Part 4 guardian linking, demo cut: a single-use link on F-ID-04's invitations, possession-bound until phone OTP, accepted by one SECURITY DEFINER function · ACCEPTED · 2026-09-26

**Context:** the demo ends with "a parent opens the app and sees their child's result". D-306 created `guardian_users` and the parent read on `results`, with no client writer, so a link needed a service-role insert. F-ID-04's `workspace_invitations` (hashed tokens, statuses, expiry) exists in the database but has no UI and no client grant. Guardians are private contact rows holding a phone only, no email (D-103), and sign-in is email and password: phone OTP is not built and `profiles.phone` is self-editable, so neither a phone nor an email binding can be verified for a guardian today. Most BD schools reach parents on WhatsApp, not email.

**Decision:**

1. **Reuse `workspace_invitations`**, no second table: a guardian invitation is `role = 'parent'` with new `guardian_id` + `student_id` columns and a composite FK to `guardians (id, student_id, workspace_id)` — an invitation for another student or another school's guardian cannot be stored at all, even by a direct insert. Checks: both or neither, and `role = 'parent'` ⇔ a guardian is named. `guardian_users` gains `invitation_id`.
2. **`public.invite_guardian(ws, guardian)`** — owner/admin (`students.guardian.invite`, F-AC-02 §2; the class teacher of F-ID-04 OQ-6 is not included yet). A 256-bit token (only its SHA-256 stored, returned once), `channel = 'phone'` with the guardian's phone on record, **30 days** (F-ID-04 §4.5). A new link revokes the guardian's earlier pending one. Refuses `GUARDIAN_NOT_FOUND` (another school, or a deleted student), `GUARDIAN_ALREADY_LINKED`, and **`RATE_LIMITED` after 60 guardian invitations in an hour per school** (F-AC-02 §7's 60/h). No `guardian_users 'invited'` row is written: `user_id` is not null and is unknown until acceptance.
3. **Binding: the link is the credential** — like a join code, but for one child, once, for 30 days. The lead's brief: "if SMS can't send, show a copyable link/code the school can WhatsApp; that's the BD reality". The school sees which account accepted (name and email) on the student's page and can remove it. This deviates from F-ID-04 §4.2 step 4 (no identity substitution for guardians), which needs a verified phone; **when phone OTP ships, guardian invitations bind to the verified phone** — one check in `accept_guardian_invitation`.
4. **`public.accept_guardian_invitation(token)`** — signed in only; `INVITATION_NOT_FOUND`, `INVITATION_EXPIRED` (checked by the function, not a sweeper), `INVITATION_ACCEPTED`/`REVOKED`/`DECLINED`, `PLAN_READ_ONLY` (D-301). It inserts an active `parent` membership or reuses an active parent membership (a parent of two children has one membership and two links); anyone already in the school in another role, or with a removed membership, gets `MEMBERSHIP_CONFLICT` instead of being turned into a parent. Then an active `guardian_users` row for exactly the invitation's guardian and student, and the invitation becomes `accepted`. A second tap by the same account is not an error. **`public.guardian_invitation_preview(token)`** (signed in) names the school and child while pending, and only the status afterwards.
5. **`public.revoke_guardian_link(ws, link)`** — owner/admin, idempotent, `LINK_NOT_FOUND` across schools. `app.is_guardian_of` reads `status = 'active'`, so the child and the results disappear at once. **When it was the parent's last active link in that school, the parent membership is set to `removed`** (audited on `workspace_members`), so a leaked link keeps no workspace-level reach (PR #78 security review, MEDIUM). **Removing a membership by any path** (the members screen, leaving, this function) **revokes that person's active guardian links** in the school (trigger `workspace_members_revoke_guardian_links`); revoking a link is removing access, so `app.tg_require_writable` allows it in a read-only school. A removed parent comes back only by accepting a new link: `app.tg_workspace_members_guard` lets a member change their own status only for `parent → parent`, `removed → active`, with no other column changed, when the membership names a guardian invitation that this person accepted in the same transaction (`accepted_at = now()`). Invitations are written only by SECURITY DEFINER functions and a PostgREST PATCH is its own transaction, so a direct self-PATCH never qualifies (PR #78 security re-check, HIGH: the first version accepted any removed parent holding an active link). `app.is_privileged_context()` cannot cover this path: it reads the `role` setting, which SECURITY DEFINER does not change.
6. **Parents read their linked children's `students` rows** (new policy `students_select_guardian`: parent member + `app.is_guardian_of(id)`, not deleted) — public columns only; date of birth and guardians stay private. `/family` lists "Your children" above the results. `56_publish_results.sql`'s "a parent cannot read the students table" becomes "reads only their own linked child's row".
   6a. **Parents never read the school's internal files or plan usage** (lead decision, PR #78 review): `files_select_member`'s `visibility = 'workspace'` branch and `usage_counters_select` now require owner/admin/teacher/staff. The school's name, profile and calendar stay readable to an active parent. **`app.accept_invitation` / `app.decline_invitation` ignore guardian invitations** (`guardian_id is null`), so a guardian link is accepted only through the guardian path (review, LOW).
7. **Screens.** The student profile's guardians card (owner/admin) shows "Parent app": linked accounts with "Remove access" (confirm dialog), and "Invite {guardian} to the parent app" per unlinked guardian, which opens a dialog with the link, "Copy link" and "Send on WhatsApp" (`wa.me/` + the guardian's phone, with a message). The link is `/invite#<token>`: the token is in the fragment, reaches the server only in server-action POST bodies, is moved to `localStorage` and removed from the address bar, and the page sets `referrer: no-referrer`. Signed out, the page offers Sign in (`next=/invite`) or Create an account (`/register?next=/invite`, carried through `/verify`, the resend and the confirmation email's `emailRedirectTo`; `/invite` is the only allowed value, anything else keeps `/onboarding`), with "No email? Ask the school office for help." After acceptance the school becomes the active workspace and the parent lands on `/family`. Accept and preview errors are translated by code; `MEMBERSHIP_CONFLICT` explains that this is a staff account and offers "Sign out and create a parent account".

**Next Parts, in order:** (1) **phone sign-up (OTP)** — the dependency for parents with no email and for binding links to a verified phone; (2) **a staff or teacher member who is also a parent**: let an active staff/teacher member hold `guardian_users` links and gate `/family` and the results RLS on `app.is_guardian_of` instead of `role = 'parent'`, replacing today's `MEMBERSHIP_CONFLICT`; (3) **invite rights for the class teacher of the student's current section** (F-ID-04 OQ-6).

**Why:** the smallest honest path to a real parent link: one table, one writer per transition, every rule in SQL and tested, and the WhatsApp reality instead of an SMS gateway that does not exist yet.

**Consequences:** migration `20260926065723_guardian_linking.sql`; `supabase/tests/38_guardian_linking.sql` (66 assertions); `12_function_grants_invariant.sql` allow-lists the four functions; permission `students.guardian.invite` (owner, admin); journey `apps/web/e2e/journeys/guardian-invite.spec.ts` (skip-gated, OQ-27). Deferred: phone binding (phone OTP), email/SMS sending, a pending-invitations list with resend/revoke, decline, class-teacher invites, one invitation for several children, the expiry sweep, `invite.received`/`guardian.linked` notifications, curated `guardian.invited`/`guardian.linked` audit actions (the generic table audit records both rows), revoking links on transfer-out or soft delete, and a teacher who is also a parent (one membership per school).

## D-307 — Marks submit, lock and entry window demo cut: submit warns but never blocks, lock needs a submitted paper, unlock takes a reason and clears stale results, admins write late with a reason · ACCEPTED · 2026-09-26

**Context:** F-AC-06 Part 4 was "offline queue, submit and lock". D-71 moved the offline queue to F-ID-11 Part 3, so this Part is the rest: `submitExamSubject` with an incomplete warning, lock/unlock per paper with reasons, the marks-progress screen, and the entry date window that D-304 left as a `TODO(F-AC-06 Part 4)` in `save_marks`. D-304's publish gate (`MARKS_INCOMPLETE`) already exists and stays as is. F-ID-07 still has no delivery, so `marks.due` / `marks.submitted` notifications would reach no one (as for D-306).

**Decision:**

1. **The window (§5.11).** `exam_subjects` gains `entry_opens_on` and `entry_closes_on` (a check keeps close ≥ open). The effective window is `app.marks_entry_window`: opens on the date set, else the paper's `exam_date`; closes on the date set, else 7 days after it opens (§5.11's default; a per-school `entry_window_days` waits until a school asks — `ponytail:` comment). A paper with no date at all has no window, so exams without dates behave exactly as before. "Today" is `app.school_today` (the school's timezone, D-104). The screen mirrors the rule in `marksEntryWindow` (`packages/domain/src/academic/marks.ts`), unit-tested on its edges.
2. **Outside the window** (`save_marks`): a teacher — the paper's teacher or the class teacher — is refused `OUTSIDE_ENTRY_WINDOW`. An owner/admin must send `late_reason` (1–500 characters; `REASON_REQUIRED` without it); the rows written are stamped `marks.edited_after_window = true` (like attendance's `edited_after_window`, D-104) and the reason goes into that save's `marks.entered` audit event. A later in-window write clears the stamp. The owner/admin edits the dates on the exam page's paper row ("Marks open" / "Marks close"), which is how a window is extended.
3. **Submit (§7 `submitExamSubject`).** `public.submit_exam_subject(ws, paper, confirm_incomplete)` — only the paper's own teacher (`exam_subjects.teacher_id`) or an owner/admin; **the class teacher may enter marks but not submit** another teacher's paper. Only while the exam is in `marks_entry` and the paper is not locked. Missing students come back as `{submitted: false, missing: [{student, name, roll}]}` and nothing changes; with `confirm_incomplete` the paper is submitted anyway — the spec's `INCOMPLETE_ENTRY` is a warning, not a block, and D-304's gate still stops publishing. A pending paper passes through `entering`. One `marks.submitted` audit event with the missing count. Submitting again is a no-op. Marks can still be corrected after submitting, until the paper is locked.
4. **Lock / unlock (§7, owner/admin, new permission `marks.lock`).** `lock_exam_subject` takes a **submitted** paper to `locked` (`NOT_SUBMITTED`, `ALREADY_LOCKED`); an admin who wants to lock an unsubmitted paper submits it first. `unlock_exam_subject` needs a reason (`REASON_REQUIRED`), takes the paper back to `submitted` and stores the reason in the new `exam_subjects.status_reason`; `marks.unlocked` is audited with the reason (AC13). **If the exam is `marks_locked`, it goes back to `marks_entry` with the same reason**, so the existing `app.tg_exams_clear_results` deletes the stale results and logs `results.cleared` — a result never survives a change to its marks. A published or archived exam refuses (`EXAM_PUBLISHED`): unpublish first. The paper chain (`app.tg_exam_papers_lock`) gains its one reversal, `locked → submitted`, only with a reason, so a direct UPDATE cannot skip it; a forward step clears the reason.
5. **Marks progress** on the exam page (owner/admin, from `marks_entry` on): one 72 px row per paper with subject · section, the teacher, "n/N marked" (D-304's `exam_marks_progress`), a status badge (Not started / Entering / Submitted / Locked) and Lock or Unlock (a reason sheet). A summary line "x of y papers submitted · z locked". The reason sheet is the exam page's reversal sheet, moved to `reason-sheet.tsx` and shared.
6. **Marks screen.** Shows the window ("Marks entry: from to"), a read-only notice for a teacher outside it, a reason field above Save for an owner/admin outside it, and "Submit marks" below the list for the paper's teacher or an admin (disabled with "Save your changes before submitting" while anything is unsaved); the incomplete warning opens a sheet listing roll and name with "Submit anyway". English and Bangla, Western digits.
7. **Notifications: none yet.** `TODO(D-307)` in `submit_exam_subject` marks where `marks.submitted` (to admins) and `marks.due` (a day before the window closes) go once F-ID-07 delivers. The audit events are the record.

**Why:** the rules that protect a school's marks — who may finish a paper, that a locked paper is final until someone says why, that a result is never left standing on changed marks, that late changes are visible — live in the database, and the one thing a teacher needs to hear (who is still missing) is a warning they can act on, not a wall.

**Review of PR #79 (2026-09-26):**

- **Security LOW-1:** a direct UPDATE could unlock a paper (`locked → submitted` with a `status_reason`) and skip `EXAM_PUBLISHED` and the `marks_locked → marks_entry` move. The paper chain now refuses that transition unless `unlock_exam_subject` set the transaction-local flag `app.unlock_via_rpc` (`UNLOCK_VIA_RPC_ONLY`); pgTAP: a direct update is refused with and without a reason.
- **Security LOW-2:** `edited_after_window` is sticky — a later in-window write no longer clears it (item 2 above is superseded on this point).
- **Security LOW-3:** the late-reason and every reason sheet say "Don't write student details here / শিক্ষার্থীর ব্যক্তিগত তথ্য লিখবেন না".
- **Default close (deviation from §5.11):** a paper with no close date closes 7 days after the **later of its opening and the exam's `ends_on`** — a Bangladeshi exam runs about two weeks, so "7 days after the paper" would close early papers before the last paper is sat. Same rule in `app.marks_entry_window(paper, exam_ends_on)` and `marksEntryWindow`.
- **Backfill:** the migration sets `entry_closes_on = today + 7` on papers of exams already in `marks_entry` whose effective window closes sooner, so nobody is locked out on deploy.
- **After an unlock:** the unlock sheet says to change "Marks close" if the dates are over; each progress row shows its close date and a "Closed / বন্ধ" badge, with **"Reopen 7 days"** (owner/admin: `entry_closes_on = today + 7`, a plain update audited as `exam_subjects.update`).
- The admin's late-reason field moved from the sticky bar to the header (the phone keypad covered it); window dates use the exam date format (Bangla month names, Western digits); Bangla labels "নম্বর দেওয়া শুরু" / "নম্বর দেওয়ার শেষ দিন"; the late help no longer says the dates are over (the admin may write before they open).

**Consequences:** migration `20260926063741_marks_submit_lock.sql` (renamed to the real UTC time before ready); `supabase/tests/57_marks_submit_lock.sql` (53 assertions); `12_function_grants_invariant.sql` allow-lists the three new functions; `marks.integration.test.ts` runs the new columns and RPCs through real PostgREST (D-73). Deferred: notifications (F-ID-07), "Request unlock" for teachers, a per-school window length, the custody gate on lock (§5.16, Part 9), locking every paper when the exam locks, the offline queue (F-ID-11 Part 3).

## D-74 — Vercel creates deployments for `main` only (`git.deploymentEnabled`), so branch pushes stop using the Hobby deploy quota · ACCEPTED · 2026-09-26

**Context:** D-70 made Vercel _build_ `main` only through `ignoreCommand`, but every push to any branch still _created_ a deployment that was then cancelled. Cancelled deployments count toward the Hobby plan's daily deployment limit: on 2026-09-25/26 about 100 builder WIP pushes used the quota up, so several `main` merges were refused ("Deployment rate limited — retry in 24 hours") and production lagged until a later `main` deploy got through. The owner asked for a workaround instead of upgrading to Pro.

**Decision (lead, under owner authorization 2026-09-26):** `apps/web/vercel.json` sets `"git": { "deploymentEnabled": { "*": false, "main": true } }`. Branch pushes (feature branches, `handoff/live`, the changesets branches) create no Vercel deployment at all; `main` deploys as before. `ignoreCommand` stays as a second guard.

**Consequences:** no preview URLs for PRs, which we already didn't use (CI runs Playwright against its own build, D-70). The Vercel check on PRs disappears; it was never required. Pro stays optional: revisit only if `main` alone ever exceeds the daily limit.

## D-308 — Offline Part 1: the signed-in shells' pages are cached network-first as full HTML only, and wiped whenever the user, workspace or role changes · ACCEPTED · 2026-09-26

**Context:** F-ID-11 Part 1 (D-71, as amended by the #59 review: the purge ships with the cache). The existing worker used serwist's `defaultCache`, which in production already cached signed-in pages, RSC payloads and every `GET /api/*` (signed file URLs, PDFs) under generic names, with no purge at all.

**Decision:**

1. **What is cached.** Only full page loads of `/app`, `/family` and `/personal`, network-first, in `acadigma-data-pages` (60 entries, 30 days, only a plain 200 — never a redirect). RSC payloads, `/api/*`, auth pages, `/account`, `/platform` and cross-origin requests are network-only. Build output and static images keep their own caches (no user data). The old worker's caches are deleted on activate.
2. **OQ-3: no RSC caching.** An RSC payload depends on the router state it was fetched from, so a cached one can be the wrong tree. When an RSC fetch fails offline, Next falls back to a full navigation, which the page cache answers. A page reached by an in-app navigation is fetched once in the background (header `x-acadigma-cache-page`, stored by the worker under the same rules) — at most once per URL per 10 minutes.
3. **The purge rule** (`decidePurge`, `apps/web/lib/offline/purge.ts`): the device keeps the last seen `{userId, workspaceId, role}`; `GET /api/offline/session` (re-validates the JWT and the active membership) answers now. Signed out or session revoked → wipe and forget; a different user, workspace or role → wipe; no active membership (removed, suspended) → wipe; network error or 503 → change nothing. Runs on every app open, every return online, on arriving at `/login` (where every sign-out and expired session ends), and after a workspace switch; the user menu's sign-out wipes before the session ends. The first check after a sign-in does not wipe (the `/login` check already did), so the first pages a user opens are kept.
4. **"Last updated"** is the shell's server render time, which travels with a cached copy: shown on the document the shell was loaded with when it is offline or was more than 2 minutes old at load.
5. **"Needs internet / ইন্টারনেট দরকার"** (`OnlineOnly`): report card, bulk report cards, sample PDF, PDF download, Publish, student import, Create school, guardian invitations (create and accept). A disabled button with a one-line reason; nothing queues.

**Why:** the cache holds children's names and marks, so it may only exist while the same person, in the same school, with the same role, is looking — and the cheapest cache that is always right is whole pages, network-first.

**Consequences:** no migration. The snapshot lives in `localStorage` (non-secret ids) until Part 2a brings IndexedDB `meta`. The service worker is 48.5 KB raw, 14.4 KB gzipped with its precache manifest (budget 15 KB). Deferred: warm-up of the teacher's class pages (§4.2), `storage.persist()` and the quota banner (Part 5), byte-accurate eviction, "a failed request means offline" (Part 2a).
