---
"@acadigma/web": minor
"@acadigma/contracts": minor
"@acadigma/domain": minor
"@acadigma/db": minor
---

F-AC-01 demo cut (D-102): `sections` and `subjects` with T2 RLS, tenant-bound foreign keys and read-only guards; `/app/classes` lists each grade's sections for the current year (add with class teacher, room, capacity; archive) and the subject catalogue (add; copy the NCTB starter list), in English and Bangla.
