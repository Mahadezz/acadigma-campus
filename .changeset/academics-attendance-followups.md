---
"@acadigma/web": patch
"@acadigma/domain": patch
"@acadigma/db": patch
---

F-AC-03 follow-ups (D-105): any active teacher may take any section's attendance (substitutes cover); the class list for a date follows `enrolled_on`/`ended_on`; a saved roll call no longer re-stamps "Mark all present"; names render through `BnEnText`; the unused `sectionDayRate` is removed.
