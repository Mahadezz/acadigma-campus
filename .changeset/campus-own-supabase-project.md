---
"@acadigma/web": patch
---

Campus now runs on its own Supabase project `kekfmibwjejdhxjkmezo` (DECISION-LOG D-53, superseding D-19): the Next.js remote-image host, `.env.example`, CI's public env and `supabase/config.toml` point at the new project. The marketing site keeps the old project for its waitlist.
